- `/governance` severity is now **derived from independent recorded facts** rather than chosen: every finding states a trigger, an impact (`I1`–`I9`, giving the base band), every applicable exposure (`E1`–`E6`, strongest raise first then any cap) and an epistemic status — and `BLOCKING` is defined as a derived band of CRITICAL or HIGH, which supersedes the per-agent "blocks merge" text in the individual briefs. Contract violations (Resource Ledger omission, a direct `CHANGELOG.md` edit, a build or test leg this change broke, non-RAII cleanup in new C++, a false-green test offered as closure evidence, and violation of any explicit catastrophic-if-violated invariant) gate as explicit **policy floors** instead, because an honest derivation floors them at INFO. The Codex `/governance` runner now loads the same rule instead of carrying its own blocking contract — the two had drifted into enforcing different gates. #2604 calibrated the merge threshold but left the bands themselves undefined, so CRITICAL and HIGH had no criterion separating them while the ledger recorded the native band and CLAUDE.md acted on it.
