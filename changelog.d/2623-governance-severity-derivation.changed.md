- `/governance` severity is now **derived rather than chosen**: every finding states a trigger, a consequence (closed list `C1`–`C7`) and a reachability (closed list `R1`–`R6`), and the band follows from those three — `BLOCKING` is defined as a derived band of CRITICAL or HIGH. #2604 calibrated the merge threshold but left the bands themselves undefined, so CRITICAL and HIGH had no criterion separating them while the ledger recorded the native band and CLAUDE.md acted on it.
