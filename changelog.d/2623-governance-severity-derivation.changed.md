- `/governance` severity is now **derived from independent recorded facts** rather than chosen: every finding states a trigger, an impact (`I1`–`I9`, giving the base band), every applicable exposure (`E1`–`E6`, strongest modifier wins) and an epistemic status — and `BLOCKING` is defined as a derived band of CRITICAL or HIGH. Contract violations (Resource Ledger omission, direct `CHANGELOG.md` edit, a broken build or test leg on any supported platform, non-RAII manual cleanup in new C++) gate as explicit **policy floors** instead, because an honest derivation floors them at INFO. #2604 calibrated the merge threshold but left the bands themselves undefined, so CRITICAL and HIGH had no criterion separating them while the ledger recorded the native band and CLAUDE.md acted on it.
