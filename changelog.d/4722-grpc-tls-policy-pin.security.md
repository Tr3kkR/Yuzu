- **Server TLS listeners pin a TLS 1.2 cipher allow-list and refuse to start on an unresolvable policy.** The server pins `GRPC_SSL_CIPHER_SUITES` for both gRPC listeners (agent, management) and the gateway-command client, applies the same six-suite ECDHE allow-list to the HTTPS dashboard listener and its certificate hot-reload validation, and self-checks the resolved policy at boot — refusing to start rather than silently serving if the allow-list resolves to zero usable TLS 1.2 ciphers. Two startup log lines report the effective TLS 1.2 list and disclose that TLS 1.3 suite selection remains OpenSSL's own default (gRPC exposes no ciphersuite setter). Proven over real loopback handshakes against the production credential builders, including legacy-protocol refusal, non-allow-listed-cipher refusal, certless/wrong-CA client refusal, and the outbound gateway-command client's own cipher enforcement. The agent side is not yet pinned by this change (tracked as a follow-up).
