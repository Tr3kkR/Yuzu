- **New read-only `system_hardening` agent plugin — `posture` (Windows, Linux, macOS).** One row per allowlisted exploit-mitigation / kernel-hardening key: Linux `/proc/sys` (eleven keys), macOS `sysctlbyname` (four keys), Windows the `Session Manager\kernel` mitigation registry plus the agent's own `GetProcessMitigationPolicy`; rung 1, no subprocess, no mutation, `Security:Read`. A key the OS reports missing is its own `absent` row and never a failure; a refused or failed read is `unreadable` with a reason and reports `PERMISSION_DENIED` or `CONSTRAINED`, never an empty success.
