- Guardian file guards (`file-exists` / `file-hash-equals`) now arm on macOS via FSEvents: existing cross-platform Baselines begin real-time file drift detection and compliance reporting on Mac endpoints at agent upgrade. Detection-only (a file guard never writes), no new entitlements or permission prompts; targets on network volumes are re-verified on a 15-minute cadence (FSEvents does not report remote-origin changes).
