- **`/governance` gains Gate 0 — claim discipline, and its rules ship to every session.** `docs/claim-discipline.md` is a new shared reference: six rules (numbered 1, 2 with sub-triggers 2a-2f, 4-7 — 3 is retired and its number is not reused) for prose that asserts a checkable fact about the codebase or product, each with an authoring TRIGGER+CHECK and a reviewer-facing REVIEW CHECK. `docs/claim-discipline-patterns.txt` is the companion grep list for the unqualified-universal and false-sufficiency patterns rules 2f and 6 sweep for, each pattern tied to a real closed governance finding. A new `scripts/hooks/claim-rules-context.sh`, wired via `.claude/settings.json`'s `SessionStart` hook, injects the rules doc into every Claude Code session in this repo — the doc is their single home; the hook only reads it. `/governance` now runs a Gate 0 pass against these rules, unconditionally, before Gate 1: it applies the same TRIGGER/IMPACT/EXPOSURE severity derivation and the same run ledger as every other gate (no new severity scale, no new ledger record kind, no waiver path), halts on any finding for an operator fix-or-proceed call, and re-applies on any Gate 8 fix diff that touches prose. Mirrored in `.codex/skills/governance/SKILL.md` by reference, matching that runner's existing defer-to-canonical pattern for the severity block and ledger.
