- The `/governance` findings ledger records WHAT KIND of reviewer found each finding (`source`: `agent` / `collaborator` / `external-model`) and can record that a claim was `refuted` with evidence (`refuted_by`), distinct from merely `rejected`. A ledger fragment is explicitly appendable after the run ends, so an absent `collaborator` row means nothing was recorded rather than that no external review happened. The prose-ownership split is also now stated where agents actually read it: `docs-writer`'s Gate 2 brief names in-code comments and log/error strings among what it reviews, and a *missing* required doc is classed as a truth finding derived as `I7` rather than a wording observation capped at NICE.
