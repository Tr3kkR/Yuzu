- **`docs/os-capability-matrix.md` restructured into sections, re-verified against code, and updated for the macOS-parity batch.** The single matrix now groups rows under divider sections — agent core, Guardian guards, Spark mechanisms, DEX, TAR capture sources, inventory/daily-sync, live snapshot, network quality, and all 49 agent plugins (one row each). Re-verification corrected drift: all 13 TAR sources listed (the four-valued `OsSupportStatus` incl. `kSupportedConstrained` now in the legend), the file guard marked observe-only on every platform, and the stale Spark "rung 2/3" note replaced with the rung-7 consumer status. The matrix now also reflects the shipped macOS-parity work: `msi_packages` gains macOS support (so the plugin tally is 43 cross-platform / 4 Windows-only / 2 uneven), macOS network-quality throughput, TAR netqual via `nstat`, DEX crash/hang app version, new **Antivirus posture** and **Wi-Fi current connection** rows, and honest-sentinel corrections across `interaction`, `wol`, `flush_dns`, `registry`/`sccm`/`rdp_control` and the DNS-cache reads.
