- Windows CI server-suite timeouts under runner contention: `[pg]` tests now clone their ephemeral database from a per-process pre-migrated template (`PgTestTemplate` + `YUZU_REQUIRE_PG_DB_TPL`) instead of re-running each store's migration DDL per test, templates are dropped at `testRunEnded` (the static-destructor path could outlive OpenSSL and leak `yuzu_test_tpl_*` databases on filtered runs), and the server suite's meson timeout is recalibrated 600s → 900s (#2091).
