- The server test suite is split into two meson entries over the same binary — `server unit tests` (`~[pg]`) and `server pg unit tests` (`[pg]`) — so the shards run in parallel and each gets its own 600s budget (was one 900s entry); `flake-retry.py` now *replaces* a shard's positional tag filter with the case name when isolating a retry (Catch2 ORs positional specs — appending would re-run the whole shard), and its pure-logic selftest is wired into the `docs` meson suite (#2092).
