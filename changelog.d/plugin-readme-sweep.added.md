- Every agent plugin now ships its own reference at `agents/plugins/<name>/README.md` — what it does, per-OS capability and mechanism, privileges as measured, the data contract, real captured output on Windows, macOS and Linux (`docs/samples/`), and known gaps. Definition files under `content/definitions/` carry a description, value vocabulary, real example and platform list for every result column, which is the text `get_definition` and `discover_instructions` return to MCP clients.
