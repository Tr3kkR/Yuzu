- **Linux CI's `Test` step isolates the 8 server pg shards from the ~24 cheap suites sharing their concurrency pool, and bumps the isolated pool from `--num-processes 2` to `3`.** A live run of the cross-job slot gate (#3582, same day) showed shards E and G TIMEOUT at 600.51s/600.54s (SIGKILL'd) despite acquiring the cross-job slot in 0s — with zero cross-job contention, a cheap `agent`/`docs`/`proto`/`tar`/`gateway` test occupying one of only 2 shared `--num-processes` slots partway through was still enough to extend the pg shards' own effective queue. The Test step now runs 3 `flake-retry.py` invocations, cheap-first (fail-fast): the ~24 non-Postgres tests run uncapped first, then the 8 pg shards run last, isolated by exact name into their own `with-test-slot.sh`-gated call with the full pool dedicated to them. The cross-job slot count itself is unchanged at 2 (a different, already-fixed axis — #3443 AC4).
