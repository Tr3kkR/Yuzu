- **Device DEX/Guardian lenses and the `/dex` app-perf dashboard now read through the `DexApi`/`DexPerfApi`/`GuardianApi` seams instead of the store directly** (closing the #4576 device-lens deferral and #4626's dashboard deferral). Two small, disclosed rendering changes: a device with zero reported guards now shows "No guards evaluated" even if the fleet-wide rule catalogue is itself degraded (previously it showed a generic degraded state); the app-perf dashboard's various "not configured"/"degraded" placeholders are now one consistent message, "App performance data unavailable (not configured or degraded) — retry shortly." Internally, `AppPerfProviders` is retired and `device_lens_routes.cpp`/`dex_perf_ui.cpp`/`dex_app_perf_ui.{hpp,cpp}` are enrolled in `check-seam-closure.py`'s seam enforcement.
