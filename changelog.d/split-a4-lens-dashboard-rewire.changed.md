- **Device DEX/Guardian lenses and the `/dex` app-perf dashboard now read through the `DexApi`/`DexPerfApi`/`GuardianApi` seams instead of the store directly** (closing the #4576 device-lens deferral and #4626's dashboard deferral). Small, disclosed rendering changes: a device with zero reported guards now shows "No guards evaluated" even if the fleet-wide rule catalogue is itself degraded (previously it showed a generic degraded state); the F2b trend/app-list and per-device app-perf placeholders are now one consistent message, "App performance data unavailable (not configured or degraded) — retry shortly." (the fleet-now Performance tab and the version-devices drill keep their own unchanged wording); the per-device DEX signal-history percentile strips now render "no comparison data" over an empty snapshot rather than omitting the section on a wired-but-empty perf provider. Internally, `AppPerfProviders` is retired, the now-dead `DexRoutes::PerfFn` parameter is removed, and `device_lens_routes.cpp`/`dex_perf_ui.cpp`/`dex_app_perf_ui.{hpp,cpp}` are enrolled in `check-seam-closure.py`'s seam enforcement.
