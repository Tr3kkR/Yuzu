- **The Meson `gateway ct` test now actually runs the gateway's Common Test suites (#4800).** `scripts/test_gateway.py` invoked `rebar3 ct` without `--dir apps/yuzu_gw/test/ct`, so it discovered zero suites, printed `All 0 tests passed.` and passed on every CI leg that runs it. That false green hid #4707 and #4708. The gate now runs all 52 CT cases on the Linux, Windows and macOS CI legs. The macOS leg had never installed Erlang, so Meson silently skipped the whole gateway there (#4841). It now installs OTP 28, and every CI leg that runs `meson test` (ci.yml, nightly, sanitizer-tests) checks right after configure that the gateway tests were registered (`scripts/ci/assert-gateway-tests.py`), so a runner missing Erlang goes red instead of silently skipping the gateway. A `-Drequire_gateway` Meson option (default off) gives local builds the same fail-closed behaviour. The wrapper also fails any eunit or ct run that exits 0 but executes zero tests or prints no recognisable summary. `gateway ct`'s Meson timeout rises from 300 s to 600 s so the wrapper's own 540 s hang-diagnosis deadline fires first, as it already did for eunit. The same zero-executed rule now applies to `/test`'s `scripts/test/eunit-gate.sh` and the release workflow's EUnit step, which previously passed a run in which every EUnit set was cancelled (`Failed: 0.  Skipped: 0.  Passed: 0.`). `gateway/test_runner.sh ct` had the same no-`--dir` defect and now runs the suites. Note that it runs the perf suite at full default sizing unless `YUZU_PERF_*` is set, and `./test_runner.sh cover` runs cover again. The one stale CT case the gate unmasked (#4708) now mocks grpcbox's real error shape. The gateway's `{error, {Status, Message}}` contract for upstream gRPC status errors is unchanged.
