- **Guaranteed State events and Guardian dashboard reads are now confined to the caller's scope.** Per-device `GET /api/v1/guaranteed-state/events?agent_id=` reads are now management-group-scoped and confine service-scoped API tokens to their own service's agents; the fleet-wide (no `agent_id`) shape now denies service-scoped tokens outright and is consistently access-audited. The Guardian dashboard's fleet status, event timeline, per-Guard drilldown, baselines list, and per-Baseline views likewise now deny service-scoped API tokens, and the per-Guard drilldown records an access-audit entry.
