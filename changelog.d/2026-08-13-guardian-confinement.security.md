- **Fleet-wide Guardian and DEX device-list reads are now confined to the caller's scope.** Per-device `GET /api/v1/guaranteed-state/events?agent_id=` reads are now management-group-scoped and confine service-scoped API tokens to their own service's agents; the fleet-wide (no `agent_id`) shape, `GET /api/v1/dex/signals/{obs_type}`, `GET /api/v1/dex/perf/devices`, and `GET /api/v1/network/devices` now all deny service-scoped tokens outright and are consistently access-audited. The Guardian dashboard's fleet status, guards list, event timeline, per-Guard drilldown, baselines list, and per-Baseline views likewise now deny service-scoped API tokens; both the denial and the per-Guard drilldown's successful opens are access-audited.
