- **`docs/postgres-migration-ladder.md` and `docs/postgres-store-playbook.md` corrected (no code change).** The ladder document declared the Postgres migration program complete after `OffloadTargetStore` (ADR-0059) merged, but its tracked scope was always `*Store` classes plus the auth DB — it never covered 7 other production-wired SQLite components (`UpdateRegistry`, `DirectorySync`, `PatchManager`, `WorkflowEngine`, and the `ExecutionTracker`/`ApprovalManager`/`ScheduleEngine` trio). Added as a new `Wave 4` section with ADR numbers reserved; fixed the re-derivation grep, the stale "1 remaining store" banner, and three playbook inaccuracies (`open_with_migrations` doesn't exist, `ResponseStore` isn't the fresh-start-log model anymore, the upgrade-test assertion is superseded for skip-by-default stores).
