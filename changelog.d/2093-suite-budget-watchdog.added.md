- CI test steps now warn when any meson suite exceeds 80% of its timeout budget (`::warning` with measured/budget seconds, e.g. `server unit tests at 512/600s (85%)`) and append a per-suite duration/budget table to the job summary on every run — green included — so duration creep toward a fixed budget degrades into a visible signal instead of a discontinuous wall of TIMEOUT failures (#2093).
