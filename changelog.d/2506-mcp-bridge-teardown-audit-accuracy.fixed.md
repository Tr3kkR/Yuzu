- MCP progress bridge: the audit row written when a parked streamed result is force-expired under memory pressure now records what actually reached the client (#2506). The publish ladder can fall through to a prebuilt fallback result, poison the session and publish nothing, or fail before it is even reached, but the row always claimed the error frame had been synthesized - and the audit sink stamped every one of these rows `result="success"` regardless of what the detail said. Rows now name the outcome and carry `result="failure"` when the action did not complete, including the case where nothing was published and the stream was deliberately *not* poisoned (which must not be reported as a poisoning). The class-level invariant documentation was also still describing the pre-#2409 two-step teardown, which is the exact inference that would reintroduce that bug.
