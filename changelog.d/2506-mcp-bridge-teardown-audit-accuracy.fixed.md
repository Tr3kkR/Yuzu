- MCP progress bridge: the audit row written when a parked streamed result is force-expired under memory pressure now names what actually reached the client (#2506). The publish ladder can fall through to a prebuilt fallback result, or poison the session and publish nothing at all, but the row always claimed the error frame had been synthesized - evidence of a delivery that never happened. The class-level invariant documentation was also still describing the pre-#2409 two-step teardown, which is the exact inference that would reintroduce that bug.
