- Added the `YuzuMcpEngineRevalidateStoreUnreachable` alert rule to `docs/prometheus/yuzu-alerts.yml`, matching what `docs/user-manual/metrics.md` has recommended since #2367 but which was never actually present in the shipped rules catalogue. Syntax-validated via the repo's promtool harness (`tests/prometheus/run_promtool_tests.py`'s `check rules` pass); no dedicated behavior test case was added to `tests/prometheus/yuzu-alerts.test.yml`.
