- `content_dist` staged-payload execution now runs through the shared bounded subprocess runner (`run_bounded_subprocess`) instead of a private `CreateProcessW`/`fork`+`execvp` path: hard 30s deadline, 16 MiB output cap, group kill, and — on Linux — a TOCTOU-safe verified exec of the staged binary by file descriptor (size-pinned to the hash-verified staged file). The plugin's grandfathered raw-spawn lexical-gate entry is removed.
