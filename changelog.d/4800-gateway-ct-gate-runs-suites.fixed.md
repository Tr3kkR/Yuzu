- **The Meson `gateway ct` test now actually runs the gateway's Common Test suites (#4800).** `scripts/test_gateway.py` invoked `rebar3 ct` without `--dir apps/yuzu_gw/test/ct`, so it discovered zero suites, printed `All 0 tests passed.` and passed on every platform in CI. That false green hid #4707 and #4708. The gate now runs all 52 CT cases. The wrapper also fails any eunit or ct run that exits 0 but executes zero tests or prints no recognisable summary. `gateway ct`'s Meson timeout rises from 300 s to 600 s so the wrapper's own 540 s hang-diagnosis deadline fires first, as it already did for eunit. The one stale CT case the gate unmasked (#4708) now mocks grpcbox's real error shape. The gateway's `{error, {Status, Message}}` contract for upstream gRPC status errors is unchanged.
