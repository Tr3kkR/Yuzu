- MCP streamed-POST (`McpPostPump`) now has dedicated test coverage proving a revoked credential closes the response within one tick — the same CH-4 outcome the GET SSE channel has always asserted, previously proven only for its wait/check *timing*, not the actual close. The chaos test design doc's CH-4/CH-5/CH-6 rung labels are corrected to reflect what 2f PR 3b actually verified, replacing a stale "P0 PR 2" that implied streamed-POST coverage which did not exist at PR 2 time.
