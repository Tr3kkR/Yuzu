- **`TagStore::agents_with_tag_checked` now fails closed on a mid-scan read error, not just a prepare-time one.** Previously, a `sqlite3_step` failure partway through a scan (I/O error, corruption) silently returned whatever rows had already been collected as a genuine, non-degraded answer, instead of the function's own documented "degraded reads as `nullopt`, never as a smaller-but-real result" contract. This affects `derive_exec_visible`'s service-scoped-token dispatch confinement (`/api/command`, MCP `execute_instruction`/`execute_bundle`), which already fails closed on a `nullopt` result (deny-all, logged, `yuzu_server_tag_store_degraded_total`) — only *when* that path fires changes, and only in the rare direction of denying more conservatively, never less. No operator action needed.
