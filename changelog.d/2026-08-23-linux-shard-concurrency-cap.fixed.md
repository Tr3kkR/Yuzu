- **Linux CI no longer times out server pg test shards under within-job contention.** The Linux `Test` step now caps `meson test` to `--num-processes 2` (mirroring the existing Windows setting), preventing the ~8 server Postgres shards from fanning out and running concurrently inside a single job. Shards E and G had been clipping their 600s per-test timeout with zero margin; corrected the now-inaccurate "Big Tam scales flat, needs no gate" claim in `docs/ci-architecture.md`, `scripts/ci/with-test-slot.sh`, and the Windows Test step's own comment in `ci.yml`.
