- **REST v1 + MCP read twins for three TAR dashboard fragments** (#4027, API-parity programme Batch A): `GET /api/v1/tar/process-tree` and `GET /api/v1/tar/capture-sources` (operator-scoped device pickers, including offline devices) and `GET /api/v1/tar/retention-paused` (the calling operator's most recent retention-paused source scan). MCP twins `list_tar_process_tree_devices`, `list_tar_capture_sources_devices`, and `list_tar_retention_paused` share the exact same pure JSON builders as their REST siblings. TAR had zero MCP presence before this change. `GET /fragments/tar/process-tree/result` and `.../detail` are deliberately not twinned — both depend on artifacts mintable only via the dashboard-only `/run` route, itself excluded as dispatch-shaped (batched with #3994); recorded as `exception:` rows in `scripts/ci/api-parity/tar.json` rather than left silently `planned`.
