- **Spark detect→delivery latency waypoints for the criterion-10 benchmark campaign (#4606).** The agent and server now emit structured, always-on `info`-level log lines correlatable by `event_id` across the Guardian detect→delivery path: `T_detect`/`T_fire`/a diagnostic `T_handler` waypoint (`guardian_spark_runtime.cpp`, consuming the existing `T_mechanism` timestamp already stamped by `SparkEngine::emit_event()`), `T_wire` (both the live Spark send path and the legacy non-Spark Guardian drift-sink path in `agent.cpp` — DEX signal-observation traffic is deliberately excluded), and `T_server` (`guardian_ingest.cpp`, the application-observed instant a Guardian event's transaction commits, gated off ruleless DEX observations). Purely diagnostic: zero change to any existing eval/dispatch/ingest decision. No new database column — `T_server` is a benchmark-diagnostic field on the in-memory insert result, not a durable timestamp.
