- Engine-principal liveness cache: a burst of engine-principal writes (bulk revoke, an access-review remediation sweep, a credential-rotation loop) no longer silently disables the whole liveness cache process-wide for its duration. The cache-poisoning guard now tracks a per-principal generation instead of a single global counter, so a write to one principal no longer defeats every OTHER principal's concurrent cache-insert.
