# Placeholder: gRPC gateway / TLS termination layer
# Will be expanded to support HTTP/JSON transcoding via grpc-gateway (or
# a native C++ equivalent) for REST-based management clients.
message(STATUS "yuzu: server/gateway placeholder — TBD")
