{
  "actions": [
    {
      "action": "browsers",
      "definition_ids": [
        "crossplatform.browser_inventory.browsers"
      ],
      "legs": {
        "linux": {
          "fallback": "presence-only; no version/channel detection in this package",
          "mechanism": "fixed system binary path presence (/opt/google/chrome/chrome, /opt/microsoft/msedge/msedge, /usr/lib/chromium/chromium)",
          "rung": "1",
          "support": "constrained"
        },
        "macos": {
          "fallback": "follows as its own PR",
          "mechanism": "/Applications/{Google Chrome,Microsoft Edge}.app Info.plist + ~/Library/Application Support/{Google/Chrome,Microsoft Edge} walk; Safari bundle + .appex containers",
          "rung": "1",
          "support": "planned"
        },
        "windows": {
          "fallback": "follows as its own PR",
          "mechanism": "ProfileList walk + %LOCALAPPDATA% User Data; Program Files Application\\\\<semver> dirs",
          "rung": "1",
          "support": "planned"
        }
      }
    },
    {
      "action": "profiles",
      "definition_ids": [
        "crossplatform.browser_inventory.profiles"
      ],
      "legs": {
        "linux": {
          "fallback": "",
          "mechanism": "~/.config/{google-chrome,chromium,microsoft-edge}/Local State JSON read",
          "rung": "1",
          "support": "supported"
        },
        "macos": {
          "fallback": "follows as its own PR",
          "mechanism": "/Applications/{Google Chrome,Microsoft Edge}.app Info.plist + ~/Library/Application Support/{Google/Chrome,Microsoft Edge} walk; Safari bundle + .appex containers",
          "rung": "1",
          "support": "planned"
        },
        "windows": {
          "fallback": "follows as its own PR",
          "mechanism": "ProfileList walk + %LOCALAPPDATA% User Data; Program Files Application\\\\<semver> dirs",
          "rung": "1",
          "support": "planned"
        }
      }
    }
  ],
  "caveats": [
    "**macOS and Windows legs — including Safari on macOS — are PLANNED, not implemented, this wave.** Every action on those two OSes returns the fixed `status|<action>|unsupported|<os_tag>:planned` sentinel and `UNAVAILABLE`/`PARTIAL`; the real per-OS reads (macOS: `/Applications/{Google Chrome,Microsoft Edge}.app` Info.plist + `~/Library/Application Support/{Google/Chrome,Microsoft Edge}` walk, plus the Safari bundle/`.appex` containers; Windows: `ProfileList` walk + `%LOCALAPPDATA%` User Data, plus `Program Files\\Application\\<semver>` dirs) each follow as their own PR. Firefox is out of scope for every leg in this package and follows separately.",
    "**Chrome fixtures are SYNTHETIC; Microsoft Edge fixtures are a REAL CAPTURE (the-rig, reduced and redacted).** Neither Chrome nor Firefox is installed on the build host this wave, so Alex explicitly signed off SYNTHETIC Chrome fixtures on 2026-09-21 (run-context.md Phase-0 item 2) on condition they mirror the real Edge capture's on-disk shape (`tests/unit/fixtures/wave10/browser_inventory/chrome/`, every file's `_provenance` key states SYNTHETIC or RECONSTRUCTION). The Edge fixtures (`tests/unit/fixtures/wave10/browser_inventory/edge/`) are a real the-rig capture of `Local State`, reduced (subtrees dropped) and redacted (account/host values replaced) before being committed — see that directory's `provenance.txt` for the full capture log and hash chain.",
    "**The `extensions` action is deferred and follows as its own PR.** This release reads no file inside any profile directory — only each browser's `Local State` at the browser root — so no extension id, name, version or enable state is emitted anywhere. The per-profile `Secure Preferences`/`Preferences` read, its parser, fixtures and tests land together with that action; the real Edge capture behind it showed `extensions.settings` lives in `Secure Preferences` only, which is what that parser will prefer.",
    "**DEFAULT-OFF behind the server-side plugin-config kill switch.** Neither action dispatches until an operator explicitly `PUT`s `/api/v1/plugin-config/browser_inventory/kill-switch {\"enabled\":true}` — on a fresh install or an unconfigured fleet, every dispatch attempt is refused at the server layer, not reported as a plugin-level failure.",
    "**Linux `browsers` is presence-only this wave.** It detects whether the known system-wide binary paths exist; it does not probe the installed version or release channel (descriptor rung 1) — a distinct value-add over `installed_apps`' broader inventory that this plugin does not yet close."
  ],
  "definitions": [
    {
      "approval_mode": "role-gated",
      "author_roles": [
        "content-author"
      ],
      "description": "Which Chromium-family browsers (Google Chrome, Microsoft Edge) are installed on this host. Linux this wave: presence-only detection of the system-wide binary; version is always \"-\" (no version/channel probe in this package). macOS and Windows legs are PLANNED and follow as their own PR.",
      "display_name": "Browser Inventory - Installed Browsers",
      "execute_roles": [
        "admin"
      ],
      "gather": {
        "ttlSeconds": 300
      },
      "id": "crossplatform.browser_inventory.browsers",
      "platforms": [
        "windows",
        "linux",
        "darwin"
      ]
    },
    {
      "approval_mode": "role-gated",
      "author_roles": [
        "content-author"
      ],
      "description": "Per-browser profile directories, read from each browser's \"Local State\" file, across every user this leg discovers. Each row names the local OS user it belongs to; never emits a browsing-account identifier -- see the plugin README's PRIVACY CONTRACT. Linux this wave; macOS and Windows legs are PLANNED and follow as their own PR.",
      "display_name": "Browser Inventory - Profiles",
      "execute_roles": [
        "admin"
      ],
      "gather": {
        "ttlSeconds": 300
      },
      "id": "crossplatform.browser_inventory.profiles",
      "platforms": [
        "windows",
        "linux",
        "darwin"
      ]
    }
  ],
  "description": "Chromium-family browser and profile inventory",
  "how_it_works": "`browsers` and `profiles` are two independent reads, dispatched and gated separately — a failure in one never blocks the other. On Linux this wave, `browsers` checks presence of the known Chromium-family system-wide binaries (`google-chrome`, `microsoft-edge`, `chromium`) and emits one row per candidate, present or not, with version always `-` (no version/channel probe in this package). `profiles` walks every discoverable OS user's `~/.config/{google-chrome,chromium,microsoft-edge}` directory and reads that browser's `Local State` JSON, emitting one row per profile directory listed in `profile.info_cache`; no profile directory is opened and no file inside one is read. Every action's wire stream leads with a `status|<action>|<supported|constrained>|<reason>` row before any data row (the same CC-07 typed-status pairing `peripherals`/`autoruns` use), so a consumer never reads silence and infers \"nothing checked.\" macOS and Windows legs — including Safari on macOS — are PLANNED and follow as their own PR; Firefox follows separately on every OS. Every row is Forensics-gated, single-target, and DEFAULT-OFF behind the server-side plugin-config kill switch until an operator explicitly enables it.\n\n**PRIVACY CONTRACT.** No row this plugin emits, on any action or any OS leg, ever carries a browsing-account identifier, browsing history, cookies or bookmarks — no `gaia_id`, no e-mail address, no Chromium `info_cache` `user_name`/`gaia_name`. The dedicated-identifier half of this is enforced structurally in `browser_inventory_parsers.hpp`: `BrowserProfileRow` — the JSON row model every OS leg builds from — simply has no `gaia_id`/`user_name`/`gaia_name` field. The e-mail-address half is enforced BY VALUE: `profile_dir` and `display_name` are free-text fields Chromium populates from the account (a signed-in Edge profile in particular is often keyed by its account e-mail), so an e-mail-shaped value in either field is redacted to `[redacted-email]` before the row is built (`looks_like_email_address`/`kRedactedEmailPlaceholder`, adversarial-review finding, 2026-09-22 — an earlier revision of this contract incorrectly claimed the dedicated-identifier structural argument covered e-mail addresses too, which it did not). Two deliberate, accepted exceptions, both decided 2026-09-22: **(1)** the `profiles` row's LOCAL OS/home-directory username (e.g. `alice`) — it identifies which of several local users on a shared machine a profile belongs to, is machine-local (not a personal/cloud identifier), and is added only when the Linux leg formats the wire row, never inside the JSON parser, never a `BrowserProfileRow` field. **(2)** `display_name` (`info_cache[dir].name`, a `BrowserProfileRow` field) — Chromium-family browsers commonly auto-populate a profile's display name from the signed-in account's real name, so **a non-e-mail-shaped value in this field can legitimately carry a personal name**; it is emitted as-is (it is the whole point of a profile inventory — telling \"Work\" apart from \"Personal\") rather than filtered, because a reliable real-name/PII detector does not exist and a false sense of redaction is worse than an honest, documented residual risk — only the e-mail SHAPE is filtered, per the paragraph above. No leg opens any file inside a profile directory (`Preferences`, `Secure Preferences`, `History`, `Cookies`, ...) — see caveat 3.\n\n**WHY Forensics, not Inventory.** Unlike a plain installed-software inventory, this plugin reads per-user browser profile state for a SINGLE named machine — the kind of evidence a rogue or compromised operator identity could otherwise use to fingerprint a target's activity undetected (which browsers and browser profiles exist on a specific user's account). That is the same operator-triage rationale `execution_artifacts` and `app_usage` already carry, and this plugin shares their exact `Forensics:Read`/`AdminOrApproval` boundary — see `server/core/src/capability_decls/plugin_action_catalogue_browser_inventory.hpp` for the fuller rationale, copied field-for-field from `execution_artifacts`'.",
  "inputs": [],
  "kind": {
    "collector": true,
    "gathered": true,
    "mutating": false
  },
  "leg_hash": "66c026fa18f1",
  "manifest_version": 1,
  "name": "browser_inventory",
  "outputs": [
    {
      "columns": [
        {
          "description": "Discriminates the row shape - \"status\" or \"browser\".",
          "example": "browser",
          "name": "row_kind",
          "platforms": null,
          "type": "string",
          "values": null
        },
        {
          "description": "status row: the action name (\"browsers\"). browser row: the browser identifier.\n",
          "example": "chrome",
          "name": "field_1",
          "platforms": null,
          "type": "string",
          "values": null
        },
        {
          "description": "status row: \"supported\" or \"constrained\". browser row: \"1\" if the binary is present, \"0\" otherwise.\n",
          "example": "1",
          "name": "field_2",
          "platforms": null,
          "type": "string",
          "values": null
        },
        {
          "description": "status row: the constraint reason, \"-\" when supported. browser row: version, always \"-\" this wave (no version/channel probe).\n",
          "example": "-",
          "name": "field_3",
          "platforms": null,
          "type": "string",
          "values": null
        }
      ],
      "definition_id": "crossplatform.browser_inventory.browsers"
    },
    {
      "columns": [
        {
          "description": "Discriminates the row shape - \"status\" or \"profile\".",
          "example": "profile",
          "name": "row_kind",
          "platforms": null,
          "type": "string",
          "values": null
        },
        {
          "description": "status row: the action name (\"profiles\"). profile row: the owning OS user, redacted per the sample-capture convention in non-sample contexts this is the real local username.\n",
          "example": "jdoe",
          "name": "field_1",
          "platforms": null,
          "type": "string",
          "values": null
        },
        {
          "description": "status row: \"supported\" or \"constrained\". profile row: the browser identifier.\n",
          "example": "chrome",
          "name": "field_2",
          "platforms": null,
          "type": "string",
          "values": null
        },
        {
          "description": "status row: the constraint reason, \"-\" when supported. profile row: the profile directory name (the Local State info_cache key), e.g. \"Default\", \"Profile 1\" — redacted to \"[redacted-email]\" when the raw value has the shape of an e-mail address (some Chromium builds key a signed-in profile by its account e-mail).\n",
          "example": "Default",
          "name": "field_3",
          "platforms": null,
          "type": "string",
          "values": null
        },
        {
          "description": "status row: empty. profile row: the profile's user-editable display name — redacted to \"[redacted-email]\" under the same e-mail-shape rule as field_3, otherwise emitted as-is (may legitimately be a personal name).\n",
          "example": "Work",
          "name": "field_4",
          "platforms": null,
          "type": "string",
          "values": null
        }
      ],
      "definition_id": "crossplatform.browser_inventory.profiles"
    }
  ],
  "outputs_note": "Pipe-delimited rows. Every action's stream leads with a `status|<action>|<supported|constrained>|<reason>` row (CC-07 typed-status pairing), followed by zero or more data rows discriminated by their own leading tag (`browser|...`, `profile|...`); every untrusted string field (usernames, profile directory and display names) is escaped through `safe_output_field`. A directory or file that cannot be opened or read anywhere in the walk (`ConstraintAccumulator`) sets the action's `status` row to `constrained` with the accumulated reason — a genuinely absent browser install or user directory is never treated as a failure, only as fewer rows.",
  "platforms": {
    "linux": "supported",
    "macos": "planned",
    "windows": "planned"
  },
  "privileges": [
    {
      "grant": "n/a this wave — the Windows leg is a FINAL placeholder; real implementation and its own privilege measurement follow as its own PR",
      "if_refused": "always the PLANNED sentinel row, `UNAVAILABLE`/`PARTIAL`",
      "measured": "not measured (no mechanism exists yet)",
      "os": "Windows",
      "runs_as": "agent service account (LocalSystem today, #1442)"
    },
    {
      "grant": "n/a this wave — the macOS leg (including Safari) is a FINAL placeholder; real implementation and its own privilege measurement follow as its own PR",
      "if_refused": "always the PLANNED sentinel row, `UNAVAILABLE`/`PARTIAL`",
      "measured": "not measured (no mechanism exists yet)",
      "os": "macOS",
      "runs_as": "agent service account"
    },
    {
      "grant": "None — every read is unprivileged: `browsers` presence-checks fixed system-wide binary paths (`/opt/google/chrome/chrome`, `/opt/microsoft/msedge/msedge`, `/usr/lib/chromium/chromium`), `profiles` reads under a discoverable OS user's own home directory (`~/.config/{google-chrome,chromium,microsoft-edge}`); no elevation, no subprocess",
      "if_refused": "`constrained` with a named reason (`ConstraintAccumulator`-sourced, e.g. a directory-open or file-read failure token) — a genuinely absent browser or user reports absent/zero rows, distinct from `constrained`",
      "measured": "not yet measured on real hardware this wave (Chrome fixtures are SYNTHETIC, sign-off 2026-09-21 — neither browser is installed on the build host; see Caveats)",
      "os": "Linux",
      "runs_as": "agent service account"
    }
  ],
  "readme": "agents/plugins/browser_inventory/README.md",
  "result_status": [
    {
      "completeness": "full",
      "provenance": "*(empty)*",
      "status": "`OK`",
      "when": "The leg's walk completed with no unreadable directory or malformed file anywhere along the way (`status|supported|-`)."
    },
    {
      "completeness": "partial",
      "provenance": "`linux:browser_inventory:local_state_malformed` · `linux:browser_inventory:local_state_too_large` · `linux:browser_inventory:busy` · `linux:browser_inventory:not_a_directory` · `linux:browser_inventory:not_regular` · `linux:browser_inventory:open_failed` · `linux:browser_inventory:permission_denied` · `linux:browser_inventory:readdir_error:home` · `linux:browser_inventory:row_cap:home` · `linux:browser_inventory:symlink_refused`",
      "status": "`CONSTRAINED`",
      "when": "A `Local State` file existed but failed to parse as valid JSON, exceeded the 1 MiB read cap (distinct from malformed — a size-policy refusal, not invalid content), or a directory/file in the walk (a user's config directory, a browser's data directory, or the \"browsers\" action's binary-path probe) could not be opened, read, or safely followed (symlink refusal, non-directory, non-regular file, busy/locked, permission denied, a `readdir` failure, or the per-user row cap) for a reason other than benign absence."
    },
    {
      "completeness": "partial",
      "provenance": "`macos:planned` · `windows:planned`",
      "status": "`UNAVAILABLE`",
      "when": "macOS and Windows, every action, this wave — both legs are FINAL placeholders; the real implementation (including Safari on macOS) follows as its own PR. The row itself is `status|<action>|unsupported|<os_tag>:planned`."
    }
  ],
  "samples": {
    "linux": {
      "actions": [
        {
          "action": "browsers",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "FULL",
            "provenance": "",
            "status": "OK"
          },
          "row_count": 4,
          "rows": [
            "status|browsers|supported|-",
            "browser|chrome|0|-",
            "browser|edge|0|-",
            "browser|chromium|0|-"
          ],
          "truncated": false
        },
        {
          "action": "profiles",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "FULL",
            "provenance": "",
            "status": "OK"
          },
          "row_count": 1,
          "rows": [
            "status|profiles|supported|-"
          ],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-22",
        "host_class": "container",
        "leg_hash": "66c026fa18f1",
        "os": "linux",
        "os_version": "Debian GNU/Linux 13 (trixie) aarch64",
        "privilege": "euid 0"
      }
    }
  },
  "security": [
    {
      "action": "browsers",
      "dispatch_class": "ReadOnly",
      "execute_gate": "AdminOrApproval",
      "mutability": "None",
      "operation": "Read",
      "risk_tier": "High",
      "securable": "Forensics"
    },
    {
      "action": "profiles",
      "dispatch_class": "ReadOnly",
      "execute_gate": "AdminOrApproval",
      "mutability": "None",
      "operation": "Read",
      "risk_tier": "High",
      "securable": "Forensics"
    }
  ],
  "source": {
    "capability_rows": [
      "server/core/src/capability_decls/plugin_action_catalogue_browser_inventory.hpp"
    ],
    "changelog": [],
    "definitions": [
      "content/definitions/browser_inventory.yaml"
    ],
    "plugin": [
      "agents/plugins/browser_inventory/src/browser_inventory_legs.hpp",
      "agents/plugins/browser_inventory/src/browser_inventory_linux.cpp",
      "agents/plugins/browser_inventory/src/browser_inventory_linux_parsers.hpp",
      "agents/plugins/browser_inventory/src/browser_inventory_macos.cpp",
      "agents/plugins/browser_inventory/src/browser_inventory_parsers.hpp",
      "agents/plugins/browser_inventory/src/browser_inventory_plugin.cpp",
      "agents/plugins/browser_inventory/src/browser_inventory_win.cpp"
    ],
    "privilege_row": true,
    "tests": [
      "tests/unit/test_browser_inventory_linux_parsers.cpp",
      "tests/unit/test_browser_inventory_local_dispatcher.cpp",
      "tests/unit/test_browser_inventory_parsers.cpp"
    ]
  },
  "version": "1.0.0",
  "where_the_data_goes": [
    "**Instruction result only.** Rows travel over the agent's mTLS gRPC channel as the command response, land in the ResponseStore, and are queryable at `/api/responses/{id}` — single-target only; fleet or scope targeting is refused at the server dispatch layer before this plugin ever runs.",
    "**Not consumed by** daily-sync inventory, the TAR warehouse, DEX, or metrics. Nothing here runs on a schedule; each action dispatches only when an operator or workflow explicitly requests it, and only once the plugin-config kill switch is enabled.",
    "**Sensitivity.** `profiles` rows name a real local OS username and a browser-supplied `display_name` that MAY be the signed-in account's real name (see the PRIVACY CONTRACT's two accepted exceptions above) for a single named machine — they let a reader reconstruct which browsers and profiles a specific person on that machine uses, and potentially who that person is, which is exactly the kind of per-user fingerprint an unauthorized reader could misuse. No `gaia_id`, e-mail address, browsing history or cookie is ever included.",
    "**Siblings:** `execution_artifacts` and `app_usage` are the other Forensics-gated, default-off evidence sources in the catalogue; none share a store or a format with this plugin.",
    "**MCP / REST.** Discover: `discover_plugins` (summary) → `yuzu://plugin-docs` (this page as data) → `discover_instructions` / `get_definition(\"crossplatform.browser_inventory.profiles\")`. Run: `execute_instruction {definition_id, parameters}` (single explicit `agent_id` required). Read: `/api/responses/{id}`."
  ]
}
