{
  "actions": [
    {
      "action": "check",
      "definition_ids": [
        "device.wol.check"
      ],
      "legs": {
        "linux": {
          "fallback": "requires net.ipv4.ping_group_range to admit the process group for ICMP; falls back to a TCP connect on port 443, and reports CONSTRAINED if neither mechanism is usable",
          "mechanism": "SOCK_DGRAM ICMP ping socket + TCP-connect fallback",
          "rung": "1",
          "support": "constrained"
        },
        "macos": {
          "fallback": "",
          "mechanism": "SOCK_DGRAM ICMP ping socket + TCP-connect fallback",
          "rung": "1",
          "support": "supported"
        },
        "windows": {
          "fallback": "",
          "mechanism": "IcmpSendEcho + TCP-connect fallback",
          "rung": "1",
          "support": "supported"
        }
      }
    },
    {
      "action": "wake",
      "definition_ids": [
        "device.wol.wake"
      ],
      "legs": {
        "linux": {
          "fallback": "",
          "mechanism": "raw UDP broadcast socket",
          "rung": "1",
          "support": "supported"
        },
        "macos": {
          "fallback": "",
          "mechanism": "raw UDP broadcast socket",
          "rung": "1",
          "support": "supported"
        },
        "windows": {
          "fallback": "",
          "mechanism": "raw UDP broadcast socket",
          "rung": "1",
          "support": "supported"
        }
      }
    }
  ],
  "caveats": [
    "**`wake` needs no elevated privilege.** The plugin opens a plain `SOCK_DGRAM`/`IPPROTO_UDP` broadcast socket on every OS (`wol_plugin.cpp:215`) — no `SOCK_RAW`, so no `cap_net_raw` or root; `docs/agent-privilege-model.md:120` states the same. Earlier revisions of that row claimed a `cap_net_raw` grant; treat any remaining reference to it elsewhere as stale.",
    "**`check` accepts an undeclared `timeout_ms` parameter.** The code reads `timeout_ms` (default `1000` ms, clamped 100-5000; `wol_plugin.cpp:290-298`), but `content/definitions/wol.yaml`'s `check` parameters declare only `host` and `count` — an operator calling through the definition schema has no way to set it, and it silently stays at 1000 ms.",
    "**`wake`'s success-path output was never captured.** Both fixture runs deliberately rejected `wake` for a missing `mac` (an Irreversible, AdminOrApproval-gated action), so the `port`/`bytes_sent` field shapes in this README are derived from source (`wol_plugin.cpp:248-249`), not observed in any sample — the Outputs table marks those examples as such rather than showing a captured value.",
    "**The Linux sample ran as root, not the unprivileged path `check`'s fallback exists for.** `linux.txt` was captured at euid 0 in a container, so the ICMP-denied → TCP-fallback branch (`net.ipv4.ping_group_range`) was never actually exercised by any sample; every OS sample shows the same plain-ICMP-succeeds path.",
    "**The historic shelled-out `ping` is gone; do not reintroduce it.** `check` used to spawn a system `ping` via `popen` (rung 3); the migration to native ICMP + TCP-connect fallback removed the last subprocess spawn from this plugin (`changelog.d/20260818-wave2-network-actions-wol-services-native-argv.changed.md`). `icmp_probe.hpp`'s own header comment (lines 6-10) is stale — it still describes `wol.check` as an unmigrated shell-out that \"still spawns the system `ping` binary\"; the plugin's actual behaviour is the native path in `wol_plugin.cpp`."
  ],
  "definitions": [
    {
      "approval_mode": "role-gated",
      "author_roles": [
        "content-author"
      ],
      "description": "Checks whether a host has become reachable, typically used after sending a Wake-on-LAN packet to confirm the target machine has booted. Probes over unprivileged ICMP echo, falling back to a TCP connect on port 443 when ICMP cannot be used or gets no reply (e.g. Linux net.ipv4.ping_group_range denying the process an ICMP socket) — no shell-out, no ping binary. Validates the host parameter to prevent command injection. Returns a reachability status plus a mechanism row naming which probe produced the verdict. The count parameter controls how many samples are attempted per mechanism.",
      "display_name": "Check Host Reachability",
      "execute_roles": [
        "endpoint-admin",
        "endpoint-operator"
      ],
      "gather": {
        "ttlSeconds": 0
      },
      "id": "device.wol.check",
      "platforms": [
        "windows",
        "linux",
        "darwin"
      ]
    },
    {
      "approval_mode": "role-gated",
      "author_roles": [
        "content-author"
      ],
      "description": "Sends a Wake-on-LAN magic packet to the specified MAC address. The magic packet consists of 6 bytes of 0xFF followed by the target MAC address repeated 16 times, broadcast via UDP. The target machine must have WoL enabled in its BIOS/UEFI and NIC settings. This is a network action that may wake a powered-off device. An optional port parameter can override the default WoL port (9).",
      "display_name": "Wake-on-LAN",
      "execute_roles": [
        "endpoint-admin"
      ],
      "gather": {
        "ttlSeconds": 0
      },
      "id": "device.wol.wake",
      "platforms": [
        "windows",
        "linux",
        "darwin"
      ]
    }
  ],
  "description": "Sends Wake-on-LAN magic packets and checks host reachability",
  "how_it_works": "`wake` builds a 102-byte magic packet — six bytes of `0xFF` followed by the target MAC repeated 16 times — and sends it once as a UDP broadcast to `255.255.255.255` on the given port (default 9). It has no way to know whether the target actually powered on, which is why `check` exists as a companion action. `check` is a pure reachability probe: it tries unprivileged ICMP echo first (native on every OS — `IcmpSendEcho` on Windows, an unprivileged ping socket on POSIX), and only spends a TCP connect on port 443 once ICMP has failed to prove reachability. `classify_check()` (`wol_check_plan.hpp`) is the single place that turns raw probe facts into a verdict, and it deliberately distinguishes \"checked and got nothing\" (`checked_no_reply`) from \"neither mechanism could even be attempted\" (`unavailable`) — the latter degrades honestly to `CONSTRAINED`/`PARTIAL` rather than being reported as a fabricated `unreachable`. Neither action shells out to an external binary; the historic `ping`/`popen` implementation was removed in favour of the native probes. `wol` is deliberately not a reachability-history or scheduling plugin — each `check` call is one point-in-time read, with polling left to the caller.",
  "inputs": [
    {
      "constraints": {
        "maxLength": 253,
        "minLength": 1,
        "pattern": "^[a-zA-Z0-9.:\\-]+$"
      },
      "default": null,
      "definition_id": "device.wol.check",
      "description": "Hostname or IPv4/IPv6 address to check for reachability, e.g. 192.168.1.50 or desktop-01.example.com. Must contain only alphanumeric characters, dots, hyphens, and colons.\n",
      "name": "host",
      "required": true,
      "type": "string"
    },
    {
      "constraints": {
        "pattern": "^[0-9]+$"
      },
      "default": "3",
      "definition_id": "device.wol.check",
      "description": "Number of probe samples attempted per mechanism (1-10); each mechanism stops early on a positive result. Defaults to 3 if not specified.\n",
      "name": "count",
      "required": false,
      "type": "string"
    },
    {
      "constraints": {
        "maxLength": 17,
        "minLength": 17,
        "pattern": "^([0-9A-Fa-f]{2}[:\\-]){5}[0-9A-Fa-f]{2}$"
      },
      "default": null,
      "definition_id": "device.wol.wake",
      "description": "Target MAC address in colon or hyphen-separated format (e.g., AA:BB:CC:DD:EE:FF or AA-BB-CC-DD-EE-FF).\n",
      "name": "mac",
      "required": true,
      "type": "string"
    },
    {
      "constraints": {
        "pattern": "^[0-9]+$"
      },
      "default": "9",
      "definition_id": "device.wol.wake",
      "description": "UDP port for the magic packet broadcast. Standard WoL port is 9. Some setups use port 7 or other values.\n",
      "name": "port",
      "required": false,
      "type": "string"
    }
  ],
  "kind": {
    "collector": false,
    "gathered": true,
    "mutating": true
  },
  "leg_hash": "dcc02549b1eb",
  "manifest_version": 1,
  "name": "wol",
  "outputs": [
    {
      "columns": [
        {
          "description": "The `host` parameter value, echoed back unmodified. Values: free text (hostname or IP literal).",
          "example": "127.0.0.1",
          "name": "host",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "Reachability verdict: `reachable` if either mechanism produced a positive result, `unreachable` if both were genuinely attempted with no positive result, or `unavailable` if neither mechanism could even be attempted. Values: reachable, unreachable, unavailable.",
          "example": "reachable",
          "name": "status",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "The `count` parameter value, echoed back as a string. Values: integer 1-10, as a string.",
          "example": "3",
          "name": "count",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        }
      ],
      "definition_id": "device.wol.check"
    },
    {
      "columns": [
        {
          "description": "Outcome of the magic-packet send attempt. Values: error, success.",
          "example": "error",
          "name": "status",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "Failure reason on `error`, or a confirmation of the packet sent on `success`. Values: free text.",
          "example": "Missing required parameter: mac",
          "name": "message",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "The UDP port the magic packet was sent to, formatted as `port <n>`; emitted only on `success`. Values: `port <n>` (formatted string, not a bare number).",
          "example": "not observed in any capture (code: wol_plugin.cpp:249)",
          "name": "port",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "Number of bytes sent, formatted as `<n> bytes`; emitted only on `success`. Values: `<n> bytes` (formatted string, not a bare number).",
          "example": "not observed in any capture (code: wol_plugin.cpp:249)",
          "name": "bytes_sent",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        }
      ],
      "definition_id": "device.wol.wake"
    }
  ],
  "outputs_note": "Every action writes pipe-delimited rows via `write_output()`; field 0 is a literal discriminator matching the action name. `wake`'s field count is not constant — an error row carries only `status|message`; `port` and `bytes_sent` are appended only on the success path, never padded with `-`. `check` additionally emits a second, separate row discriminated `mechanism` — a real output field that is not declared in `result.columns` below (see Caveats).\n\n\n\n**Supplementary `mechanism` row.** `check` always emits a second row, `mechanism|<label>`, immediately after the `check` row. `<label>` is one of `icmp`, `tcp-fallback`, `tcp-refused`, `icmp+tcp-fallback` (both mechanisms genuinely attempted, neither succeeded), or `unavailable` (neither could be attempted) — `wol_check_plan.hpp:96-113`.",
  "platforms": {
    "linux": "supported",
    "macos": "supported",
    "windows": "supported"
  },
  "privileges": [
    {
      "grant": "`wake`: none beyond default socket access — Winsock `SOCK_DGRAM` broadcast (`wol_plugin.cpp:215`), no PowerShell; `check`: none — `IcmpSendEcho` (iphlpapi) and the TCP connect are both unprivileged",
      "if_refused": "not observed in any sample; `wake`'s only failure path is a `sendto` error, reported as `wake|error|Failed to send magic packet to <mac>`",
      "measured": "2026-09-07, bare metal, as `SYSTEM`",
      "os": "Windows",
      "runs_as": "agent service account (LocalSystem today, #1442)"
    },
    {
      "grant": "`wake`: none — `docs/agent-privilege-model.md:120` and `wol_plugin.cpp:215` agree: the plugin opens `SOCK_DGRAM`/`IPPROTO_UDP` (not `SOCK_RAW`), which needs no such capability — the doc's grant is over-stated for the current code, see Caveat 1. `check`: none — the unprivileged `SOCK_DGRAM` ICMP ping socket \"works out of the box on macOS\" (`icmp_probe.hpp:19-20`)",
      "if_refused": "not observed; no permission-denied path is exercised in code or samples",
      "measured": "2026-09-07, bare metal, at euid 501 (unprivileged)",
      "os": "macOS",
      "runs_as": "agent daemon (`_yuzu`)"
    },
    {
      "grant": "`wake`: none — `docs/agent-privilege-model.md:120` and `wol_plugin.cpp:215` agree: the plugin opens `SOCK_DGRAM`/`IPPROTO_UDP` (not `SOCK_RAW`), which needs no such capability — see Caveat 1. `check`: no agent-side grant — the unprivileged ICMP ping socket needs the *host's* `net.ipv4.ping_group_range` sysctl to admit the process's group, else the leg falls back to the TCP-connect probe (`matrix.md`)",
      "if_refused": "a denied ICMP socket open (`EACCES`/`EPERM`) sets `icmp_session_ok=false` (`icmp_probe.hpp:370`) and the action silently falls through to the TCP fallback rather than surfacing a distinct error",
      "measured": "2026-09-06, container, at euid 0 (root) — see Caveats: the capture never ran unprivileged",
      "os": "Linux",
      "runs_as": "agent daemon (`yuzu`)"
    }
  ],
  "readme": "agents/plugins/wol/README.md",
  "result_status": [
    {
      "completeness": "`PARTIAL`",
      "provenance": "`wol_plugin:check_unavailable`",
      "status": "`CONSTRAINED`",
      "when": "`check` only, when neither ICMP nor the TCP fallback could even be attempted (`wol_plugin.cpp:358-359`)"
    }
  ],
  "samples": {
    "linux": {
      "actions": [
        {
          "action": "wake",
          "not_captured": null,
          "params": "",
          "rc": 1,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 1,
          "rows": [
            "wake|error|Missing required parameter: mac"
          ],
          "truncated": false
        },
        {
          "action": "check",
          "not_captured": null,
          "params": "host=127.0.0.1",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 2,
          "rows": [
            "check|127.0.0.1|reachable|3",
            "mechanism|icmp"
          ],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-06",
        "host_class": "container",
        "leg_hash": "dcc02549b1eb",
        "os": "linux",
        "os_version": "Debian GNU/Linux 13 (trixie) aarch64",
        "privilege": "euid 0"
      }
    },
    "macos": {
      "actions": [
        {
          "action": "wake",
          "not_captured": null,
          "params": "",
          "rc": 1,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 1,
          "rows": [
            "wake|error|Missing required parameter: mac"
          ],
          "truncated": false
        },
        {
          "action": "check",
          "not_captured": null,
          "params": "host=127.0.0.1",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 2,
          "rows": [
            "check|127.0.0.1|reachable|3",
            "mechanism|icmp"
          ],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-07",
        "host_class": "bare-metal",
        "leg_hash": "dcc02549b1eb",
        "os": "macos",
        "os_version": "macOS 26.6.2 arm64",
        "privilege": "euid 501 (alex)"
      }
    },
    "windows": {
      "actions": [
        {
          "action": "wake",
          "not_captured": null,
          "params": "",
          "rc": 1,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 1,
          "rows": [
            "wake|error|Missing required parameter: mac"
          ],
          "truncated": false
        },
        {
          "action": "check",
          "not_captured": null,
          "params": "host=127.0.0.1",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 2,
          "rows": [
            "check|127.0.0.1|reachable|3",
            "mechanism|icmp"
          ],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-07",
        "host_class": "bare-metal",
        "leg_hash": "dcc02549b1eb",
        "os": "windows",
        "os_version": "Microsoft Windows NT 10.0.26200.0 x64",
        "privilege": "SYSTEM"
      }
    }
  },
  "security": [
    {
      "action": "wake",
      "dispatch_class": "Mutating",
      "execute_gate": "AdminOrApproval",
      "mutability": "Irreversible",
      "operation": "Write",
      "risk_tier": "Medium",
      "securable": "Infrastructure"
    },
    {
      "action": "check",
      "dispatch_class": "ReadOnly",
      "execute_gate": "AdminOrApproval",
      "mutability": "None",
      "operation": "Read",
      "risk_tier": "Low",
      "securable": "Infrastructure"
    }
  ],
  "source": {
    "capability_rows": [
      "server/core/src/capability_decls/plugin_action_catalogue_c.hpp"
    ],
    "changelog": [
      "changelog.d/20260818-wave2-network-actions-wol-services-native-argv.changed.md",
      "changelog.d/2209-wol-macos-ping-timeout.added.md"
    ],
    "definitions": [
      "content/definitions/wol.yaml"
    ],
    "plugin": [
      "agents/plugins/wol/src/wol_check_plan.hpp",
      "agents/plugins/wol/src/wol_plugin.cpp"
    ],
    "privilege_row": true,
    "tests": [
      "tests/unit/test_wol_check_plan.cpp"
    ]
  },
  "version": "1.0.0",
  "where_the_data_goes": [
    "**Instruction result only.** Rows travel over the agent's mTLS gRPC channel as the command response and land in the ResponseStore, queryable at `/api/responses/{id}`.",
    "**Not consumed by** daily-sync inventory, the TAR warehouse, DEX, or metrics — grepping the server tree for `wol` finds only the capability-declaration rows in `plugin_action_catalogue_c.hpp`; nothing else reads this plugin's output.",
    "**Sensitivity.** `check`'s `host` row echoes back the caller-supplied hostname or IP address being probed — a device identifier. `wake` has no declared identifying column, though its uncaptured success-path `message` text likely names the target MAC address as confirmation text (see Caveat 3 — never observed in a sample). Neither action's rows carry a person's identity or installed-software information.",
    "**Siblings:** `network.probe.icmp` / `network.probe.tcp` (`netprobe.yaml`) share `check`'s underlying `icmp_probe.hpp` mechanism for their own reachability probes.",
    "**MCP / REST.** Discover: `discover_plugins` (summary) → `yuzu://plugin-docs` (this page as data) → `discover_instructions` / `get_definition(\"device.wol.wake\")`. Run: `execute_instruction {definition_id, parameters}`. Read: `/api/responses/{id}`."
  ]
}
