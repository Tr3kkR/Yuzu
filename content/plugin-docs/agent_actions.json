{
  "actions": [
    {
      "action": "info",
      "definition_ids": [
        "device.agent_actions.info"
      ],
      "legs": {
        "linux": {
          "fallback": "",
          "mechanism": "agent_config_read",
          "rung": "1",
          "support": "supported"
        },
        "macos": {
          "fallback": "",
          "mechanism": "agent_config_read",
          "rung": "1",
          "support": "supported"
        },
        "windows": {
          "fallback": "",
          "mechanism": "agent_config_read",
          "rung": "1",
          "support": "supported"
        }
      }
    },
    {
      "action": "set_log_level",
      "definition_ids": [
        "device.agent_actions.set_log_level"
      ],
      "legs": {
        "linux": {
          "fallback": "",
          "mechanism": "spdlog_runtime",
          "rung": "1",
          "support": "supported"
        },
        "macos": {
          "fallback": "",
          "mechanism": "spdlog_runtime",
          "rung": "1",
          "support": "supported"
        },
        "windows": {
          "fallback": "",
          "mechanism": "spdlog_runtime",
          "rung": "1",
          "support": "supported"
        }
      }
    }
  ],
  "caveats": [
    "**No typed result status.** The plugin never calls a result-status setter; every capture on every OS shows `UNDECLARED / UNKNOWN /` for both actions (`docs/samples/{windows,macos,linux}.txt`).",
    "**The `info` capture artifact is real production behavior with the config sync skipped, not a broken leg.** `do_info` reads config through the module-global `g_ctx`, populated by `init()` and then kept current by `agent.cpp`'s `sync_master_config_to_plugins`; `tests/unit/test_plugin_loader.cpp:411-417` states this sync is \"the only thing that makes `agent_actions::info` return a populated `agent.plugins.count` instead of `(not set)`.\" The capture driver loads the plugin without going through that agent-boot sync path, so every sample legitimately shows all five keys as `(not set)`.",
    "**Validation failures don't match the declared schema.** A missing or unrecognized `level` emits one `error|<message>` line (agent_actions_plugin.cpp:83-86, 94-97) instead of the declared `status`/`level` columns — the YAML `result.columns` describe only the success path.",
    "**`plugins_count` is declared `int32` but always emitted as text.** Every `info` line, `plugins_count` included, is a `key|value` string (agent_actions_plugin.cpp:121-124); the code never emits a numeric type.",
    "**No privilege-model row.** `docs/agent-privilege-model.md` carries no entry for this plugin; the identities above are only what each sample capture measured, not a documented production guarantee."
  ],
  "definitions": [
    {
      "approval_mode": "auto",
      "author_roles": [
        "content-author"
      ],
      "description": "Returns agent runtime information from the config context: agent ID, version, server address, heartbeat interval, and loaded plugin count.",
      "display_name": "Agent Runtime Info",
      "execute_roles": [
        "endpoint-admin",
        "endpoint-operator"
      ],
      "gather": {
        "ttlSeconds": 300
      },
      "id": "device.agent_actions.info",
      "platforms": [
        "windows",
        "linux",
        "darwin"
      ]
    },
    {
      "approval_mode": "auto",
      "author_roles": [
        "content-author"
      ],
      "description": "Changes the agent's spdlog log level at runtime. Accepts any valid spdlog level: trace, debug, info, warn, error, critical, off. The change takes effect immediately and persists until the agent restarts or the level is changed again.",
      "display_name": "Set Agent Log Level",
      "execute_roles": [
        "endpoint-admin"
      ],
      "gather": {
        "ttlSeconds": 0
      },
      "id": "device.agent_actions.set_log_level",
      "platforms": [
        "windows",
        "linux",
        "darwin"
      ]
    }
  ],
  "description": "Agent runtime actions — set log level, query agent info",
  "how_it_works": "`set_log_level` reads the required `level` parameter, lowercases it, and validates it through spdlog's own `from_str` before calling `spdlog::set_level()` in-process — no OS call, no subprocess. On success it writes two `key|value` lines, `status|ok` then `level|<normalized>`; on a missing or unrecognized level it instead writes a single `error|<message>` line and returns 1, never touching the `status`/`level` shape. `info` reads five `agent.*` keys from the agent's config context and echoes each as `key|value`, substituting the literal `(not set)` for anything unpopulated. Both actions run identically on Windows, macOS, and Linux — the descriptor declares the same `spdlog_runtime`/`agent_config_read` mechanism at rung 1 on all three legs, because neither reaches the OS. The plugin deliberately is not a log reader or shipper: it only changes the live logger's verbosity and reads config already held in memory — no file writes, no subprocesses, no network.",
  "inputs": [
    {
      "constraints": {
        "enum": [
          "trace",
          "debug",
          "info",
          "warn",
          "error",
          "critical",
          "off"
        ]
      },
      "default": null,
      "definition_id": "device.agent_actions.set_log_level",
      "description": "The desired spdlog log level, case-insensitive: trace, debug, info, warn, error, critical, or off (e.g. \"info\").\n",
      "name": "level",
      "required": true,
      "type": "string"
    }
  ],
  "kind": {
    "collector": false,
    "gathered": true,
    "mutating": true
  },
  "leg_hash": "2688724a5dd5",
  "manifest_version": 1,
  "name": "agent_actions",
  "outputs": [
    {
      "columns": [
        {
          "description": "The agent's agent.id config value, or the literal \"(not set)\" when the config context has not populated it. Values: free text, or literal \"(not set)\".",
          "example": "(not set)",
          "name": "agent_id",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "The agent's agent.version config value, or the literal \"(not set)\" when unpopulated. Values: free text, or literal \"(not set)\".",
          "example": "(not set)",
          "name": "agent_version",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "The agent's agent.server_address config value, or the literal \"(not set)\" when unpopulated. Values: free text, or literal \"(not set)\".",
          "example": "(not set)",
          "name": "server_address",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "The agent's agent.heartbeat_interval config value, or the literal \"(not set)\" when unpopulated. Values: free text, or literal \"(not set)\".",
          "example": "(not set)",
          "name": "heartbeat_interval",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "The agent's agent.plugins.count config value. Always emitted as text, never a JSON number — the raw config string or the literal \"(not set)\". Values: free text, or literal \"(not set)\".",
          "example": "(not set)",
          "name": "plugins_count",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "int32",
          "values": null
        }
      ],
      "definition_id": "device.agent_actions.info"
    },
    {
      "columns": [
        {
          "description": "Literal \"ok\" once the level was applied. A missing or unrecognized level instead produces a single \"error|<message>\" line that carries neither this column nor level.",
          "example": "ok",
          "name": "status",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": [
            "ok"
          ]
        },
        {
          "description": "The log level the agent applied — a lowercased echo of the level parameter.",
          "example": "info",
          "name": "level",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": [
            "trace",
            "debug",
            "info",
            "warn",
            "error",
            "critical",
            "off"
          ]
        }
      ],
      "definition_id": "device.agent_actions.set_log_level"
    }
  ],
  "outputs_note": "Both actions emit `write_output()` lines of the form `key|value` rather than disk-plugin-style single pipe-delimited rows. `set_log_level` writes exactly two lines on success (`status`, then `level`); a validation failure instead writes one `error|<message>` line that matches neither declared column. `info` writes exactly five lines, one per config key, in a fixed order; an unpopulated key renders the literal `(not set)` rather than being omitted.",
  "platforms": {
    "linux": "supported",
    "macos": "supported",
    "windows": "supported"
  },
  "privileges": [
    {
      "grant": "None — both actions are in-process calls, no subprocess on either leg",
      "if_refused": "N/A — no OS permission check exists",
      "measured": "2026-09-07, bare-metal, `SYSTEM` (`docs/samples/windows.txt:1`)",
      "os": "Windows",
      "runs_as": "not documented for this plugin (no row in `docs/agent-privilege-model.md`); captured as `SYSTEM`"
    },
    {
      "grant": "None",
      "if_refused": "N/A — same as above",
      "measured": "2026-09-07, bare-metal, euid 501 (`docs/samples/macos.txt:1`)",
      "os": "macOS",
      "runs_as": "not documented; captured unprivileged, `euid 501 (jsmith)`"
    },
    {
      "grant": "None",
      "if_refused": "N/A — same as above",
      "measured": "2026-09-06, container, euid 0 (`docs/samples/linux.txt:1`)",
      "os": "Linux",
      "runs_as": "not documented; captured as root, `euid 0`, container"
    }
  ],
  "readme": "agents/plugins/agent_actions/README.md",
  "result_status": [],
  "samples": {
    "linux": {
      "actions": [
        {
          "action": "set_log_level",
          "not_captured": null,
          "params": "level=info",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 2,
          "rows": [
            "status|ok",
            "level|info"
          ],
          "truncated": false
        },
        {
          "action": "info",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 5,
          "rows": [
            "agent.id|(not set)",
            "agent.version|(not set)",
            "agent.server_address|(not set)",
            "agent.heartbeat_interval|(not set)",
            "agent.plugins.count|(not set)"
          ],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-06",
        "host_class": "container",
        "leg_hash": "2688724a5dd5",
        "os": "linux",
        "os_version": "Debian GNU/Linux 13 (trixie) aarch64",
        "privilege": "euid 0"
      }
    },
    "macos": {
      "actions": [
        {
          "action": "set_log_level",
          "not_captured": null,
          "params": "level=info",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 2,
          "rows": [
            "status|ok",
            "level|info"
          ],
          "truncated": false
        },
        {
          "action": "info",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 5,
          "rows": [
            "agent.id|(not set)",
            "agent.version|(not set)",
            "agent.server_address|(not set)",
            "agent.heartbeat_interval|(not set)",
            "agent.plugins.count|(not set)"
          ],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-07",
        "host_class": "bare-metal",
        "leg_hash": "2688724a5dd5",
        "os": "macos",
        "os_version": "macOS 26.6.2 arm64",
        "privilege": "euid 501 (jsmith)"
      }
    },
    "windows": {
      "actions": [
        {
          "action": "set_log_level",
          "not_captured": null,
          "params": "level=info",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 2,
          "rows": [
            "status|ok",
            "level|info"
          ],
          "truncated": false
        },
        {
          "action": "info",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 5,
          "rows": [
            "agent.id|(not set)",
            "agent.version|(not set)",
            "agent.server_address|(not set)",
            "agent.heartbeat_interval|(not set)",
            "agent.plugins.count|(not set)"
          ],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-07",
        "host_class": "bare-metal",
        "leg_hash": "2688724a5dd5",
        "os": "windows",
        "os_version": "Microsoft Windows NT 10.0.26200.0 x64",
        "privilege": "SYSTEM"
      }
    }
  },
  "security": [
    {
      "action": "set_log_level",
      "dispatch_class": "Mutating",
      "execute_gate": "None",
      "mutability": "Reversible",
      "operation": "Write",
      "risk_tier": "Medium",
      "securable": "Infrastructure"
    },
    {
      "action": "info",
      "dispatch_class": "ReadOnly",
      "execute_gate": "None",
      "mutability": "None",
      "operation": "Read",
      "risk_tier": "Low",
      "securable": "Infrastructure"
    }
  ],
  "source": {
    "capability_rows": [
      "server/core/src/capability_decls/plugin_action_catalogue_d.hpp"
    ],
    "changelog": [],
    "definitions": [
      "content/definitions/agent_actions.yaml"
    ],
    "plugin": [
      "agents/plugins/agent_actions/src/agent_actions_plugin.cpp"
    ],
    "privilege_row": false,
    "tests": []
  },
  "version": "0.1.0",
  "where_the_data_goes": [
    "**Instruction result only.** Rows travel over the agent's mTLS gRPC channel as the command response into the ResponseStore. The dashboard renders `agent_actions` output as a generic `Agent | Key | Value` table — it is in the `kKeyValuePlugins` set, not a custom schema (`server/core/src/result_parsing.hpp:53-67`).",
    "**Not consumed by** daily-sync inventory, the TAR warehouse, DEX, or metrics — no reference to `agent_actions` or `device.agent_actions.*` exists outside the capability catalogue, `result_parsing.hpp`, and `agent_registry.cpp`'s action-description registry.",
    "**Sensitivity.** `info` rows can carry `agent_id` (this device's own identifier) and `agent_version` (the installed agent software's version); `server_address`, `heartbeat_interval`, and `plugins_count` describe the deployment rather than a device, person, or third-party software. `set_log_level` rows carry nothing beyond a log-level name.",
    "**Siblings:** none — both actions are this plugin's whole surface; no other definition shares its config-read or log-level mechanism.",
    "**MCP / REST.** Discover: `discover_plugins` (summary) → `yuzu://plugin-docs` (this page as data) → `discover_instructions` / `get_definition(\"device.agent_actions.info\")`. Run: `execute_instruction {definition_id, parameters}`. Read: `/api/responses/{id}`, rendered as an `Agent | Key | Value` table (`server/core/src/result_parsing.hpp:53-67`)."
  ]
}
