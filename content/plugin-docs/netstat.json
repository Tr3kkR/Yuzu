{
  "actions": [
    {
      "action": "attribution",
      "definition_ids": [
        "device.network.netstat_attribution"
      ],
      "legs": {
        "linux": {
          "fallback": "",
          "mechanism": "/proc/net/* + /proc/[pid]/{comm,exe,fd}",
          "rung": "1",
          "support": "supported"
        },
        "macos": {
          "fallback": "",
          "mechanism": "libproc",
          "rung": "1",
          "support": "supported"
        },
        "windows": {
          "fallback": "",
          "mechanism": "IP Helper API + QueryFullProcessImageNameW",
          "rung": "1",
          "support": "supported"
        }
      }
    },
    {
      "action": "netstat_list",
      "definition_ids": [
        "device.network.netstat_list"
      ],
      "legs": {
        "linux": {
          "fallback": "",
          "mechanism": "/proc/net/{tcp,udp}[6]",
          "rung": "1",
          "support": "supported"
        },
        "macos": {
          "fallback": "",
          "mechanism": "libproc",
          "rung": "1",
          "support": "supported"
        },
        "windows": {
          "fallback": "",
          "mechanism": "GetExtendedTcpTable/GetExtendedUdpTable",
          "rung": "1",
          "support": "supported"
        }
      }
    }
  ],
  "caveats": [
    "**UDP's remote address is not the same sentinel on every OS.** macOS and Windows report the literal `*` with `remote_port=0` for a UDP row (`macos_socket_walk.hpp:181,185`; `netstat_plugin.cpp:579,606`). Linux instead runs UDP rows through the same parser as TCP (`netstat_plugin.cpp:203-249`), so it reports the real value parsed from `/proc/net/udp*` — normally `0.0.0.0`, not `*`. The Linux sample below has no UDP rows to demonstrate this; it follows from the shared code path.",
    "**The Linux capture returned zero rows for both actions.** `docs/samples/linux.txt` was taken in a container with no active TCP/UDP connections at capture time (`euid 0`, i.e. privileged) — this is an empty result, not a failure; the plugin has no placeholder-row convention (see Outputs) so an idle host legitimately shows nothing.",
    "**`/proc/[pid]/fd` access on Linux is per-owner, not blanket-readable.** `build_inode_to_pid_map`/`build_socket_and_proc_maps` silently skip a PID whose `fd` directory they can't open (`netstat_plugin.cpp:163-165,311-313`); under the agent's default (non-root) Linux identity, sockets owned by other users resolve `pid=-1` in `netstat_list` and empty `process_name`/`process_path` in `attribution`, with no status flagging the gap. The captured sample ran as root in a container and can't show this.",
    "**TCP state name sets differ slightly per OS.** Linux's `tcp_state_str` has no `CLOSED`; Windows's `tcp_state_str_win` alone has `DELETE_TCB`; only Linux has a plain `CLOSE` (`netstat_plugin.cpp:76-103,453-482`; `macos_socket_walk.hpp:54-82`). A consumer matching on exact state strings across a mixed fleet should treat the set as a per-OS union, not one fixed enum.",
    "**`attribution`'s macOS dedup is deliberate, not inherited by accident.** `enumerate_and_stream_attribution` explicitly passes `dedup=true` to match `netstat_list`'s semantics; the retired `sockwho` used the opposite (one row per fd-holder). Do not revert this to `dedup=false` to restore sockwho's old shape (`netstat_plugin.cpp:429-435`)."
  ],
  "definitions": [
    {
      "approval_mode": "auto",
      "author_roles": [
        "content-author"
      ],
      "description": "Enumerates all active TCP/UDP connections and listening sockets on the endpoint, resolving the owning process for each and returning protocol, local and remote addresses and ports, connection state, owning PID, process name, and executable path. Covers TCP4, TCP6, UDP4, and UDP6. On Windows uses the Extended TCP/UDP Table APIs and resolves process paths via QueryFullProcessImageNameW with a per-PID cache. On Linux scans /proc to build inode-to-PID and PID-to-process maps in a single pass. On macOS uses libproc for per-process FD enumeration with proc_name and proc_pidpath. Pipe characters in process names and paths are escaped. Useful for security investigations to identify which application owns a suspicious connection.",
      "display_name": "Network Connections with Process Attribution",
      "execute_roles": [
        "endpoint-admin",
        "endpoint-operator"
      ],
      "gather": {
        "ttlSeconds": 30
      },
      "id": "device.network.netstat_attribution",
      "platforms": [
        "windows",
        "linux",
        "darwin"
      ]
    },
    {
      "approval_mode": "auto",
      "author_roles": [
        "content-author"
      ],
      "description": "Enumerates all active TCP/UDP connections and listening sockets on the endpoint, returning protocol, local and remote addresses and ports, connection state, and owning PID. Covers TCP4, TCP6, UDP4, and UDP6. On Windows uses the Extended TCP/UDP Table APIs with per-row PID ownership. On Linux reads /proc/net/* and maps socket inodes to PIDs via /proc/[pid]/fd. On macOS uses libproc to iterate per-process file descriptors for socket types.",
      "display_name": "Network Connections (netstat)",
      "execute_roles": [
        "endpoint-admin",
        "endpoint-operator"
      ],
      "gather": {
        "ttlSeconds": 30
      },
      "id": "device.network.netstat_list",
      "platforms": [
        "windows",
        "linux",
        "darwin"
      ]
    }
  ],
  "description": "Enumerates active network connections and listening sockets",
  "how_it_works": "`netstat_list` enumerates active TCP/UDP connections and listening sockets, returning protocol, local/remote address and port, connection state, and owning PID. `attribution` runs the same enumeration and additionally resolves the owning process's name and executable path — this is the retired `sockwho` plugin's functionality, folded in as netstat's second action (#3403). `attribution` emits `netstat_list`'s 7 fields as a prefix with `process_name`/`process_path` appended (9 total); `pid` stays in its `netstat_list` position (6th field), not sockwho's old 1st-column layout.\n\nOn macOS both actions walk the same shared `agents/shared/macos_socket_walk.hpp` libproc enumeration with `dedup=true`, so a socket shared across a fork (multiple PIDs holding the same fd) collapses to one row — a deliberate match to `netstat_list`'s semantics, and a deliberate *difference* from the retired `sockwho`, which emitted one row per (pid, fd). On Linux, `attribution` builds its inode→PID and PID→process maps in one `/proc` scan rather than reusing `netstat_list`'s separate scan. On Windows, `attribution` resolves each owning PID via `QueryFullProcessImageNameW`, cached per PID for the duration of one call. `escape_pipes()` (`netstat_parsers.hpp`) escapes `|` and strips CR/LF from `process_name`/`process_path` so an attacker-controlled process name can't split one row into extra server-visible lines.\n\nNeither action takes parameters, sets a typed result status, filters by protocol/state, or schedules itself — both are one-shot, unfiltered reads dispatched on demand.",
  "inputs": [],
  "kind": {
    "collector": true,
    "gathered": true,
    "mutating": false
  },
  "leg_hash": "f31d206bb2ee",
  "manifest_version": 1,
  "name": "netstat",
  "outputs": [
    {
      "columns": [
        {
          "description": "Transport protocol and IP version of the socket.",
          "example": "tcp",
          "name": "proto",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": [
            "tcp",
            "tcp6",
            "udp",
            "udp6"
          ]
        },
        {
          "description": "The connection's local IP address, in the address family matching proto. Values: free text (IPv4 or IPv6 address).",
          "example": "100.109.177.77",
          "name": "local_addr",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "The connection's local TCP or UDP port number. Values: integer, 0-65535.",
          "example": 52882,
          "name": "local_port",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "int32",
          "values": null
        },
        {
          "description": "The connection's remote IP address. UDP sockets report the literal '*' on macOS and Windows (no fixed peer); Linux instead parses the real value from /proc/net/udp* (normally 0.0.0.0). Values: free text (IPv4 or IPv6 address) or \"*\".",
          "example": "100.123.53.121",
          "name": "remote_addr",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "The connection's remote port; always 0 for UDP (no fixed peer). Values: integer, 0-65535.",
          "example": 22,
          "name": "remote_port",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "int32",
          "values": null
        },
        {
          "description": "TCP connection state name; always empty for UDP rows (never a fabricated LISTEN). The exact name set differs slightly per OS. Values: ESTABLISHED, SYN_SENT, SYN_RECV, FIN_WAIT1, FIN_WAIT2, TIME_WAIT, CLOSE_WAIT, LAST_ACK, LISTEN, CLOSING, UNKNOWN, CLOSE (Linux only), CLOSED/DELETE_TCB (Windows only), or empty (UDP).",
          "example": "ESTABLISHED",
          "name": "state",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "PID of the process holding the socket, or -1 (Linux only) when no matching /proc/[pid]/fd inode was found. Values: integer, or -1 on Linux.",
          "example": 9653,
          "name": "pid",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "int32",
          "values": null
        },
        {
          "description": "Name of the process owning the socket (Linux: /proc/[pid]/comm; macOS: libproc proc_name, 16-char truncated; Windows: basename of the QueryFullProcessImageNameW path). Pipe and CR/LF characters are escaped/stripped. Empty when the owning process could not be resolved. Values: free text, or empty when unresolved.",
          "example": "ssh",
          "name": "process_name",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "Full executable path of the owning process (Linux: /proc/[pid]/exe symlink target; macOS: libproc proc_pidpath; Windows: QueryFullProcessImageNameW). Pipe and CR/LF characters are escaped/stripped. Empty when the owning process could not be resolved. Values: free text (absolute path), or empty when unresolved.",
          "example": "/usr/bin/ssh",
          "name": "process_path",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        }
      ],
      "definition_id": "device.network.netstat_attribution"
    },
    {
      "columns": [
        {
          "description": "Transport protocol and IP version of the socket.",
          "example": "tcp",
          "name": "proto",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": [
            "tcp",
            "tcp6",
            "udp",
            "udp6"
          ]
        },
        {
          "description": "The connection's local IP address, in the address family matching proto. Values: free text (IPv4 or IPv6 address).",
          "example": "100.109.177.77",
          "name": "local_addr",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "The connection's local TCP or UDP port number. Values: integer, 0-65535.",
          "example": 52882,
          "name": "local_port",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "int32",
          "values": null
        },
        {
          "description": "The connection's remote IP address. UDP sockets report the literal '*' on macOS and Windows (no fixed peer); Linux instead parses the real value from /proc/net/udp* (normally 0.0.0.0). Values: free text (IPv4 or IPv6 address) or \"*\".",
          "example": "100.123.53.121",
          "name": "remote_addr",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "The connection's remote port; always 0 for UDP (no fixed peer). Values: integer, 0-65535.",
          "example": 22,
          "name": "remote_port",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "int32",
          "values": null
        },
        {
          "description": "TCP connection state name; always empty for UDP rows (never a fabricated LISTEN). The exact name set differs slightly per OS. Values: ESTABLISHED, SYN_SENT, SYN_RECV, FIN_WAIT1, FIN_WAIT2, TIME_WAIT, CLOSE_WAIT, LAST_ACK, LISTEN, CLOSING, UNKNOWN, CLOSE (Linux only), CLOSED/DELETE_TCB (Windows only), or empty (UDP).",
          "example": "ESTABLISHED",
          "name": "state",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "PID of the process holding the socket, or -1 (Linux only) when no matching /proc/[pid]/fd inode was found. Values: integer, or -1 on Linux.",
          "example": 9653,
          "name": "pid",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "int32",
          "values": null
        }
      ],
      "definition_id": "device.network.netstat_list"
    }
  ],
  "outputs_note": "Pipe-delimited rows, one per socket, written via `ctx.write_output()`. `netstat_list` emits 7 fields; `attribution` emits the same 7 as a prefix with `process_name`/`process_path` appended (9 total). A leg that finds no connections emits zero rows and sets no status — there is no one-row placeholder convention here (contrast `disk_actions`); an empty action block in the sample output below means the host genuinely had nothing to report at capture time.",
  "platforms": {
    "linux": "supported",
    "macos": "supported",
    "windows": "supported"
  },
  "privileges": [
    {
      "grant": "None — `GetExtendedTcpTable`/`GetExtendedUdpTable` and `QueryFullProcessImageNameW(PROCESS_QUERY_LIMITED_INFORMATION)` need no elevation",
      "if_refused": "No typed error path: a table-query failure (return code other than `NO_ERROR`/`ERROR_INSUFFICIENT_BUFFER`) or a failed `QueryFullProcessImageNameW` is swallowed — the call returns with whatever rows it already emitted, or an attribution row with empty `process_name`/`process_path` (see `tcp|192.168.0.131|139|0.0.0.0|0|LISTEN|4||` in the Windows sample below, PID 4)",
      "measured": "2026-09-07, bare metal, as SYSTEM",
      "os": "Windows",
      "runs_as": "LocalSystem today (#1442); intended target is `NT SERVICE\\YuzuAgent`"
    },
    {
      "grant": "None beyond the agent's actual (root) identity; libproc reads need no separate entitlement",
      "if_refused": "`walk_sockets`/`resolve_proc_name_path` silently skip a PID libproc can't inspect (`proc_pidinfo`/`proc_name`/`proc_pidpath` returning ≤0); the socket, or its process fields, is simply omitted — never a typed status",
      "measured": "2026-09-07, bare metal, at euid 501 (alex) — captured unprivileged, not as the production root daemon",
      "os": "macOS",
      "runs_as": "root — the LaunchDaemon has no `UserName` key (#1455 tracks narrowing this)"
    },
    {
      "grant": "None declared — but see Caveats: `/proc/[pid]/fd` on another user's process needs a matching uid or `CAP_SYS_PTRACE`, which the \"default\" grant does not include",
      "if_refused": "`opendir()`/`ifstream` failures return silently (`if (!fd_dir) continue;` / `if (!f) return;`, `netstat_plugin.cpp:164-165,197-198`); an unresolved socket's `pid` stays `-1` (netstat_list) or its `process_name`/`process_path` stay empty (attribution) — never a typed status",
      "measured": "2026-09-06, container, at euid 0 (root) — captured privileged, not as the least-privilege agent",
      "os": "Linux",
      "runs_as": "dedicated unprivileged `_yuzu`/`yuzu` account (`docs/agent-privilege-model.md:95`, `netstat.*` row: default/default/default)"
    }
  ],
  "readme": "agents/plugins/netstat/README.md",
  "result_status": [],
  "samples": {
    "linux": {
      "actions": [
        {
          "action": "netstat_list",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 0,
          "rows": [],
          "truncated": false
        },
        {
          "action": "attribution",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 0,
          "rows": [],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-06",
        "host_class": "container",
        "leg_hash": "f31d206bb2ee",
        "os": "linux",
        "os_version": "Debian GNU/Linux 13 (trixie) aarch64",
        "privilege": "euid 0"
      }
    },
    "macos": {
      "actions": [
        {
          "action": "netstat_list",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 105,
          "rows": [
            "tcp|100.109.177.77|52882|100.123.53.121|22|ESTABLISHED|9653",
            "tcp|192.168.0.66|52771|160.79.104.10|443|ESTABLISHED|9039",
            "tcp|192.168.0.66|52775|160.79.104.10|443|ESTABLISHED|9039",
            "tcp|192.168.0.66|52773|160.79.104.10|443|ESTABLISHED|9039",
            "tcp|192.168.0.66|52789|160.79.104.10|443|ESTABLISHED|9039"
          ],
          "truncated": false
        },
        {
          "action": "attribution",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 105,
          "rows": [
            "tcp|100.109.177.77|52882|100.123.53.121|22|ESTABLISHED|9653|ssh|/usr/bin/ssh",
            "tcp|192.168.0.66|52771|160.79.104.10|443|ESTABLISHED|9039|claude|/Users/alex/.vscode/extensions/anthropic.claude-code-2.1.263-darwin-arm64/resources/native-binary/claude",
            "tcp|192.168.0.66|52775|160.79.104.10|443|ESTABLISHED|9039|claude|/Users/alex/.vscode/extensions/anthropic.claude-code-2.1.263-darwin-arm64/resources/native-binary/claude",
            "tcp|192.168.0.66|52773|160.79.104.10|443|ESTABLISHED|9039|claude|/Users/alex/.vscode/extensions/anthropic.claude-code-2.1.263-darwin-arm64/resources/native-binary/claude",
            "tcp|192.168.0.66|52789|160.79.104.10|443|ESTABLISHED|9039|claude|/Users/alex/.vscode/extensions/anthropic.claude-code-2.1.263-darwin-arm64/resources/native-binary/claude"
          ],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-07",
        "host_class": "bare-metal",
        "leg_hash": "f31d206bb2ee",
        "os": "macos",
        "os_version": "macOS 26.6.2 arm64",
        "privilege": "euid 501 (alex)"
      }
    },
    "windows": {
      "actions": [
        {
          "action": "netstat_list",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 97,
          "rows": [
            "tcp|0.0.0.0|22|0.0.0.0|0|LISTEN|5148",
            "tcp|100.123.53.121|22|100.109.177.77|53137|ESTABLISHED|5148",
            "tcp|0.0.0.0|135|0.0.0.0|0|LISTEN|1008",
            "tcp|192.168.0.131|139|0.0.0.0|0|LISTEN|4",
            "tcp|0.0.0.0|3389|0.0.0.0|0|LISTEN|9048"
          ],
          "truncated": false
        },
        {
          "action": "attribution",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 97,
          "rows": [
            "tcp|0.0.0.0|22|0.0.0.0|0|LISTEN|5148|sshd.exe|C:\\Windows\\System32\\OpenSSH\\sshd.exe",
            "tcp|100.123.53.121|22|100.109.177.77|53137|ESTABLISHED|5148|sshd.exe|C:\\Windows\\System32\\OpenSSH\\sshd.exe",
            "tcp|0.0.0.0|135|0.0.0.0|0|LISTEN|1008|svchost.exe|C:\\Windows\\System32\\svchost.exe",
            "tcp|192.168.0.131|139|0.0.0.0|0|LISTEN|4||",
            "tcp|0.0.0.0|3389|0.0.0.0|0|LISTEN|9048|svchost.exe|C:\\Windows\\System32\\svchost.exe"
          ],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-07",
        "host_class": "bare-metal",
        "leg_hash": "f31d206bb2ee",
        "os": "windows",
        "os_version": "Microsoft Windows NT 10.0.26200.0 x64",
        "privilege": "SYSTEM"
      }
    }
  },
  "security": [
    {
      "action": "netstat_list",
      "dispatch_class": "ReadOnly",
      "execute_gate": "None",
      "mutability": "None",
      "operation": "Read",
      "risk_tier": "Low",
      "securable": "Infrastructure"
    },
    {
      "action": "attribution",
      "dispatch_class": "ReadOnly",
      "execute_gate": "None",
      "mutability": "None",
      "operation": "Read",
      "risk_tier": "Low",
      "securable": "Infrastructure"
    }
  ],
  "source": {
    "capability_rows": [
      "server/core/src/capability_decls/plugin_action_catalogue_c.hpp"
    ],
    "changelog": [
      "changelog.d/3403-netstat-attribution-action.added.md"
    ],
    "definitions": [
      "content/definitions/netstat.yaml",
      "content/definitions/netstat_attribution.yaml"
    ],
    "plugin": [
      "agents/plugins/netstat/src/netstat_parsers.hpp",
      "agents/plugins/netstat/src/netstat_plugin.cpp"
    ],
    "privilege_row": true,
    "tests": [
      "tests/unit/agent/test_netstat_attribution.cpp",
      "tests/unit/test_netstat_parsers.cpp"
    ]
  },
  "version": "1.0.0",
  "where_the_data_goes": [
    "**Instruction result only.** Rows travel over the agent's mTLS gRPC channel as the command response and land in the ResponseStore (90-day default retention — `docs/yaml-dsl-spec.md:194`, `response_store.hpp:167`), queryable at `/api/responses/{id}` and dashboard-rendered: `netstat_list` uses a static 8-column schema (`result_parsing.hpp:56`), while `attribution`'s 9-column schema is resolved from the content-definition `result` schema instead of a static entry (`result_parsing.hpp:50-55`).",
    "**Not consumed by** daily-sync inventory, the TAR warehouse, DEX, or metrics — grepping `server/` and `agents/plugins/tar/` for `netstat` finds only the dashboard-schema and command-catalogue sites above; nothing runs on a schedule.",
    "**Sensitivity.** `netstat_list` rows carry local/remote IPs and a bare `pid` — enough to fingerprint the device's active network identity, but nothing else. `attribution` rows add `process_name`/`process_path`, which name installed software and, for a per-user-profile install, can embed a person's account name in the path (the macOS sample below shows a VS Code extension path under `/Users/alex/...`).",
    "**Siblings:** `network_diag` (connection/socket diagnostics), `netprobe` (active reachability probes), the retired `sockwho` (folded into `attribution`, #3403).",
    "**MCP / REST.** Discover: `discover_plugins` (summary) → `yuzu://plugin-docs` (this page as data) → `discover_instructions` / `get_definition(\"device.network.netstat_attribution\")`. Run: `execute_instruction {definition_id, parameters}`. Read: `/api/responses/{id}`."
  ]
}
