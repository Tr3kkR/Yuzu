{
  "actions": [
    {
      "action": "clear_queue",
      "definition_ids": [
        "crossplatform.printing.clear_queue"
      ],
      "legs": {
        "linux": {
          "fallback": "measured in a Debian/Ubuntu cupsd container: container root/@SYSTEM (SystemGroup root lpadmin) cancelling another user's job succeeds (status 0x0000, I93-7) — but the production Linux agent runs unprivileged (docs/agent-privilege-model.md), never root or @SYSTEM, so an ordinary non-owning cancel is correctly refused (403) before Cancel-Job is ever reached; reliable only for a job the agent's own identity owns",
          "mechanism": "IPP Cancel-Job on one job id over the CUPS Unix socket with Authorization: PeerCred",
          "rung": "1",
          "support": "constrained"
        },
        "macos": {
          "fallback": "PROVISIONAL — cupsd.conf Cancel-Job policy requires @OWNER/@AUTHKEY(system.print.operator)/@admin/@lpadmin (no @SYSTEM); header accepted by cupsd, authorisation outcome for a non-owned job UNMEASURED",
          "mechanism": "IPP Cancel-Job on one job id over the CUPS Unix socket with Authorization: PeerCred",
          "rung": "1",
          "support": "constrained"
        },
        "windows": {
          "fallback": "cancel measured only under Administrators and SYSTEM (SYSTEM is today's agent identity, #1442); a least-privileged identity cancelling any job (its own or another user's) is unmeasured, and that a PRINTER_ACCESS_USE handle suffices for it is an extrapolation - see docs/agent-privilege-model.md",
          "mechanism": "winspool SetJobW JOB_CONTROL_CANCEL on one job id",
          "rung": "1",
          "support": "supported"
        }
      }
    },
    {
      "action": "jobs",
      "definition_ids": [
        "crossplatform.printing.jobs"
      ],
      "legs": {
        "linux": {
          "fallback": "localhost:631 fallback for reads when no socket is found",
          "mechanism": "IPP Get-Jobs (which-jobs=not-completed) over the CUPS Unix socket",
          "rung": "1",
          "support": "supported"
        },
        "macos": {
          "fallback": "localhost:631 fallback for reads when no socket is found",
          "mechanism": "IPP Get-Jobs (which-jobs=not-completed) over the CUPS Unix socket",
          "rung": "1",
          "support": "supported"
        },
        "windows": {
          "fallback": "",
          "mechanism": "winspool EnumJobsW level 2",
          "rung": "1",
          "support": "supported"
        }
      }
    },
    {
      "action": "printers",
      "definition_ids": [
        "crossplatform.printing.printers"
      ],
      "legs": {
        "linux": {
          "fallback": "localhost:631 fallback for reads when no socket is found",
          "mechanism": "IPP CUPS-Get-Printers over the CUPS Unix socket (cpp-httplib)",
          "rung": "1",
          "support": "supported"
        },
        "macos": {
          "fallback": "localhost:631 fallback for reads when no socket is found",
          "mechanism": "IPP CUPS-Get-Printers over the CUPS Unix socket (cpp-httplib)",
          "rung": "1",
          "support": "supported"
        },
        "windows": {
          "fallback": "",
          "mechanism": "winspool EnumPrintersW level 2",
          "rung": "1",
          "support": "supported"
        }
      }
    }
  ],
  "caveats": [
    "**`jobs` reports only not-completed jobs; there is no completed-job history.** A job that has finished, failed, or was already cancelled by a previous call simply stops appearing — there is no lookup for it."
  ],
  "definitions": [
    {
      "approval_mode": "role-gated",
      "author_roles": [
        "content-author"
      ],
      "description": "Cancels EXACTLY ONE queued print job, identified by printer and job id (both required). This is a state-changing, Destructive+Irreversible action requiring role-gated approval — there is no purge-all/cancel-all path. On Windows, uses SetJobW(JOB_CONTROL_CANCEL). On macOS/Linux, sends an IPP Cancel-Job request over the CUPS Unix domain socket with Authorization: PeerCred, and refuses (never falls back to TCP) when no socket is found.",
      "display_name": "Cancel Print Job",
      "execute_roles": [
        "endpoint-admin"
      ],
      "gather": {
        "ttlSeconds": 60
      },
      "id": "crossplatform.printing.clear_queue",
      "platforms": [
        "windows",
        "linux",
        "darwin"
      ]
    },
    {
      "approval_mode": "auto",
      "author_roles": [
        "content-author"
      ],
      "description": "Lists queued print jobs, optionally filtered to one printer: printer, job id, owner, document name, status, submission time (ISO-8601 UTC), and size in bytes. The document field is the job's user-supplied title/filename — it is treated as instruction-result user content (server instruction-result retention only; never forwarded to daily-sync, TAR, or DEX). It exists so an operator can identify which job a subsequent clear_queue call should target.",
      "display_name": "List Print Jobs",
      "execute_roles": [
        "endpoint-admin",
        "endpoint-operator"
      ],
      "gather": {
        "ttlSeconds": 60
      },
      "id": "crossplatform.printing.jobs",
      "platforms": [
        "windows",
        "linux",
        "darwin"
      ]
    },
    {
      "approval_mode": "auto",
      "author_roles": [
        "content-author"
      ],
      "description": "Lists every printer known to the local print system: name, state (idle/processing/stopped/unknown), state reasons, whether it is the system default, make and model, uri/port, and queued job count.",
      "display_name": "List Printers",
      "execute_roles": [
        "endpoint-admin",
        "endpoint-operator"
      ],
      "gather": {
        "ttlSeconds": 120
      },
      "id": "crossplatform.printing.printers",
      "platforms": [
        "windows",
        "linux",
        "darwin"
      ]
    }
  ],
  "description": "Printer/job inventory plus a single narrowly-scoped clear_queue cancellation",
  "how_it_works": "`printers` and `jobs` are plain IPP reads: encode a request (RFC 8010, no libcups — `printing_ipp.hpp` is a from-scratch minimal codec), send it over the CUPS Unix domain socket (falling back to `localhost:631` over TCP when no socket is found), decode the response, and format the rows. On Windows the same two actions go through winspool (`EnumPrintersW`/`EnumJobsW`) instead of IPP; there is no libcups dependency on any platform. The plugin keeps no history: `jobs` only ever reports not-completed jobs, and nothing here retains a record of a job once it leaves the queue. `clear_queue` is the plugin's only mutation — it cancels exactly one job id (`SetJobW(JOB_CONTROL_CANCEL)` on Windows, IPP `Cancel-Job` over the same CUPS Unix socket on macOS/Linux), never a purge-all; the named `printer` is bound to the job on every OS — Windows because the spooler itself looks the job up on that printer, macOS/Linux because the agent first lists that printer's not-completed jobs and only sends `Cancel-Job` for an id on the list (cupsd's own `Cancel-Job` looks a job up by id alone and ignores the printer, so a nonexistent printer name with a valid id would otherwise cancel that job and report success); a human resubmitting the document from their own application is the real-world path back, not a Yuzu dispatch.",
  "inputs": [
    {
      "constraints": {
        "maxLength": 256,
        "minLength": 1
      },
      "default": null,
      "definition_id": "crossplatform.printing.clear_queue",
      "description": "The printer the job is queued on. The job must be on this printer: a job id that belongs to a different printer, or a printer that does not exist, is reported as not_found and nothing is cancelled.",
      "name": "printer",
      "required": true,
      "type": "string"
    },
    {
      "constraints": {
        "pattern": "^[1-9][0-9]{0,8}$"
      },
      "default": null,
      "definition_id": "crossplatform.printing.clear_queue",
      "description": "The job identifier to cancel, from the jobs action's job_id column.",
      "name": "job_id",
      "required": true,
      "type": "string"
    },
    {
      "constraints": {
        "maxLength": 256
      },
      "default": null,
      "definition_id": "crossplatform.printing.jobs",
      "description": "Optional printer name to filter to. When omitted, jobs across every printer are listed.",
      "name": "printer",
      "required": false,
      "type": "string"
    }
  ],
  "kind": {
    "collector": false,
    "gathered": true,
    "mutating": true
  },
  "leg_hash": "a6ac30dd4945",
  "manifest_version": 1,
  "name": "printing",
  "outputs": [
    {
      "columns": [
        {
          "description": "The printer the target job was on (as given), or \"-\" when the param itself was missing.",
          "example": "Office-LaserJet",
          "name": "printer",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "The job id targeted, or 0 when the param itself was missing/invalid.",
          "example": "42",
          "name": "job_id",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "int64",
          "values": null
        },
        {
          "description": "Values: canceled, not_found, refused, error. `canceled` means the spooler/cupsd ACCEPTED the cancel request, not that the job has already left the queue: an actively printing job can take a moment to clear, so confirm with `jobs`.",
          "example": "canceled",
          "name": "outcome",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "\"-\" on a successful cancel; otherwise a <os>:<source>:<detail> failure token, or missing_printer/invalid_job_id for a parameter validation failure.",
          "example": "-",
          "name": "detail",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        }
      ],
      "definition_id": "crossplatform.printing.clear_queue"
    },
    {
      "columns": [
        {
          "description": "The printer this job is queued on (URI on the IPP leg, printer name on Windows), or \"-\" when unavailable.",
          "example": "ipp://localhost/printers/Office-LaserJet",
          "name": "printer",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "Job identifier — the value clear_queue's job_id parameter targets. Values: 1 or greater.",
          "example": "42",
          "name": "job_id",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "int64",
          "values": null
        },
        {
          "description": "The submitting user, or \"-\" when unavailable.",
          "example": "alex",
          "name": "owner",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "The job's document name/title, or \"-\" when unavailable. User content — see this definition's retention note above.",
          "example": "hosts",
          "name": "document",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "Job status. Values: pending, held, processing, stopped, canceled, aborted, completed, unknown.",
          "example": "processing",
          "name": "status",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "ISO-8601 UTC submission timestamp, or \"-\" when unavailable.",
          "example": "2026-09-08T12:00:00Z",
          "name": "submitted_at",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "Job size in bytes, or -1 (unknown).",
          "example": "1024",
          "name": "size_bytes",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "int64",
          "values": null
        }
      ],
      "definition_id": "crossplatform.printing.jobs"
    },
    {
      "columns": [
        {
          "description": "Printer name/identifier as reported by the platform mechanism.",
          "example": "Office-LaserJet",
          "name": "name",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "Reported printer state. Values: idle, processing, stopped, unknown.",
          "example": "idle",
          "name": "state",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "Comma-joined printer-state-reasons keywords, or \"-\" when none reported. winspool has no equivalent vocabulary and always reports \"-\".",
          "example": "-",
          "name": "state_reasons",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "Whether this is the system default printer. Values: 1, 0 — a digit, never true/false.",
          "example": "1",
          "name": "is_default",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "boolean",
          "values": null
        },
        {
          "description": "Make/model string (IPP printer-make-and-model, or the winspool driver name), or \"-\" when unavailable.",
          "example": "Local Raw Printer",
          "name": "make_model",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "Printer URI (IPP printer-uri-supported, or the winspool port name), or \"-\" when unavailable.",
          "example": "ipp://localhost/printers/Office-LaserJet",
          "name": "uri",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "string",
          "values": null
        },
        {
          "description": "Number of jobs currently queued on this printer. Values: 0 or greater, or -1 (unknown — the mechanism did not report a count).",
          "example": "0",
          "name": "queued_jobs",
          "platforms": [
            "windows",
            "linux",
            "darwin"
          ],
          "type": "int64",
          "values": null
        }
      ],
      "definition_id": "crossplatform.printing.printers"
    }
  ],
  "outputs_note": "Every row is pipe-delimited, one row per printer/job. `printers`/`jobs` report `<kind>|none` when the read succeeded but nothing was found (an honest empty result, never conflated with a read failure). A field the platform mechanism did not report is `-` for a string field or `-1` for a count/size the mechanism could not determine.",
  "platforms": {
    "linux": "supported",
    "macos": "supported",
    "windows": "supported"
  },
  "privileges": [
    {
      "grant": "None for reads — `EnumPrintersW`/`EnumJobsW`/`OpenPrinterW` need no elevated right (measured under admin and `SYSTEM` only). `clear_queue` opens the printer with `PRINTER_ACCESS_USE`; cancel was measured only under Administrators and `SYSTEM` (both already elevated), and a least-privileged identity cancelling any job (its own or another user's) is **not** measured — see `docs/agent-privilege-model.md`. `EnumPrintersW` (the only call at risk of blocking against an unreachable network print server — `PRINTER_ENUM_CONNECTIONS`) is bounded (`yuzu::shared::bounded_call`, 5s); the per-printer `OpenPrinterW`/`EnumJobsW` pair is deliberately NOT — bounding a call holding a live `HANDLE` the caller closes on return would race a timed-out detached thread against that close (see `enum_jobs_raw`'s own comment) — `EnumJobsW` instead caps at 5000 rows, not wall-clock time; a genuinely unreachable per-printer RPC can still hang (tracked as a follow-up, not closed here)",
      "if_refused": "n/a — reads do not fail closed; a win32 failure or bounded-call timeout on `EnumPrintersW` reports `CONSTRAINED`, never a permission error. `clear_queue` is different: `ERROR_ACCESS_DENIED` from `OpenPrinterW` or `SetJobW` reports `PERMISSION_DENIED` (`refused`)",
      "measured": "the-rig, `SYSTEM`, 2026-09-08 (`docs/samples/windows.txt`); `clear_queue` cancel also under the admin SSH identity",
      "os": "Windows",
      "runs_as": "agent service account"
    },
    {
      "grant": "None to read",
      "if_refused": "n/a — reads do not fail closed",
      "measured": "this Mac, euid 501, 2026-09-14 (`docs/samples/macos.txt`)",
      "os": "macOS",
      "runs_as": "agent daemon — **root** by documented exception (`docs/agent-privilege-model.md:14`: launchd's LaunchDaemon has no `UserName` key; the `_yuzu` unprivileged-account model is not yet applied on macOS), not the dedicated-unprivileged-account design line 12 describes for the other two platforms"
    },
    {
      "grant": "None to read",
      "if_refused": "n/a — reads do not fail closed",
      "measured": "container, euid 0, 2026-09-14 (`docs/samples/linux.txt`)",
      "os": "Linux",
      "runs_as": "agent daemon, dedicated unprivileged account (`yuzu`), never root by design (`docs/agent-privilege-model.md:12`)"
    }
  ],
  "readme": "agents/plugins/printing/README.md",
  "result_status": [
    {
      "completeness": "full",
      "provenance": "(empty)",
      "status": "`OK`",
      "when": "every platform: a successful read, or `clear_queue` cancelling the job"
    },
    {
      "completeness": "partial",
      "provenance": "`windows:winspool:enum_printers_failed` / `windows:winspool:open_printer_failed` / `windows:winspool:enum_jobs_failed` — `\"CUPS-Get-Printers: transport failed\"` / `linux:cups:connect_failed` / `macos:cups:connect_failed` — `\"CUPS-Get-Printers: response did not decode\"` / `linux:cups:decode_failed` / `macos:cups:decode_failed` — `\"Get-Jobs: transport failed\"` / `\"Get-Jobs: response did not decode\"` — `\"CUPS-Get-Printers: unexpected status 0x⟨hex⟩\"` / `\"Get-Jobs: unexpected status 0x⟨hex⟩\"` / `linux:cups:unexpected_status` / `macos:cups:unexpected_status`",
      "status": "`CONSTRAINED`",
      "when": "Windows `printers`: `EnumPrintersW` failed or timed out (a bounded call — see Privileges and prerequisites). Windows `jobs`: the same `EnumPrintersW` failure, OR a specific printer's queue could not be opened (`OpenPrinterW` denied — NOT a bounded call), or its `EnumJobsW` read failed or was truncated at the row cap (also NOT a bounded call — see Privileges and prerequisites for why) — each is a per-printer skip, surfaced as `CONSTRAINED`/`PARTIAL` alongside whatever rows did succeed, never silently. macOS/Linux `printers`/`jobs`: the IPP round trip failed at the transport/decode step, or cupsd returned a status outside the successful range (RFC 8010 `0x0000`–`0x00FF`) — e.g. an unrecognised printer-uri, never silently read as zero rows"
    },
    {
      "completeness": "partial",
      "provenance": "`missing_printer` / `invalid_job_id` — `windows:winspool:open_printer_failed` / `windows:winspool:printer_not_found` (names with a path separator or colon) / `windows:winspool:job_not_found` / `windows:winspool:set_job_failed` — `linux:cups:connect_failed` / `macos:cups:connect_failed` — `linux:cups:decode_failed` / `macos:cups:decode_failed` — `linux:cups:no_identity` / `macos:cups:no_identity` — `\"Cancel-Job: unexpected status 0x⟨hex⟩\"` / `linux:cups:unexpected_status` / `macos:cups:unexpected_status`",
      "status": "`UNAVAILABLE`",
      "when": "`clear_queue` parameter validation failed (`printer` empty or `job_id` unparseable — a fully-refused request, no OS call made), or the cancel itself could not complete: Windows `OpenPrinterW` failed for a reason other than not-found or access-denied (spooler stopped, RPC unavailable — the Win32 error is in the status detail); Windows reported a nonexistent job (`GetJobW`, or `SetJobW` if the job finished in between — `ERROR_INVALID_PARAMETER`), which is `PARTIAL` rather than `FULL` because that code was measured only under elevated identities; Windows `OpenPrinterW` returned `ERROR_INVALID_PRINTER_NAME` for a name containing a path separator or colon (`\\\\server\\queue`, the `//server/queue` form the `printers`/`jobs` rows render, `http://…`), which the OS also returns for an unreachable server, so it is reported as `printer_not_found` but not as definitive; macOS/Linux could not resolve the agent's effective-user identity (an `error`, not a denial: the request is not sent and cupsd is never asked); or `SetJobW` failed for a reason other than access-denied or a vanished job (the Win32 error is in the status detail); macOS/Linux the IPP Cancel-Job round trip failed at the transport/decode step, or cupsd returned a status this plugin doesn't otherwise classify"
    },
    {
      "completeness": "full",
      "provenance": "`windows:winspool:printer_not_found` (plain local names) — `linux:cups:not_found` / `macos:cups:not_found`",
      "status": "`UNAVAILABLE`",
      "when": "`clear_queue`: the target printer or job no longer exists — Windows `OpenPrinterW` (`ERROR_INVALID_PRINTER_NAME`) for a plain local printer name (no path separator or colon); macOS/Linux: the named printer does not exist (cupsd answers the pre-cancel `Get-Jobs` with `client-error-not-found`, IPP `0x0406`), or the job is not on that printer's not-completed queue (including a job that was already cancelled or completed), or cupsd's own `0x0406` from `Cancel-Job`"
    },
    {
      "completeness": "full",
      "provenance": "`linux:cups:socket_unavailable` / `macos:cups:socket_unavailable` — `windows:winspool:access_denied` — `linux:cups:access_denied` / `macos:cups:access_denied`",
      "status": "`PERMISSION_DENIED`",
      "when": "`clear_queue` refused before or after attempting the cancel: macOS/Linux found no CUPS Unix socket at all (refuses outright rather than falling back to the `localhost:631` TCP path `printers`/`jobs` use for reads — a mutating action never takes that fallback); or the cancel was authorized-denied — Windows `OpenPrinterW` or `SetJobW` returned `ERROR_ACCESS_DENIED`, macOS/Linux cupsd's HTTP layer (401/403) or IPP response (`client-error-forbidden`/`not-authenticated`/`not-authorized`, `0x0401`/`0x0402`/`0x0403`) refused the caller. A `client-error-bad-request` (`0x0400`) is a protocol fault, not an authorization decision, and surfaces as `unexpected_status` — see Privileges and prerequisites for who is actually authorized to cancel whose job on each platform"
    }
  ],
  "samples": {
    "linux": {
      "actions": [
        {
          "action": "printers",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "FULL",
            "provenance": "",
            "status": "OK"
          },
          "row_count": 1,
          "rows": [
            "printer|yuzu_test|stopped|paused|0|Local Raw Printer|ipp://localhost:631/printers/yuzu_test|1"
          ],
          "truncated": false
        },
        {
          "action": "jobs",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "FULL",
            "provenance": "",
            "status": "OK"
          },
          "row_count": 1,
          "rows": [
            "job|ipp://localhost:631/printers/yuzu_test|1|-|-|pending|2026-09-14T17:27:40Z|1024"
          ],
          "truncated": false
        },
        {
          "action": "clear_queue",
          "not_captured": "Destructive/Irreversible: mutating action, not executed in a docs capture",
          "params": "",
          "rc": 0,
          "result_status": null,
          "row_count": 0,
          "rows": [],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-14",
        "host_class": "container",
        "leg_hash": "a6ac30dd4945",
        "os": "linux",
        "os_version": "Debian GNU/Linux 13 (trixie) aarch64",
        "privilege": "euid 0"
      }
    },
    "macos": {
      "actions": [
        {
          "action": "printers",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "FULL",
            "provenance": "",
            "status": "OK"
          },
          "row_count": 1,
          "rows": [
            "printer|none"
          ],
          "truncated": false
        },
        {
          "action": "jobs",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "FULL",
            "provenance": "",
            "status": "OK"
          },
          "row_count": 1,
          "rows": [
            "job|none"
          ],
          "truncated": false
        },
        {
          "action": "clear_queue",
          "not_captured": "Destructive/Irreversible: mutating action, not executed in a docs capture",
          "params": "",
          "rc": 0,
          "result_status": null,
          "row_count": 0,
          "rows": [],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-14",
        "host_class": "bare-metal",
        "leg_hash": "a6ac30dd4945",
        "os": "macos",
        "os_version": "macOS 26.6.2 arm64",
        "privilege": "euid 501"
      }
    },
    "windows": {
      "actions": [
        {
          "action": "printers",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 4,
          "rows": [
            "printer|OneNote (Desktop)|idle|-|0|Send to Microsoft OneNote 16 Driver|nul:|0",
            "printer|Microsoft XPS Document Writer|idle|-|0|Microsoft XPS Document Writer v4|PORTPROMPT:|0",
            "printer|Microsoft Print to PDF|idle|-|1|Microsoft Print To PDF|PORTPROMPT:|0",
            "printer|Fax|idle|-|0|Microsoft Shared Fax Driver|SHRFAX:|0"
          ],
          "truncated": false
        },
        {
          "action": "jobs",
          "not_captured": null,
          "params": "",
          "rc": 0,
          "result_status": {
            "completeness": "UNKNOWN",
            "provenance": "",
            "status": "UNDECLARED"
          },
          "row_count": 1,
          "rows": [
            "job|none"
          ],
          "truncated": false
        },
        {
          "action": "clear_queue",
          "not_captured": "Destructive/Irreversible: mutating action, not executed in a docs capture",
          "params": "",
          "rc": 0,
          "result_status": null,
          "row_count": 0,
          "rows": [],
          "truncated": false
        }
      ],
      "stamp": {
        "date": "2026-09-08",
        "host_class": "bare-metal",
        "leg_hash": "a6ac30dd4945",
        "os": "windows",
        "os_version": "Windows 10.0.26200 x86_64",
        "privilege": "LocalSystem (elevated)"
      }
    }
  },
  "security": [
    {
      "action": "printers",
      "dispatch_class": "ReadOnly",
      "execute_gate": "None",
      "mutability": "None",
      "operation": "Read",
      "risk_tier": "Low",
      "securable": "Inventory"
    },
    {
      "action": "jobs",
      "dispatch_class": "ReadOnly",
      "execute_gate": "None",
      "mutability": "None",
      "operation": "Read",
      "risk_tier": "Low",
      "securable": "Inventory"
    },
    {
      "action": "clear_queue",
      "dispatch_class": "Destructive",
      "execute_gate": "AdminOrApproval",
      "mutability": "Irreversible",
      "operation": "Write",
      "risk_tier": "Medium",
      "securable": "Infrastructure"
    }
  ],
  "source": {
    "capability_rows": [
      "server/core/src/capability_decls/plugin_action_catalogue_printing.hpp"
    ],
    "changelog": [
      "changelog.d/wave9-pr91b-printing.added.md",
      "changelog.d/wave9-pr91b2-printing-clear-queue.added.md"
    ],
    "definitions": [
      "content/definitions/printing.yaml"
    ],
    "plugin": [
      "agents/plugins/printing/src/printing_ipp.hpp",
      "agents/plugins/printing/src/printing_parsers.hpp",
      "agents/plugins/printing/src/printing_plugin.cpp"
    ],
    "privilege_row": true,
    "tests": [
      "tests/unit/server/test_printing_clear_queue_gate.cpp",
      "tests/unit/test_printing_local_dispatcher.cpp",
      "tests/unit/test_printing_parsers.cpp"
    ]
  },
  "version": "1.0.0",
  "where_the_data_goes": [
    "**Instruction result.** Every row is server instruction-result data — the standard retention policy, served over REST `/api/responses`. Nothing here is a durable server-side table of its own.",
    "**Not consumed by** daily-sync, TAR, DEX, or metrics — the plugin never runs on a schedule outside an explicit instruction dispatch, and there is no agent daily-sync source for print queues.",
    "**Sensitivity.** `jobs.document` is the job's user-supplied document title/filename — it is treated as user content: instruction-result retention only, never forwarded to daily-sync, TAR, or DEX. `jobs.owner` is a local account name on the target device.",
    "**Siblings:** `crossplatform.printing.printers`, `crossplatform.printing.jobs`."
  ]
}
