name: Zizmor GitHub Actions static analysis

# Zizmor (https://docs.zizmor.sh) is a static analyzer for GitHub Actions
# workflow YAML. It catches template-injection (`${{ github.event.* }}`
# interpolated into shell), over-privileged GITHUB_TOKEN grants, mutable
# action tag references that bypass SHA-pinning, artifact-exfil patterns,
# dangerous triggers (`pull_request_target` without ref discipline), etc.
#
# Triggered on workflow-touching PRs + pushes to main + weekly + manual.
# Findings are uploaded as SARIF to the GitHub Security tab (category
# `zizmor`) so they live alongside CodeQL and Scorecard findings.
#
# References:
#   - https://github.com/zizmorcore/zizmor
#   - https://github.com/zizmorcore/zizmor-action
#   - LLVM reference: https://github.com/llvm/llvm-project/blob/main/.github/workflows/zizmor.yml

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/**'
      - '.github/actions/**'
      - '.github/workflows/zizmor.yml'
  pull_request:
    branches: [main, dev]
    paths:
      - '.github/workflows/**'
      - '.github/actions/**'
      - '.github/workflows/zizmor.yml'
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  zizmor:
    name: Zizmor workflow audit
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      security-events: write
      contents: read
      actions: read

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # Deterministic guard against the deprecated `save-always: true`
      # input on `actions/cache`. The cache maintainers documented it as
      # "does not work as intended" and recommend a split
      # `actions/cache/restore` + `actions/cache/save` (gated on
      # `if: always() && cache-hit != 'true'`) instead. This step fails
      # the workflow if `save-always` reappears in any workflow YAML so
      # the non-use becomes structural rather than convention.
      # Reference: https://github.com/actions/cache/tree/main/save#always-save-cache
      - name: Guard — no save-always on actions/cache
        run: |
          set -eu
          # Match only the actual YAML key (line begins, after optional
          # whitespace, with `save-always:`). Comments containing the
          # phrase — including this one — are excluded by the leading
          # whitespace + literal `s` anchor. `|| true` keeps set -e happy
          # on the no-match case.
          hits=$(grep -rIn --include='*.yml' --include='*.yaml' \
            -E '^[[:space:]]*save-always:' \
            .github/workflows .github/actions 2>/dev/null || true)
          if [ -n "$hits" ]; then
            echo "::error::save-always: is deprecated and forbidden — see https://github.com/actions/cache/tree/main/save#always-save-cache"
            echo "$hits"
            exit 1
          fi
          echo "save-always check: clean"

      # Control-plane guard for the tracker close automation (ADR-3001 A1 §9:
      # the deliberate replacement for the descoped CODEOWNERS gate). The
      # tampering that matters is exactly three shapes: deleting the workflow,
      # putting a `paths:` filter on it (a filtered close workflow silently
      # stops closing), or moving it off the trusted `push:` trigger (a
      # pull_request run for a fork/Dependabot PR gets a read-only token and
      # the close job 403s -- see A1 §3). Deterministic, needs no admin.
      - name: Guard — close-linked-issues control plane
        run: |
          set -eu
          f=.github/workflows/close-linked-issues.yml
          if [ ! -f "$f" ]; then
            echo "::error::$f is missing — the tracker close automation must not be deleted (ADR-3001 A1 §9)"
            exit 1
          fi
          if grep -nE '^[[:space:]]*paths(-ignore)?:' "$f"; then
            echo "::error::$f carries a paths filter — a filtered close workflow silently stops closing (ADR-3001 A1 §9)"
            exit 1
          fi
          if ! grep -qE '^[[:space:]]*push:' "$f"; then
            echo "::error::$f is not push:-triggered — pull_request runs for fork/Dependabot PRs get a read-only token (ADR-3001 A1 §3)"
            exit 1
          fi
          if grep -nE '^[[:space:]]*(pull_request|pull_request_target|workflow_call):' "$f"; then
            echo "::error::$f gained a PR-context trigger — forbidden for the close automation (ADR-3001 A1 §3)"
            exit 1
          fi
          if grep -nE '^[[:space:]]*(workflow_dispatch|schedule):' "$f"; then
            echo "::error::$f gained a dispatch/cron trigger — dev-only workflows accept neither until promoted to main, and the close automation must stay push:-only (ADR-3001 Costs; A1 §3)"
            exit 1
          fi
          echo "close-linked-issues control-plane check: clean"

      - name: Run zizmor
        uses: zizmorcore/zizmor-action@6599ee8b7a49aef6a770f63d261d214911a7ce02 # v0.6.0
        with:
          # Audit everything under .github/workflows/ and reusable
          # composite actions. The action uploads SARIF itself.
          inputs: ./.github
