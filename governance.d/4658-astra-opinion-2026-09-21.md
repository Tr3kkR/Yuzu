# External review: Astra (gpt-6-astra) opinion on the #4658 finish advice

Model: gpt-6-astra (Astra), reasoning high, read-only sandbox, static reads only. Reviewed branch fix/spark-4658-file-worker-pass-failure-backoff at f3ac2e566 (base 09e738db5). Produced 2026-09-21 by the codex skill in opine mode for the #4658 governance run; it is an external MODEL opinion, not a person, and not a governance approval. Verbatim output follows.

I agree with the overall finish: keep #4658 focused, track gap (a), put its flip-gate record in this PR, preserve the commits, and obtain independent Windows execution. **I disagree with several important parts of Fable’s reasoning and proposed wording.**

This opinion is based on static reads at `f3ac2e566`, with `origin/dev` at `756890eeb`. The worktree was clean; there are 26 commits and 11 changed files, with no overlap against the upstream changes since `09e738db5`. I ran no builds, tests, mutations, governance, network calls, or git writes. Windows results below are records I inspected, not executions I performed. Current issue bodies, labels, deduplication results, and remote PR status are **UNVERIFIED**.

**1. Recommendations: agree, partly, or disagree**

| Evidence | Fable recommendation | Opinion |
|---|---|---|
| `guardian_engine.cpp:1895`, `:1987`; design doc `:1350` | One new issue for gap (a), overloaded inert state, and N2 | **AGREE**, subject to the operator confirming deduplication. These belong together. Correct the proposed design and acceptance criteria as described below. |
| `spark_file.cpp:2525`, `:3313` | Retain the final-wake override and focused implementation | **AGREE.** It represents an explicit retry obligation correctly. Per-producer gating would complicate the implementation without fixing a demonstrated remaining defect. |
| `spark_file.cpp:949`; `guardian_spark_runtime.cpp:1223` | Boot-time capability field makes rules Accepted pending establishment | **PARTLY.** Separate admission capability from runtime health, but Guardian receipts do not wait for OS establishment. This is the largest error in the advice. |
| `guardian_engine.cpp:795`; `guardian_arm_ack.cpp:357` | Heartbeat re-sweep as a smaller fallback | **DISAGREE with “smaller.”** It adds policy reconstruction, iteration, acknowledgment ownership, removal handling, and bounded-work requirements. Prefer correcting classification. |
| `metrics.md:1332`, `:1337`, `:1414` in the design doc | Reuse #2084/#4279 for alerting and counters | **PARTLY.** Sensible consolidation if those issues actually cover the work. Their live scope is UNVERIFIED. Keep alert activation a concrete F14 obligation. |
| `spark_file.cpp:2921`; `spark_mechanism.hpp:401` | Bundle helper, log rate limiting, cause, and missing test into draft B | **PARTLY.** Consolidation is reasonable, but the missing test needs no new production seam. Shared constants and richer diagnostics are not automatically F14 blockers merely because they share an issue with operational hardening. |
| Design doc `:1403` | Document blocked logging and extend #4666 | **AGREE with qualification.** The running worker can stall indefinitely. The 20-second deadline limits shutdown, not runtime unavailability. Confirm the issue covers mechanism workers before linking. |
| `guaranteed-state.md:459`; ledger `:43` | Reuse #4608 for flushing | **PARTLY.** Appropriate if its scope matches. Documentation repairs diagnosis; it does not fix flushing. |
| `spark_file.cpp:2534` | Do not change sub-millisecond polling | **AGREE on scope; PARTLY on disposition.** The bounded polling is real. Do not record it as disproven. Decline the optional rounding improvement with its bound stated. |
| `spark_file.cpp:2586`; tests `:13318` | No post-dispatch seam exists; defer qe-3(d) | **DISAGREE.** An existing hook throws inside `run_off_lock()`, and an existing test uses it. Narrow the gap to missing assertions for the new counter/backoff behavior. |
| Ledger `:41`; implementer `PROGRESS.md:46` | Preserve evidence and independently run final code | **AGREE.** Durability and independence are separate requirements. Neither checksums nor moving logs into Git makes author-run results independent. |
| Flip gate on `origin/dev`, `:1524`, `:1546` | Put the six-field entry in this PR | **AGREE**, with substantial corrections to the draft detector and acknowledgment claims. The extra narrative paragraph is optional; I would omit it to reduce duplication. |
| #4659 diff against `756890eeb`; design doc `:1401` | Land #4658 before #4659; merge, never rebase; retain commits | **AGREE.** Both branches change the design doc and mechanism tests. Exact merge-conflict results remain UNVERIFIED here. |
| Ledger `:1`, `:60`; governance schema `:1723` | Repair the ledger before opening the PR | **AGREE on repair; DISAGREE with several proposed repairs.** Preserve native labels, do not inflate independent-reporter counts, and do not mark partially addressed findings wholly fixed. |
| h4 full logs; `flake-retry.py:12` | Treat named suite failures as non-blocking reruns | **DISAGREE with blanket exoneration.** The evidence shows failures can occur without PF tests. It does not prove this change cannot increase their frequency. |

I also agree with retaining the CPU assertions, leaving suite runtime alone, avoiding a quarantine-list change here, and performing no filing or publishing in this consult.

**2. Gap (a): the design I would choose**

[guardian_engine.cpp:1895](/home/dgr/yuzu-4658-file-backoff/agents/core/src/guardian_engine.cpp:1895) supports the diagnosis: with Spark preferred, runtime `inert` removes the type from `supported`; the Unsupported branch withdraws legacy enforcement, detaches Spark, records the rule, and returns `Inert`. `apply_rules()` counts that outcome as applied. Clearing the mechanism bit does not trigger reconciliation.

**Separate arm capability from runtime degradation. Preserve the existing heartbeat meaning.**

A field such as `start_refused` can work, with these corrections:

- Populate it for **every mechanism participating in this filter**, including Service. File/Registry setters alone would misclassify a boot-inert Service mechanism as armable. Service’s existing producers are at `spark_service.cpp:231`, `:238`, `:1493`, and `:1501`.
- Define it as the outcome of facility initialization for the active lifecycle, not “this object once experienced a failed start, forever.” File explicitly permits retrying a failed initialization before a running lifecycle exists (`spark_file.cpp:697`).
- Keep `stats().inert` as the existing union for heartbeat consumers and #4659’s overlay.
- Update `guardian_spark_bridge.hpp:102`, whose present contract explicitly requires the heartbeat’s non-inert filter.
- Retain the single reconciliation chokepoint and the no-silent-fallback rule.

**Fable conflates three states:**

| State | What it establishes |
|---|---|
| Guardian `Accepted` receipt | A runtime arm claim remains unresolved. |
| Guardian `Committed` / `Armed` | The runtime adopted a successful backend subscription. |
| Spark `SubscriptionEstablishment` | The mechanism’s separately reported OS coverage and establishment timestamp. |

The code demonstrates that separation:

- `guardian_spark_backend.hpp:90` forwards to `SparkEngine::arm()`.
- `spark_file.cpp:949` accepts a watch into Deferred state after admission refusal; `:959` also accepts certain immediate establishment failures into retry state.
- `guardian_spark_runtime.cpp:1223` marks the adopted subscription `Committed`.
- `spark_engine.cpp:2156` exposes establishment through a separate query.
- `guardian_arm_ack.cpp:357` examines receipts, not coverage.

Consequently, **the capability-field fix can allow a rule to become Committed while its OS establishment is still pending**. That preserves the existing acknowledgment contract. It does not create the establishment gate Fable describes.

The K waiver is narrower still. `guardian_spark_runtime.cpp:1767` requires a dispatched, timed-out claim that remains the FIFO head. A worker’s three failed passes, or `coverage == None`, does not satisfy that predicate. #4659 only overlays the establishment query; it does not change receipts.

I would therefore leave acknowledgment/K semantics unchanged and explicitly test:

- Full sync during File and Registry runtime-inert episodes.
- Successful subscription acceptance while establishment remains pending.
- Recovery without another policy push.
- Genuine backend timeout retaining the existing K behavior.
- Removal, disablement, superseding policy, and stop during the episode.
- Production boot ordering: wiring before `start_local()`. The usual reconcile fixture does not exercise that ordering (`guardian_engine.cpp:490`).

Fable’s “armed within 30 seconds of the first successful pass” criterion needs qualification. One successful pass may be a no-op; backend retries have their own schedules, and absent targets legitimately retain `coverage == None`. Require eventual coverage under a recovered, available backend and explicit scheduling assumptions. Separately require that runtime degradation never creates permanent Unsupported placement.

I would reject a synchronous worker-to-Guardian reconciliation callback. However, Fable names the wrong lock: the tripwire protects **GuardianEngine’s `mtx_`**, not the runtime’s `registry_mu_` (`guardian_spark_runtime.cpp:865`; Guardian design `:2471`). A queued, coalesced notification is technically possible, but unnecessary for the preferred classification fix.

Finally, N2 does **not** become true automatically. A registered File mechanism whose IOCP initialization failed still exists on that host; “has no mechanism … routine cross-platform gap” remains misleading. Distinguish absent implementation, initialization refusal, and transient degradation.

**3. Independently derived severities**

The derivation below uses [PREAMBLE.md:59](/home/dgr/gov-4658/PREAMBLE.md:59), including I5(c), and its definitions of I4 and E6. All my behavioral conclusions are **likely**, based on source inspection.

| Finding | Trigger | Impact and exposure | My derivation |
|---|---|---|---|
| sec-F2 stale deadline, before its fix | A failed pass’s deadline expires during its tail or between clock samples; no independent obligation remains to wake the worker | I5 + I8; recorded E0 + E5 | **HIGH through I5(c)** under the recorded exposure facts. |
| sre8-1 misleading log recipe | Operator uses missing failed-pass lines to distinguish boot refusal from runtime failure, while those lines remain buffered | I4; E3 + E5 | **HIGH** for the former guidance. |
| ER8-2 detector parentheticals | Operator uses the endorsed Linux sum comparison on an all-inert fleet and receives an empty result | I4; E3 | **HIGH** for the former guidance. |
| Gap (a) today | Runtime-inert episode overlaps reconciliation with Spark preferred | I1/I3 as recorded; E4 + E5 + E6 | **LOW after E6**. |
| Gap (a) after enabling Spark enforcement | Same overlap on the live enforcement path | E6 no longer applies | **HIGH**. |

For **sec-F2**, I disagree with the “not a closed state” exemption. The retry/recovery state machine loses its autonomous progress obligation and can wait indefinitely. An unrelated control wake rescuing it does not mean it was never wedged. The table does not require an irrecoverable process-wide deadlock.

There is a separate exposure question: Fable simultaneously says no production pass can fail because nothing arms File, yet retains E0/E5 and rejects E6. Those positions require reconciliation. **A live heartbeat reader alone does not prove reachability of the bad writer state; an unarmed mechanism alone is not a complete E6 proof either.** If a complete production-call-path proof establishes E6 for this historical defect, apply its LOW cap explicitly. It is not a reason to suppress I5(c) and manufacture MEDIUM.

For the two **guidance defects**, a named damaging action is not mandatory: I4 also covers guidance that fails while concealing the real state. Empty detector results and an invalid negative-log inference fit that alternative. “Eventually flushes,” “another signal exists,” and “it was a parenthetical” do not make the recommended diagnostic reliable.

Both guidance defects and sec-F2 have recorded fixes. Correcting their historical derivation does not itself establish a remaining blocker.

For **gap (a)**, E6 is stronger: `try_spark` is explicitly gated by `prefer_spark_`, and production construction leaves it false. The specific wrong placement outcome cannot occur through that branch today. I would defend LOW today and HIGH at the flip, with an explicit tracked precondition.

**4. PR path and DGRHP evidence**

The inspected graph supports preserving the 26 commits and landing #4658 first. Upstream touches none of its 11 changed files. #4659 modifies `spark_engine.cpp` and adds the inert establishment overlay, plus changes to the shared design document and tests.

The second lander must reconcile the **meaning**, even if Git merges the tests automatically:

- File now has runtime-inert episodes.
- File’s `pass_failed` belongs in the first-reader failure-detector rule.
- The establishment overlay does not change Guardian acknowledgment.
- Combined tests must run on Windows.

I did not execute a merge simulation. In particular, Fable’s `git merge-tree --write-tree` is not a read-only check: it writes Git objects.

I would use the six-field flip-gate entry **in this PR**, after the tracking issue exists. It should say “not risk-accepted.” I would omit the extra paragraph unless it adds information the entry cannot carry.

The DGRHP set is broadly right, but I would strengthen its evidential focus:

1. Independently synchronize and identify the final revision, build with the supported MSVC workflow, and record source/binary identity.
2. Run the passfail filter three times and `[spark][windows]` once.
3. Run the **old stale-deadline mutant** against PF-11/PF-12. That directly checks the disputed fix added after the earlier independent runs.
4. Run the proposed `min` mutant, retaining the CPU assertions.
5. Restore pristine sources, rebuild, re-hash, and rerun green.
6. Run the full agent suite once; preserve any failure and its rerun.
7. Compile and run the Linux agent suite for Linux/header integration. That does not establish Windows or macOS correctness.

Do not insist on exactly 152 assertions: conditional timing checks can change assertion counts. Record executed cases, skipped checks, exit status, and the particular red assertions.

The branch is unpushed, so Fable’s `fetch`/`checkout` recipe cannot obtain its new commits from origin by itself. Use an explicitly identified transfer or existing synchronized checkout.

The h4 artifacts do show three successful suites with PF tests and two successful suites without them, with one failure in the third configuration. For example:

- `d4658_h4_testlog_all_1.txt:11067`: 3,483 cases, no failed cases.
- `d4658_h4_testlog_nopf_2.txt:10976`: 3,469 cases, one failed case.

That supports **“PF tests are not necessary for a failure.”** It does not support Fable’s proposed PR sentence “PF tests are not the cause.” No reviewer’s Windows execution is evidenced here. Dave’s execution would be independent of the implementing agent, while still not constituting a non-author governance adjudication.

**5. Blind spots and corrections from reading the worker**

[ spark_file.cpp:2525 ](/home/dgr/yuzu-4658-file-backoff/agents/core/src/spark_file.cpp:2525) implements the right scheduling invariant: an open episode owns a retry, including when its deadline has passed. `any = true` preserves that obligation. Successful passes clear it; failed passes restamp it. Stop is checked before absorption; real completions retain their separate consumption path.

I found these important qualifications:

- **Existing post-dispatch injection was missed.** `spark_file.cpp:2586` calls `emit_bookkeeping_hook` after probe launches, inside `run_off_lock()`. `test_spark_mechanism.cpp:13318` injects precisely that throw. The test already exercises recovery, although it does not pin #4658’s counter/backoff contract. Extend that scenario rather than inventing a production tail hook or describing the whole path as untested.

- **The zero-cadence finding is false through the supported seam.** `apply_test_controls()` accepts cadence overrides only when positive (`spark_file.cpp:1203`). Zero leaves the prior value unchanged. Do not add Fable’s proposed zero-cadence ledger row.

- **The “only failed completions go dark” statement is too narrow.** A successful completion whose recovery-time reissue fails also loses its handle and enters Deferred (`spark_file.cpp:3081`). Ancestor reissue failure similarly leaves a dead marker (`:3049`). This belongs in the failed-completion/failed-rearm characterization.

- **Thirty seconds is a retry-delay cap, not a universal delivery bound.** The override postpones health edges, establishment reports, resyncs, confirmation work, and dead-ancestor processing. Continuing failing real completions can repeatedly move the deadline; synchronous tail work can delay processing beyond it. Documenting “up to 30 seconds” requires the assumptions that no further failures reset the deadline and the worker can run.

- **Control wakes still consume work.** Absorption avoids the full pass, but each wake re-enters the loop and recomputes deadlines across the watch collections (`:2439`, `:2500`, `:3313`). “No full pass” does not mean zero CPU or bounded wake-queue pressure under a control-wake storm. This is a characterization concern, not evidence of an unbounded production leak.

- **Allocation churn is plausible; unbounded RSS is not established.** The five reservations at `:3266` recur per completion pass, but that pass’s work is destroyed at `:3298`. Measure allocation rate and allocator retention under the compound storm. Do not infer linear retained memory growth merely from repeated `reserve()` calls.

- **Existing test-control concurrency restrictions matter.** The API says set-then-use with no concurrent-access support (`spark_mechanism.hpp:387`). The off-lock hook reader at `spark_file.cpp:2586` is not synchronized merely because `apply_test_controls()` writes under `mu_`. New tests should install that hook before start and toggle captured atomics. I have not established a violating interleaving in the current PF suite.

- **#4659’s storm is a different scenario.** Its test at `5a195da96:test_spark_mechanism.cpp:8535` parks the establishment sink and joins 64 keys onto one directory. That is useful overlap, but it does not close a multi-directory completion storm with repeated pass failure and log pressure.

I did not find a new, demonstrated production ownership defect introduced by this diff. That is a static-review conclusion, not proof that the Windows lifecycle is defect-free.

**6. Ledger: corrections I would require in the record**

[governance ledger:1](/home/dgr/yuzu-4658-file-backoff/governance.d/4658-spark-file-worker-pass-failure-backoff.oCEbO1.jsonl:1) contains 76 rows, 44 finding identities, and five `adjudicated_by` fields. Folding supersessions leaves the 12 open findings Fable lists.

Several proposed repairs need changing:

- **Preserve `severity_native`.** The schema at `.claude/skills/governance/SKILL.md:1723` explicitly preserves the reporter’s original vocabulary. `LOW` native and `SHOULD` derived are not inherently inconsistent. Correct derived facts/mapping and explain the disagreement; do not rewrite the original reviewer’s label to MEDIUM.

- **Do not change independent reporters to three.** The schema at `:1729` counts discovery without prior exposure to the finding. `GATE3_FINDINGS.md:3` expressly says those reviewers were shown Gate 2’s findings. Independent portable reproductions add corroboration, not independent discovery.

- **qe-3 is only partially closed.** Ledger `:60` says `(a)(b)(c)` fixed and `(d)` remains, but marks the finding `fixed`. Split out the residual or retain a truthful partial/open disposition. Also correct the residual’s explanation to acknowledge the existing seam and test.

- **“Fixed by documentation” depends on the original obligation.** Rows `:49` through `:51` leave behavior unchanged. They may close accurately scoped disclosure findings. They cannot close an underlying liveness or observability defect just by labeling it `absence`. UP-2’s documentation and runtime-stall halves should be separate.

- **co-1 has a genuine derivation mismatch.** It records I9/E0 with `severity_mapped: SHOULD`. Those facts derive INFO/NICE. Either identify the actual stronger obligation/impact or correct the mapping. Evidence durability remains worthwhile regardless.

- **The resource-ledger closure depends on an uncommitted artifact.** Row `:64` says the Gate 1 addendum must be carried into the PR body. Make that evidence retrievable when the PR opens; do not assume the JSONL summary substitutes for the ownership analysis.

- **Correct the I4/I5(c) adjudication record.** “Fixed rather than adjudicated down” does not resolve contradictory historical severity reasoning. Record the competing derivation and the actual basis selected.

- **Do not fabricate terminal tracking.** An intended issue/comment is not `deferred-to-issue` or `linked-to`. Conversely, leaving a row honestly open while awaiting operator action is preferable to a false terminal state.

The author names and same-session reviewers do not establish independent approval. The ledger itself admits this limitation. I would not call that admission dishonest; I would reject using it as proof of separation.

This consult supplies independently reasoned analysis, not a completed governance gate or approval attestation. If retained as external review evidence, preserve this response with its reviewed SHA and a retrievable reference.

**7. D1, D2, and the order I would follow**

**D1: Yes.** Have the operator file one gap (a) issue before opening the PR, after confirming deduplication. Include capability/runtime-health separation, the misleading Guardian log, acknowledgment semantics, recovery, lifecycle behavior, and File/Registry parity. P2 with an explicit F14 blocker is defensible; matching sibling priorities is not itself a severity derivation.

**D2: Yes.** Add the six-field section-5 entry in this PR. Correct Fable’s draft:

- The fleet gauges are aggregates, not endpoint-correlated evidence. `instance` identifies the scraped server, not an endpoint.
- `mechanism="file"|"registry"` is not valid PromQL syntax.
- Absence of a later log line is not reliable proof of absent reconciliation.
- “Owner: future fix author” is not an assigned owner.
- State the actual compensating control, the tracking issue, and “not risk-accepted.”
- Omit the accepted-pending-establishment claim.

**Ranked next actions**

1. Correct the gap (a) design note, severity reasoning, ledger semantics, and evidence claims.
2. File the consolidated gap issue and add its six-field flip-gate entry in this PR.
3. Close or explicitly track the narrowly defined post-dispatch counter/backoff coverage gap using the existing seam.
4. Perform the independent final Windows red/green set, particularly PF-11/PF-12 against the stale-deadline mutant; retain failures and provenance.
5. Make curated evidence and the resource-ledger addendum durably retrievable.
6. Keep the commits intact; have the authorized operator/orchestrator open against `dev`, with #4658 landing before #4659 and the second lander testing the combined result.

**I would not** rebase or squash, redesign Guardian inside #4658, add a synchronous recovery callback, invent a new tail hook, file the zero-cadence finding, infer host identity from fleet gauges, treat a successful rerun as causal exoneration, or describe this opinion as merge approval.