## Review — approving (no CRITICAL/HIGH)

Round 4, two-sided (Kimi K3 trusted-dynamic, Codex Sol reasoning-high), on rung 9c PR-1 at head `706f11003` (whole PR `df93548e9..706f11003`, 15 files; fix diff since the round-3 tree `360819482..706f11003` restricted to PR-1 files). Both reviewers reached PASS in Phase 1 and held PASS through cross-examination; the merged set is six findings (one MEDIUM, five LOW), every one adjudicated by the synthesizer against `/home/dgr/advrev-9c-pr1-r4/tree` with the deciding lines below. Every blocker from rounds 1-3 is re-derived closed by both reviewers and by the synthesizer.

### Prior-blocker closure (re-derived from the code, not the commit messages)
- **r1 C1/K1' (commit-to-publish window):** adopted commits publish and pop inside step (1) under one `registry_mu_` acquisition; `keys_.emplace(...).second == false` is a hard error; `detach_rule_locked` Case-0 skips committed claims (`!index_held`/`Committed`). CLOSED (Kimi re-ran the two round-1 fix tests; Codex re-read the drain).
- **r1 C2/K5 (ownership guard after fallible allocations):** `compensating = **r` is the first statement of `on_arm_complete` (`guardian_spark_runtime.cpp:392` in r2 numbering, still first in the current drain); `finished`/`live`/`verdicts` reserved before any registry mutation. CLOSED.
- **r2 C1 (disarm claim allocated after the durable detach mutation):** the claim is built and pushed under the detach lock BEFORE `remove_rule`/`rules_.erase`/`keys_.erase`; a throw inside `remove_rule` pops it and restores `active`. CLOSED.
- **r2 C2 (index_held cleared before the throwing removal):** superseded by the round-3 structural fix: `SparkKeyRuleIndex::erase_rule()` (`spark_key_rule_index.hpp:96-115`) is the `remove_rule` walk without its returned key copy, erases by iterator, and is `noexcept`; `release_claim_index_locked` (`guardian_spark_runtime.cpp`, `noexcept`) calls it and clears `index_held` only after. CLOSED.
- **r2 C3/K5 (compensating disarm under `registry_mu_`):** one shared `run_compensating_disarm` lambda runs in step (2) and step (2b), both before the step-(3) lock; no backend call remains under `registry_mu_` on the drain. CLOSED.
- **r3 C2 (uncontained throw on the `noexcept` drain path):** `fail_all_claims_locked` (`:173-205`) contains the per-claim reason copy and tombstones a claim whose release failed; the refill's admission-failure branch is inside a try; `release_claim_index_locked` cannot throw. CLOSED.
- **r3 C3 (`begin_stop` walk):** per-claim containment, always reaches `io_executor_.stop()`. CLOSED.
- **r3 C4 (post-erase allocations in `detach_rule_locked`):** lifecycle-kind string and all claim allocations precede the durable erase; `outbox_.drop_rule` contained and counted (`detach_post_commit_failures`). CLOSED.
- **r3 C5 (D10 wording/stamp):** per-instance ceiling + current verification stamp. CLOSED.
- **r1 C3 / r3 C1 (Codex: alive count released before OS-thread exit):** not re-raised by either reviewer this round; the standing adjudication (pre-existing since rung 7, byte-identical at base `74090cf4a`, closed by doc wording) stands.

### Should fix

**1. Ruling-14 text contradicts its own taxonomy (C1 / K4, found by Codex, confirmed by Kimi; MEDIUM, doc truth).** `docs/spark-stage2-guardian-consumer-design.md:415-418` defines *admission rejection* as "the executor refuses the operation synchronously (`CapacityExhausted`, `CeilingExhausted`, `AlreadyRunning`, `LaunchFailed`); no backend call was attempted", but `:520` says "(backend refused, worker threw, arm queue full - that is, an admission rejection)", and the reason/phase list at `:538-540` names only admission-expiry / admission-rejection / dispatched-timeout. Backend refusal and `WorkerThrew` are recorded AFTER dispatch (`ClaimEnd::BackendRefused` / `WorkerThrew` in the drain, `guardian_spark_runtime.cpp`), and a claim is pushed to `entry.fifo` before `io_executor_.submit` (`:1033-1035` vs `:388-395`), so "never reached the per-key queue" is also false for `CapacityExhausted`. Fix: drop "that is, an admission rejection"; add backend-refused and worker-threw as distinct post-dispatch terminal outcomes in the reason/phase list; replace "never reached the per-key queue" with "the backend call was never attempted"; align registry row A3 (`docs/spark-legacy-delta-registry.md:143`) to the same vocabulary. No code change.

### Minor

**2. Mutation-table legend overcounts (C2 / K1, found by both).** `governance.d/spark-9c-pr1-mutation-table.md:13` says 47 valid rows; the committed table supports 43 completed red-then-green pairs (51 data rows minus 4 no-match, the line-38 attempt whose "green" still failed, two mutation-survived first attempts, and a duplicated header/blank row at `:60-62`). The `ddab120f6` message says 48. Fix: define "valid" as a completed pair, delete the duplicated header/blank rows, recompute the legend to 43, and state 43 in the PR body (commit messages are history; do not rewrite).

**3. Stale header comment on the compensating disarm (C3 / K5).** `guardian_spark_runtime.hpp:930-931` says "direct only when the executor is stopping"; `run_compensating_disarm` falls back to a direct `backend_->disarm` on ANY non-timeout executor failure (`guardian_spark_runtime.cpp:320`, `:513` comment is correct). Fix the header sentence to "direct on a non-timeout executor refusal or failure, including Stopped".

**4. `index_add_rollback` still calls the allocating `remove_rule` (K2 / C5, found by Kimi, confirmed by Codex).** `guardian_spark_runtime.cpp:1000-1003`: the rollback fires mid-unwind (typically under `bad_alloc`); `remove_rule` allocates its returned key before mutating (`spark_key_rule_index.hpp`), `~GuardianRollback` swallows and counts (`guardian_scope_guard.cpp:17-32`), so a double fault leaves a ghost rule-to-key mapping. Fix: `index_->erase_rule(rule_id);` (return unused). One line.

**5. Comment overclaims the zero-observation guarantee (K3 / C6).** `guardian_io_executor.hpp:865-877` says the count cannot read zero during "even the trivial capture-destruction tail"; `~TicketCore` decrements alive (`:897-900`) and then runs `cv.notify_all()` (`:904`), the State handle release and the trampoline epilogue. Fix: exempt R5.1's stated runtime-only tail (or drop the phrase) so the local comment matches the INVARIANTS block the same file already corrected.

**6. Stale commit narratives (C4 / K6).** `8b9e0bb15` ("released only in ~TicketCore at OS-thread exit") and `85102826f` ("runs no library code ... F3's grace absorbs it") state the pre-correction wording. This repo does not rewrite history (no squashing); the PR body must carry the final accepted F3 wording and note that those two messages predate it.

### Adjudication
- C1 / K4 graded MEDIUM as a truth defect in an authoritative design section (CLAUDE.md rule 3: a comment or doc that contradicts the code is a truth finding at native severity); it changes no behaviour and does not gate.
- Kimi's Phase-1 note that the ruling-14 text was "reviewer-agreed" does not displace C1: the doc contradicts itself within four paragraphs regardless of provenance.
- Codex's arithmetic on the mutation table (43) is adopted over Kimi's (which omitted line 38); the synthesizer did not recount every row but checked the four disputed lines.
- No finding this round touches the executor's release mechanics, the claim state machine's ownership, or F3; the recurring allocation-failure class that drove rounds 1-3 surfaced only as the one-line K2/C5 residual in a swallowed rollback.
- Both reviewers cleared: scope exclusions (no deadline/quarantine/K-bound/`arm_failed` classification; `apply_rules()` not moved; no telemetry; `spark_engine.cpp` untouched); #3816 and #3831 shapes; the governance fold hp-1 (both prior-key and retained target-key disarms are now driven, red-first test present); the changelog fragment; the ledger's self-run disclosure.

**Empiricism:** Kimi K3 trusted-dynamic both phases: compiled the review tree, ran `[spark][ioexecutor]` 35/35, `[spark][runtime]` 137/137, `[spark][guardian][reconcile]` 49/49, `meson test --suite agent` Ok 2/Fail 0, re-ran the rounds 1-3 fix tests, recounted the mutation table (provenance `test-run` on K1). Codex Sol reasoning-high, workspace-write: static-read both phases, recompiled the changed TUs. No TSan run by either reviewer (author's TSan spark-tag evidence not re-run). No CI (unpushed). Synthesizer: built the tree (`BUILD_EXIT=0`), smoked `[spark][ioexecutor]` 248 assertions / 35 cases, read `erase_rule`, `release_claim_index_locked`, `fail_all_claims_locked`, `dispatch_arm_off_lock`, `begin_stop`'s walk, the design-doc lines `:414-419`/`:518-522`/`:537-541`, `index_add_rollback` and `~GuardianRollback` before ruling. Kimi quota: no 403 this round.
