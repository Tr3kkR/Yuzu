[C1]  LOW · CONFIDENCE(hi) · PROVENANCE(static-read)
windows-asan progress/testlog upload does not cover cancelled scheduled/default runs
- Location:  .github/workflows/nightly.yml:625
- Claim:     The Windows ASan artifact upload still drops the progress log for a job-level cancellation on scheduled/default-all runs, so the new stall diagnostic is only complete for ordinary failures and `jobs=windows-asan` dispatches.
- Evidence:  `.github/workflows/nightly.yml:630` is `if: failure() || (always() && inputs.jobs == 'windows-asan')`; the adjacent comment says a "90-minute job-level timeout is a CANCEL" and "that's exactly when the progress log matters most" at `.github/workflows/nightly.yml:626-628`.
- Scenario:  A scheduled nightly or default `jobs=all` dispatch hits the Windows ASan 90-minute job timeout after a slow build or long test stall -> the job is cancelled, not failed -> `inputs.jobs` is not `windows-asan` -> `progress.log` is not uploaded.
- Inference: Meson's per-test timeout is now 600s x `--timeout-multiplier 2`, and `YUZU_TEST_SLOT_TIMEOUT_MIN=30` reduces the chance of reaching the job-level timeout first, so this is a diagnostic gap rather than a correctness failure in the test split.
- Anchor:    judgment; CLAUDE.md "CI architecture" `.github` failure-path invariant requires an explicit status function, and this line has status functions, so this is not a contract-blocking invariant violation.
- Precedent: .github/workflows/nightly.yml:416 uses `if: failure() || cancelled()` for the TSan testlog/stack-capture upload.
- Mitigations: Meson timeout should normally fail before the 90-minute job timeout; the live Actions log may still contain flushed stderr progress lines.
- Fix:       Use `if: failure() || cancelled() || (always() && inputs.jobs == 'windows-asan')`.
- Falsifier: n/a (LOW).

VERDICT:  PASS — no CRITICAL/HIGH findings; the test split and workflow guards satisfy the reviewed contracts with one low-severity diagnostic hole.
COVERAGE: Went deep on correctness/logic for Meson registration and GitHub `if:` guards, resource/concurrency safety for the listener's `FILE*` path and test scheduling consequences, cross-platform portability for Catch2/MSVC-facing APIs and Windows workflow paths, cross-component/contract consistency against CI architecture, and test adequacy via compile, Meson introspection, listener smoke test, and the requested agent suite. Security/privilege was reviewed for workflow permissions/process snapshot/log exposure; no deep product-auth surface exists in this diff.
RAN:      `meson compile -C /home/dgr/yuzu-4018/build-linux tests/yuzu_agent_tests` -> OK, no work to do. `meson test -C /home/dgr/yuzu-4018/build-linux --suite agent --print-errorlogs` initially failed before tests because ccache tried `/home/dgr/.ccache` on a read-only filesystem; rerun with `CCACHE_DIR=/tmp/yuzu-review-ccache` rebuilt `yuzu_spark_alloc_budget_tests`, passed `spark allocation budget`, then failed `agent unit tests` with 2761 passed / 2 failed / 6 skipped. Failures were `test_passwd_lookup.cpp:200` and `test_net_quality_sampler.cpp:152`, neither touched by this diff. `meson setup /tmp/yuzu-review-asan ... -Db_sanitize=address -Dbuild_server=false -Dbuild_tests=true` succeeded after retrying with explicit `CC=gcc-15 CXX=g++-15`; the first default-compiler attempt failed CMake CLI11 discovery and the native-file attempt failed because local `mold` is unavailable. `meson introspect /home/dgr/yuzu-4018/build-linux --tests` -> plain build has `agent unit tests` with `~[tsan-heavy]` and no heavy entry. `meson introspect /tmp/yuzu-review-asan --tests` -> ASan build has `agent unit tests` with `~[tsan-heavy]` and `agent tsan-heavy checkpoints` with `[tsan-heavy]`, timeout 600. Direct listener smoke test with `YUZU_TEST_PROGRESS=1 YUZU_TEST_PROGRESS_FILE=/tmp/yuzu-progress-review.log` on `compare_versions: empty strings` passed and wrote RUN/START/END lines to stderr and file. `git diff --check 02859ac1d..e330b0522` -> OK. PyYAML parsed `.github/workflows/nightly.yml` -> OK; `actionlint` unavailable. CI status on PR head: no PR head exists / not pushed per prompt, so no remote CI status checked.
FILES:    CLAUDE.md; docs/ci-architecture.md; docs/cpp-conventions.md; .claude/routed-concerns.md; .claude/routed-concerns-access-control.md; .codex/skills/cpp-expert/SKILL.md; .codex/skills/cpp-safety/SKILL.md; .codex/skills/yuzu-build/SKILL.md; .codex/skills/yuzu-meson/SKILL.md; .github/workflows/nightly.yml; .github/workflows/ci.yml; .github/workflows/sanitizer-tests.yml; tests/meson.build; tests/unit/test_progress_listener.cpp; tests/unit/test_guardian_spark_runtime.cpp; tests/unit/server/test_pg_template_cleanup.cpp; scripts/ci/with-test-slot.sh; meson/native/linux-gcc15.ini; vcpkg_installed/x64-linux/include/catch2/interfaces/catch_interfaces_reporter.hpp; vcpkg_installed/x64-linux/include/catch2/reporters/catch_reporter_event_listener.hpp; vcpkg_installed/x64-linux/include/catch2/reporters/catch_reporter_registrars.hpp; build-linux/meson-logs/testlog.txt.
