[CDX-P1-01]  HIGH · CONFIDENCE(hi) · PROVENANCE(static-read)
The required C++ Resource Ledger is absent from the review packet
- Location:  review packet / PR body (absent); C++ changes are at `server/core/src/dashboard_routes.cpp:916`, `server/core/src/dashboard_routes.hpp:169`, `server/core/src/dispatch_destructive_gate.hpp:97`, and `server/core/src/server.cpp:20149`
- Claim:     This C++ diff has no supplied Gate-1 Resource Ledger, which is an explicit policy-floor omission even though inspection found no new owning raw-resource boundary.
- Evidence:  The range changes four C++ files; neither the supplied change description nor any range-specific artifact outside peer-review files contains a Resource Ledger, while CLAUDE.md standing rule 2 names “Resource Ledger omission” as a policy floor.
- Scenario:  The change is merged on the strength of code review without the mandatory ownership/lifetime attestation -> the governance record cannot prove whether the new callbacks, borrowed store pointer, and metrics pointer add or transfer ownership.
- Inference: A complete ledger can legitimately say “none”: the new code uses borrowed pointers, `std::function`, `std::optional`, and automatic containers and creates no fd/HANDLE/SOCKET/thread/process/C allocation.
- Derivation: TRIGGER = this C++ range is reviewed without a range-specific Resource Ledger; IMPACT = I9; EXPOSURE = E3; EPISTEMIC STATUS = verified; POLICY FLOOR = Resource Ledger omission on a C++ diff.
- Anchor:    CLAUDE.md standing rule 2, policy-floor list; `docs/cpp-conventions.md` “Resource ownership and lifetime”.
- Precedent: none found in this range; CLAUDE.md Gate 1 requires Change Summary + Resource Ledger.
- Mitigations: independent `cpp-safety` inspection found no owning raw resource, manual cleanup, escaping borrow, callback-lifetime change, or thread change; the policy floor nevertheless applies to the missing artifact.
- Fix:       Add the range-specific Resource Ledger, explicitly recording that no owning raw-resource boundaries are added or touched and that the stored callbacks/pointers remain borrowed for `DashboardRoutes`’ lifetime.
- Falsifier: Produce the pre-existing range-specific Resource Ledger supplied with this local review/PR body.

[CDX-P1-02]  HIGH · CONFIDENCE(hi) · PROVENANCE(static-read)
The upgrade runbook tells dashboard callers to send a field the endpoint ignores
- Location:  `docs/user-manual/upgrading.md:141` and `docs/user-manual/upgrading.md:146`; endpoint parsing at `server/core/src/dashboard_routes.cpp:729-744` and `server/core/src/dashboard_routes.cpp:886-893`
- Claim:     The remediation and verification instructions say dashboard automation should supply non-empty `agent_ids`, but `/api/dashboard/execute` never reads that field and represents one explicit agent as `scope=<agent-id>`.
- Evidence:  The handler extracts only `instruction` and `scope`; only a non-empty non-group/non-`__all__` `scope` is pushed into its local `agent_ids`. The shipped form likewise names its hidden target input `scope` (`server/core/src/dashboard_ui.cpp:443`).
- Scenario:  An authenticated automation owner follows the upgrade guide and changes an affected dashboard POST to `agent_ids=dev-A` with no `scope` -> the field is ignored -> the request is still classified as untargeted and refused instead of restoring the intended single-agent dispatch.
- Inference: The safe refusal prevents a destructive broadcast, but the documented migration leaves the dashboard capability unavailable and can cause an operator to misdiagnose a correct gate as broken.
- Derivation: TRIGGER = an affected dashboard client follows the documented `agent_ids` remediation; IMPACT = I4, I6; EXPOSURE = E3; EPISTEMIC STATUS = verified.
- Anchor:    `.claude/routed-concerns-access-control.md` “Dispatch targeting” names `docs/user-manual/upgrading.md` as the contract/update obligation; CLAUDE.md standing rule 3 makes contradictory changed prose a truth finding.
- Precedent: `server/core/src/dashboard_ui.cpp:443` and `tests/unit/server/test_dashboard_destructive_gate.cpp:380` use `scope=dev-A` correctly.
- Mitigations: the gate fails safe; operators using the browser UI submit the correct `scope` field automatically.
- Fix:       Split the remediation by surface: MCP/REST use non-empty `agent_ids`; dashboard automation must send exactly one agent as `scope=<agent-id>`. Make the verification paragraph use the same split.
- Falsifier: Show a production parsing path that consumes a dashboard request’s `agent_ids` field and passes it to dispatch, or show that these instructions are expressly scoped away from dashboard callers.

[CDX-P1-03]  HIGH · CONFIDENCE(hi) · PROVENANCE(static-read)
The documented verification selector is invalid PromQL
- Location:  `docs/user-manual/upgrading.md:149`
- Claim:     The newly updated verification expression uses `route="command"|"mcp"|"dashboard"`, which PromQL cannot parse as a label matcher.
- Evidence:  The document prints `yuzu_server_dispatch_target_rejected_total{route="command"|"mcp"|"dashboard",reason="destructive_untargeted"}`; PromQL alternatives require a regex matcher such as `route=~"command|mcp|dashboard"` or separate selectors.
- Scenario:  An operator follows the release verification step after upgrading -> Prometheus/Grafana rejects the query -> the documented method cannot confirm that the new dashboard series is exercising.
- Inference: The underlying metric and pre-seeded dashboard label are correct; only the operational verification instruction is unusable.
- Derivation: TRIGGER = an operator copies the changed verification selector into Prometheus/Grafana; IMPACT = I4; EXPOSURE = E3; EPISTEMIC STATUS = verified.
- Anchor:    `.claude/routed-concerns-access-control.md` “Dispatch targeting” names `docs/user-manual/upgrading.md` as the update obligation; CLAUDE.md standing rule 3.
- Precedent: `docs/user-manual/metrics.md:1401` uses the valid `result=~"success|fresh"` alternation form.
- Mitigations: operators can discover the correct syntax independently; the metric implementation is unaffected.
- Fix:       Replace the matcher with `route=~"command|mcp|dashboard"` (or show three valid selectors).
- Falsifier: Demonstrate that the printed selector parses successfully in the supported Prometheus query engine.

[CDX-P1-04]  HIGH · CONFIDENCE(hi) · PROVENANCE(static-read)
The audit guide says refused targets reach server logs, but targeted confinement drops leave no such log
- Location:  `docs/user-manual/audit-log.md:129`; denial path at `server/core/src/dashboard_routes.cpp:1040-1073`
- Claim:     The new audit entry says requested `agent_ids`/`scope` “reach the server log only,” but the dashboard `scope_violation` path neither audits nor logs the requested agent id.
- Evidence:  The targeted arm converts `scope=dev-A` to `agent_ids={"dev-A"}`, confines it, and on an empty result writes only `reason=scope_violation <plugin>:<action>` before returning; there is no logging call in that arm. The warning at lines 1013-1016 belongs only to `RefuseUntargeted` and logs `scope_expr`, which is empty for an explicit single-agent target.
- Scenario:  Incident response finds a `command.dispatch|denied` `scope_violation` and follows the audit guide to reconstruct the requested blast radius from server logs -> no target is present -> responders cannot determine which endpoint was attempted and may incorrectly treat the absence as log loss/tampering.
- Inference: The final caveat that the audit row alone is insufficient is correct; the narrower claim that the missing target exists in server logs is not.
- Derivation: TRIGGER = an incident responder follows the new audit-log guidance for a dashboard explicit-agent confinement denial; IMPACT = I4; EXPOSURE = E3; EPISTEMIC STATUS = verified.
- Anchor:    CLAUDE.md standing rule 3; anchor document `docs/user-manual/audit-log.md` must match code.
- Precedent: `server/core/src/dashboard_routes.cpp:1013-1016` explicitly logs the supplied scope for the distinct untargeted refusal.
- Mitigations: principal, pair, reason, and time remain in the audit row; the requested agent is absent from both evidence channels.
- Fix:       Either log a sanitised/capped requested agent id in the `scope_violation` arm, or correct the guide to state that the target is not retained in either the audit row or server log for that arm.
- Falsifier: Identify a production log statement reached by the targeted `scope_violation` branch that records the requested agent id.

[CDX-P1-05]  LOW · CONFIDENCE(hi) · PROVENANCE(test-run)
The only route-level proof that legitimate Destructive targeting still dispatches is PG-gated
- Location:  `tests/unit/server/test_dashboard_destructive_gate.cpp:369-405`; skip gate at `tests/unit/server/test_mgmt_group_pg_helper.hpp:60-63`
- Claim:     On the supplied macOS setup, every always-running route test proves denial or non-Destructive behavior; the sole test proving `Targeted` reaches dispatch skips when `YUZU_TEST_POSTGRES_DSN` is absent.
- Evidence:  The focused case exited 4 with `1 skipped / assertions: none`; `[destructive]` reported 12 cases, 11 passed, 1 skipped, 75/75 assertions. A route mutation that always refuses the `Targeted` arm would leave those 11 cases green locally.
- Scenario:  A future route edit turns the `Targeted` arm into unconditional refusal -> macOS and Windows local runs remain green because the only discriminating route case skips -> the regression is found only on a PG-enabled Linux CI leg.
- Inference: Current production code is correct by static inspection (`Targeted` breaks to `dispatch_fn_` after non-empty confinement), and Linux CI provisions PG, so this is degraded local/cross-platform assurance rather than a demonstrated product defect or false-green CI closure.
- Derivation: TRIGGER = a regression in the dashboard `Targeted` arm is tested on a no-PG macOS/Windows leg; IMPACT = I8; EXPOSURE = E5; EPISTEMIC STATUS = verified.
- Anchor:    judgment; `.claude/routed-concerns-access-control.md` “Per-device dispatch visibility” clause (5) informs the exact-send-set standard but the PG-enabled CI case does assert the exact forwarded list.
- Precedent: `tests/unit/server/test_dispatch_confined_arms.cpp:567-590` supplies an always-running real-registry exact-send-set test without a database.
- Mitigations: pure helper tests cover `Targeted` and confinement independently; the PG-enabled test checks exact `agent_ids={"dev-A"}`; Linux CI is configured to require a PG DSN before PG-tagged tests. No Linux CI run exists yet for this local-only head.
- Fix:       Add a narrow injectable visible-agents seam for this handler test, or otherwise construct an always-running route fixture that returns a present visible set and asserts the exact dispatch call; retain the PG integration case.

VERDICT:  BLOCK — four HIGH policy/documentation findings block even though the Destructive gate’s production logic passed review.
COVERAGE: Deep on security/privilege and bypass shapes (single route inventory, form/body/query target parsing, classifier wiring/miss policy, nullopt inversion, per-device confinement, D3 schedule/result-set callers), correctness/logic, docs/metrics/audit contract truth, C++ ownership/lifetime, and targeted tests; static portability review covered C++23/GCC-Clang-MSVC-Apple-Clang constructs and the warm Apple-Clang build. Skimmed Linux runtime and real MSVC execution because this local invocation has neither host; Linux remains unverified and the supplied head is local-only. Resource safety found no acquisition/release, raw owner, escaping borrow, thread, callback-context, syscall/process, or ABI change; sanitizer runs were not indicated by this ownership-neutral diff.
RAN:      `git diff --check 35bb76a73..cc07a97acc51771544f9cc69c40f2c4d21271e95` (clean); `meson compile -C build-macos yuzu_server_tests` first failed before source compilation because sandboxed ccache could not create a host-cache temp file, then `CCACHE_TEMPDIR=.cache/advrev-6b/ccache-tmp meson compile -C build-macos yuzu_server_tests` passed (Apple Clang link warning only: duplicate `-lresolv`); `./build-macos/tests/yuzu_server_tests '[destructive]'` (11 passed, 1 PG-skipped, 75/75); `./build-macos/tests/yuzu_server_tests 'the shared confined-dispatch seam*'` (1 passed); focused positive case (1 skipped, zero assertions, exit 4); `./build-macos/tests/yuzu_server_tests '[server][dashboard][execute]'` (10 passed, 10 PG-skipped, 74/74). CI status: none for this local-only head; no Linux claim. The supplied range is 17 files / +924 -22, not the brief’s 14-file count.
FILES:    All 17 changed files: `.claude/routed-concerns-access-control.md`; both changed `changelog.d` fragments; `docs/mcp-server.md`; `docs/user-manual/{audit-log,metrics,rest-api,upgrading}.md`; `server/core/src/{dashboard_routes.cpp,dashboard_routes.hpp,dispatch_destructive_gate.hpp,server.cpp}`; `tests/meson.build`; `tests/unit/server/{test_dashboard_destructive_gate.cpp,test_dashboard_tar_fragments.cpp,test_dispatch_confined_arms.cpp,test_real_capability_registry.hpp}`. Supporting contract/path inspection: `CLAUDE.md`, `docs/cpp-conventions.md`, `docs/workstreams.md`, `server/core/src/{agent_registry.hpp,dashboard_ui.cpp,management_group_store.cpp,management_group_store.hpp,schedule_runner.cpp,schedule_arming_check.hpp,rest_api_v1.cpp,web_utils.hpp}`, `tests/unit/server/{test_dispatch_destructive_gate.cpp,test_mgmt_group_pg_helper.hpp}`, `.github/workflows/ci.yml`, and both C++ review skill files. `STREAM.md` was absent. No peer review file was read.
