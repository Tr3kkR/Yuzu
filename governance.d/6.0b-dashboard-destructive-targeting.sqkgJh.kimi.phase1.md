# kimi — Phase 1 independent review: PR6.0b dashboard Destructive targeting gate

Range reviewed: `35bb76a73..cc07a97acc51771544f9cc69c40f2c4d21271e95` (4 commits, 17 files, +924/-22). Working tree clean at `cc07a97ac`; `build-macos` current.

## Adversarial checklist results (brief items a–e)

**(a) Reachable bypass of the gate from this surface — none found.**

- All four other `dispatch_fn_` call sites in `dashboard_routes.cpp` dispatch hardcoded pairs: `tar.sql` (line 1277, `DispatchClass::ReadOnly` — `plugin_action_catalogue_a.hpp:381-382`), `tar.status` (1488, ReadOnly — catalogue_a:257-259), `tar.configure` (1700, Mutating — catalogue_a:305-307), and `tar.purge_source` (1869, Destructive but dispatched with explicit `{device_id}`, `scope_expr=""`, behind its own per-device mgmt-group visibility check at 1662-1687, plus the seam's #1788 intersection). None is a free-form, fan-out-capable Destructive surface.
- The exec-console handler (`dashboard_routes.cpp:725-1183`) has exactly one dispatch call (1081), positioned after the gate (916-1078). Every earlier return (empty instruction 746, help 808, unknown command 817, supplied-empty scope 895) returns without dispatching.
- `agent_ids`/`scope_expr` are mutually exclusive by construction in this handler (888-902): `group:*` → scope_expr only; `__all__` → scope_expr only; anything else non-empty → single agent_id. So `evaluate_destructive_targeting` receives exactly one of (true,false) / (false,true) / (false,false). All three Destructive shapes refuse.
- Percent-encoded field name `sc%6fpe=` decodes into `req.params` and is read by `get_param_value` first (742) — pinned by the `sc%6fpe` test, which I ran (green).
- Case tricks: `SCOPE=` unmatched → treated as omitted → Destructive refused (fail-closed). `scope=%20__all__` → treated as a single agent id → confined to visible set → no such agent → deny-all refusal. Both fail safe.
- Body-vs-query precedence: httplib parses the query string into `req.params` before the form body, so first-wins on duplicates; both orders (`?scope=__all__`+body `scope=dev-A`, and reverse) either refuse (Destructive) or narrow to the single agent. The more permissive value is never the one that survives.
- Non-form Content-Type: raw-body fallback (`extract_form_value`, 744) still sees `scope=` — UP-10 fix present in both the execute and tar-sql handlers.
- `classify` is case-insensitive on both plugin and action (`command_capability.hpp:176-209`, `ci_equal` byte loop), so the gate's classify and the seam's lowercase-normalised classify (`dispatch_confined`, server.cpp:10979-10981) resolve the same row — the case-normalisation-order gap I probed for does not exist.

**(b) D3 placement holds.** `ScheduleRunner::dispatch_tracked` (schedule_runner.cpp:294-353) verified: schedules carry no `agent_ids`; empty `scope_expression` is mapped to `kBroadcastScope` at the call site (340-342); a seam-level Destructive gate would zero-reach every scheduled Destructive fire, approval-gated included. The seam's own authz (`build_classified_command`, server.cpp:11002-11007) still classifies+authorizes every dispatch before targeting, so the route-level gate is additive, not a moved check. Documented residuals (`POST /api/instructions/{id}/execute`, MCP `execute_bundle`) are pre-existing, unchanged by this diff, and now correctly named in rest-api.md / upgrading.md / mcp-server.md / both changelog fragments. The D3 pin test exists and passes (ran it).

**(c) nullopt=deny-all — verified, NOT inverted.** `DestructiveVisibleAgents` has an explicit constructor (dispatch_destructive_gate.hpp:267) and `confine_destructive_targets` returns `{}` when `visible.agents` is nullopt (288-289). The call site (dashboard_routes.cpp:1048-1052) leaves `vis` as nullopt when `mgmt_group_store_` is null and forwards the store's own nullopt-on-degrade (`management_group_store.cpp:846-879`: not-open/pool-timeout/query-error all `return std::nullopt`). Result: deny-all + audited `scope_violation` refusal. The opposite convention of `authz::VisibleSet` is not leaked across the boundary — the explicit constructor forces the spelling.

**(d) Tests cannot stay green with the gate deleted.** The refusal cases assert `h.calls.empty()` (the dispatch callback is never reached) plus the exact refusal message, metric value 1.0, and audit count. Deleting the gate block turns `instruction=purge&scope=__all__` into a real dispatch → `calls.size()==1` → red. Deleting only the confinement turns the no-store case into a dispatch → red. The `Targeted` in-scope case asserts the exact handed-over set `{"dev-A"}` (test line 384), satisfying routed-concern clause (5) at this seam (the downstream #1788 intersection is bound separately in `test_dispatch_confined_arms.cpp`, which this diff extends with the D3 pin asserting `outcome.sent == 3` — the exact send set, not a mock handoff).

**(e) Doc edits — true, with one minor overstatement (K-1 below).** Verified: 17 Destructive rows in the catalogue (grep count 5+1+3+4+4 across the five catalogue fragments); RBAC-off confinement no-op claim (`get_visible_agents` RBAC-off → `all_member_agents()`, management_group_store.cpp:855-856; every enrolled agent auto-added to root group, agent_service_impl.cpp:487 + gateway_service_impl.cpp:396); the RBAC-on global-grant-operator refusal shape in upgrading.md matches the `Targeted`→empty arm exactly (message `no reachable in-scope agent`, audit `reason=scope_violation`, not counted on the target_rejected series); metrics.md closed-set now includes `dashboard` and matches the boot seed (server.cpp:1218-1222); rest-api.md's 200-with-message description matches the handler.

## Findings

```
[K-1]  LOW · CONFIDENCE(hi) · PROVENANCE(static-read)
audit-log.md's new command.dispatch row overstates field sanitisation for the dashboard emitter
- Location:  docs/user-manual/audit-log.md (new `command.dispatch` row: "both caller-supplied
             fields sanitised and capped at 128 bytes") vs server/core/src/dashboard_routes.cpp:950-951,
             1020-1023, 1062-1063
- Claim:     the doc says the <plugin>:<action> fields in `reason=<r> <plugin>:<action>` details are
             sanitised, but the dashboard's three new denial arms write plugin/action raw into the
             audit detail (and the spdlog lines at 946-949 / 1013-1016 sanitise only scope_expr).
- Evidence:  dashboard_routes.cpp:1020-1023 — `"reason=" + std::string(...kReasonDestructiveUntargeted)
             + " " + plugin + ":" + action` — no sanitize_for_log on either field; contrast the
             /api/command sibling at server.cpp:14830-14831 which wraps both in
             `onbehalf::sanitize_for_log(..., 128)`.
- Scenario:  in the RefuseUntargeted/scope_violation arms, plugin/action must have classified as a
             catalogue Destructive row — `ci_equal` is length-bound exact-match, so the values are
             case-variants of compile-time constants and cannot carry injected bytes. The only arm
             where agent-advertised (attacker-influencable) names reach the detail raw is
             `classifier_unavailable`, which requires the production classifier wiring to have
             regressed — a fail-closed state that refuses every dispatch anyway. Net: a SIEM author
             trusting the doc could assume sanitisation that this surface does not perform; no
             reachable injection in the shipped configuration.
- Inference: doc inaccuracy + deviation from the sibling site, not an exploitable injection today.
- Anchor:    judgment (anchor 6 "docs must match the code" motivates the review, no MUST clause bit)
- Precedent: server.cpp:14830-14831 (/api/command sanitises both fields); dashboard's own
             pre-existing success row (dashboard_routes.cpp:1177-1181) also writes plugin:action raw,
             so the new rows match their own surface's convention.
- Mitigations: catalogue-derived values are injection-free in the two reachable arms; the third arm
             requires a wiring regression. This is the only guard for the doc claim, hence LOW not INFO.
- Fix:       either wrap plugin/action in onbehalf::sanitize_for_log(..., 128) in the three new audit
             details + two log lines (match the sibling and the doc), or qualify the audit-log.md row.
```

```
[K-2]  INFO · CONFIDENCE(hi) · PROVENANCE(test-run)
Positive-path (Targeted with a live visible-agent store) coverage is Postgres-gated and skipped locally
- Location:  tests/unit/server/test_dashboard_destructive_gate.cpp:369-405 ([pg] tag)
- Claim:     the only test proving a Destructive action DOES dispatch to a legitimately targeted agent
             (and that an out-of-scope id is confined out by a real store read) skips without
             YUZU_TEST_POSTGRES_DSN; no non-PG substitute exists because ManagementGroupStore is
             Postgres-only.
- Evidence:  local run: `12 cases | 11 passed | 1 skipped, 75/75 assertions` — the skip is this case.
- Scenario:  a regression in the handler→store→confine seam escapes local/dev runs; caught only by CI.
- Inference: CI is the enforcement point: scripts/ci/ensure-postgres.sh guarantees a reachable instance
             on every CI server-test leg and set-but-broken DSN FAILS the test rather than skipping.
             Residual risk is CI-leg loss, not silent skip. Structural, accepted convention
             (YUZU_REQUIRE_PG_DB), recorded here because the brief put it in scope.
- Anchor:    judgment
- Mitigations: CI PG leg; the [pg] case itself asserts the exact send set {"dev-A"} and the
             refusal of dev-Z and __all__, so when it runs it is discriminating.
- Fix:       none required; optional: a comment in the file header noting CI is the only executor.
```

## Non-findings considered and rejected

- **Missing JIT-elevation `require_permission(cap.securable, cap.operation)` on the dashboard gate arms** (the /api/command gate does it, server.cpp:14795-14797): the dashboard's per-capability authz is the seam's base-grants-only `has_permission` inside `build_classified_command`, which is pre-existing and fail-closed (more restrictive — a JIT-elevated operator without the base grant is denied on this surface). D4 in the header documents this split deliberately. Not introduced by this diff.
- **200-with-fragment refusal shape instead of 4xx**: matches every other refusal in this htmx handler; rest-api.md now documents it explicitly. Not a defect.
- **Empty catch around the metric increment** (dashboard_routes.cpp:1006): NOLINT-tagged, identical to both pre-existing sibling sites; audit and response cannot be skipped by a metrics failure by design.
- **`caller.principal` vs `sess->username`**: `derive_dispatch_caller` sets `.principal = sess.username` (server.cpp:10503) — identical key to what /api/command passes `get_visible_agents`. Consistent.
- **Resource Ledger policy floor**: no new fd/handle/thread/C-string/subprocess/temp-path in this diff — pure logic, one `std::function` member, string building. Ledger content would be "no resource changes"; nothing for cpp-safety to own. Changelog fragment present (`changelog.d/6.0b-dashboard-destructive-targeting.security.md`), no direct CHANGELOG.md edit, no non-RAII cleanup. Floors clear.
- **`test_dashboard_tar_fragments.cpp` comment** ("the pairs they use are classify-misses") — the harness's `resolve_to={"os_info","version"}` may in fact classify (os_info.* rows exist); either way the gate's Miss/NotDestructive arms both fall through to dispatch, so the comment's imprecision cannot mask a behaviour change. Nitpick not filed.

VERDICT:  PASS — the gate is at the adjudicated seam, fail-closed on every adversarial angle checked (encoding, precedence, case, omission, unwired classifier, nullopt visibility), tests are discriminating rather than false-green, and the only defect found is a LOW doc/sanitisation overstatement.
COVERAGE: deep — security/privilege (all dispatch sites, param parsing, RBAC-off/RBAC-on postures), correctness (verdict mapping, arm exclusivity, Policy-B backstop at the seam), test adequacy (false-green analysis, exact-send-set clause, PG-skip gap), doc truth (all five edited docs + both changelog fragments verified line-by-line against code). Skimmed — concurrency (handler adds no shared state; `classify_fn_` is write-once-at-boot then read per request, same pattern as the pre-existing `fleet_read_fn_`), cross-platform (no platform-specific code; Windows rig result accepted from the brief, not reproduced), resource safety (no resources added).
RAN:      `git log/diff` on the pinned range; `./build-macos/tests/yuzu_server_tests '[destructive]'` → 12 cases, 11 passed, 1 skipped ([pg], no local DSN), 75/75 assertions — matches the brief's empirical state; `./build-macos/tests/yuzu_server_tests 'the shared confined-dispatch seam*'` → 1 case passed (D3 pin, sent==3); `./build-macos/tests/yuzu_server_tests '[dashboard]'` → 12 passed, 39 skipped (PG-gated), 80/80 assertions. `grep -rc DispatchClass::Destructive capability_decls/` → 17 rows, matching the docs. No recompile needed (build dir current, tree clean at HEAD). No CI exists for this branch — LOCAL ONLY, never pushed.
FILES:    .claude/routed-concerns-access-control.md (rows 21-22 in full), CLAUDE.md (standing rule 2), .claude/skills/governance/SKILL.md (Gate 1 ledger definition), server/core/src/dashboard_routes.cpp (execute handler 700-1183, tar-sql 1230-1315, scan 1400-1500, reenable 1600-1715, purge 1789-1869), dashboard_routes.hpp (ClassifyFn block), dispatch_destructive_gate.hpp (full), command_capability.hpp (classify), dispatch_target_shape.hpp (reasons), on_behalf_guard.hpp (sanitize_for_log), management_group_store.{hpp,cpp} (get_visible_agents/all_member_agents), server.cpp (10969-11080 dispatch_confined, 14740-14870 /api/command gate, 20140-20253 wiring, 1180-1225 metric seeding, 10496-10512 derive_dispatch_caller, 5582-5594 root group), schedule_runner.cpp (294-353), agent_service_impl.cpp:487, tests/unit/server/test_dashboard_destructive_gate.cpp (full), test_dispatch_confined_arms.cpp (new pin), test_real_capability_registry.hpp (full), test_dashboard_tar_fragments.cpp (diff + harness), tests/meson.build, docs/user-manual/{rest-api,metrics,audit-log,upgrading}.md (diffs), docs/mcp-server.md (diff), changelog.d/ fragments (diffs).
