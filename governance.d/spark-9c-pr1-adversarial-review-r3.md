## Review — requesting changes (round 3, Codex-only; Kimi unavailable, quota 403 at launch)

Adversarial re-review round 3 of `74090cf4a..360819482` (rung 9c PR-1; fix diff `85102826f..360819482`), reviewed by Codex Sol (reasoning high, workspace-write) only. Kimi K3 failed at launch with `403 You've reached your 5-hour usage limit. Your quota will reset when the current 5-hour window ends` and was not retried (standing no-silent-retry rule); no Phase 2 was run for either side because there was no peer report to cross-examine. This synthesis therefore carries the second read itself: every Codex finding, every author-flagged residual, and every round-2 blocker closure was re-derived by the synthesizer against `/home/dgr/advrev-9c-pr1-r3/tree` with file:line.

**Round-2 blockers: both CLOSED (re-derived).** (1) `detach_rule_locked` (`guardian_spark_runtime.cpp:1186-1245`) now builds the Disarm claim (`make_shared`, key copy, `claims_.try_emplace`, `fifo.push_back` with its own rollback) BEFORE `index_->remove_rule` (`:1233`), predicts the ->0 edge from `index_->refcount` under the same lock, and pops the pre-pushed claim + restores `active` if `remove_rule` throws (`:1234-1244`); `keys_` still owns the subscription on every throw path. (2) `release_claim_index_locked` (`:106-119`) calls `remove_rule` first and clears `index_held` after; `remove_rule` is strong-guarantee, so a throw leaves mapping and flag intact for a retry. (3, the MEDIUM) no backend call remains under `registry_mu_` on the drain: both compensating disarms go through `run_compensating_disarm` at step (2) `:718` and the new step (2b) `:731`, both before the step-(3) `lock_guard`. Codex's own Phase 1 also states the round-2 blockers are closed.

### Should fix

**1. Executor header sentence about the post-decrement tail is false on two counts (from C1; the mechanism claim itself is ruled down, see Adjudication).** `agents/core/src/guardian_io_executor.hpp:74` says the tail after `~TicketCore`'s decrement "runs no library code, and F3's orphan grace absorbs it". Neither holds: the tail runs `state->cv.notify_all()` (`:898`), the `State` shared_ptr release, `operator delete` of the payload and the CRT thread-exit path; and no grace covers it, because `wait_for_workers_to_drain` returns on the first zero (`hard_exit.hpp:95-101`) and `main.cpp:924` skips the wait entirely when the sample is already zero. Reword to: "the tail runs only process-lifetime runtime code (libstdc++/libc thread exit), never the Guardian/OpenSSL/libsystemd/Win32-RPC code F3 names; no grace covers it; identical exposure for every `run()` worker since rung 7; accepted." CLAUDE.md standing rule 3 (a comment that contradicts the code is a truth finding).

**2. Refill-failure cleanup can throw out of a `noexcept` callback (C2, MEDIUM).** `on_arm_complete` is `noexcept` (`:368`) and its step (4) refill calls `dispatch_arm_off_lock`, whose admission-failure branch calls `fail_all_claims_locked` (`:359`) -> `release_claim_index_locked` -> the allocating `remove_rule`, with no containment; a `bad_alloc` there is `std::terminate`, and the executor's `try` around `on_complete` (`guardian_io_executor.hpp:692-698`) cannot help because the runtime function is itself `noexcept`. The other half of C2 (a repeated allocation failure inside the firewall's own fill-in leaves the head unpopped, key wedged until restart; `:739-749` counts it) is the author-flagged residual (a): double-fault only, bounded waiters, graded LOW. Fix for the terminate half: contain the cleanup in `dispatch_arm_off_lock` (try/catch + `claim_drain_failures_`), or make index release allocation-free (retain the removal key with the claim, or an iterator/node-based `SparkKeyRuleIndex` removal).

**3. `begin_stop`'s queued-claim walk can propagate a `remove_rule` throw (C3 = residual (b), MEDIUM on the terminate path, LOW on likelihood).** `:2193-2204` calls `release_claim_index_locked` per Queued claim with no catch; `~GuardianSparkRuntime` (`:92-97`) and `GuardianEngine::stop()` (`guardian_engine.cpp:607-613`) call `begin_stop` directly, so a throw is `std::terminate` from the destructor or an aborted stop sequence. Reachable only if a Queued Arm claim still holds index ownership at stop (a direct concurrent caller; the engine-driven path abandons its waiter first). Fix: catch per claim, count, drop the claim consistently, continue.

**4. Two allocations after the durable erase in `detach_rule_locked` contradict its "nothing can throw from here" comment (C4 = residual (c) plus one more site, MEDIUM).** `:1246-1248` states nothing can throw after `remove_rule`, but `outbox_.drop_rule` (`:1252-1254`) constructs an owning `Key` with a `std::string` per match (`guardian_outbox.hpp:260-264`), and `std::string(lifecycle_kind)` is built at `:1278` before `enqueue_lifecycle_locked`'s internal firewall. A throw there unwinds past the claim hand-off (`return work`): the Disarm claim is queued and `keys_` already erased, so nothing leaks, but the caller never drives the claim and the audit record is lost; the subscription then waits for the next same-key event or restart (the author's residual (c), now with a second site). Fix: build the lifecycle-kind string before the mutation, make `drop_rule` allocation-free (erase by iterator / heterogeneous lookup) or contain and count it, and ensure every post-commit exit still returns the queued claim.

**5. Registry row D10 mislabels the ceiling and carries a stale verification stamp (C5, MEDIUM truth).** `docs/spark-legacy-delta-registry.md:312` says "process-wide ceiling" while the header documents a per-executor-instance ceiling (`guardian_io_executor.hpp:49-60`; the state reader and the arm/disarm executor admit independently); `:316` still asserts the executor diff since `21e023aa8` is comment-only, false since `submit()` and the two-count state landed. Reword and restamp with the current range.

### Adjudication

- **C1 (Codex CRITICAL, "F3 count reaches zero before the OS thread exits") ruled DOWN to item 1.** This is round 1's C3 re-raised with a stronger grade. The deciding facts are unchanged: the release point is identical at base `74090cf4a` (`guardian_io_executor.hpp:36-37` there already said "at true OS-thread-exit time ... the worker's own captured copy, destroyed by the trampoline after the worker lambda has fully returned"); this range moves nothing. The post-decrement tail executes only process-lifetime runtime code (`notify_all`, `operator delete`, libstdc++/libc thread exit), never the Guardian/OpenSSL/libsystemd/Win32-RPC code that F3's §24 clause names as the hazard, and a self-decremented count can never certify its own exit. Codex's fix (joinable handle + reaper/join on both platforms) contradicts the detached-at-creation, no-`std::terminate`-path design chosen in rung 5/7 and re-litigates a shipped, previously governed decision. What IS new and valid in C1 is that the fix-round wording overclaimed the mitigation ("grace absorbs it", "no library code"); that is item 1, a doc-truth Should-fix, not a mechanism change. #4147's acceptance (physical binding, nonzero across the post-`fn()` callback window) is met and tested.
- **C2 split**: the `noexcept`-terminate half is real and new (the refill path was added in the fix round); the firewall-double-fault half is the author's residual (a), LOW.
- **C3 = residual (b)**, graded as above; the author's "cannot fire at destruction" argument holds for the engine-driven path only.
- **C4 = residual (c) plus `drop_rule`**, which the author's mutation-site audit missed; the "nothing can throw from here" comment is a truth finding in its own right.
- **C5** confirmed against the registry text.
- Scope discipline: no claim deadline, quarantine, K-bound or `arm_failed` classification added; `spark_engine.cpp` untouched; `ClaimEnd` remains a facts-only plug point and is now named as such in R5.2 (`docs/spark-stage2-guardian-consumer-design.md`, fix commit `360819482`).

**Empiricism:** ONE-SIDED. Codex Sol, reasoning high, workspace-write sandbox, Phase 1 only, `static-read` provenance on all five findings (it recompiled the changed TUs and read the tree; no test run reported). Kimi K3: did not run (`403 You've reached your 5-hour usage limit. Your quota will reset when the current 5-hour window ends`, `/home/dgr/advrev-9c-pr1-r3/kimi.p1.run.log`). No cross-examination this round. Synthesizer: built the tree (`BUILD_EXIT=0`, no FAILED), ran `[spark][ioexecutor]` (35 cases / 248 assertions green), and read `detach_rule_locked`, `release_claim_index_locked`, `on_arm_complete` steps (2)/(2b)/(3), `begin_stop`, `drop_rule`, `hard_exit.hpp:90-102`, `main.cpp:924` and the executor trampoline/ticket before ruling. Author's suite/TSan claims not re-run this round. No CI (unpushed). A two-sided pass on the next head is recommended once Kimi's window resets, since three consecutive rounds have each found a new allocation-failure ownership site.
