# Codex Phase 2 — cross-examination and revised review

## Peer cross-examination

| PEER-ID | label | evidence I checked (file:line / command) | my severity |
|---|---|---|---|
| K-1 | confirmed-independently | `docs/user-manual/audit-log.md:129` promises sanitised/capped plugin and action fields; the dashboard writes raw `plugin + ":" + action` at `dashboard_routes.cpp:950-951,1020-1023,1062-1063`, unlike `server.cpp:14830-14831`. Production wiring at `server.cpp:20158-20161` and exact catalogue classification mitigate the reachable arms. | LOW |
| K-2 | disagrees | Same gap as my [CDX-P1-05]. `./build-macos/tests/yuzu_server_tests '[destructive]'` again produced 11 passed, 1 PG-skipped, 75/75; the skipped exact-send assertion is `test_dashboard_destructive_gate.cpp:369-405`. The supplied rubric assigns degraded-but-correct assurance I8=LOW, not INFO. | LOW |

Severity adjudication: K-1 is a real changed-doc/code contradiction, but the wrong operational outcome is capped at LOW because the production classifier is unconditionally wired and the normally reachable Destructive arms accept only catalogue-bounded case variants. K-2 is not INFO under the provided derivation: local positive-path assurance is genuinely degraded even though PG-enabled CI is the mitigation.

## Coverage adoption and rebuttal

- Adopted independently: Kimi's route inventory, D3 placement, nullopt inversion, encoded-key handling, case-insensitive classification, and exact-send-set analysis. The sole free-form console dispatch is after the gate (`dashboard_routes.cpp:916-1081`); `confine_destructive_targets` maps nullopt to `{}` (`dispatch_destructive_gate.hpp:267-289`); the D3 seam test and both decoded-scope tests behaved as described. These are non-findings.
- Adopted independently: the tests fail discriminatingly if the denial gate or nullopt confinement is removed. The PG-enabled case asserts the exact forwarded list `{"dev-A"}`; the shared seam binds the actual registry send set at `test_dispatch_confined_arms.cpp:569-590`.
- Adopted K-1 into the revised set after verifying both the documentation and all three new dashboard denial arms.
- Rebutted Kimi's Resource Ledger conclusion: absence of an owning raw-resource change permits a ledger entry of “none”; it does not waive the explicit “Resource Ledger omission on a C++ diff” policy floor in `CLAUDE.md:60` and the supplied severity contract.
- Kimi went deeper on duplicate query/body precedence and hardcoded sibling dashboard dispatch sites; I independently checked those paths and found no bypass to adopt. I went deeper on the changed upgrade/audit prose and governance artifact, producing [CDX-P1-01] through [CDX-P1-04], which Kimi omitted rather than refuted.

## Revised findings

[CDX-P1-01]  HIGH · CONFIDENCE(hi) · PROVENANCE(static-read) · unchanged
The required C++ Resource Ledger is absent from the review packet
- Location:  review packet and four commit messages (absent); C++ changes at `server/core/src/dashboard_routes.cpp:916`, `dashboard_routes.hpp:169`, `dispatch_destructive_gate.hpp:234`, `server.cpp:20151`
- Claim:     The C++ range has no range-specific Resource Ledger, which is an explicit policy-floor omission even though the ledger can truthfully record no owning-resource changes.
- Evidence:  `CLAUDE.md:60` lists “Resource Ledger omission” as a policy floor; all four range commit bodies and the supplied packet omit one, while the range changes four C++ files.
- Scenario:  The C++ change is accepted without the mandatory ownership/lifetime attestation -> the governance record contains no accountable proof for the new stored callback and borrowed store/metrics pointers.
- Inference: Independent cpp-safety review found no fd, handle, allocation, thread, callback-context ownership transfer, manual cleanup, or escaping borrow; the required ledger content is therefore “none,” not a resource defect.
- Derivation: TRIGGER = this C++ range proceeds without a range-specific ledger; IMPACT = I9; EXPOSURE = E3; EPISTEMIC STATUS = verified; POLICY FLOOR = Resource Ledger omission.
- Anchor:    `CLAUDE.md` standing rule 2, policy-floor list; `docs/cpp-conventions.md` “Resource ownership and lifetime”.
- Precedent: none found in the range.
- Mitigations: cpp-safety inspection found no ownership defect; policy-floor status remains.
- Fix:       Add a Resource Ledger stating that no owning raw-resource boundary changes and that `DashboardRoutes` borrows the injected pointers while owning the copied callbacks.
- Falsifier: Produce the pre-existing range-specific Resource Ledger supplied for this local review.

[CDX-P1-02]  HIGH · CONFIDENCE(hi) · PROVENANCE(static-read) · unchanged
The upgrade runbook tells dashboard callers to send a field the endpoint ignores
- Location:  `docs/user-manual/upgrading.md:121-127,141-147`; parser at `server/core/src/dashboard_routes.cpp:729-744,886-893`
- Claim:     The remediation and verification tell both affected surfaces to supply `agent_ids`, but the dashboard endpoint never reads that field and encodes one explicit agent as `scope=<agent-id>`.
- Evidence:  The handler extracts only `instruction` and `scope`; only a non-empty, non-group, non-`__all__` `scope` becomes local `agent_ids`. The shipped form names the target input `scope` (`dashboard_ui.cpp:443`).
- Scenario:  An authenticated dashboard automation owner follows the runbook and changes the POST to `agent_ids=dev-A` without `scope` -> the field is ignored -> the destructive command remains untargeted and refused instead of restoring single-agent dispatch.
- Inference: The failure is safe but leaves the advertised migration unavailable and directs the operator away from the actual correction.
- Derivation: TRIGGER = an affected dashboard client follows the changed remediation; IMPACT = I4, I6; EXPOSURE = E3; EPISTEMIC STATUS = verified.
- Anchor:    `.claude/routed-concerns-access-control.md:22` “Dispatch targeting” (upgrading guide update obligation); `CLAUDE.md:61` changed prose that contradicts code is a truth finding.
- Precedent: `server/core/src/dashboard_ui.cpp:443`; `tests/unit/server/test_dashboard_destructive_gate.cpp:380` correctly use `scope=dev-A`.
- Mitigations: browser UI sends the right field; the gate fails safe.
- Fix:       Split by surface: MCP/REST use non-empty `agent_ids`; dashboard automation uses exactly one `scope=<agent-id>`. Correct the verification paragraph likewise.
- Falsifier: Show a production dashboard parser that consumes `agent_ids`, or text expressly excluding dashboard callers from this remediation.

[CDX-P1-03]  HIGH · CONFIDENCE(hi) · PROVENANCE(static-read) · unchanged
The documented verification selector is invalid PromQL
- Location:  `docs/user-manual/upgrading.md:149`
- Claim:     `route="command"|"mcp"|"dashboard"` is not valid PromQL label-matcher syntax, so the new verification instruction cannot be executed.
- Evidence:  The changed inline selector prints that syntax; PromQL alternatives use a regex matcher such as `route=~"command|mcp|dashboard"` or separate selectors. `docs/user-manual/metrics.md:1401` demonstrates the repository's valid `=~` form.
- Scenario:  An operator copies the documented release-verification query into Prometheus/Grafana -> parsing fails -> the runbook cannot confirm exercise of the new dashboard refusal series.
- Inference: The implementation and boot-seeded metric are correct; the operational verification procedure is not.
- Derivation: TRIGGER = operator executes the changed selector; IMPACT = I4; EXPOSURE = E3; EPISTEMIC STATUS = verified.
- Anchor:    `.claude/routed-concerns-access-control.md:22` “Dispatch targeting” update obligation; `CLAUDE.md:61` prose-truth rule.
- Precedent: `docs/user-manual/metrics.md:1401`.
- Mitigations: an operator familiar with PromQL can repair the query manually.
- Fix:       Use `route=~"command|mcp|dashboard"` or three valid selectors.
- Falsifier: Demonstrate that the printed selector parses in the supported Prometheus query engine.

[CDX-P1-04]  HIGH · CONFIDENCE(hi) · PROVENANCE(static-read) · unchanged
The audit guide says refused targets reach server logs, but targeted confinement drops retain no target
- Location:  `docs/user-manual/audit-log.md:129`; `server/core/src/dashboard_routes.cpp:1040-1073`
- Claim:     The new guide says requested `agent_ids`/`scope` reach the server log, but a dashboard `scope_violation` records neither the requested agent in audit nor any server log entry.
- Evidence:  The targeted arm confines `agent_ids`, then emits only `reason=scope_violation <plugin>:<action>` at lines 1062-1063. The warning at lines 1013-1016 belongs to the distinct untargeted arm and its `scope_expr` is empty for an explicit agent.
- Scenario:  Incident response follows the guide after a `scope_violation` denial -> searches server logs for the requested target/blast radius -> the promised evidence does not exist -> the attempted target cannot be reconstructed.
- Inference: Principal, pair, reason, and time remain available, but the guide overstates the forensic evidence.
- Derivation: TRIGGER = responder investigates a dashboard explicit-agent confinement denial using the changed guide; IMPACT = I4; EXPOSURE = E3; EPISTEMIC STATUS = verified.
- Anchor:    `docs/user-manual/audit-log.md` truth obligation supplied as anchor 6; `CLAUDE.md:61` prose-truth rule.
- Precedent: `dashboard_routes.cpp:1013-1016` logs the supplied scope in the separate untargeted refusal.
- Mitigations: audit retains principal, pair, reason, and timestamp; no target-bearing second channel exists.
- Fix:       Log a sanitised/capped requested agent before confinement, or state that this denial retains no requested target in audit or server logs.
- Falsifier: Identify a production log statement reached by this `scope_violation` branch that records the requested agent.

[CDX-P1-05]  LOW · CONFIDENCE(hi) · PROVENANCE(test-run) · unchanged (peer graded INFO)
The only route-level proof that legitimate Destructive targeting dispatches is PG-gated
- Location:  `tests/unit/server/test_dashboard_destructive_gate.cpp:369-405`; skip at `tests/unit/server/test_mgmt_group_pg_helper.hpp:60-63`
- Claim:     Without `YUZU_TEST_POSTGRES_DSN`, every always-running route test proves denial or non-Destructive behavior; the only route test proving the `Targeted` arm reaches dispatch skips.
- Evidence:  Phase-2 rerun: `[destructive]` reported 11 passed, 1 skipped, 75/75; the skipped case contains the exact `agent_ids == {"dev-A"}` assertion at line 384.
- Scenario:  A future edit makes the targeted arm refuse unconditionally -> no-PG macOS/Windows local runs remain green -> only a PG-enabled CI leg catches the loss of legitimate destructive dispatch.
- Inference: Current code is correct by static inspection and PG-enabled CI is the enforcement point; this is degraded local assurance, not a current production defect.
- Derivation: TRIGGER = targeted-arm regression tested on a no-PG local leg; IMPACT = I8; EXPOSURE = E5; EPISTEMIC STATUS = verified.
- Anchor:    judgment; `.claude/routed-concerns-access-control.md:21` clause (5) informs exact-send expectations.
- Precedent: `tests/unit/server/test_dispatch_confined_arms.cpp:569-590` is an always-running actual-registry exact-send test.
- Mitigations: pure helper tests, PG-enabled CI, and the skipped integration test all discriminate when run.
- Fix:       Add an always-running route fixture with an injected present visible set and exact dispatch assertion; retain the PG integration test.
- Falsifier: n/a (non-blocking).

[CDX-P2-06]  LOW · CONFIDENCE(hi) · PROVENANCE(static-read) · new-from-cross-exam
The audit guide overstates plugin/action sanitisation for dashboard denials
- Location:  `docs/user-manual/audit-log.md:129`; `server/core/src/dashboard_routes.cpp:946-951,1013-1023,1062-1063`
- Claim:     The guide says both caller-supplied plugin/action fields are sanitised and capped at 128 bytes, but all three new dashboard denial details concatenate them raw and both new log sites also pass them raw.
- Evidence:  Lines 950-951, 1020-1023, and 1062-1063 contain raw `plugin + ":" + action`; `/api/command` sanitises both at `server.cpp:14830-14831`.
- Scenario:  A SIEM author relies on the documented cap -> dashboard denial rows do not implement it; only a classifier-wiring regression could expose non-catalogue values through the new fail-closed branch in this head.
- Inference: Normal classified arms are catalogue-bounded, and production wires the classifier unconditionally, so the current wrong operational outcome is prevented; the documentation remains false.
- Derivation: TRIGGER = SIEM consumer relies on the claimed dashboard field cap; IMPACT = I9; EXPOSURE = E3, E6; EPISTEMIC STATUS = verified; E6 caps at LOW.
- Anchor:    judgment; anchor 6 requires changed docs to match code.
- Precedent: `server/core/src/server.cpp:14830-14831`.
- Mitigations: exact classification constrains reachable destructive values; production classifier wiring is unconditional.
- Fix:       Sanitize/cap plugin and action in all three new audit details and two log statements, or narrow the documentation claim.
- Falsifier: n/a (non-blocking).

No findings were withdrawn. No reachable Destructive bypass, nullopt inversion, C++ ownership/lifetime defect, concurrency defect, ABI change, or platform-specific construct was found.

VERDICT:  BLOCK — the production gate passes, but the missing mandatory Resource Ledger and three harmful changed-document contradictions remain HIGH blockers under the supplied policy floor and I4 derivation.
COVERAGE: Deep on security/privilege and bypass shapes, D3 scheduling interaction, nullopt fail-closed semantics, parser encoding/precedence, correctness, audit/metrics/upgrade documentation truth, exact-send test adequacy, and C++ ownership/lifetime/portability. Linux and real MSVC execution remain unverified locally; no Linux claim is made. Resource/concurrency/ABI axes were inspected and found unchanged, so sanitizer execution was not indicated.
RAN:      Phase 2: `git diff --check 35bb76a73..cc07a97acc51771544f9cc69c40f2c4d21271e95` clean; `[destructive]` 11 passed/1 PG-skipped/75 assertions; `the shared confined-dispatch seam*` passed; decoded-scope gate case passed (6 assertions); percent-encoded supplied-empty scope case PG-skipped. One initially mistyped percent-encoded filter matched no tests (exit 2), then the exact filters above were run. Phase 1: warm Apple-Clang `CCACHE_TEMPDIR=.cache/advrev-6b/ccache-tmp meson compile -C build-macos yuzu_server_tests` passed; dashboard execute suite 10 passed/10 PG-skipped/74 assertions. CI status: none for this local-only head; Linux not built.
FILES:    Both Phase-1 reviews; all 17 changed files; anchors `.claude/routed-concerns-access-control.md`, `CLAUDE.md`, `docs/cpp-conventions.md`; supporting `dashboard_ui.cpp`, `agent_registry.hpp`, `management_group_store.{hpp,cpp}`, `schedule_runner.cpp`, `schedule_arming_check.hpp`, `rest_api_v1.cpp`, `web_utils.hpp`, `test_dispatch_destructive_gate.cpp`, `test_mgmt_group_pg_helper.hpp`, `.github/workflows/ci.yml`; both C++ skill files. `STREAM.md` is absent.

## Delta since Phase 1

- Added [CDX-P2-06] from Kimi after independently confirming the audit sanitisation mismatch; graded LOW.
- Retained [CDX-P1-01] despite Kimi's objection because the explicit ledger-omission policy floor does not depend on resource changes.
- Retained all three documentation blockers after rechecking their exact failure scenarios; Kimi did not refute them.
- Retained the PG-only positive-path gap at LOW rather than Kimi's INFO because the supplied rubric explicitly maps I8 to LOW.
- Final position remains BLOCK; disagreement remains on the ledger floor, three missed doc blockers, and K-2 severity.
