# kimi — Phase 2 cross-examination: PR6.0b dashboard Destructive targeting gate

Own Phase 1: `.cache/advrev-6b/kimi.phase1.md`. Peer: `.cache/advrev-6b/codex.phase1.md`.
All peer findings were re-verified against the working tree at `cc07a97ac` before labelling.

## 1. Cross-examination of peer findings

| PEER-ID | label | evidence I checked | my severity |
|---|---|---|---|
| CDX-P1-01 (Resource Ledger absent) | confirmed-independently | `ls governance.d/` — no wave6/6.0b ledger; diff file list contains no ledger artifact; CLAUDE.md:60 standing rule 2 floor list names "Resource Ledger omission" as gating "regardless of derived band"; the brief's own POLICY FLOORS list repeats it verbatim | HIGH (policy floor — gates regardless of band; trivially curable) |
| CDX-P1-02 (upgrading.md tells dashboard callers to send `agent_ids`, which the endpoint ignores) | confirmed-independently | `dashboard_routes.cpp:729-743` — handler reads only `instruction` and `scope` (`get_param_value("scope")` + `extract_form_value` fallback); grep shows `agent_ids` is only ever a local vector built from `scope` (line 886-891), never a parsed request field. upgrading.md:141 "**What to do:** switch any such call to explicit, non-empty `agent_ids`" and :146 "**Verify:** re-run the affected dispatch with explicit `agent_ids`" follow a "Who this affects" paragraph that explicitly names the dashboard surface. rest-api.md:6692-6698 correctly describes the dashboard surface in `scope` terms, so upgrading.md is also internally inconsistent with its sibling | MEDIUM — see adjudication below |
| CDX-P1-03 (invalid PromQL selector) | confirmed-independently | upgrading.md:149 prints `...{route="command"|"mcp"|"dashboard",reason="destructive_untargeted"}` as a copy-paste verification query. `|` is not valid inside a label matcher (it is the vector-union operator); the alternation form is `route=~"command\|mcp\|dashboard"` (precedent: metrics.md:1401 `result=~"success\|fresh"`). No promtool on this host to execute the parse; syntax fact is well-established | LOW — see adjudication below |
| CDX-P1-04 (audit-log.md claims refused targets reach server log; targeted `scope_violation` arm logs nothing) | confirmed-independently | audit-log.md `command.dispatch` row caveat: "the requested `agent_ids`/`scope` reach the server log only, not the audit row." `dashboard_routes.cpp:1053-1073` (Targeted→empty arm): audit write + response, **no spdlog call**. The only gate-arm log is `dashboard_routes.cpp:1013-1016` in RefuseUntargeted, which logs `scope_expr` — empty for an explicit single-agent target. So for `scope=dev-A` confined to empty, the requested id is in neither channel | MEDIUM — see adjudication below |
| CDX-P1-05 (positive-path Targeted coverage PG-gated) | agrees-with-mine | same defect as my [K-2]; peer's `test_dispatch_confined_arms.cpp:567-590` precedent (always-running exact-send-set without a DB) is a valid sharper fix pointer than mine | LOW (I re-grade my K-2 INFO→LOW to match peer's I8/E5 derivation) |

### Severity adjudications where I disagree with peer

- **CDX-P1-02: peer HIGH, mine MEDIUM.** Peer derives I4 (harmful operator guidance) + I6. I reject I4: the wrong remediation **fails safe** — following it leaves the Destructive dispatch *refused*, so no unsafe action results; the harm is a migration path that does not work plus misdiagnosis of a correct gate. That fits I6 (capability effectively unreachable via the documented path, base MEDIUM) with no I6→HIGH raise condition met (not false assurance of a security control — the control demonstrably *is* in force; not dormant authz code). E3, no change. Derived: **MEDIUM**. I accept HIGH is reachable only by reading "harmful" as "any misleading guidance," which would make every doc bug HIGH — the "(caps at HIGH)" note on I4 implies it is reserved for guidance that leads the operator into damage.
- **CDX-P1-03: peer HIGH, mine LOW.** The bad selector is rejected by Prometheus with a parse error on first use — self-revealing, no wrong operational decision is ever taken from it. I8 (degraded but a correct path exists and is discoverable), E3 → **LOW**. I4 again requires the guidance to cause harm; a query that fails loudly harms nothing.
- **CDX-P1-04: peer HIGH, mine MEDIUM.** The misdirection is in the forensic-evidence doc for a security gate: a responder hunting the requested blast radius in server logs finds nothing and may suspect log loss/tampering. That is worse than CDX-P1-03 (evidence-location misdirection, not self-revealing) but still not guidance toward a harmful act — the audit row itself (principal, pair, reason, time) is present and correct. **MEDIUM**. The underlying code gap (the `scope_violation` arm could log the sanitised requested id, matching the RefuseUntargeted arm's `scope_expr` log) is the better fix.
- **CDX-P1-01: I withdraw my Phase-1 dismissal.** My Phase-1 non-finding argued "no resource changes → nothing for the ledger to own → floor clear." The floor text (CLAUDE.md:60 and this brief) gates the **artifact's omission on a C++ diff**, explicitly bypassing derivation — "most derive at or below MEDIUM ... which is exactly why they are not run through it." Peer is right on the contract. It blocks until a ledger (content: verified "no new owning raw-resource boundary; stored callbacks/pointers are borrowed for `DashboardRoutes`' lifetime") is produced. Substantively I confirm the ledger's content would be empty-of-findings: the diff adds one `std::function` member, borrowed store/metrics pointers, string building — no fd/HANDLE/thread/C-string/subprocess/temp-path.

## 2. Peer coverage adoption / rebuttal

- **Docs/runbook truth (peer deep, me shallow-but-present):** my Phase 1 (e) verified upgrading.md's RBAC-on refusal *shape* and metrics.md's label set, but I did not check the remediation *field name* against the parser, the PromQL's validity, or the audit-log caveat's per-arm truth. Peer caught three real defects there. **Adopted** (all three independently re-verified above, quoted lines re-read).
- **Resource Ledger floor (peer raised, me dismissed):** adopted after re-reading CLAUDE.md:60; my Phase-1 reasoning was wrong on the contract text.
- **Compile breadth:** peer ran `meson compile -C build-macos yuzu_server_tests` (ccache TEMPDIR workaround) and an extra tag run `[server][dashboard][execute]` (10 passed / 10 PG-skipped / 74 assertions) — consistent with my runs; adopted as corroboration.
- **Axes peer deep on that match mine:** bypass shapes, nullopt inversion, D3 placement, false-green — peer's conclusions agree with mine; nothing to rebut. No peer axis remains unaddressed.

## 3. Defence of my own findings

- **[K-1] (audit-log.md "both caller-supplied fields sanitised" overstatement):** not contradicted by peer; CDX-P1-04 attacks a *different clause* of the same row (the log-vs-audit caveat, not the sanitisation claim). Re-verified: `dashboard_routes.cpp:1020-1023` writes `"reason=... " + plugin + ":" + action` raw, and the spdlog at 1013-1016 sanitises only `scope_expr`. Stands, unchanged, LOW (no reachable injection: in both reachable arms plugin/action are catalogue-derived `ci_equal` exact-matches).
- **[K-2] (PG-gated positive path):** peer filed the same defect as CDX-P1-05 with a sound I8/E5 → LOW derivation. I re-grade my K-2 from INFO to **LOW** and adopt peer's sharper fix pointer (`test_dispatch_confined_arms.cpp:567-590` pattern — an always-running, DB-free exact-send-set route fixture via an injectable visible-agents seam).

## 4. Revised finding list

```
[K-1]  LOW · CONFIDENCE(hi) · PROVENANCE(static-read) · unchanged
audit-log.md's new command.dispatch row overstates field sanitisation for the dashboard emitter
- Location:  docs/user-manual/audit-log.md (`command.dispatch` row: "both caller-supplied fields
             sanitised and capped at 128 bytes") vs server/core/src/dashboard_routes.cpp:1020-1023
             (audit) and 1013-1016 (log)
- Claim:     the doc says plugin/action in `reason=<r> <plugin>:<action>` are sanitised; the
             dashboard's new denial arms write them raw.
- Evidence:  dashboard_routes.cpp:1020-1023 — `"reason=" + ... + " " + plugin + ":" + action`,
             no sanitize_for_log; sibling /api/command at server.cpp:14830-14831 wraps both in
             `onbehalf::sanitize_for_log(..., 128)`.
- Scenario:  in both reachable arms plugin/action are catalogue rows matched by length-bound
             `ci_equal` exact-match — case-variants of compile-time constants, injection-free.
             The only arm where agent-advertised names could flow raw is classifier_unavailable,
             which requires a wiring regression and refuses everything anyway.
- Inference: doc inaccuracy + deviation from the sibling site, not an exploitable injection today.
- Anchor:    judgment (standing rule 3 truth finding; native severity derives I9/I8)
- Precedent: server.cpp:14830-14831
- Mitigations: catalogue-derived values; wiring regression required for hostile bytes. Only guard
             for the doc claim, hence LOW not INFO.
- Fix:       wrap plugin/action in onbehalf::sanitize_for_log(..., 128) in the three new audit
             details + two log lines, or qualify the audit-log.md row.
```

```
[K-2]  LOW · CONFIDENCE(hi) · PROVENANCE(test-run) · severity-changed (was INFO; matches CDX-P1-05)
Positive-path (Targeted with a live visible-agent store) coverage is Postgres-gated and skipped locally
- Location:  tests/unit/server/test_dashboard_destructive_gate.cpp:369-405 ([pg] tag)
- Claim:     the only test proving a Destructive action DOES dispatch to a legitimately targeted
             agent (exact send set {"dev-A"}, dev-Z/__all__ refused through a real store read)
             skips without YUZU_TEST_POSTGRES_DSN; a regression making Targeted always-refuse
             stays green on no-PG macOS/Windows runs.
- Evidence:  local run: 12 cases, 11 passed, 1 skipped, 75/75 — the skip is this case;
             ManagementGroupStore is Postgres-only so no non-PG substitute exists in this file.
- Scenario:  future route edit breaks the Targeted arm -> local/dev runs green -> caught only on
             a PG-enabled CI leg.
- Inference: scripts/ci/ensure-postgres.sh guarantees a reachable instance on CI server legs and
             set-but-broken DSN fails rather than skips, so the residual is CI-leg loss, not
             silent skip. Derivation: I8 / E5 -> LOW.
- Anchor:    judgment (routed-concern clause (5) informs the exact-send-set standard; the PG
             case does satisfy it when it runs)
- Precedent: tests/unit/server/test_dispatch_confined_arms.cpp:567-590 — always-running,
             DB-free exact-send-set test (peer's pointer, verified present).
- Mitigations: CI PG leg; pure-helper tests cover Targeted/confinement independently.
- Fix:       add an injectable visible-agents seam so an always-running route fixture asserts the
             exact dispatch set; keep the PG integration case.
```

```
[K-3]  HIGH · CONFIDENCE(hi) · PROVENANCE(static-read) · new-from-cross-exam (adopts CDX-P1-01)
Resource Ledger omitted for a C++ diff — policy floor
- Location:  no artifact in governance.d/ or the review packet for range 35bb76a73..cc07a97ac;
             diff touches server/core/src/{dashboard_routes.cpp,dashboard_routes.hpp,
             dispatch_destructive_gate.hpp,server.cpp}
- Claim:     the range has no Resource Ledger; the floor gates on the artifact's omission, not on
             whether resources changed.
- Evidence:  CLAUDE.md:60 standing rule 2: "policy floors gate as CONTRACT violations and bypass
             the derivation ... Resource Ledger omission". `ls governance.d/` shows per-range
             *.resource-ledger.md precedent files and none for this range.
- Scenario:  merge proceeds without the Gate-1 ownership/lifetime attestation the governance
             record requires for every C++ diff.
- Inference: substantive content is verified empty — the diff adds one std::function member and
             borrowed store/metrics pointers; no fd/HANDLE/thread/C-string/subprocess/temp-path,
             no manual cleanup. The floor is nonetheless artifact-based; my Phase-1 dismissal is
             withdrawn.
- Anchor:    CLAUDE.md standing rule 2, policy-floor list (contract, not judgment)
- Precedent: governance.d/881-quarantine-hardening.9sPY8b.resource-ledger.md
- Mitigations: independent inspection (both reviewers) found no resource defect — but floors
             bypass derivation, so this does not clear it.
- Fix:       produce the range's Resource Ledger recording "no new owning raw-resource boundary;
             callbacks/pointers borrowed for DashboardRoutes' lifetime".
- Falsifier: produce the pre-existing range-specific Resource Ledger.
```

```
[K-4]  MEDIUM · CONFIDENCE(hi) · PROVENANCE(static-read) · new-from-cross-exam (adopts CDX-P1-02
       at MEDIUM, not HIGH)
upgrading.md's remediation/verify steps name a field the dashboard endpoint never parses
- Location:  docs/user-manual/upgrading.md:141 ("switch any such call to explicit, non-empty
             agent_ids") and :146 ("re-run the affected dispatch with explicit agent_ids");
             handler at server/core/src/dashboard_routes.cpp:729-743
- Claim:     the "Who this affects" paragraph explicitly names the dashboard surface, but the
             remediation field `agent_ids` is REST/MCP-only; the console endpoint reads only
             `instruction` and `scope` (one explicit agent is `scope=<agent-id>`).
- Evidence:  dashboard_routes.cpp:729-743 — `extract_form_value(req.body, "instruction")`,
             `req.get_param_value("scope")`, `extract_form_value(req.body, "scope")`; no request
             field named agent_ids is read anywhere in the handler (grep: agent_ids appears only
             as the local vector built from scope at 886-891). Sibling rest-api.md:6692-6698
             describes the same surface correctly in scope terms.
- Scenario:  console-automation owner follows the runbook, switches to `agent_ids=dev-A` (no
             scope) -> field ignored -> Destructive dispatch still refused as untargeted ->
             capability unrestored and a correct security gate misdiagnosed as broken. Fails
             safe; no broadcast results.
- Inference: derivation I6 (capability unreachable via the documented path) / E3 -> MEDIUM.
             Peer's HIGH reads I4 ("harmful operator guidance"); I reject I4 because the failure
             is safe-direction — see adjudication above.
- Anchor:    .claude/routed-concerns-access-control.md "Dispatch targeting" row names
             upgrading.md as the update obligation; CLAUDE.md standing rule 3 (truth finding,
             native severity)
- Precedent: rest-api.md:6692-6698 (scope-correct description of the same surface);
             tests/unit/server/test_dashboard_destructive_gate.cpp:380 (scope=dev-A)
- Mitigations: fail-safe direction; browser UI submits the correct `scope` field automatically
             (dashboard_ui.cpp:443).
- Fix:       split the remediation and verify steps by surface: MCP/REST -> non-empty agent_ids;
             dashboard automation -> exactly one agent as scope=<agent-id>.
```

```
[K-5]  LOW · CONFIDENCE(hi) · PROVENANCE(static-read) · new-from-cross-exam (adopts CDX-P1-03
       at LOW, not HIGH)
upgrading.md's verification selector is invalid PromQL
- Location:  docs/user-manual/upgrading.md:149
- Claim:     `{route="command"|"mcp"|"dashboard",reason="destructive_untargeted"}` is not
             parseable; label matchers have no `|` alternation.
- Evidence:  the printed selector (quoted above); the valid alternation form `=~"a|b|c"` is used
             elsewhere in the same docs, e.g. metrics.md:1401 `result=~"success|fresh"`.
- Scenario:  operator pastes the verify query -> Prometheus/Grafana parse error on first use ->
             self-revealing; no wrong decision is taken from it.
- Inference: I8 / E3 -> LOW. Peer's HIGH again routes through I4, which I reject for loudly-
             failing, harm-free guidance. No promtool on this host; syntax fact not executed,
             flagged honest.
- Anchor:    judgment / standing rule 3
- Precedent: docs/user-manual/metrics.md:1401
- Mitigations: the metric itself and the boot-seeded dashboard label are correct; the error is
             immediate and obvious.
- Fix:       `route=~"command|mcp|dashboard"` or three separate selectors.
```

```
[K-6]  MEDIUM · CONFIDENCE(hi) · PROVENANCE(static-read) · new-from-cross-exam (adopts CDX-P1-04
       at MEDIUM, not HIGH)
audit-log.md claims refused targets reach the server log; the targeted scope_violation arm logs nothing
- Location:  docs/user-manual/audit-log.md `command.dispatch` row caveat ("the requested
             agent_ids/scope reach the server log only, not the audit row") vs
             server/core/src/dashboard_routes.cpp:1053-1073
- Claim:     for an explicitly-targeted Destructive dispatch confined to empty (scope=dev-A, not
             visible), the requested agent id is written to no channel — the arm audits
             reason=scope_violation and returns; the only gate-arm log (1013-1016, RefuseUntargeted)
             logs scope_expr, which is empty in the targeted shape.
- Evidence:  dashboard_routes.cpp:1053-1073 — audit_fn_ + set_content + return, no spdlog call;
             the RefuseUntargeted warn at 1013-1016 belongs to the other verdict arm.
- Scenario:  incident responder sees command.dispatch|denied reason=scope_violation, follows the
             guide to recover the requested target from server logs -> finds nothing -> may treat
             the absence as log loss/tampering; intended blast radius unrecoverable.
- Inference: I4-lite (forensic misdirection, evidence itself intact) / E3 -> MEDIUM. Peer HIGH;
             I cap lower because the audit row (principal, pair, reason, time) is present and
             correct — the missing datum is the target id only.
- Anchor:    judgment / standing rule 3 (audit-log.md is a named anchor doc for this change)
- Precedent: dashboard_routes.cpp:1013-1016 — the sibling arm does log the sanitised supplied
             scope; the fix is to match it.
- Mitigations: none for the targeted arm; this doc sentence is the only place the expectation
             is set.
- Fix:       either log the sanitised, capped requested agent id in the scope_violation arm
             (preferred — matches the sibling arm), or qualify the caveat per-arm.
```

## Delta since Phase 1 (3–5 lines)

- Withdrew my Phase-1 Resource-Ledger dismissal: the floor is artifact-based (CLAUDE.md:60), peer correct — adopted as blocking [K-3].
- Adopted three doc-truth defects peer caught and I missed (upgrading.md `agent_ids` remediation [K-4], invalid PromQL [K-5], audit-log log-channel caveat [K-6]) — all independently re-verified against the tree; I grade them MEDIUM/LOW/MEDIUM against peer's HIGH/HIGH/HIGH on I4-rejection grounds.
- Re-graded my K-2 INFO→LOW to match peer's I8/E5 derivation (CDX-P1-05).
- K-1 stands unchanged; my Phase-1 verdicts on the gate itself (bypass, D3, nullopt, false-green) were not attacked and remain.

VERDICT:  BLOCK — on the Resource-Ledger policy floor [K-3] alone (trivially curable; substantive ledger content verified empty), plus three confirmed doc-truth defects ([K-4]–[K-6]) at MEDIUM/LOW that should be fixed in the same respin; the gate's production logic itself passed both reviewers.
COVERAGE: deep — security/privilege (all dispatch sites, param parsing, RBAC postures), correctness (verdict mapping, arm exclusivity, D3, nullopt inversion), test adequacy (false-green, exact-send-set, PG gap), doc truth (now including the three peer-caught items, re-verified line-by-line), policy floors (re-derived). Skimmed — concurrency (no new shared state), cross-platform (no platform-specific code; Windows result from brief), resource safety (no new resources — which is precisely what the missing ledger must attest).
RAN:      Phase-2 verification commands: `sed -n` reads of upgrading.md:120-165, audit-log.md command.dispatch row, dashboard_routes.cpp:725-760 / 940-1085; `grep -n agent_ids` over dashboard_routes.cpp (handler reads no such field) and the four edited docs; `ls governance.d/` (no range ledger); grep of CLAUDE.md standing rules 2-3 (floor text + truth-finding rule quoted verbatim); `which promtool` (absent — PromQL claim is static syntax knowledge, not executed). No recompile needed (tree unchanged since Phase 1; peer's fresh compile passed). No CI for this local-only head.
FILES:    docs/user-manual/upgrading.md (full Destructive section), docs/user-manual/audit-log.md (command.dispatch row), docs/user-manual/rest-api.md:6613-6698, docs/user-manual/metrics.md:1401, server/core/src/dashboard_routes.cpp:725-760, 940-1085, CLAUDE.md:56-61, governance.d/ (listing), plus all Phase-1 files.
