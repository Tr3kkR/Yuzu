<!-- The security-posture half of the "Routed concerns" table of CLAUDE.md
     (repo root), pulled in by its @-import — loaded every session, same
     authority as CLAUDE.md itself. Split out of `routed-concerns.md` (Wave 8)
     so the routed-concern tables stay under the ceiling; platform/product/
     data/observability concerns stay in `routed-concerns.md`, auth and
     access-control in `routed-concerns-access-control.md`, and Forensics /
     per-user-software-data concerns in `routed-concerns-software-estate.md`.
     Row discipline unchanged: catastrophic-if-violated invariants + doc
     pointers only — detail goes in the plugin README. -->

| Concern | Doc | Loaded by |
|---|---|---|
| `app_control` plugin — read-only Windows WDAC/AppLocker posture (`Security`, ExecuteGate::None): registry reads (`CI\Policy`, `SrpV2`), a `CiPolicies\Active` listing, a `SiPolicy.p7b` presence stat and `wmi_bounded` CIM only. **CATASTROPHIC-IF-VIOLATED:** never raw COM, never PowerShell, never a policy mutation (#282's add_rule/remove_rule are a separate Destructive-class PR); a failed read returns `constrained` + reason, never an empty success. | `agents/plugins/app_control/README.md` | `security-guardian` + `cpp-safety` on `agents/plugins/app_control/`, `plugin_action_catalogue_app_control.hpp` |
| `platform_security` plugin — read-only Secure Boot / code-signing posture (`Security:Read`, both actions `ReadOnly`/`None`, no mutation). CATASTROPHIC-IF-VIOLATED: (1) failure never reads as absent — a definitive not-present (missing efivar, no lockdown LSM, absent registry value) is its own `absent` row with no failure token and status `OK`; a refused, malformed or oversized read is `unreadable` with a `<key>:<cause>` token through the shared `ConstraintAccumulator`, and a refusal (`EACCES`/`EPERM`/`ERROR_ACCESS_DENIED`) reports `PERMISSION_DENIED` on every OS leg; (2) the whole `execute()` body sits inside ONE `try/catch(...)` (frozen-seam rule), never an exception across the plugin ABI; (3) macOS `code_integrity` is two rung-2 literal-argv leaves (`/usr/sbin/spctl --status`, `/usr/bin/csrutil status`), no shell, no PATH search, each with a sink-manifest row (`platform_security/do_code_integrity#1`/`#2`); the descriptor's rung and support must match the code. The plugin's priority is asserted, not evidenced — the README states that plainly; do not inflate it. **Amended 2026-09-23 (review 5289352195, fjarvis):** clause (1)'s "missing efivar, no lockdown LSM" wording is a definitive-absence case ONLY when the parent pseudo-filesystem (efivarfs/securityfs) is confirmed mounted first (`platform_security_linux.cpp`'s `surface_unavailable`, statfs-magic-checked) — a leaf `ENOENT` from an UNMOUNTED surface is `unreadable`, not `absent`, since the two are indistinguishable from `open()`'s errno alone and a real, shipped container deployment can run with either unmounted. `securityfs` is fully closed by this check; `efivarfs` has a KNOWN, DISCLOSED residual gap in containers that don't bind-mount `/sys/firmware/efi` (the plugin's own README "Caveats and known gaps" #6) — this is a deploy-side fix, not a further source-logic fix, and does not lower this row's own bar for any FUTURE reader/reviewer of this plugin. | `agents/plugins/platform_security/README.md` + `docs/agent-spawn-sink-manifest.md` | `security-guardian`+`cpp-safety` on `agents/plugins/platform_security/`, `plugin_action_catalogue_platform_security.hpp`; `docs-writer` on the plugin README |
| `privacy_permissions` plugin — per-app sensitive-permission grants (camera/microphone/location/full-disk-access equivalents), read-only (`Forensics` securable, `AdminOrApproval`, default-off via the server-side plugin-config kill switch — same posture as `execution_artifacts`): the plugin never requests, revokes or modifies a grant on any platform, only reads one; a refused read (SIP/TCC denial, `ERROR_ACCESS_DENIED` anywhere in the registry walk, a refused session-bus socket or `AccessDenied`-shaped D-Bus error) is `denied`, never silently collapsed into `absent` -- one state for a decoded refusal and a refused read, told apart by `raw` (native value vs `<subject>:<cause>` token; category `-` for a whole source); the one exception is a refused Windows `LastUsedTime*` read, which keeps the row's decoded state, reads the field `unreadable` and still promotes the status to `PERMISSION_DENIED`; every category of every source is a row or covered by a whole-source failure (`category` `-`) row, and a failure is never backfilled as `absent` -- Windows `ProfileList` discovery included: a refused, errored or capped walk is its own row, never a prefix read as complete; the Windows walk is bounded by ONE run-wide retention budget (the ConsentStore is owner-writable) that stops with `collection:budget_exceeded`, and the Linux lookups by one total D-Bus deadline (`<category>:timeout`); precedence is most-restrictive -- only a successfully read HKLM ConsentStore `Deny` overrides a profile at the same level, an HKLM `Allow` defers (`merge_with_hklm`, Microsoft's device-toggle model, confirmed on the-rig 2026-09-23); the capability toggle, the `NonPackaged` desktop-apps toggle and each app are separate rows, and no effective state is computed; TCC.db is opened in-process, never via a `sqlite3` CLI shellout. Disclosed residuals (README caveats): macOS reads the system + every `/Users` per-user TCC.db and an agent without Full Disk Access reads all of them `denied` -- the production root LaunchDaemon's real outcome and a real refusal's `sqlite3_errmsg` are unmeasured; Linux rows are the agent's OWN session's grants, never machine-wide (a system-service agent reports `UNAVAILABLE`, saying nothing about interactive users); Windows is measured on one non-MDM host (no offline-hive arm, no HKLM `Deny`, no refused read yet). | `agents/plugins/privacy_permissions/README.md` | `security-guardian` + `cpp-safety` on `agents/plugins/privacy_permissions/`, `plugin_action_catalogue_privacy_permissions.hpp` |
