<!-- The auth / access-control / request-admission half of the "Routed
     concerns" table of CLAUDE.md (repo root), pulled in by its @-import —
     loaded every session, same authority as CLAUDE.md itself. Split out of
     `routed-concerns.md` (#2899) so both files stay under the 40k-per-file
     ceiling; the platform/product/data/observability concerns remain in the
     sibling `routed-concerns.md`. Row discipline unchanged:
     catastrophic-if-violated invariants + doc pointers only — detail goes in
     the routed doc. -->

| Concern | Doc | Loaded by |
|---|---|---|
| Authentication, RBAC, headers, tokens, self-target principal-destruction guard (#397/#403). Every new list/fan-out read of per-agent data MUST use the admit-then-filter `authorize_list_read` chokepoint (World A, ADR-0017) — never a bare global `require_permission` (inert for confined operators — the World A gap; `require_permission`/`require_scoped_permission` themselves fail **closed** on any `rbac_store` degrade — the PostgreSQL authorization substrate denies on a pool-acquire timeout / query error / store-not-open (deny-on-degrade, ADR-0041, extending the #1717 corrupt-store fix; `yuzu_server_rbac_read_degrade_total`) — but a bare gate still discloses the whole fleet to any global-permission holder, unnarrowed by management-group confinement). **Engine principals** (ADR-1005 class; `engine_principal_store.*`, ADR-0031) are default-deny: they resolve authority RBAC-only in `require_permission`/`require_scoped_permission` (never the pre-RBAC legacy or service-scoped fallback — 403 RBAC-off/no-grant, 503 store-unavailable) and can **NEVER** hold admin/built-in/wildcard (`dangerous`-class gate `validate_assignment`). Fleet-wide grants are authored via `/api/v1/engine-principals/{id}/roles` + MCP twins; the boot-time `engine:` namespace-collision scan fails **closed** (a non-open/degraded `rbac_store` refuses boot). | `docs/auth-architecture.md` + `docs/adr/0031-engine-principal-store.md` | `security-guardian` on auth/RBAC/crypto/header/token change, incl. `engine_principal_store.*` / `/engine-principals` routes / `principal_quota*.{hpp,cpp}` (cap + gate + denial) |
| **Service-scoped API token confinement — durable default-deny (guardian-confinement-2298 PR 3, "the flip")** — a service-scoped API token (`session->token_scope_service` non-empty) must never reach fleet-wide data/actions unfiltered. CATASTROPHIC: (1) **`AuthRoutes::require_permission`'s service-scoped branch is now the PRIMARY closure mechanism** — ITServiceOwner is the authority ceiling only; a `(securable_type, operation)` pair must ALSO clear the seeded-EMPTY `kServiceScopeGlobalSafe` allow-list (`service_scope_policy.hpp`) or it 403s. Widening that allow-list is a security decision (security-guardian sign-off + policy-table entry, never inferred from one route being changed). (2) **Branch order in `require_permission`/`require_scoped_permission` is fixed — `elevated → engine → mcp_tier → service → RBAC-enforced → legacy` — never reorder it**: the elevated branch is guarded (`is_elevated && token_scope_service.empty()`); the `mcp_tier` branch must never `return true` on its own (deny or fall through into the service branch only) — a tier-admit that bypasses the service check reopens the flip on every route at once. (3) **MCP's mirror is C8's `ServiceScopeClass`** (`denied` default; `confined` requires a REAL downstream confinement mechanism — a label with none is the same lie this concern exists to prevent; `global_safe` boot-validates against the same allow-list). (4) A route reaching agent/fleet data via NO `require_permission`/`require_scoped_permission`/C8 call at all is untouched by the flip and needs its OWN explicit deny — **EXTEND an existing per-file helper, never fork a new one**: `AuthRoutes::deny_service_scoped_session` (`server.cpp`), `ComplianceRoutes::deny_service_scoped_`, `WorkflowRoutes`' local `deny_service_scoped_schedule_list`/`deny_service_scoped_scope_estimate`, `deny_service_scoped_`/`deny_service_scoped_mutation_` (`guardian_routes.*`), `deny_service_scoped_` (`dex_routes.*`, `deployment_routes.*`, `preflight_routes.*`), the shared `deny_fleet_wide_service_scoped` lambda (`rest_api_v1.cpp`, `mcp_server.cpp`), plus inline checks in `device_routes.cpp`/`network_routes.cpp`/`inventory_routes.cpp`/`tar_tree_routes.cpp`. These per-file helpers are now a SECOND, largely-redundant layer for routes the flip already covers — scheduled for Phase 2 retirement, not removed here. `schedule_routes.*`'s `deny_service_scoped_schedule` was the first FULLY retired (#3290 Phase 2 bucket 1a, 2026-08-21) — all four of its call sites were provably dead (fired after `require_permission`), unlike the still-live-but-redundant helpers above, which fire before their route's gate and so are a different, more cautious retirement class. (5) A blanket deny's A4 error body MUST NOT name a `.permission` the caller does not currently hold if holding it would not, by itself, admit the caller — omit the field rather than claim a false self-remediation grant (found live in this PR's own `require_permission`/`require_scoped_permission` service branches — fixed in the same round that wrote this clause). This MUST binds: every branch inside `require_permission`/`require_scoped_permission`'s service-scoped path; every NEW gate-less-route deny added after this clause was written; and every site in (4) already migrated to the no-`.permission` `a4_denial` shape (as of this round: `AuthRoutes::deny_service_scoped_session`, `ComplianceRoutes::deny_service_scoped_`, `WorkflowRoutes`' `deny_service_scoped_schedule_list`/`deny_service_scoped_scope_estimate`, `GuardianRoutes::deny_service_scoped_`/`deny_service_scoped_mutation_`, `DexRoutes::deny_service_scoped_`, `DeploymentRoutes::deny_service_scoped_`; #3167 closed the remainder — `PreflightRoutes::deny_service_scoped_`'s 3 call sites plus its separate inline deny on the MUTATING `POST /fragments/auto/run`, `schedule_routes.*`'s `deny_service_scoped_schedule` + its 4 call sites [each already passed a correctly-typed permission per operation — the defect was unactionability, not a wrong permission TYPE, a claim this row previously made in error], and the inline checks in `device_routes.cpp`/`network_routes.cpp`/`inventory_routes.cpp` (x2)/`tar_tree_routes.cpp` (x2)). It does NOT retroactively bind one known, tracked exception: pre-existing `deny_fleet_wide_service_scoped` call sites that fire AFTER the route's own `perm_fn` already confirmed the caller holds that exact grant, where `.permission` names a grant the caller demonstrably already has — a different, informational claim. (`GET /api/v1/inventory/software` no longer illustrates this — #3290 retired its after-gate deny entirely, migrating the route onto `require_fleet_read`. NO LIVE EXAMPLE CURRENTLY EXISTS: an exhaustive check of every remaining `deny_fleet_wide_service_scoped` call site — all 20 in `rest_api_v1.cpp` and (as of #3290 Phase 2 bucket 1a, 2026-08-21) all 5 remaining in `mcp_server.cpp` — found every REST site fires BEFORE its route's `perm_fn`, not after [`docs/security-reviews/service-scope-phase2-migrations-2026-08.md`'s migration checklist]. `deny_service_scoped_schedule` (previously 4 sites, `permission` param defaulting empty) and the one MCP site whose surrounding `perm_fn` fired first (`get_dex_group_app_perf`) are no longer part of this count at all — both were retired outright in bucket 1a as provably dead, not merely candidates that failed to match the exception shape. The exception clause is a live invariant with no current instance to test it against — do not let that read as "no longer needed": the next route to add an after-gate `deny_fleet_wide_service_scoped` call with a non-empty `.permission` becomes the example, and this clause still governs it.) A gate-less-route deny not yet audited against this clause is NOT assumed compliant. (6) **The tag being read to decide admission must never be mutable by that same admission (#3289).** `require_scoped_permission`'s service branch reads the target agent's `service` tag to decide whether to admit a `Tag:Write`/`Tag:Delete` — so without a separate guard it would authorize the very write that changes that tag, letting a service-scoped token move an agent out of (or a different agent into) its own confinement. EXTEND `authz::service_scope_may_mutate_tag_key` (`service_scope_policy.hpp`) at every tag-mutation site — REST v1 PUT/DELETE `/api/v1/tags`, the legacy dashboard `/api/tags/set`/`/api/tags/delete` (via `AuthRoutes::deny_service_scoped_service_tag_mutation`), and MCP `set_tag`/`delete_tag` — checked BEFORE the scoped gate, value-blind (no read-compare TOCTOU: even a no-op rewrite of the token's own current value is denied). The agent's own gRPC `Register`→`TagStore::sync_agent_tags` path is the same class of gap on a DIFFERENT axis (no gate at all, not a TOCTOU) — `authz::is_service_tag_key` is filtered there too, silently dropping an agent-claimed `service` value rather than refusing the whole sync (a pre-existing agent-authored row self-heals on the next sync). A live agent's in-memory self-reported tags shadowing the store during scope-DSL evaluation (`agent_registry.cpp`) is a THIRD, distinct mechanism this clause does not cover — tracked separately (#3295). Full design, decisions, and the closed route inventory: `docs/adr/1006-service-scope-default-deny.md` + `docs/auth-architecture.md` "Service-scoped token fleet-wide confinement" + `docs/security-reviews/service-scope-flip-route-inventory-2026-08.md`. | `docs/adr/1006-service-scope-default-deny.md` + `docs/auth-architecture.md` "Service-scoped token fleet-wide confinement" | `security-guardian` on any change to `require_permission`/`require_scoped_permission`/`service_scope_policy.hpp`/MCP's `ServiceScopeClass`, any NEW instance of a gate-less fleet-data route, or any widening of `kServiceScopeGlobalSafe`; `architect` before consolidating the per-file helpers into one chokepoint (Phase 2) |
| PKI / internal CA — `ca.db` (`CaStore`) holds cert metadata + CRL versions + the root key's opaque `key_ref` ONLY; the CA root **private key is never in the DB** (behind `KeyProvider`). `sign_agent_csr` is the single shared signer for direct `Register` AND gateway `ProxyRegister` — subject/SAN/EKU server-chosen, CSR ignored. Revoke is serial-scoped (DENY an agent to stop re-issuance). Shipped images + the reference composes are TLS secure-by-default (#1314 dropped `--no-tls`/`--no-https` from the image CMDs; the agent auto-discovers the install CA) — a **`--no-tls`/plaintext gateway `:50051` is a fleet-RCE edge, do not internet-expose it**. Gateway transport TLS is driven by the mounted grpcbox `sys.config`, NOT the still-inert `YUZU_GW_TLS_*` env vars (#1291). | `docs/pki-architecture.md` | `security-guardian` + `cpp-safety` + `docs-writer` on `x509_ca.{hpp,cpp}` / `key_provider.{hpp,cpp}` / `secure_buffer.hpp` / `aes_gcm.hpp` / `pg/secret_codec.{hpp,cpp}` (ADR-0010 secrets seam — also on any new `KeyProvider`/`KekProvider` subclass) / `ca_store.{hpp,cpp}` / `agent_csr.{hpp,cpp}` / `ca_routes.{hpp,cpp}` / `/api/v1/ca/`; `gateway-erlang` on `gateway/config/sys.config*` / gateway `*_pb.erl` / `yuzu_gw_app.erl` TLS |
| AuthDB invariants — Postgres schema `auth` (ADR-0006; SQLite `auth.db` retired), fail-closed construction, `SecretCodec` (TOTP, ADR-0010 first consumer), fresh-start seed-vs-live, role-field-ignored, gate audit, MFA fail-closed on secret-read failure, cleanup cadence, snapshot-and-release. Sessions are in-memory-only — no durable session surface on this store. | `.claude/agents/authdb.md` | `authdb` agent on `auth_db.{hpp,cpp}` / `auth_routes.*` / `auth.{hpp,cpp}` change |
| **SCIM ↔ OIDC/SAML identity-link revoke chokepoint (ADR-2001, CC6.8)** — closes a SCIM deprovision revoking zero of a federated (SSO) user's credentials while reporting success, because a SCIM slug and its `oidc:<iss>#<sub>` / `saml:<entity_id>#<NameID>` login identities are disjoint `auth.users` rows / sessions. CATASTROPHIC: (1) `oidc_principal_id(iss,sub)` (`oidc_principal.hpp`) is the SINGLE builder for the `oidc:<iss>#<sub>` RBAC/session/link key across every construction site (login mint, deprovision resolver) — EXTEND, never hand-build; a format drift silently misses tokens on revoke ("reported success, revoked nothing"). Same chokepoint class as `dispatch_target_shape.hpp`/`authz_topology_floor.hpp`/`body_cap_policy.hpp`. (2) **Single-issuer precondition (ADR §5).** Link formation is safe ONLY under one trusted OIDC issuer — `ScimStore::find_by_external_id`/`find_unique_active_by_external_id` are global `WHERE external_id=$1` with NO `iss` predicate, and `scim_resources` carries no `iss` column. A future multi-IdP OIDC change MUST partition the externalId space by issuer (add `iss` to the SCIM resource or the match) before this design is safe. (3) Deprovision (`deprovision_revoke.{hpp,cpp}`) is credentials-FIRST (tokens before sessions, every linked principal) and MUST NOT reuse `session_revoke_fn` (`server.cpp`) — that helper is session-first and carries `caller=self|admin` metric semantics that do not apply to a termination; always the dedicated per-principal orchestrator here. (4) **D1 preserves #2021** — a SCIM slug elevated to admin *outside* SCIM is NOT auto-revoked on an IdP's unilateral deprovision call; the refusal is made LOUD instead (`scim.user.deprovision_role_refused_with_link` audit row + `yuzu_scim_deprovision_role_refused_with_active_link_total`), never a silent gap. (5) **PR3 deny-at-login (§4) — three invariants, silently-regressible:** `ScimStore::linked_resource_active` MUST join `identity_links` to `scim_resources` with a LEFT JOIN, never INNER — an INNER join collapses a hard-DELETEd (orphaned-link) resource to zero rows, which reads as "no link" and lets a fully-deprovisioned identity re-authenticate; the decision is fail-**CLOSED** — a store-unavailable `linked_resource_active` result denies the login, it never proceeds as "unlinked"; and the orphaned branch (`active == nullopt`) DENIES unless an active reprovision sibling exists for the login's externalId (`find_unique_active_by_external_id`) — an UNCONDITIONAL proceed reopens the same orphan/DELETE bypass (a genuinely-deprovisioned identity re-authenticates) **while both other invariants still hold**, so it is guarded only by this branch's logic + its mutation-checked test, not by the LEFT-JOIN/fail-closed pair. The reprovision check is scoped to the orphaned branch ONLY: the `external_id` unique index means an explicitly-deactivated (row-present) resource can have no active same-externalId sibling, so the deactivated branch stays unconditional-deny. (6) **SAML side (ADR-2001 PR4a, `saml_principal_id(entity_id,name_id)` = `"saml:" + entity_id + "#" + name_id`, `saml_principal.hpp`) is the SINGLE builder for the SAML RBAC/session/link key, same chokepoint discipline as (1)** — both the session-mint site (`AuthManager::create_saml_session`) and the deprovision resolver (`deprovision_revoke.cpp`'s SAML pass) route through it; a drift here silently misses a SAML session on revoke the identical way. Two SAML-specific invariants that do NOT apply on the OIDC side: the **single-IdP precondition is structural, not a runtime partitioning rule** — Yuzu accepts assertions from exactly one pinned `--saml-idp-cert`/`--saml-idp-entity-id`, so a bare NameID→`externalId` match is safe by construction without an `iss` predicate; and a **SAML link forms ONLY for a STABLE NameID Format** (`persistent` or SAML 1.1 `emailAddress`, `saml::is_linkable_name_id_format`, `saml_scim_link.hpp`) — a `transient`/`unspecified`/missing Format MUST NEVER be linked (the login still succeeds; that identity is simply unrevocable via SCIM), and Yuzu MUST NEVER normalize an unstable NameID into a linkable one. (7) **PR4b SAML deny-at-login — the same three (5) invariants, ported unchanged in shape:** `ScimStore::saml_linked_resource_active` MUST LEFT JOIN `saml_identity_links` to `scim_resources`, never INNER, for the identical reason (5) states; the decision is fail-**CLOSED** on store-unavailable; and the orphaned branch DENIES unless `find_unique_active_by_external_id(name_id)` finds an active reprovision sibling — SAML has no separate link-claim value, so this always resolves against `name_id` itself. `saml::saml_login_denied_deprovisioned` (`saml_scim_link.{hpp,cpp}`) is the single decision function both `/saml/acs` call sites (primary pre-mint, post-mint re-check) share — a hand-built third copy is the same drift (1) warns against. Denies redirect to the byte-identical `/login?error=saml`, audit `auth.saml.deprovisioned_denied`, metric `yuzu_auth_saml_deprovisioned_denied_total`; inherits the `--scim-enable` gate for free via the shared, already-null-checked `AuthRoutes::scim_store_` member — no new feature-off coupling. (8) **SAML D2 observability (#3072) — three invariants:** the unconditional `record_saml_login_observation` write in `link_saml_login_to_scim` is OBSERVE-ONLY — the NameID is NEVER normalized before recording, and the observation NEVER promotes an otherwise-non-linkable (unstable-Format) NameID into a link; `scim_routes.cpp`'s `maybe_flag_saml_d2_unlinked` MUST query `saml_links_for_scim_id` SPECIFICALLY, never OIDC's `links_for_scim_id` or a `principals.size()` proxy — an OIDC link on the same `scim_id` must never mask a missing SAML link, and vice versa (the identical PR4a C2 lesson (6) already states, re-applied here); and the two new login-time audit verbs (`auth.saml.link_unmatched`, `auth.saml.link_lookup_failed`) are OBSERVE-AND-PROCEED — every `SamlScimLinkOutcome` branch still falls through to session mint, NEVER a deny (PR4b's deny-at-login backstop above is the only SAML-side path that can refuse the login). | `docs/adr/2001-scim-oidc-identity-linkage.md` + `docs/auth-architecture.md` "SCIM ↔ OIDC identity linkage for deprovision" + "SAML ↔ SCIM identity linkage" + "SAML D2 observability (#3072)" | `security-guardian` + `authdb` on `deprovision_revoke.{hpp,cpp}` / `oidc_scim_link.{hpp,cpp}` / `oidc_principal.hpp` / `saml_scim_link.{hpp,cpp}` / `saml_principal.hpp` / `scim_store.*`'s `identity_links`/`oidc_login_observations`/`saml_identity_links`/`saml_linked_resource_active`/`saml_login_observations`/`record_saml_login_observation`/`saml_observation_matches`/`find_unique_active_by_external_id_checked` change, or `scim_routes.cpp`'s `maybe_flag_saml_d2_unlinked` |
| Periodic Access Reviews (SOC 2 CC6.2) — fleet-wide grant export + attestation-campaign lifecycle across all three RBAC principal types. CATASTROPHIC-IF-VIOLATED: (1) **#2225 global gate is deliberate — do NOT "fix" it onto `authorize_list_read`.** A grant/role assignment has no per-agent/management-group boundary to admit-then-filter against, and a confinement-filtered slice would be worthless as CC6.2 evidence (an auditor needs the *complete* grant population, every time) — the global gate is a *stronger* least-privilege posture via the seeded `Reviewer` role, not a weaker one. The gate is the **dedicated** `AccessReview:Read`/`AccessReview:Attest` securable (seeded ONLY to Administrator + Reviewer) — **never** `AuditLog:Read`/`AuditLog:Attest`: an earlier round used the latter and over-disclosed the full fleet-wide grant population to the `Operator`/`PlatformEngineer` roles (both hold `AuditLog:Read` for unrelated reasons, neither holds `UserManagement:Read`/`EnginePrincipal:Read`, the permissions gating the equivalent-sensitivity `/rbac/roles`/`/engine-principals/{id}/roles` routes (the latter moved off `Security:Read` in #2376)) — do NOT revert to `AuditLog:*` for this surface (#2225 round 2). (2) **flag ≠ revoke.** `record_attestation`'s `decision:"flagged_revoke"` records evidence ONLY — never wire it to an actual RBAC/`EnginePrincipalStore` mutation; acting on a flag stays a separate, explicit, separately-gated operator action. (3) **Grant-table-driven completeness (UP-1) — never revert to a roster walk.** `build_access_review` must read the grant table directly (`RbacStore::list_all_principal_roles_checked()`) so a grant belonging to a principal outside every roster (deleted user, stale IdP row, orphan OIDC/SSO principal) is surfaced as `source="orphan"` rather than silently omitted — a roster-walk regression reintroduces a silent false-negative in exactly the evidence this feature exists to produce. (4) **Deliberate no-prune — never copy the `/auto` Preflight 14-day-prune cadence.** A closed campaign IS the compliance evidence and persists indefinitely by design; any retention policy is a separate, explicit, separately-reviewed decision. | `docs/auth-architecture.md` "Periodic access reviews" + `docs/security-reviews/access-reviews-2026-07-21.md` | `security-guardian` + `docs-writer` on `access_review_model.*` / `access_review_store.*` / `/api/v1/access-reviews` |
| Enterprise A&A roadmap — RBAC, OIDC, SAML, SCIM, MFA, AD/Entra, API tokens, session lifecycle, audit | `.claude/skills/auth-and-authz/SKILL.md` | invoke `/auth-and-authz` for any A&A planning, audit, or implementation work |
| MCP server architecture, tier-before-RBAC ordering, kill switches, audit pattern; Streamable HTTP transport (sessions, GET SSE, progress bridge) is committed direction — ADR-1005 exec-plan Decision 15 (binding security/bounds pre-commitments a–k) / track 2f. CRITICAL: `StreamBudget` (`stream_budget.hpp`) is the ONE admission budget for EVERY held-open SSE response on the shared httplib pool — MCP GET, streamed POST (2f 3b), `/api/v1/events`, the dashboard drawer, and legacy `/events` ALL lease from it; a new streaming surface leases from that instance, never a parallel counter (httplib is thread-per-connection: an unleased stream pins a worker nobody is counting). The pool is derived FROM `--max-sse-streams`, not the reverse (ADR-0034). Other channel invariants: `mcp_stream.hpp` header block | `docs/mcp-server.md` | `security-guardian` on `/mcp/v1/`, `mcp_server.{hpp,cpp}`, `mcp_jsonrpc.hpp`, `mcp_policy.hpp`, `mcp_transport.{hpp,cpp}`, `mcp_session.{hpp,cpp}`, `mcp_stream.{hpp,cpp}`, `stream_budget.hpp`, `mcp_stream_bridge.{hpp,cpp}` change |
| Per-device dispatch visibility (#1788) + the registry-independent authz model (ADR-0033 §2). CATASTROPHIC: (1) **one intersection, not per-caller** — `dispatch_confined_arms` (`server/core/src/dispatch_confined_arms.hpp`) decides WHO IS REACHED for every operator dispatch surface; `ServerImpl::dispatch_confined` and the `/api/command` inline handler both route through it, and a second copy is the drift this seam exists to remove (two byte-identical copies previously existed). Callers own only target RESOLUTION and error shaping. (2) **`nullopt` and present-EMPTY are not interchangeable** — `nullopt` = unfiltered, present-empty = deny-all (ADR-0033 §1); an unwired `ExecVisibleFn` MUST NEVER resolve to `nullopt`, or a missing derivation reads as "no filter". What it resolves to INSTEAD is by route class, and the two are not alternatives to pick from: where the derivation is defense-in-depth behind a per-target `scoped_perm_fn` gate (REST bundles, TAR retention purge, DEX live query) it MUST substitute present-empty — the primary gate already 500s when IT is unwired, so the misconfiguration is reported there; where the derivation is the route's ONLY per-device authorization (the three async result-set producers, which admit on a bare global `Execution:Execute` and then dispatch by scope/`__all__`) it MUST refuse with an AUDITED 500 rather than substitute anything, because a silent deny-all is answered to the operator as "no agents reached in the target scope", which reads as an empty fleet and hides the broken gate. Substituting present-empty on a producer, or a 500 where a primary gate already reports, is a violation of this clause in either direction. A caller wanting genuinely unfiltered dispatch wires a callback that RETURNS `nullopt`; leaving the callback empty is never a synonym for it. (3) A management group / scope expression / `__all__` is a TARGETING mechanism, never an authz exemption. (4) Service-scoped-token confinement takes precedence over the minter's own global grant. (5) Confinement tests MUST assert the exact send set — route mocks that only observe the `VisibleSet` being handed over stay green while the intersection is deleted (the false-green closure evidence that shipped once already); `tests/unit/server/test_dispatch_confined_arms.cpp` is the binding layer. | `docs/authz-model.md` + `docs/adr/0033-access-control-spine.md` | `security-guardian` + `architect` on `authz_model.hpp` / `dispatch_confined_arms.hpp` / `derive_exec_visible` / any new `ExecVisibleFn` wiring or dispatch surface |
| **Dispatch targeting — omitted means the fleet, supplied-but-names-nothing is a 400 (#2500/#2492).** ONE rule, `check_targeting_shape` in `server/core/src/dispatch_target_shape.hpp`, shared by MCP `execute_instruction` and the REST twins — a second copy is how the identical defect survived on REST after MCP was fixed. It MUST be run on the PARSED BODY, never on extraction output: `extract_json_string_array` returns `{}` for omitted, empty, non-array and parse-failure alike, and that erasure IS the defect. It requires a JSON **object** — `contains()` is false for an array, which reads as "no target" and broadcasts. Broadcast is named, never inferred: `kBroadcastScope` (`__all__`, a PUBLISHED ground scope kind) is the only way to reach `send_to_all` from the shared `command_dispatch_fn`, whose empty+empty case reaches NOBODY. `classify_dispatch_arm` decides the branch for ALL FOUR dispatch closures — an explicit `agent_ids` list ALWAYS wins over a broadcast request; putting the broadcast branch first is how a widening got added by the change that removed widenings. A new dispatch caller must name `__all__`, never rely on empty-fallthrough. | `docs/mcp-server.md` (targeting bullet) + `docs/user-manual/rest-api.md` | `security-guardian` + `architect` on `dispatch_target_shape.hpp` / `command_dispatch_fn` / any new dispatch caller / `/api/command` / `/api/instructions/*/execute` / result-set `parent_id` |
| Agent privilege model — dedicated `_yuzu` / `yuzu` / `NT SERVICE\YuzuAgent` account, narrow sudo NOPASSWD + LSA privileges, per-plugin privilege matrix, prod virtual-service-account vs dev local-user, install scripts `scripts/install-agent-user.{sh,ps1}`. | `docs/agent-privilege-model.md` | `security-guardian` on any plugin shell-out / sudoers / setcap change; `cross-platform` on any change that gates a plugin behind a privileged command; `plugin-developer` when adding a new privileged plugin (the doc has the procedure) |
| **Headless platform, process decomposition & access-control spine (ADR-1005 + ADR-0031/0032/0033)** — ADR-1005 (merged): every capability behavior reachable by an authenticated external principal via versioned REST **and** MCP (or a recorded ledger exception), discoverable, A4 envelope, RBAC + audit at the API — **no UI-only capabilities** (a dashboard fragment is not an API twin). **On-behalf-of rejected on every ingress** (sole exception: the four health-probe paths). **Use-case interpretation lives in separately-deployed engines, NOT the server** — the first module re-homes the server-side NVD sync/matching (strangler); M3 = the parity **+ confinement** gate. ADR-0031/0032/0033 (accepted 2026-07-14, **design-only, binds prospectively**): presentation / core / engine are separate binaries; **core owns the public API and is the sole authority** — others may only **narrow** authority. **No private core API** (INV-31-4); **no cross-component DB access** (INV-31-3). Engines are **headless** (voids 2c F-10 + exec-plan Decision 3): core admits every run, mints a short-lived **audience-bound grant**, and confines every engine read against the **admitting operator's current** authority; **no grant is a bearer capability over data** — a cached result is redeemed at core. **Interlock: no ballot-A5 code ships before engine principals + the ADR-0017 gate (#1714; prerequisite #1715 open — #1716 is the closed doc-honesty companion, NOT the gate) + the audit-evidence schema + the release log + the capability registry.** **One core-owned approval primitive — no new approval gate outside it**; four-eyes compares distinct **human roots**; effects cross core as plan-hash-bound **Execution Plans**; mutations fail closed on audit failure. | `docs/adr/1005-...md` + `docs/adr-1005-execution-plan.md` + `docs/adr/003{1,2,3}-*.md` | `consistency-auditor` on every capability-adding PR; `architect` + `security-guardian` on any new REST route / MCP tool / dashboard fragment / ingress / **approval gate** / engine surface, and on `on_behalf_guard.hpp` / `grpc_on_behalf_interceptor.hpp` / `principal_class.hpp` / the pre-routing chokepoint in `server.cpp`; `docs-writer` on the 2c / host-requirements / exec-plan docs |
| **Authorization-topology floor (#2376)** — `authz_topology_floor.hpp`'s `kTopologyFloor` is the SINGLE chokepoint deciding which reads require admin **regardless of the RBAC on/off toggle** (RBAC ships OFF, so the legacy fallback's "every Read is allowed" rule otherwise discloses the authorization topology — the access-review export, `/rbac/roles`, the engine-principal grant graph — to any authenticated session). CATASTROPHIC: (1) **EXTEND the set, never fork it** — a second floor list is the drift this seam exists to remove, and it is `dispatch_confined_arms`'s failure mode; (2) the floor is applied ONLY inside the legacy branch of `require_permission`/`require_scoped_permission`, strictly BELOW the `rbac_enforcement_in_effect` return — moving it above shadows a live RBAC grant and silently destroys the non-admin `Reviewer`'s `AccessReview:Read`, which is the entire reason #2324 cut a dedicated securable; (3) BOTH legacy branches carry it — no floored pair reaches the scoped one today, and it is floored defensively so a future scoped topology read cannot bypass it. `EnginePrincipal:Read` exists to keep the engine-principal inventory/grant-graph reads OUT of the over-broad `Security:Read`, which also gates quarantine, CA issued-certs and KEK status — do not re-merge them. (4) **The floor SET is derived from the DATA, never from the routes you happen to be changing.** Any surface emitting authorization topology — the access-review export, the RBAC role graph, the engine-principal grant graph — MUST be gated on a floored securable, or split so the topology half is. `/api/v1/discover/permissions` + MCP `discover_permissions` served the FULL role→permission grid on the unfloored `Infrastructure:Read` and were a live bypass past a 14-agent review; they are now split (taxonomy at `Infrastructure:Read`, grid behind `UserManagement:Read`, omission DECLARED via `roles_omitted`). Before adding a route that reads `get_role_permissions`/`list_roles`/`list_all_principal_roles_checked`, check its securable against the floor. | `docs/auth-architecture.md` "authorization topology floor" + `docs/security-reviews/authz-topology-floor-2026-08-05.md` | `security-guardian` + `architect` on `authz_topology_floor.hpp` / the legacy branches of `require_permission`/`require_scoped_permission` / any change to the `EnginePrincipal` securable or its route set |
| **Pre-auth request-body cap (#2407)** — `body_cap_policy.hpp`'s `kBodyCapTable` is the SINGLE per-route body-cap chokepoint deciding how many bytes an unauthenticated caller may make the server buffer; EXTEND it, never fork it — a second cap list is the same drift `authz_topology_floor.hpp` and `dispatch_target_shape.hpp` exist to prevent for their own chokepoints. CATASTROPHIC: (1) enforced in `server.cpp`'s PRE-ROUTING handler, before httplib buffers the body — a handler-level check is too late, the oversized payload is already buffered by then; (2) `path_class` is the ONLY legal metric label — never the raw, attacker-controlled path; (3) matching is SEGMENT-BOUNDARY, never bare `starts_with` (`/api/v1/bundlesevil` must not inherit `/api/v1/bundles`'s cap); (4) a new mutating route needs its own entry or a reviewed catch-all decision — prefer any-method rows, since verb-scoping silently drops the whole prefix to the catch-all; (5) `requires_measurable` defaults OFF — on an opted-out class only an UNMEASURABLE body is uncapped, a measured over-cap body still 413s. | `docs/user-manual/rest-api.md` "Pre-Auth Request Body Caps" | `security-guardian` + `architect` + `cpp-safety` on `body_cap_policy.hpp` / the pre-routing cap branch in `server.cpp` |
