# do-not-close.txt -- issue numbers that NO automated code path may ever close.
# Authority: docs/agents/issue-standard.md section 5.1 (ADR-3001, Amendment A1 par.4).
#
# Union semantics with the `do-not-close` and `security` labels: automation honours
# any one signal. This committed file is the authoritative backstop because labels
# are mutable and have been wrong before (#318 and #391 are security-relevant issues
# that lacked the `security` label at audit time). Only a reviewed PR changes this
# file; the label is the maintainer's instant, no-PR marker.
#
# Automation encountering these numbers posts an advisory comment and stops.
# Format: one issue number per line; everything after '#' is a comment.

# Live security surface, verified still reachable at dev (2026-07-14) and
# deliberately held open by the maintainer:
318    # SOC2-E1 encryption at rest for SQLite stores (security label added late; enhancement-mislabelled before 2026-07-14)
391    # [Security] TLS certificate upload fails -- cert dir path ignores --data-dir (carries NO security label; do not rely on label guards)
393    # [Security] mcp_tier:readonly tokens have full admin
419    # [Security][High] ManagementService returns Unimplemented (fails open)
520    # security(auth): API token scope / MCP tier not enforced -- claimed by merged PR #795 ("Closes #520, #630") but verified still reachable; held open on purpose
639    # Security: SSRF via unvalidated webhook URL hostname
647    # Security: incomplete SSRF protection in http_client plugin
779    # Security: agent impersonation via unchecked agent_id
1717   # Security (HIGH): list-read gate fails OPEN on corrupt/load-failure

# Explicitly-partial closes: the claiming PR says in its own body that it does NOT
# fully resolve the issue -- a keyword parser must never override that:
1634   # P0: response/execution reads not management-group-scoped -- PR #1711 says in bold "does NOT close #1634"
2011   # P1: PR #2074 closed the lock half only ("Closes #2011 (lock half)")
2014   # P1: PR #2051 resolved "by policy" in prose, not in code
