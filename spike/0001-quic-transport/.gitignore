# Spike build artefacts and certs are not committed.
cpp-server/build/
cpp-server/vcpkg_installed/
erlang-client/_build/
certs/*.pem
certs/*.key
certs/*.crt
certs/*.csr
certs/*.srl

# Erlang VM crash dumps — never commit (CLAUDE.md standing pitfalls table).
erl_crash.dump
**/erl_crash.dump
