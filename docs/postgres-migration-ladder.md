# Postgres migration ladder — existing server stores

The committed end state (ADR-0006, 2026-06-22 Update) is that **every** server-side store reaches
PostgreSQL — none stays on SQLite. The agent stays SQLite. This is the ordered queue. It is
**mutable state that drains over time**, not a contract; the timeless how-to is
`docs/postgres-store-playbook.md` and the contract is ADR-0012.

Each row migrates behind **its own per-store ADR + PR** (ADR-0006). Ordering follows the ADR-0006
priorities: cross-store-join + durable + high-write stores first; security/authz authoritative
stores next; trivial config and secret-gated stores last. **Posture and secret columns below are
provisional** — each is finalized in that store's per-store ADR after reading the code.

Schema name = `snake_case(FullClassName)` incl. the `Store` suffix (ADR-0008 Update).

> **For the 2 remaining stores (`OffloadTargetStore`, `RuntimeConfigStore`):
> do NOT build `migrate_from_sqlite()`.** ADR-0009's 2026-08-25 fresh-start-by-default amendment
> applies to both — `InstructionStore` (ADR-0058) already migrated under this default and is off
> this banner's list; no production fleet has ever run a pre-Postgres build of any Yuzu store,
> so there is no real legacy data to protect. Skip the legacy-file copy entirely (same as
> `ResponseStore`'s existing precedent — see the playbook's Backfill bullet for what to build
> instead for a secret-bearing store), including its test suite, its fixture helper, and its
> ops-runbook. **This holds only while "no production fleet" stays true** — re-derive per
> ADR-0009's amendment in your own store's ADR if that premise has changed by the time you
> migrate; don't treat this banner as blanket cover once it has. See each store's own row below
> for the specific pointer, and ADR-0009's amendment for the full rationale. `AuditStore`'s
> already-shipped backfill stays — the case where this would matter most if the premise ever
> flipped — and so does every OTHER already-migrated store's, this is not unique to it.
> **`WebhookStore` (PR #3563) merged with a full `migrate_from_sqlite()` already built, before
> this amendment could reach it** — too late to apply the "don't build it" guidance
> retroactively; it instead joins the other already-migrated stores whose backfill TEST suite is
> being retired in this same sweep (production-code removal deferred as a separate later step,
> same as the others, tracked in #3623). See its own row below.

## Done

| Store | Schema | Notes |
|---|---|---|
| `OfflineEndpointStore` | `offline_endpoint_store` | First born-on-Pg store (#1320 PR 3). **Schema rename pending**: ships today as `endpoint_state`; renamed to conform to the ADR-0008 naming rule (safe pre-alpha — data reconstructs from heartbeats). Construction fail-closed (ADR-0012 §1); runtime durability-on-top. |
| `SoftwareInventoryStore` | `software_inventory_store` | Born-on-Pg (ADR-0016). Typed projection for the daily-sync `installed_software` source (normalized rows, no JSONB). authoritative reads; ingest fail-soft (next sync + weekly floor self-heal). **Coexists with the generic `InventoryStore` (Done row below) — it is NOT that store's migration.** |
| `AppPerfFleetStore` | `app_perf_fleet_store` | Born-on-Pg (DEX app-perf-over-time B2). Fleet aggregate `(app, version, day)` + fixed-bucket CPU/WS histogram + device_count, 180-day retention (the trend window). **Single-schema owner** — it does NOT write itself; the B1→B2 roll-up is the dedicated cross-store query owner `AppPerfRollup` (ADR-0012 §3 seam: one lease, schema-qualified `INSERT … SELECT` spanning `app_perf_daily_store` → `app_perf_fleet_store`). authoritative reads; derived aggregate (rebuilt by the next roll-up). No `agent_id` (no per-device attribution). No backfill (greenfield). |
| `AppPerfDailyStore` | `app_perf_daily_store` | Born-on-Pg (DEX app-perf-over-time B1). Typed per-device daily app-version perf projection for the daily-sync `app_perf` source (plain table, PK `(agent_id, app_name, version, day)`, 31-day per-agent prune, no JSONB). authoritative reads; ingest fail-soft. **Hash-less** (perf changes daily → no hash-skip), so unlike `SoftwareInventoryStore` it stores no content hash and a hash-only report is answered `need_full`. No backfill (greenfield). |
| `VulnFindingStore` | `vuln_finding_store` | Born-on-Pg (ADR-0023 M1a, vuln-scan CAVM). Typed CVE-findings projection + per-agent assessment-coverage tallies (`finding` PK `(agent_id, cve_id, package_name)` with `resolved_at_ms IS NULL` = open; `agent_coverage` PK `(agent_id)`). Construction fail-closed (ADR-0012 §1); runtime durability-on-top. `reconcile_agent` runs one agent's matching pass in ONE txn: per-agent advisory lock (namespaced `hashtextextended('vuln_finding_store:'||agent,0)` — disjoint from `SoftwareInventoryStore`'s bare key on the same agent) → in-txn **monotonic run_ts** (NTP-step-back safe) → upsert-always → **authoritative-gated** disappear-sweep + `disposed_clean` delete + coverage clobber (keep-last-good on a suspect/partial pass). **UP-2 backstop**: an authoritative pass observing NOTHING against a prior-open agent is demoted to non-authoritative so a producer bug can't mass-false-resolve the fleet. Timestamps are epoch **ms** (SoftwareInventoryStore is **seconds** — never cross the boundary). AUTHORITATIVE reads (coverage tri-state Ok/NotFound/Degraded; `fleet_summary` nullopt-on-degrade, GLOBAL Vuln:Read gated in PR 6). **DORMANT until PR 4** (constructed + wired into /readyz + /healthz; no engine calls `reconcile_agent` yet). No backfill (greenfield). **Re-homing note (ADR-1005, 2026-07-07):** grandfathered-interim — scheduled for re-homing into the vuln UCE module and deletion from the server (incl. /readyz unwiring) by the execution plan's Phase-7 strangler; see `docs/adr-1005-execution-plan.md` § "Relationship to ADR-0023 and ADR-4001" before investing further here. |
| `PreflightRunStore` | `preflight_run_store` | Born-on-Pg (ADR-0006). Persists `/auto` pre-flight run metadata, the frozen target cohort, and the computed per-device check grid (`runs` + `run_device`, FK cascade). Construction fail-closed (ADR-0012 §1: `!is_open()`→`startup_failed_`); runtime durability-on-top (a transient lease error degrades /auto to a note). 14-day retention via best-effort prune in the background runner; owner-scoped reads. Concurrency/lifecycle contract: the background `PreflightRunner` (60 s tick, tick wrapped try/catch → `yuzu_preflight_tick_errors_total`) is joined BEFORE the stores in `server.cpp` `stop()`; it re-dispatches the READ-ONLY checks to not-yet-answered FROZEN targets until the window closes — **safe ONLY because every check is read-only/idempotent; any future MUTATING check must re-resolve `devices_fn(creator)∩group` per dispatch, never reuse the frozen cohort**. Pure `compute_device_results` is the single verdict path; BOTH the result route and the runner persist the grid + complete via the shared `preflight::persist_and_maybe_complete` — the store's `complete_run` CAS is the single completion authority, a run completes **only on persist success** (UP-1), and `persist_grid` is status-guarded so a stale route-persist can't overwrite a completed run's final grid. |
| `DeploymentRunStore` | `deployment_run_store` | Born-on-Pg (ADR-0006). Persists the `/auto` DEPLOY stage — a deployment's artifact spec + source pre-flight run + the per-device stage→execute **state machine** (`deployments` + `deployment_device`, FK cascade). Construction fail-closed (ADR-0012 §1); runtime durability-on-top. Unlike PreflightRunStore it does NOT recompute a grid — it exposes **guarded one-way transitions**, and `claim_for_exec` (staged→executing `RETURNING`) is the **execute-once CAS** (the installer runs at most once per device across concurrent advances / restart). Owner-scoped reads; 14-day prune. The `deployment::advance` engine ticks: poll stage+exec responses → guarded transitions → mark out-of-scope skipped → CAS-claim+dispatch stage(pending) then execute(staged) → refresh counts → complete-if-settled (settled-check IN SQL). **ANY future store mutation must stay a guarded transition (never an unconditional grid overwrite) or the execute-once guarantee breaks.** |
| `ApiTokenStore` | `api_token_store` | Migrated Postgres, fresh-start/no-backfill (ADR-0030, PR 4.1, engine-principals program). Adds `principal_kind TEXT NOT NULL DEFAULT 'human' CHECK IN ('human','engine')` — inert until a later PR arms `'engine'`. AUTHORITATIVE/fail-hard (ADR-0012 §1): construction fail-closed, a runtime read error denies, a revoke/delete error surfaces (never a silent success — backs "Sign out everywhere"/stolen-laptop). Hash-only (SHA-256 verify-only `token_hash`, never plaintext) — ADR-0009's secret-material carve-out reasoning does not bind this store; ADR-0030 records the reconciliation. **No backfill**: legacy `api-tokens.db` is no longer opened; existing API/MCP tokens are invalidated on upgrade and operators must re-mint them. |
| `EnginePrincipalStore` | `engine_principal_store` | Born-on-Pg (ADR-0031, engine-principals PR 4.2). Identity store for the new engine-principal class, keyed on the reserved `engine:<slug>` namespace (`principal_id TEXT PRIMARY KEY`, DB `CHECK` mirrors the C++-side prefix/slug guard). Construction fail-closed (ADR-0012 §1: `!is_open()`→`startup_failed_`). `get_for_auth` is the authoritative auth-lookup chokepoint with a **three-state** result (`Active` / `MissingOrRevoked` terminal-401-class / `StoreUnreachable` retryable-503-class) — both non-Active outcomes deny, the split changes retry behavior only, never the authorization outcome (no downgrade path from "unreachable" to "admitted"). Its sibling `get_for_auth_revalidate` (#2367) serves ONLY the per-tick stream-liveness re-check, from a bounded 15 s Active-only cache (jittered TTL, short failure backoff, invalidated on `revoke`/`transfer_owner`); it returns liveness WITHOUT a row and flags `from_cache`, which the pump needs so a cached answer does not reset its grace budget. Every fresh authorization decision still calls `get_for_auth` and reads through. `ApiTokenStore::create_token`'s engine block calls this at mint time via a resolver seam wired post-construction in `server.cpp`; a boot-time collision scan across `auth`/`rbac.db` refuses to start if a pre-existing `engine:`-named local user or group predates the reservation. **No operator CRUD yet** (no dashboard/REST surface — lands in PR 4.3); this release ships FLEET-WIDE engine role grants only, scoped (management-group) engine assignment is rejected outright pending Phase-5 resolution. |
| `InventoryStore` | `inventory_store` | Migrated Postgres (ADR-0037). Generic per-source blob store (backs the `kInventoryQuery` scope source + eval engine) — **coexists with `SoftwareInventoryStore` above; this is that generic store's OWN migration, not a duplicate.** Split posture (ADR-0012 §1): reads (`list_tables`/`get`/`query`/`get_agent_inventory`/`count`) are AUTHORITATIVE (`std::nullopt`/`std::expected` degrade, never a silent empty); ingest (`upsert`) is fail-soft. `migrate_from_sqlite()` backfills non-typed legacy rows once, idempotent via a reconciliable `backfill_state`, `ON CONFLICT DO NOTHING`, and fails closed (ADR-0009). The legacy file is retained for one release; whole-device erasure removes the agent from both copies. |
| `AccessReviewStore` | `access_review_store` | Born-on-Pg (ADR-0006, Periodic Access Reviews / SOC 2 CC6.2). Persists review-CAMPAIGN metadata + per-grant attestation decisions (`access_review_campaign` + `access_review_attestation`, PK the full `(campaign_id, principal_type, principal_id, role_name)` 4-tuple). AUTHORITATIVE/fail-hard (ADR-0012 §1), same posture as `EnginePrincipalStore` — construction fail-closed (`!is_open()`→`startup_failed_`), every mutator/completeness-reader returns `std::expected`. `open_campaign` freezes the complete current grant population as `pending` rows in ONE transaction (R3 — the provable "every grant that existed at open time has a reviewable row" invariant). `list_campaigns` (hardening round) returns every campaign's metadata newest-first, capped at 500 — backs `GET /api/v1/access-reviews`. **Deliberately NO prune method** — unlike the `/auto` pre-flight/deployment runs (operational scratch state, 14-day-pruned), a closed campaign IS the compliance evidence and persists indefinitely; any future retention policy is a separate, explicit decision. Does not itself compute the grant population — that's the pure read-model `access_review_model.hpp` (`build_access_review`), which reads `AuthDB`/`RbacStore`/`EnginePrincipalStore` (+ optional `ApiTokenStore`/`DirectorySync` enrichment) and fails the WHOLE export on the first read error, never a silent partial result. **Grant-table-driven (UP-1, hardening round):** the population is read directly off the grant table (`RbacStore::list_all_principal_roles_checked()`), not the three principal rosters, so a grant on a principal outside every roster is surfaced (`source="orphan"`) rather than silently omitted. No backfill (greenfield). |
| `ResultSetStore` | `result_set_store` | Migrated Postgres (ADR-0036, Wave 1). Scope-walking result-set primitive (`result_sets` + `result_set_members`, FK cascade) — inherently cross-store (every scoped dispatch + `/api/scope/estimate` resolve `from_result_set:` aliases against it). AUTHORITATIVE/fail-hard (ADR-0012 §1): construction fail-closed; mutators (`create_materialized`, `create_pending`, `pin`, `unpin`, `delete_set`, `materialize`) return `std::expected` and surface `DbError`. **Authorization/targeting-relevant reads are type-distinguishable** (`get`/`contains`/`resolve_alias`/`member_set_owned` → `std::expected<..., ResultSetError>`; `AgentRegistry::evaluate_scope` → `std::optional<...>`, `nullopt` on a degraded preload) — the 2026-07-25 program policy this store's review originated (`docs/postgres-store-playbook.md` "Authoritative reads must be type-distinguishable"), closing a concrete `NOT from_result_set:<id>` fleet-wide-match fail-open. The remaining reads (`list_by_owner`/`members`/`lineage`/counts) stay plain-optional/container (deny-or-benign failure modes; tracked follow-up, not a gap). **Standard ADR-0009 backfill** (`migrate_from_sqlite`, idempotent via a dedicated `sqlite_backfill` marker row — never row-count-inferred, since `gc_sweep` legitimately empties `result_sets` in normal operation); legacy `result_sets.db` retained read-only for one release. Per-operator quota (`kMaxPerOwner`) and pin-limit (`kMaxPinsPerOwner`) are now soft-enforced (small race window under Postgres's real concurrency, replacing the SQLite single-writer mutex) — accepted, not a security boundary. |
| `RbacStore` | `rbac_store` | Migrated Postgres (ADR-0041, Wave 2.1, PR #2703). Authorization substrate — role definitions, role→permission grants, principal→role assignments, RBAC groups + membership, the global `rbac_enabled` flag; `require_permission`/`require_scoped_permission`/`authorize_list_read` (ADR-0017 World A)/the engine-principal default-deny path (ADR-0031) all resolve through it — highest blast radius on the ladder. AUTHORITATIVE/fail-hard: every hot authz read keeps its deny-on-error `bool` contract byte-for-byte (store-not-open/pool-acquire-timeout/query-error → deny, never allow — closes the corrupt-`rbac.db` fail-open); `authorize_list_read`'s 403-vs-503 split is exposed via a separate `std::expected`/tri-state accessor so the plain-bool path can never regress to fail-open. The process-local `write_generation_` permission-cache invalidator became a durable `rbac_meta.write_generation` counter (bumped in the same transaction as every mutation) so a revoke on one replica is visible fleet-wide within a bounded refresh interval (~1s, `kRbacGenerationRefreshMs`) instead of only on that replica's own next local write — the bounded window is an accepted, recorded residual risk (LISTEN/NOTIFY is the named zero-window follow-up), not an open question. A 2026-08-11 availability-hardening update supersedes the original "clear cache on any refresh failure" rule with a bounded ~5s stale-serve tolerance (`kRbacStaleServeBoundMs`, covering both the permission cache and the cached `rbac_enabled` view) so a transient pool blip doesn't itself become a fleet-wide authorization outage; a fail-fast circuit breaker separately bounds how long an uncached check can block on a doomed pool. MANDATORY backfill (grants/roles/groups/`rbac_enabled` are irreducible operator intent) — the `rbac_enabled` flag transfer is read-back-verified before the store is considered open (losing it silently reverts the fleet to RBAC-off, the single most dangerous row to lose); an operator-revoked built-in default is preserved as absent via a dedicated `revoked_seed_defaults` suppression table (never a `deny`-effect tombstone, which would incorrectly override a principal's OTHER independently-granted roles, nor a plain DELETE, which the idempotent every-boot reseed would silently resurrect) — guarded by a `pg_advisory_xact_lock` against a marker-then-reseed race under concurrent rolling-restart writers (three earlier fix attempts each reintroduced a hazard, all caught by governance pre-merge, none ever pushed). Coordinated in the same PR with `ManagementGroupStore` below. |
| `ManagementGroupStore` | `management_group_store` | Migrated Postgres (ADR-0042, Wave 2.2, PR #2703). Confinement substrate — group hierarchy (parent/child tree), membership, group→role assignments; RbacStore's `authorize_list_read` (ADR-0017 World A) and `resolve_perm_groups` resolve an operator's visible agent set through it. AUTHORITATIVE/fail-hard: confinement-feeding reads (`get_agent_groups`/`get_ancestor_ids`/`get_descendant_ids`/`get_member_agents_in_subtrees`/`get_assignments_for_principal`/`get_visible_agents`) became degrade-distinguishable (`nullopt`/`unexpected` on store-not-open/pool-acquire-timeout/query-error, never a silent empty) — closing a deny-set fail-open that was mostly moot under SQLite's single-writer file but became reachable under a pooled Postgres connection: a degraded deny-set read silently returning empty means *nothing is denied*, a cross-management-group disclosure ADR-0017 exists to prevent (an allow-set read degrading the same way fails closed instead — the two confinement directions fail in OPPOSITE ways, which is why this needed a type change rather than a blanket one). `authorize_list_read` consumes the `nullopt` as `DenyAll`. Hierarchy traversal (`get_ancestor_ids`/`get_descendant_ids`/`get_member_agents_in_subtrees`) became recursive CTEs with a `depth < kMaxHierarchyDepth` bound plus a `DISTINCT`/cycle guard (the SQLite-era app-side BFS retired); an over-deep or cyclic legacy tree is refused at BACKFILL time rather than silently truncated at read time — a read-side cap-hit detector was rejected because it cannot tell a genuine deep chain from a cycle without false-positiving on the cycle guard's own already-correct case. MANDATORY backfill (confinement scope is irreducible operator intent). Coordinated in the same PR with `RbacStore` above — whichever of the two merged second reconciled the `authorize_list_read`/`resolve_perm_groups` consumption seam against the other's newly degrade-distinguishable reads. |
| `DeploymentStore` | `deployment_store` | Migrated Postgres (ADR-0043, Wave 2). Operator-initiated ad-hoc deployment jobs (SSH/group-policy/manual installs, Issue 7.7) — NOT `DeploymentRunStore` (the unrelated `/auto` Deploy stage store, Done row above, unchanged by this migration). AUTHORITATIVE/fail-hard (ADR-0012 §1): construction fail-closed; every reader/mutator returns `std::expected`, `get_job` type-distinguishes "not found" (`nullopt`) from a genuine DB error. `id` stays the pre-migration client-generated `TEXT` surrogate key (not switched to a PG identity column — would break the REST route's `[a-f0-9]+` id contract). `cancel_job` is a single guarded `UPDATE ... WHERE status IN ('pending','running')` (race-safe transition, no mutex). ADR-0009 backfill (`migrate_from_sqlite`), idempotent PER DISTINCT LEGACY-FILE CONTENT via a SHA-256-fingerprinted `sqlite_backfill_source` table rather than a single fleet-wide marker (hardening round — closes the "fileless replica waves through a holder replica's real data" anti-pattern this store's review originated a second, deliberately-simpler-than-`RbacStore` fix for). Per-row insert conflicts partition the compared columns into IDENTITY (target_host/os/method/created_at — write-once, any mismatch fails closed) and LIFECYCLE (status/started_at/completed_at/error — mutated by live post-migration traffic): Postgres strictly ahead, or tied at the SAME status, is a benign warn-logged no-op; legacy strictly ahead (e.g. an ADR-0009 rollback-then-roll-forward cycle) OR both sides tied at a terminal status that DISAGREES (e.g. `completed` vs. `failed`) fails closed the same as an identity mismatch (ADR-0043 Backfill section — random ids avoid *independent-generation* collisions, not ids shared by construction via a cloned/restored legacy file). A legacy row's status is also validated before it can reach Postgres at all (an unrecognised value fails the boot closed). Legacy `deployment-jobs.db` retained read-only for one release. Wired into `/readyz`/`/healthz` (gov Gate 3 sre correction — matches every other migrated authoritative store on this ladder; `open_` never flips post-construction, so this is risk-free belt-and-braces). |
| `DiscoveryStore` | `discovery_store` | Migrated Postgres (ADR-0044, Wave 2, issue #1328, PR #3064). Network-discovered devices (Issue 7.18) behind `POST /api/discovery/scan`/`GET /api/discovery/results` — single table, no FKs, no secret-bearing columns. AUTHORITATIVE: `list_devices`/`get_device` type-distinguishable (`nullopt`/`unexpected` on degrade, never a silent empty); `GET /api/discovery/results` 503s on a degraded read rather than rendering an empty device list (an operator scanning for rogue/unmanaged devices must not be told "nothing found" when the real answer is "could not ask"). `upsert_device`'s `ON CONFLICT (ip_address) DO UPDATE` preserves legacy semantics exactly: `mac_address`/`last_seen`/`subnet` always refresh, `hostname` refreshes only when non-empty (a failed-to-resolve scan doesn't blank a previously known-good one), `discovered_at`/`discovered_by`/`managed`/`agent_id` are untouched by the conflict branch (a re-scan cannot silently un-manage an already-managed device or overwrite first-seen provenance). MANDATORY fingerprint-verified backfill (`RbacStore`/#2703 sourceless-fingerprint shape, scaled to one table) — the operator-set `managed` flag is irreducible operator intent; a later boot holding its own legacy file verifies its content fingerprint against the stored one before trusting a marker possibly stamped by a sibling replica, refusing (HOLDER-SIDE VERIFICATION FAILED) on a mismatch; a concurrent live-scan row landing ahead of this replica's own backfill is reconciled value-for-value, refusing (reconciliation FAILED, naming the IP) rather than silently dropping or misattributing an operator's managed-device assignment — this does not self-resolve and needs manual reconciliation. **Breaking:** `POST /api/discovery/scan`'s response contract changed — gains `devices_failed`, `status` is `"partial"` on a mixed batch, and the endpoint 503s when every device in a batch failed to persist (previously always `200 {"status":"ok"}` regardless of outcome, including in the `discovery.scan` audit row, which is now `"success"`/`"partial"`/`"failure"`). |
| `CustomPropertiesStore` | `custom_properties_store` | Migrated Postgres (ADR-0045, Wave 2). Operator-authored per-agent metadata (free-form key/value properties + optional per-key type/validation schema), usable in scope expressions via `props.<key>` — its read path feeds dispatch/targeting decisions, not just dashboard display. AUTHORITATIVE (ADR-0012 §1, ADR-0036 typed-read policy): every scope-feeding read (`get_properties`/`get_property`/`get_value`/`get_property_map`/the new bulk `get_values_for_keys`) returns `std::expected<T, CustomPropertiesReadError>` — degraded, never collapsed into the empty/not-found success shape; non-scope-feeding admin reads (`list_schemas`/`get_schema`) stay plain-optional. **Closed a fail-open this migration would otherwise have introduced**, the same class ADR-0036 closed for `from_result_set:<id>`: `AgentRegistry::evaluate_scope`'s `props.<key>` resolver previously called the store per-agent inside the fleet loop, collapsing "no such property" and "the read failed" to the same empty string — under `NOT`/`!=` an empty resolution matches, so a transient Postgres blip would silently invert to "every agent matches." Fixed with a bulk preload (`collect_props_keys` AST walk + one `get_values_for_keys` query per scope evaluation, mirroring the existing `from_result_set:` preload) that **aborts the whole scope evaluation** (`nullopt`) on a degraded read rather than resolving atoms to "no match." No Postgres FK from `custom_properties.type` to a schema-type enum (a schema is optional per-key policy most keys never have, and `delete_schema` must not cascade onto existing property values — a FK would change that observable behavior); cross-table type/regex validation stays application-layer, unchanged from the SQLite original. No secret-bearing columns. MANDATORY backfill, `RbacStore`'s monotonic-promotion fingerprint-stamp mechanics ported unmodified (not `ManagementGroupStore`'s simpler shape) — a **governance-caught correction mid-PR**: an initial `ON CONFLICT DO NOTHING` fingerprint stamp silently interleaved two replicas' genuinely different legacy content on a concurrent first boot (the loser's rows land, then it loses the marker race, logs success, and moves its own legacy file aside — permanently discarding which rows came from which replica with no operator-recoverable record); fixed with the same promote-or-fail-closed upsert `RbacStore` uses. Does NOT carry `RbacStore`'s revoke-coordination locking or `rbac_enabled`-style read-back-verification — no revoke path or single security-critical flag exists here, so that machinery is deliberately out of scope. See `docs/ops-runbooks/custom-properties-store-backfill-recovery.md` for the operator-facing recovery procedure. |
| auth DB (`auth_db.{hpp,cpp}`) | `auth` | **SHIPPED — Wave 3, ahead of queue order (auth was pulled forward as the last major server SQLite store).** Fresh-start/no-backfill (matches `ApiTokenStore`'s 4.1 precedent): a legacy SQLite `auth.db` is never read; `AuthDB::seed_admin_if_empty` re-seeds the config-file admin on an empty `auth.users`, with a loud one-time "auth data reset on Postgres cutover" boot warning. **`SecretCodec`'s first production consumer** (ADR-0010) — `users.mfa_totp_secret` is now envelope-encrypted (AES-256-GCM, AAD-bound to `{schema,table,column,row id}`); password hashes/recovery codes/enrollment tokens stay verify-only hashes. Construction fail-closed (ADR-0012 §1). **Dropped, not migrated:** the `sessions` table (sessions are `AuthManager`'s in-memory-only `sessions_` map; the DB mirror was a permanent dead-write) and `auth_kv` (unused SQLite-era scaffolding, superseded by `SecretCodec`). Bundled hardening: MFA state reads now fail CLOSED (`AuthDBError::SecretUnavailable`) on a store/decrypt failure — see `docs/auth-architecture.md` "MFA / TOTP". See `.claude/agents/authdb.md` for the full invariant set. |
| `ScimStore` | `scim_store` | **SHIPPED in lockstep with auth DB above** (both landed in the same PR). Retires the prior SQLite `auth.db`-cohabitation exception (see removed "Notes" entry below) — SCIM tokens stay a verify-only SHA-256 hash (nothing to decrypt, `SecretCodec` not needed here). |
| `UploadGrantStore` | `upload_grant_store` | Born-on-Pg, no backfill (ADR-3004, PR1.6a, CC-06 server-side fix). Three tables — `grants` (operator-minted, single-use redemption credentials), `sessions` (the credential a grant redeems into; authenticates every chunk/status/commit/cancel call), `completed_uploads` (durable metadata for a committed upload: destination key, actual size, verified hash, retention class, received-at). AUTHORITATIVE/fail-hard (ADR-0012 §1) for every mutator that decides an authentication outcome (`mint`/`open_session`/`authenticate_session`/`advance_offset`/`commit_session`/`cancel_session`/`revoke`) — construction fail-closed; a lease/query error is `std::unexpected`/a dedicated `kUnavailable` outcome, never confusable with a legitimate negative result. `open_session`'s single-redemption guarantee is a Postgres `UPDATE ... WHERE state='minted' ... RETURNING` — a concurrent second caller deterministically sees zero rows once the first commits (MVCC row-level serialization), not an application-level lock. Hash-only credentials (`grant_secret_hash`/`session_secret_hash`, SHA-256, verify-only — the raw secret is returned exactly once, at mint and at session-open respectively, and never persisted). `expire_stale_sessions` is the ONE exception to the authoritative posture (durability-on-top, best-effort maintenance sweep — every per-request path already evaluates expiry itself against the injected clock, so this is pure hygiene, never load-bearing). Blob-content encryption at rest and a retention sweep are recorded as explicit open follow-ups in ADR-3004, not silently-closed gaps. |
| `PluginConfigStore` | `plugin_config_store` | Born-on-Pg, no backfill (ADR-3005, PR1.5b, plugin config/secret plane + per-action kill switch). Three row kinds — plain config keys (`<plugin>.<key>` → value), secret keys (sealed with a dedicated `SecretCodec` instance owned by this store, DEK enrolled in the live KEK-rotation surface, never returned in plaintext by any read API), and kill-switch rows (`<plugin>` or `<plugin>.<action>` → enabled/disabled + reason + who set it). Reads are FAIL-CLOSED: a degraded store reports "disabled," never "enabled," for the kill switch. Every config write and kill-switch flip is audited (503-on-audit-failure posture, matching `rest_audit.hpp`'s `emit_behavioral_audit` contract). List/fan-out reads route through `RbacStore::authorize_list_read`, not a bare permission check. Agent-side secret delivery (`yuzu_ctx_get_secret`) stays an explicit open gap — this store is server-side only; see ADR-3005. |
| `NotificationStore` | `notification_store` | Migrated Postgres (ADR-0046, Wave 2). Dashboard toast/badge feed (`notifications` table, no FKs, no secrets). **Split posture (ADR-0012 §1):** construction + `migrate_from_sqlite` are AUTHORITATIVE/fail-closed (`!is_open()`/backfill failure → `startup_failed_`); runtime `create`/`list_unread`/`list_all`/`mark_read`/`dismiss`/`count_unread` stay plain container/bool/void — none feed an authorization/targeting/enforcement/skip decision, so a transient DB error degrades the display only, never a silent grant. **Mandatory ADR-0009 backfill** (`migrate_from_sqlite`, right-sized from `RbacStore`'s post-#2703 holder-side-fingerprint reference shape): sourceless sentinel for an absent/schema-less legacy file, id-preserving insert (`OVERRIDING SYSTEM VALUE` + same-transaction sequence advance), `RETURNING id` + `PQntuples()==1` per-row landed-verification (never a bare `PGRES_COMMAND_OK` on `ON CONFLICT DO NOTHING`) guarded by a pre-backfill emptiness check, its own `SET LOCAL statement_timeout` (the `with_txn_for` timeout parameter only bounds pool-acquire, not execution), and a close-before-rename fix for the Windows move-aside (`legacy.close()` before `move_legacy_aside`, matching `RbacStore`/`AuditStore`). Legacy `notifications.db` retained until a verified move-aside (one release). |
| `SoftwareDeploymentStore` | `software_deployment_store` | Migrated Postgres (ADR-0051, Wave 2 batch 3). Capability 7.6 software packaging + fleet deployment catalog — three tables with their existing internal FKs preserved and now ENFORCED (`software_packages` -> `software_deployments.package_id` -> `agent_software_status.deployment_id`; the pre-migration SQLite store never ran `PRAGMA foreign_keys=ON`, so `delete_package` now fails closed on a referencing deployment where SQLite silently orphaned it). AUTHORITATIVE/fail-hard (ADR-0012 §1), `DeploymentStore`/ADR-0043's closest precedent (also TEXT-keyed, status-machined) — every reader/mutator returns `std::expected`, `kSwDeployDbErrorPrefix` classifies 503 vs 400 (no GET-by-id route exists, so not-found also classifies 400, matching `cancel_job`'s route precedent). `id` (packages, deployments) keeps the pre-migration 32-hex-char client-generated surrogate key — a DIFFERENT format from `DeploymentStore`'s own 16-hex generator; the two are not unified. **DORMANT — verified 2026-08-14, same family as `LicenseStore`/ADR-0048**: no production construction site (`acc6c481a` wired it, `2fcfb95b5` removed it, confirmed a deliberate T2-capability shelving, not a regression); re-wiring is out of scope. Not wired into `/readyz`/`/healthz` (nothing to wire a pointer that never exists into — add at re-wiring time). **Mandatory ADR-0009 backfill despite dormancy** (a wired-era upgrade can hold a real legacy file): one SHA-256 fingerprint over all three tables' canonicalized+sorted rows (`sqlite_backfill_source`, mirrors `DeploymentStore`'s per-file-content design). Three decisions beyond that single-table precedent: (1) a partial legacy schema (some but not all three tables present) fails closed rather than being treated as sourceless — the pre-migration migration always created all three together; (2) referential closure is validated CLIENT-SIDE before any Postgres round trip — Postgres enforces the FKs the legacy store never did, so a wired-era orphan (e.g. a deployment whose package was deleted) has no valid parent to satisfy and fails the whole backfill closed, naming the orphan, rather than being silently dropped; (3) the deployment-status IDENTITY/LIFECYCLE rank model is NOT copied verbatim from `DeploymentStore` — `completed` is reachable-FROM by `rollback_deployment` here (not terminal), so the derived rank is `staged`<`deploying`<`verifying`<`completed`<{`cancelled`,`rolled_back`,`failed`} tied-terminal, taken from this store's actual transition guards; the parallel `agent_software_status` rank is a documented HEURISTIC (that table's `update_agent_status` is an unguarded upsert with no live transition guard to prove the direction safe). |
| `QuarantineStore` | `quarantine_store` | Migrated Postgres (ADR-0047, Wave 2, batch 2, PR #3142). Guardian device-quarantine bookkeeping (Guardian design §11.7), single table, no FKs, no secrets. "At most one active record per agent" enforced by a partial unique index (`quarantine_records(agent_id) WHERE status='active'`) rather than the legacy in-process mutex — `quarantine_device`/`release_device` are single race-safe statements. MANDATORY fingerprint-verified backfill, but with no natural per-row key (unlike DiscoveryStore's `ip_address`) — the row inserts and the completion marker commit together in ONE advisory-locked transaction (`pg_advisory_xact_lock('quarantine_store:backfill')`) rather than a per-row `ON CONFLICT` idiom; see ADR-0047 "Backfill" for why. `list_quarantined`/`get_history`/`get_status` type-distinguishable (`nullopt`/`unexpected` on degrade); `GET /api/v1/quarantine` 503s rather than rendering an empty quarantine list on a degraded read. |
| `CaStore` | `ca_store` | Migrated Postgres (ADR-0053, Wave 3 batch 4). Internal-CA inventory + lifecycle — PKI routed-concern store: root metadata, issued-cert inventory, CRL version history (`ca_root`/`ca_issued`/`ca_crl_versions`, single consolidated v1 migration folding in the SQLite store's v1-v5 columns — no PG database predates this schema). Secrets resolved: `key_ref` is the ONLY root-key-adjacent column, an opaque `KeyProvider` reference, never the key itself — `SecretCodec` not involved. AUTHORITATIVE/fail-hard (ADR-0012 §1) with one deliberate exception: `is_revoked()` stays a plain `bool`, fails closed to `true` on ANY degradation (non-hex serial, lease timeout, query error) — the mTLS-accept security gate, mirrors `ApiTokenStore::validate_token`'s hot-path precedent rather than exposing a third state. `list_revoked()` is typed (`std::expected`); both its callers (CRL construction, the PR3 HIGH-2 re-issue-block guard) now ABORT rather than proceed on a degraded read — a silently-empty read there would either un-revoke every cert in a published CRL or let a revocation-bypass re-enrollment through. **Root-singleton first-boot race (the one genuinely new problem a shared Postgres substrate introduces over per-instance SQLite):** `try_insert_root()` (`ON CONFLICT (id) DO NOTHING RETURNING`, race-safe — every caller reads back the SAME canonical row, winner or loser) is the NEW first-boot entry point; `set_root()` keeps its pre-migration unconditional-REPLACE contract for the two callers that legitimately intend one (PR6 subordinate import, test seeding) — **never** call `set_root()` from first-boot generation, or a race loser silently clobbers the winner. `record_issued` classifies a PG unique-violation (SQLSTATE 23505) on `serial_hex` distinctly (`kCaDuplicateSerialPrefix`) — narrows, does not close, #1276's flake lead (x509_ca mints random 128-bit serials; no live-collision repro found). `revoke` is three-way (`Ok(true)` transitioned / `Ok(false)` not-found-or-already-revoked, business fact / `unexpected` genuine DB error) — a DB error must never be audited `denied`, a false compliance record. MANDATORY three-table fingerprint-verified backfill (ADR-0009), extending `LicenseStore`'s two-table design: `ca_root` is an ALL-IDENTITY singleton; `ca_issued` splits IDENTITY vs LIFECYCLE (status/reason/revoked_at) with the kickoff's explicit direction rule — legacy-revoked/PG-active fails closed (never silently un-revoke), PG-revoked/legacy-active is a benign no-op; `ca_crl_versions` fails closed on a same-version-different-DER fork, no-ops on byte-identical DER. `CaStore::publish_next_crl` (the `CrlBuilder`-callback convenience) has ZERO production callers (verified by grep) — `server.cpp`'s actual `publish_crl()` calls `next_crl_number()`/`record_crl()` directly under its own mutex; kept for API/test parity, hardened to abort rather than build over a failed read. **Adversarial review (Kimi + Codex, 2026-08-20) caught a HIGH the author missed**: `server.cpp`'s boot-time PKI wiring block (signer/revocation-checker/peer-recognizer) was left gated on `has_root()` instead of `get_root()`, so a transient Postgres failure at that one call site would silently skip wiring revocation enforcement for the process's lifetime — fixed pre-push to fail the boot closed instead (`docs/adr/0053-ca-store-postgres-migration.md` "Adversarial review"). Same round also closed a MEDIUM (`ca_store_.reset()` now explicit in `stop()` before `pg_pool_.reset()`, matching every sibling PG store) and left one pre-existing LOW (REST/MCP serial-normalization parity, not introduced by this migration) for a separate fix. |
| `LicenseStore` | `license_store` | Migrated Postgres (ADR-0048, Wave 2 batch 2). Product license activations + derived alerts (capability 22.3), two tables (`licenses` + `license_alerts`, no FKs — `license_alerts.license_id` is a soft reference). **Deliberately DORMANT on `dev`** (verified: no construction site anywhere outside tests) — licensing is shelved, an operator decision, not a defect; re-wiring construction was explicitly out of scope for this migration, so this migration is persistence-layer-only and has zero production behavioral effect until a future change re-constructs the store. AUTHORITATIVE/fail-hard (ADR-0012 §1): every reader/mutator returns `std::expected` (`get_active_license` -> `std::expected<std::optional<License>, std::string>`); `has_feature`/`seat_count`/`days_remaining`/`get_status` are typed too even though nothing currently calls the first three outside this store's own tests (ADR-0036: `has_feature` is the canonical "could a silent-false gate a grant/enforce decision" shape). `licenses.id` keeps the pre-migration 32-hex-char client-generated format exactly (NOT `DeploymentStore`'s 16-hex format — "preserve pre-migration ID contracts" is per-store, not a convergence rule); `license_alerts.id` remaps SQLite AUTOINCREMENT to a PG `BIGSERIAL` surrogate (nothing external references it as a stable id). `activate_license`'s duplicate-key check is now an atomic `ON CONFLICT (license_key_hash) DO NOTHING RETURNING id` (closes a pre-migration check-then-insert TOCTOU race). ADR-0009 backfill (`migrate_from_sqlite`) mirrors `DeploymentStore`'s fingerprinted-idempotency/IDENTITY-LIFECYCLE-partition shape most closely among this wave's stores, extended for two legacy tables: the fingerprint row-count-prefixes each table's section (a per-field length prefix alone doesn't make the TWO-SECTION sequence injective); a legacy file holding exactly one of `licenses`/`license_alerts` fails closed (the shipped binary always created both together, so a half-schema file is corruption, not a real upgrade artifact); `license_alerts` gets a deliberately simpler `UNIQUE(license_id, alert_type, triggered_at)` + `ON CONFLICT (...) DO UPDATE SET acknowledged = acknowledged OR EXCLUDED.acknowledged` backfill merge rather than `licenses`' full IDENTITY/LIFECYCLE machinery (alerts are otherwise derived/regenerable notification records, not independent entitlement state — `acknowledged` is the one exception, a write-once-monotonic operator action merged via OR rather than a plain `DO NOTHING`, which would silently discard a real dismissal on a cross-replica conflict; gov Gate 4 unhappy-path UP-2, HIGH). `identity_matches` also includes `seat_count` (gov Gate 2 security-guardian, HIGH — initially omitted). `validate()`'s status-recompute SELECT takes `FOR UPDATE` to close a lost-update race between overlapping transactions (gov Gate 4 unhappy-path UP-1, HIGH); both backfill loops and that SELECT use `ORDER BY id ASC` to avoid a deadlock from content-dependent lock-acquisition order across replicas (gov Gate 8 architect). Not wired into `/readyz`/`/healthz` — mirrors how the store is not constructed at all; wire both together when a future change re-wires construction. |
| `DeviceTokenStore` | `device_token_store` | Migrated Postgres (ADR-0052, Wave 3 secret-gated row — resolved verify-only-hash, no `SecretCodec` needed). Device authorization tokens (capability 18.8), single table `device_auth_tokens`, `token_hash` SHA-256 verify-only (same posture as `LicenseStore`'s `license_key_hash`). **Deliberately DORMANT on `dev`, and UNWIRED (not never-wired) — resolves the kickoff doc's open question**: pickaxe confirms construction was wired by `acc6c481a` and removed by `2fcfb95b5`, the same two commits that unwired `LicenseStore`/`SoftwareDeploymentStore` — all three are the identical family. AUTHORITATIVE/fail-hard (ADR-0012 §1): `list_tokens`/`revoke_token`/`revoke_by_principal` all converted to `std::expected` (were a bare `std::vector`/`bool`/`int64_t`); `validate_token`'s typed `RejectedToken`/`DeviceTokenValidateError` contract preserved exactly (#1052/#1053), every genuine transaction-level PG error collapsing to `internal_error`, never a benign rejection reason. The #823 re-registration revoke-sweep is now type-distinguishable ("nothing to revoke" vs "the revoke failed" — the pre-migration `int64_t` could not tell those apart, a fail-open-shaped gap on the store's own security control). **Correction (#3401, ADR-0052 amendment):** the sweep is `revoke_by_device` (keyed on `device_id`, the column `validate_token` binds the presenter against), not `revoke_by_principal` as an earlier version of this row stated — `principal_id` is the issuing operator's username on every production issuance path, never the agent_id, so a principal-keyed sweep matched zero rows on every real re-registration. `revoke_by_principal` remains a legitimate owner-scoped bulk revoke with its own `std::expected` conversion, just not this control. ADR-0009 backfill applies `DeploymentStore`'s single-table IDENTITY/LIFECYCLE fail-closed-on-legacy-ahead rule unchanged, specialized to a two-value field: `revoked` is monotone AND auth-relevant, so a legacy row showing `revoked=true` against a Postgres row still `revoked=false` fails the backfill closed (never a warn-and-keep) — silently keeping the stale value would resurrect a credential the operator killed; the reverse direction and a `last_used_at`-only difference are both benign no-ops. Not wired into `/readyz`/`/healthz` — same reason as its dormant siblings. Test-file drift beyond the two files the kickoff named: `test_rest_api_t2.cpp`, `test_rest_api_tokens.cpp`, `test_agent_registry_token_revocation.cpp` all stack-constructed the pre-migration store directly and needed porting to the shared `"devicetokenstore"` `PgTestTemplate` key. |
| `ProductPackStore` | `product_pack_store` | Migrated Postgres (ADR-0054, Wave 2 batch 5). Operator-installed product packs (multi-document YAML bundles installing into `InstructionStore`/`PolicyStore`/`WorkflowEngine` via `ItemInstallFn`/`ItemUninstallFn`) — two tables with a real internal FK (`product_pack_items.pack_id -> product_packs.id ON DELETE CASCADE`). AUTHORITATIVE/fail-hard (ADR-0012 §1, ADR-0036): `install`/`uninstall` kept their pre-migration `std::expected` shape; `list`/`get` widened to `std::expected` (`get` -> `std::expected<std::optional<ProductPack>, std::string>`, matching `LicenseStore`/`ResultSetStore`). **`install()` no longer holds a `pool_` lease across the `ItemInstallFn` callback loop** — the pre-migration SQLite version wrapped the whole install (metadata insert + every sibling-store `install_fn` call) in one `BEGIN IMMEDIATE`, a shape the substrate playbook forbids (sibling stores share the same `PgPool`); the pack row + successfully-installed item rows are now written in ONE transaction AFTER every `install_fn` call returns, parent-before-child, preserving the same atomicity boundary the SQLite version actually had (sibling-store writes were never inside its transaction either). `mtx_` is dropped (Postgres MVCC + pool concurrency replace it) — a small, accepted, recorded race on concurrent same-id `uninstall` calls (`ResultSetStore`'s quota-check precedent). Mandatory ADR-0009 backfill, fingerprinted (SHA-256 over both tables, `LicenseStore`'s two-table shape) — **no IDENTITY/LIFECYCLE conflict split**, unlike `LicenseStore`/`DeploymentStore`: no runtime method ever `UPDATE`s a row post-insert, so every column is write-once and a conflict is resolved by full-row-equality-or-fail-closed. Transparently handles a pre-7.13 legacy file whose `product_packs` table predates the `verified` column (probed via `pragma_table_info`, defaults `verified=false`, matching the pre-migration raw-`ALTER TABLE` shim this migration retires from the schema-creation path). `uninstall`'s new `"not_found: "` prefix is a REST contract change (`DELETE /api/product-packs/:id` 400 -> 404 on a missing id, `workflow_routes.cpp`'s `product_pack_error_status`). |
| `TagStore` | `tag_store` | Migrated Postgres (ADR-0050, Wave 2 batch 3). Per-agent key→value tags (operator/API/MCP-authored + agent-synced `scopable_tags` + the four structured categories), composite PK `(agent_id, key)` with `source` outside the key carrying the #1411 precedence rule (agent writes never clobber operator/API rows — re-expressed as `ON CONFLICT ... DO UPDATE ... WHERE tags.source = 'agent'`, semantics verified against live Postgres before shipping). DISPATCH-CRITICAL (the #2500 class): tags feed `tag:<key>` scope resolution, service-scoped token confinement, and dynamic service-group membership. AUTHORITATIVE (ADR-0012 §1, ADR-0036): EVERY read is typed (`std::expected<T, TagReadError>`) — the pre-migration B-2b two-accessor split (collapsing `agents_with_tag` + `agents_with_tag_checked`) is SUPERSEDED, the collapsing accessor removed. Closed the resolver fail-open the ADR-0045 migration closed for `props.<key>`: `evaluate_scope`'s per-agent `get_tag` loop (degrade collapsed to "" — match-all under `NOT`) became a bulk `get_values_for_keys` preload that ABORTS evaluation on degrade; deliberate asymmetry — a NULL store does not abort (in-memory `scopable_tags` legitimately answer `tag:` atoms; a DEGRADED store always aborts). MCP `preview_scope_targets` got the same preload. Mandatory ADR-0009 backfill (all four sources: server/agent/api/mcp), RbacStore post-#2703 fingerprint shape + DIRECTION-AWARE row conflicts on `updated_at` (PG-ahead/identical = benign; legacy-ahead or tied-differing = fail closed — the DeploymentStore shape; runbook `docs/ops-runbooks/tag-store-backfill-recovery.md`). |
| `BaselineStore` | `baseline_store` | Migrated Postgres (ADR-0055, Wave 2 batch 5). Guardian's deployable Baseline unit — three tables (`baselines` + `baseline_rules` + `baseline_groups`, FK cascade), bounded operator config, no retention reaper. AUTHORITATIVE (ADR-0012 §1) — uniformly, unlike ADR-0038's split-by-table-family posture (no telemetry tier here to carve out). CATASTROPHIC-READ SET (CLAUDE.md Guardian invariant): `deployed_member_rule_ids()` (both the fleet-union push/reconcile overload AND the per-Baseline REST-view overload) became `std::expected<..., std::string>` — a degrade is `std::unexpected`, never a silent empty deployed-set (closes a fail-open the pre-migration SQLite store's plain-container return shape carried); every push/reconcile/REST caller aborts on `!result`, mirroring `GuaranteedStateStore::list_rules()`'s handling exactly. New `get_members_checked()` is a degrade-distinguishable twin of the pre-existing plain `get_members()`, used ONLY by the deploy handler's live-member-into-durable-snapshot write (a lease timeout there must abort the deploy, never persist an empty `deployed_snapshot`); every other `get_members()` caller stays deny-or-benign. Mandatory fingerprint-verified backfill (TagStore/RbacStore post-#2703 shape, not ADR-0038's simpler marker-only shape which predates that lesson) — all three legacy tables read inside ONE deferred SQLite transaction (load-bearing: a torn read across parent + children could fingerprint a parent against a different instant than its children); parent rows direction-aware on `updated_at` (identical/PG-ahead = benign, legacy-ahead/tied-differing = fail closed — the DeploymentStore/TagStore shape); child rows copied ONLY for a parent freshly inserted THIS pass — a baseline that already existed live already has complete, authoritative children via `set_members`/`set_assignment`'s own atomic full-replace semantics, so re-merging legacy children would be redundant at best, a stale partial overwrite at worst (safe against a concurrent writer: an uncommitted fresh parent row is invisible to every other transaction until this one commits). Every free-text field, on every write path incl. the backfill, is `sanitize_pg_text`'d (ADR-0052/#3398/#3399 hardening ported retroactively — an embedded NUL is a wrong-row hazard on an id column, not just cosmetic); legacy `lifecycle`/assignment `disposition` are validated against their live-write enums, not just sanitized. Concurrency-at-boot (ADR-0053/#3475 precedent) verified by a genuine two-thread racing test, not a sequential simulation — the row lock on a conflicting insert serializes the racers ONLY IF both reach it (the marker check ahead of it is a plain SELECT, so a racer can instead take the holder-side-verification path if the other already committed first); same scheduling-uncertainty class as CaStore's UP-3, corrected 2026-08-24 — the test now uses the matching bounded-retry-until-observed shape rather than asserting determinism. Deliberately did NOT port DeviceTokenStore's (#3399) streaming/batched-`unnest` rewrite — disproportionate for this store's small, bounded, operator-curated scale (TagStore/GuaranteedStateStore's full-materialization precedent, not DeviceTokenStore's). See ADR-0055 for the full "Considered and rejected" and "Concurrency at boot" reasoning. |
| `InstructionStore` | `instruction_store` | Migrated Postgres (ADR-0058, Wave 2 batch 7). Content-plane catalog (`InstructionDefinition -> InstructionSet -> ProductPack`) — two tables, no FK (`instruction_set_id` is a soft TEXT reference; `delete_set` unsets it rather than cascading). AUTHORITATIVE/fail-hard (ADR-0012 §1, ADR-0036): `query_definitions`/`get_definition`/`list_sets`/`delete_definition`/`delete_set`/`export_definition_json` widened to `std::expected` (`get_definition` -> `std::expected<std::optional<InstructionDefinition>, std::string>`, matching `ProductPackStore::get`); `create_definition`/`update_definition`/`create_set`/`import_definition_json`/`import_definition_json_trusted` already were. `open_with_migrations` (named by the playbook/ADR-0008/ADR-0012 as mandatory) does not exist anywhere in the tree — hand-rolled acquire/`PgMigrationRunner::run`/smoke-read instead, matching `ProductPackStore`/`plugin_config_store.cpp`'s actually-shipped precedent. **NO legacy-SQLite backfill** (ADR-0009's fresh-start-by-default class, `ResponseStore` precedent — no production fleet has ever run a pre-Postgres build of any Yuzu store); the pre-migration `instructions.db` is never read, no flag, just a one-time boot log line. An earlier revision of this migration built a mandatory, extensively-designed backfill (two real conflict-detection bugs found and fixed across governance rounds) that was later retired in full — see `docs/adr/0058-instruction-store-postgres-migration.md`'s "Backfill" section and `governance.d/instructionstore-adr0058-gov.4uLptn.jsonl` for the history; do not resurrect that design from an earlier reading of this row. **First store on the ladder combining an unconditional every-boot bundled-content reseed loop (`kBundledDefinitions`/`kBundledSets` in `server.cpp`) with mutable post-insert rows** (`update_definition` — no analogous `update_set` exists), UNAFFECTED by the backfill retirement above — this is a separate, orthogonal concern. Four pinned Catch2 tests empirically fixed the pre-migration SQLite reseed contract before porting (content-blind id-existence conflict-skip; an operator edit survives reseed; an operator DELETE was silently RESURRECTED on the next boot; a release-upgrade's changed bundled content never reaches an existing row). Independent-model consult (Codex, opine-only) + verification against `docs/postgres-store-playbook.md`'s named "hard DELETE + unconditional reseed resurrects it" anti-pattern and `RbacStore::remove_permission`'s (#2703) real shipped fix informed a **deliberate behaviour change**: deletion is no longer a plain DELETE the reseed loop can silently undo — `deleted_seed_content(kind, id)` (RbacStore's `revoked_seed_defaults` shape, extended with a `kind` discriminator instead of two tables) records every deleted definition/set id, consulted ONLY by the seed-aware insert paths (`import_definition_json_trusted` internally, and a NEW `create_set_seed` — sets have no signature concept, so "seed" names the actual distinction rather than reusing "trusted"); never by any read path or by plain `create_definition`/`create_set`, so an operator can still freely (re)create content under any id including a previously-deleted bundled one. Every writer touching both the data and tombstone tables (`kSeedCoordLockSql`, the `2037545589` cross-store namespace constant `RbacStore`/`ProductPackStore` already use) takes the same coordination lock as its first statement, in its own transaction, strictly before the check-and-mutate — the RbacStore 4th-bug lesson (a mid-statement lock does not refresh an already-fixed READ COMMITTED snapshot). **Breaking, deliberate**: resurrection-on-delete is removed; an untouched-but-stale bundled row still never auto-refreshes to newer bundled content on upgrade (unsolved, tracked in #2555, not this migration's job). |
| `AnalyticsEventStore` | `analytics_event_store` | Migrated Postgres (ADR-0049, Wave 2 batch 3, PR #3350). Outbox/spool for analytics events (auth/SCIM routes + agent/gateway handlers), not operator state. Backfill **SKIPPABLE** (ADR-0009 — a drained row is already delivered; undrained rows at cutover are a bounded, recorded loss). Drain claim is single-sweeper via `pg_try_advisory_xact_lock('analytics_event_store:drain')`, but — unlike a pure reap — the claim (`UPDATE ... RETURNING`, one txn) and the sink send (network I/O, no lease held) are separate phases, with a third revert-on-failure phase; a batch row whose JSON fails to parse drains once and drops (fixes the SQLite original's poison-pill retry-forever). Drained-row retention is deliberately UNCHANGED (kept forever, same as the SQLite original) — flagged as a growth caveat (#2508-adjacent), not solved here. Construction posture DIVERGES from the playbook default: a migration/open failure logs and leaves the store constructed-but-degraded (`is_open()==false`, object kept alive — NOT `.reset()`, corrected 2026-08-19 after that behavior was found to regress the SQLite predecessor's own object-liveness contract) rather than setting `startup_failed_` — every caller already null-guards or degrade-guards the store and `--no-analytics` defaults off, so gating the whole server's boot on this one non-critical table would contradict its own fail-soft posture; `/readyz` reports it via a non-gating `degraded` field, never `failed_stores`. **`session_id` is `AuthManager::sha256_hex(cookie)`, never the raw bearer token** (governance Gate 2 finding, 2026-08-16 — a raw cookie under this store's unbounded retention, readable via `Infrastructure:Read`, was a session-hijack vector; see ADR-0049 §Secrets). Reachable-from-a-detached-thread UAF closed post-merge (PR #3350 review, 2026-08-20): `analytics_store_` is now `shared_ptr`/`weak_ptr`-locked rather than a raw pointer, since `forward_gateway_pending()`'s detached per-command thread can call into it well outside `stop()`'s drain window (see `server.cpp`'s `stop()` comment at the `analytics_store_.reset()` call site). |
| `WebhookStore` | `webhook_store` | Migrated Postgres (ADR-0057, Wave 3 — first `SecretCodec`-migrating store past `AuthDB`, the template for `OffloadTargetStore`/`RuntimeConfigStore`). Two tables (`webhooks` + `webhook_deliveries`, FK CASCADE). `webhooks.secret` (the HMAC signing secret) is a `SecretCodec` envelope blob; `has_secret` is the independent non-secret flag ADR-0010's anti-downgrade rule requires, DB-`CHECK`-enforced. Decrypt-on-use at the HMAC signing site (`deliver_single`, a worker-pool thread) — never batch-cached, though ADR-0010 §Consequences permits it; a decrypt failure skips the delivery entirely (never fires unsigned). Split posture (ADR-0012 §1): `webhooks` create/list/delete AUTHORITATIVE/fail-hard (`list()` type-distinguishable, `DiscoveryStore`-class — a degraded read 503s rather than rendering "no webhooks configured"); `fire_event`'s internal scan and `record_delivery` stay deliberately fail-soft (deny-or-benign — a dropped notification on a DB blip is availability degradation, not a security fail-open). Mandatory ADR-0009 backfill, BOTH tables (the delivery log has no TTL/prune, so `ResponseStore`'s skippable-telemetry justification does not hold) — `DeploymentStore`'s per-legacy-content fingerprint-set idempotency (`sqlite_backfill_source`), not `NotificationStore`'s single-marker shape, chosen because `secret` has a real (if unbuilt) post-migration lifecycle the per-row IDENTITY-compare-then-PG-wins conflict rule protects; the fingerprint deliberately EXCLUDES the plaintext secret bytes (a stored hash of it would be a SQL-insider brute-force oracle — only a has-secret bit is hashed). Orphaned legacy delivery rows fail the backfill closed (referential-closure reasoning, `SoftwareDeploymentStore`). First store to actually retain a secret-bearing legacy file under ADR-0010 §Consequences (a)–(d) (`AuthDB` was fresh-start/no-backfill) — 0600-forced (main file AND `-wal`/`-shm` sidecars, at both read time and move-aside time) before read and on the moved-aside copy, WAL/SHM sidecars carried across (`AuditStore` precedent). **Merged 2026-08-25 (PR #3563) — ADR-0009's fresh-start-by-default amendment was authored the same day, too late to reach this PR.** Its `migrate_from_sqlite()` production method stays for now (unlike a genuinely-new migration, this one already shipped it); only its backfill TEST suite is being retired, same pass as the other already-migrated stores' backfill tests. |
| `PolicyStore` | `policy_store` | Migrated Postgres (ADR-0056, Wave 2 batch 6). Compliance definition + status store — six operator-authored tables (`policy_fragments`/`policies`/`policy_inputs`/`policy_triggers`/`policy_groups`/`policy_status`) plus a new operational-only seventh, `policy_dispatch_state` (never backfilled, never read outside `claim_due_policies`). **Fragments/policies write once** — no update path exists for any of the five intent tables (only `enable_policy`/`disable_policy` and all of `policy_status` mutate post-creation), which is why the IDENTITY/LIFECYCLE backfill partition is unusually clean. Split posture (ADR-0012 §1, ADR-0036): the five intent tables + reads feeding the evaluator's claim/compliance-% are degrade-distinguishable (`PolicyReadError::kDegraded`); `policy_dispatch_state` is fail-soft/internal-only. **Headline design (the "genuinely new PG problem"):** `PolicyEvaluator`'s dispatch/collect coordination used to live entirely in per-process memory (`last_eval_`, `in_flight_`) — safe with one SQLite-era server process, unsafe under N replicas (duplicate dispatch, an unconditional every-restart `fixing`→`unknown` reset that would stomp a sibling replica's live remediation). Fixed by splitting the fix per phase: **dispatch** is a durable, fleet-wide single-sweeper CLAIM (`PolicyStore::claim_due_policies`, `pg_try_advisory_xact_lock('policy_store:dispatch')` + a `policy_dispatch_state` UPSERT with a `WHERE`-guarded conflict branch as the atomic check-and-claim; the same locked transaction also runs the stranded-`fixing` staleness sweep, replacing the old unconditional reset with one gated by `last_fix_at` age); **collect** stays per-replica/in-memory/at-least-once (only the dispatching replica ever holds the in-flight entry, and `update_agent_status`'s UPSERT is naturally idempotent against a racing manual `remediate()`/`evaluate_now()` on another replica — for the status VALUE only; the fix-attempt retry counter is not, see the Sixth correction in ADR-0056: two concurrent `remediate()` calls for the same policy each independently increment it, a real double-count. Same-process concurrency is closed by a `remediating_` reservation guard; cross-replica is still open). `update_agent_status`'s retry-cap check folds into the UPSERT itself (closes a cross-replica TOCTOU the SQLite original only avoided by accident, via a process-wide mutex). Two corrections found running the two-instance regression test (see ADR-0056 for detail): a fresh-INSERT `fixing` row's `last_fix_at` needed the same CASE the conflict branch already had (a bare 0 there is read by the new staleness sweep as infinitely stale); `evaluate_now()` needed a new unconditional stamp (`PolicyStore::record_dispatch`, no lock/guard), taken BEFORE `kickoff_check`'s blocking dispatch call rather than after (a second, narrower version of the same gap, caught by a pre-governance review round), so a manual dispatch doesn't leave the durable claim blind to it and get re-claimed by the very next automatic tick. MANDATORY ADR-0009 backfill for the five intent tables (`SoftwareDeploymentStore`/ADR-0051 fingerprint shape; runbook `docs/ops-runbooks/policy-store-backfill-recovery.md`); `policy_status` is copied with a direction-aware LIFECYCLE merge rather than fresh-started (the default 3600s eval interval would otherwise leave up to an hour of false-unknown fleet compliance on an operator-visible dashboard after every upgrade) — tolerates a legacy file predating the `fix_attempt_count` ALTER-wart (G4-UHP-POL-003), defaulting the missing column to 0 (a probe failure on that column now fails the backfill closed too, matching the sibling table-existence probe, rather than silently defaulting every row to 0). Real FK + `ON DELETE CASCADE` added from `policy_status` to `policies` (the SQLite original had none — `delete_policy` hand-wrote a second `DELETE` whose failure was silently swallowed; gone now). Construction gains a fail-closed guard it never had on SQLite (a pre-existing gap, not a new decision — the ladder already called this store authoritative). **`migrate_from_sqlite` RETIRED (2026-08-25, shortly after this migration's 2026-08-20 merge — not yet released)**: no production fleet ever ran the pre-Postgres build, so there was no real legacy data to protect — the "MANDATORY backfill" sentence above describes the ORIGINAL design, not the current code, and its runbook citation is now dead: `docs/ops-runbooks/policy-store-backfill-recovery.md` was deleted outright as part of the same retirement, not merely left stale; `policy_store.sqlite_backfill_source` is gone from the schema too. |
| `RuntimeConfigStore` | `runtime_config_store` | Migrated Postgres (ADR-0060, Wave 3 secret-gated, LAST store on the ladder). Persistent runtime-config overrides — the fixed `allowed_keys()` allow-list backing `GET`/`PUT /api/config/:key` (retention windows, `log_level`, DEX alert-routing knobs, the `oidc_*` Settings). **Per-KEY secrecy, not per-column** — `is_secret_config_key(key)` decides, at the row level, which of TWO tables a key lives in: `runtime_config` (plain) or `runtime_config_secrets` (SecretCodec-envelope-encrypted `sealed_value`, ONE row per secret key, `key` itself as the TEXT AAD pk_column). Modeled on `PluginConfigStore`'s (ADR-3005) `configs`/`secrets` split — the closer precedent than `AuthDB`'s nullable-single-column shape, since this namespace (like PluginConfigStore's) mixes many unrelated kinds of entry under one allow-list, not many rows of the SAME kind. `sealed_value` is `NOT NULL`: the row's mere presence is the "has a real, non-empty secret" signal, so the empty-secret rule (nothing to protect, `is_set` derives from non-emptiness) is preserved by NEVER writing an empty ciphertext — an explicit clear deletes the `runtime_config_secrets` row and writes `value=''` into the plain table instead, which is also how attribution (`updated_by`/`updated_at`) survives a clear. A becomes-secret-later key (a plaintext row with no ciphertext row yet) is read as a fallback until its next `set()`, which encrypts it and deletes the stale plaintext row in the same transaction — "next write envelopes it," recorded as the deliberate migration story rather than a retroactive boot-time re-encryption pass. AUTHORITATIVE/fail-closed (ADR-0012 §1, ADR-0036): `get_all()`/`get_all_with_secrets()`/`get()`/`get_with_secrets()` widened to `std::expected<..., std::string>` — the boot override pass and `GET /api/config` both fail closed on a genuine read/decrypt failure now (closes the pre-existing CH-2 defect as a consequence of the typed-read contract, not separately-scoped work); `get_value()`/`get_value_with_secrets()` deliberately NOT widened (every call site audited deny-or-benign — DEX alert-routing knobs, and `plugin_signing_required`, which feeds only a UI badge and an admin-only manually-curl'd distribution endpoint, never a live server-side enforcement gate). `kRuntimeConfigDbErrorPrefix` classifies `PUT /api/config/:key` 503 (genuine DB/crypto failure) vs 400 (caller-input validation), `TagStore`/`ProductPackStore`'s convention. NO backfill (ADR-0009's 2026-08-25 fresh-start-by-default amendment) — no production fleet has ever run a pre-Postgres Yuzu build, so there is no real legacy `runtime-config.db` to protect; the legacy file is never read, and a one-time loud boot log records the reset (`ResponseStore`/ADR-0039's reference shape for the skippable class). Own `SecretCodec` instance over the shared `auth_key_provider_` (the THIRD instance backing that one `FileKeyProvider`, after `auth_secret_codec_`/`plugin_config_secret_codec_`). **The last store started on this ladder** — `WebhookStore`/ADR-0057, `InstructionStore`/ADR-0058, and `OffloadTargetStore`/ADR-0059 are in flight in parallel and not yet merged as of this row; once all four land, every server store is on the Postgres substrate (NvdDatabase excluded by the recorded M1a owner override). |

## Wave 1 — cross-store-join / durable / high-write

These feed the vuln-graph scoring join (the headline ADR-0006/0004 benefit) or carry
high write volume or durable state. Migrate first; they also exercise the cross-store
query-owner seam (ADR-0012 §3) when scoring lands.

| Store | Schema | Provisional posture | Notes |
|---|---|---|---|
| `GuaranteedStateStore` | `guaranteed_state_store` | authoritative | Guardian state; high-write; join input. **Migrated Postgres — ADR-0038** (Wave 1.1, confirmed shipped 2026-08-25 — this row was stale, still listed as "in flight" after merge; verified directly against `pg::PgPool&` in `guaranteed_state_store.hpp`): rules/meta reads type-distinguishable (a silent-empty `list_rules` into the Push fan-out is a fleet-wide disarm — the catastrophic read); event/observation ingest fail-soft; mandatory 5-table backfill (ADR-0037 SQLSTATE discrimination); the cleanup thread's bare TTL deletes port to the #2496 clock-guard shape (single-sweeper advisory lock, lockstep events+observations reap, WS-E reaped counters kept). |
| ~~`FleetTopologyStore`~~ | — | **N/A (not a SQLite store)** | Verified 2026-07-31 against dev HEAD: an **in-memory topology cache** built `(fetcher, nvd)` — TTL slots + pushed-snapshot LRU + IP-ownership maps under mutexes, zero sqlite3/CREATE TABLE/persistence. "durable topology" in the old row was misleading; there is nothing to migrate off SQLite. Excluded from the ADR-0006 migration wave (operator decision). Persisting topology snapshots would be a NEW durability feature, not a migration — out of scope here. |
| `ResponseStore` | `response_store` | authoritative-reads / fail-soft-ingest | **Migrated Postgres — ADR-0039** (Wave 1.2, promoted after FleetTopologyStore was found N/A; confirmed shipped 2026-08-25 — this row was stale, still listed as "in flight" after merge). high-write, TTL'd 90d; backfill **SKIPPABLE** (ADR-0009 — expendable telemetry, fresh-start on cutover, unconditional — no flag); reads degrade-distinguishable at the seam — deny-or-benign for the display consumers (executions drawer / TAR / dashboard), but gate-adjacent for `/auto` Pre-flight (a degraded tick must not persist over an already-resolved device verdict or complete a run on it — #2691 finding 10); bounded ingest (2 MiB/row, 5,000 facets/row); cleanup thread → #2496 clock-guard reap (`pg_now`, `bootstrap_settled`/`no_anchor`, EXISTS-based probe, chunked capped delete); `sqlite3_changes()`→RETURNING (#1033). |
| `AuditStore` | `audit_store` | authoritative | **Migrated Postgres — ADR-0040** (Wave 1.3, confirmed shipped 2026-08-25 — this row was stale, still listed as "in flight" after merge; verified directly against `pg::PgPool&` in `audit_store.hpp`). SOC 2 retained 365d; backfill **mandatory** and history-PRESERVING — a one-time streamed, id-resumable backfill from the legacy `audit.db`, fail-closed on failure, with the legacy file moved aside after a verified backfill. Carries the SQLite schema v3 (`audit_retention_meta` k/v) plus the partial index `idx_audit_ttl_id` across. Posture: construction fails CLOSED (no SQLite fallback); writes fail HARD (`503` + `Sec-Audit-Failed`, never a silent drop); reads DENY on degrade (`503`, never a false-empty). The retention clock guard (#2360) is migration-REQUIRED behaviour, not an optimisation - dropping it reinstates an unbounded clock-driven `DELETE` on the evidence chain. **The single-writer assumption did not port; here is how it was resolved:** the dedup state (previously `last_reported_`, a per-PROCESS member) now lives durably in `audit_retention_meta`, and the sweep is gated by a per-store advisory lease (`pg_try_advisory_xact_lock('audit_store:reap')`, the ADR-0012 §3 `AppPerfRollup` pattern) so exactly one process sweeps per tick fleet-wide. `kMaxAuditDeletesPerPass` (25k) is thereby preserved as the drain rate for the ONE sweeping process — the lease makes "N × 25k" impossible. Deliberate dedup-semantics change: an identical-magnitude repeat clock step no longer re-emits `clock_anomaly_skips` every pass (only a distinct fact-set does). Two new metrics: `yuzu_server_audit_read_degrade_total{reason}`, `yuzu_server_audit_backfill_total{result}`. **Read-time `detail` sanitisation (`AuditStore::sanitized_detail`) ported and is applied at the PG row-materialisation point** — without it, pre-fix `config.update` rows disclose the plaintext OIDC client secret again through all six readers. **The mandatory backfill applies it during the copy too** (the decision this row required the migration author to make explicitly, recorded in ADR-0040): rows are deliberately not rewritten in SQLite, so an un-sanitised copy would propagate the plaintext into the substrate, where — unlike an ADR-0010 `SecretCodec` column — there is no rekey story for a credential embedded in free-form text. The unredacted original survives in the moved-aside legacy file. **#2579's missing-anchor decline is carried across as a DURABLE fact** (`bootstrap_settled` in `audit_retention_meta`, settled at the verdict rather than at the re-anchor) and counted apart on `..._retention_bootstrap_declines_total`. |

## Wave 2 — authoritative config / reference (operator state that cannot be lost)

Security/authz and operator-authored state. Authoritative; a silent empty read is a fail-open
hole — `security-guardian` gates each.

**All migrated.** `PolicyStore` (ADR-0056) and `InstructionStore` (ADR-0058) were this wave's
last two entries — both are removed from this pending table per convention (every other
completed store is removed once done, not left duplicated alongside its `## Done` row); see
the `## Done` table above for their full entries. (`origin/dev`'s standalone ADR-0009
fresh-start-by-default amendment, landed while this PR was in flight, added a caveat to this
row urging exactly the backfill-drop this migration already completed — see ADR-0009's
2026-08-25 update and ADR-0058's own "Backfill — none" section for the settled record.)

## Wave 3 — secret-gated (need the ADR-0010 secrets seam wired)

Migrate last because they require `SecretCodec` (or a verify-only-hash schema) in place.
**No `migrate_from_sqlite()` for either of these two** — ADR-0009's 2026-08-25 fresh-start-by-default
amendment supersedes only this section's former backfill-mechanics sentence ("backfill transforms
(encrypt/hash), never copies"), specifically its assumption that a backfill gets built here at all:
no production fleet has ever run a pre-Postgres build, so there is no real legacy secret material to
transform-and-carry-over. **ADR-0010's transform-never-copy RULE is unchanged and still binds any
backfill either store's own ADR later decides to build under the documented-exception case** — this
just means neither store needs one by default. Skip the legacy-file copy entirely, same as
`ResponseStore` already does; wire `SecretCodec` for the store's live write/read path only; see the
playbook's Backfill bullet for the detect-and-warn obligation this doesn't remove.

| Store | Schema | Secret handling | Notes |
|---|---|---|---|
| `OffloadTargetStore` | `offload_target_store` | `SecretCodec` | target credentials. **Do NOT build `migrate_from_sqlite()`** — see Wave 3 note above. |

## Notes

- **`NvdDatabase` (`nvd_db`) — deferred, still SQLite (2026-07-04).** The store was reshaped
  (v1→v2: flat `cve` → normalized `cve` + `cve_match` with full CPE version ranges) but
  **deliberately kept on SQLite** as an eyes-open owner override of the born-on-PG mandate,
  to ship matching precision now; the born-on-PG reshape is vuln-scan roadmap **M1a** and stays
  queued. Recorded here (not a fresh exception ADR — the store is not new) so a control-mapping
  review doesn't read the ladder as silently non-compliant. The normalized header/match split
  maps directly onto the eventual two PG tables, so this reduces migration debt rather than
  adding it.
- **`scim_resources`/`scim_tokens` exception RETIRED (2026-07-16).** The prior deliberate SQLite
  exception — SCIM's tables rode the existing `auth.db` SQLite file pending `AuthDB`'s own
  cutover — is closed: `ScimStore` now migrates independently to its own `scim_store` Postgres
  schema, shipped in lockstep with `auth DB`'s own cutover (see "Done" above). Kept here as a
  dated record of the retired exception, not as a live carve-out. Detail:
  `docs/auth-architecture.md` "SCIM v2 provisioning" § Storage.
- The count is ~27 `*Store` classes + the auth DB (now itself Postgres); the exact set of
  still-SQLite server stores is whatever currently opens a `sqlite3*` under `server/core/src/`.
  Re-derive before declaring the ladder complete: `grep -rl "sqlite3_open" server/core/src`.
- A store may be **split** or **merged** during migration if its schema warrants it — record that
  in its per-store ADR.
- When the last row clears, remove the `migration_runner.*` SQLite path from the server target and
  delete this file (its job is done).
- **Open follow-up — upgrade-test coverage per store (noted 2026-08-18, ADR-0053).**
  `scripts/test/test-upgrade-stack.sh` carries exactly one store-specific data-survival assertion
  (the original ADR-0009 generic-inventory pilot). None of the migrations since — `LicenseStore`,
  `DeploymentStore`, `SoftwareDeploymentStore`, `CaStore` — added its own dedicated
  previous-release-SQLite → new-release-Postgres assertion; each relies on the harness's generic
  `fixtures-verify` pass instead. Each migrated store's own unit-test `migrate_from_sqlite` suite
  covers the backfill logic with real legacy-schema fixtures, but not the end-to-end upgrade path.
  Not solved here — flag for whoever next touches the upgrade harness or does a ladder-completeness
  pass.
