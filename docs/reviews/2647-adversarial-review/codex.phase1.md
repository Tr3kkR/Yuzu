# Codex Phase 1 — independent review

[C-P1-1]  HIGH · CONFIDENCE(hi) · PROVENANCE(test-run)
The first ledger fragment violates its own version-1 row schema
- Location:  governance.d/2619-ledger-provenance-prose-ownership.sW31cX.jsonl:5 (+ line 6)
- Claim:     The committed first/template fragment contains an off-enum disposition and a superseding row missing two per-row mandatory fields, so a parser enforcing the field table must reject the evidence artifact this change introduces.
- Evidence:  Gate 8's field table says `disposition` is `open` / `fixed` / `deferred-to-issue #N` / the other enumerated terminals, and says a superseding row carries `recorded_by` and `reviewed_at_sha`; line 5 instead records `"disposition":"deferred-to-issue"`, while line 6 is a supersession with neither `recorded_by` nor `reviewed_at_sha`. My JSON/schema check parsed all 62 rows and reported exactly these row-level defects (as well as later occurrences of the documented `deferred-to-issue #2639` spelling, which are valid under the prose enum). Later C2 rows repair the merged finding view, but neither changes the bytes or per-row validity of lines 5–6.
- Scenario:  A validator or downstream extractor adopts the published field table → it rejects the repository's first fragment (or must special-case it) → the fragment cannot serve as the conforming template/evidence artifact the branch claims to establish.
- Inference: The supersede-never-edit rule preserves historical facts, but it cannot retroactively add per-row mandatory metadata or turn an off-enum value into a schema-valid historical row; `schema_version: 1` asserts that these rows use the schema being defined.
- Anchor:    contract — issue #2619 acceptance (“Schema carries…” and “settled before the ledger has a writer”), plus CLAUDE.md standing rule 2's policy floor for violation of an explicit MUST/standing invariant; Gate 8 says a supersession “carries” the minimum set and the schema/ledger contract is canonical.
- Fix:       Since rows may not be edited, either define and document a schema-valid corrective/migration mechanism that makes historical row validity computable, or replace this pre-publication first fragment with a wholly conforming fragment before it becomes durable evidence; do not publish schema-version-1 rows that violate version 1.
- Falsifier: A canonical sentence stating that supersession repairs the schema validity of earlier rows (not merely their field-wise live view), together with a deterministic rule by which line 6 acquires its missing per-row `recorded_by` and `reviewed_at_sha`.

[C-P1-2]  HIGH · CONFIDENCE(hi) · PROVENANCE(static-read)
Gate 2 narrows routed update obligations back to operator-facing docs
- Location:  .claude/skills/governance/SKILL.md:698 (+ canonical definition at lines 388–401)
- Claim:     The docs-writer Gate 2 prompt says required-doc item 6 covers only an “operator-facing reference,” contradicting the canonical definition that expressly includes author-facing update obligations.
- Evidence:  The canonical prose rule defines item 6 as a routed-concern update obligation “whether operator-facing … or author-facing (a migration ladder, a capability registry, a per-surface invariants doc).” The Gate 2 prompt later says “item 6 is a routed-concern row that names a doc as the operator-facing reference for the changed surface.” Issue #2620 requires “No remaining disagreement between the prose-ownership section and any agent preamble that implements it.”
- Scenario:  A change requires updating an author-facing migration ladder or capability registry under a routed-concern row → docs-writer reads its injected Gate 2 prompt → it excludes the doc because it is not operator-facing → the required-doc truth finding is never filed.
- Inference: Although the prompt also points to the shared six-item list, its immediate restatement expressly narrows that list; an agent following the local explanatory sentence can reasonably apply the narrower rule.
- Anchor:    contract — anchor-issue-2620.md, acceptance criterion “No remaining disagreement between the prose-ownership section and any agent preamble that implements it.”
- Fix:       Replace “operator-facing reference” with “update obligation for the changed surface, whether operator- or author-facing,” matching the canonical item exactly.
- Falsifier: The canonical six-item definition is changed to exclude author-facing routed update obligations, or the Gate 2 sentence is shown not to be part of the agent preamble delivered to docs-writer.

VERDICT:  BLOCK — the first evidence fragment is non-conforming to its own per-row schema, and the Gate 2 prompt still contradicts the prose rule that #2620 requires it to implement.
COVERAGE: Deep on correctness/logic, cross-component/schema consistency, test adequacy, portability of the shell recipes, and provenance/evidence semantics. Security/privilege and resource/concurrency safety were reviewed deeply at the only applicable surfaces (author-writable evidence, append/O_EXCL behavior, field precedence); there are no executable privilege, thread, handle, or C++ lifetime changes in the scoped branch delta. Cross-platform was exercised for the documented portable recipe conceptually and on Linux; no macOS host was available, so BSD `mktemp` behavior was static-read against the recipe rationale. I excluded merged `origin/dev` implementation changes from defect attribution because `origin/dev..HEAD` confirms the branch delta is the seven declared doc/evidence files.
RAN:      `python3` JSON/schema/required-iff/supersession/merge check (62 rows parsed; 48 live findings; zero live `open`; line 5 off-enum and line 6 missing supersession minimums); Gate 8 step-4 `jq -e` on the real ledger (exit 4, clean, zero matches); synthetic jq cases (clean exit 4, violation exit 0, scalar-I1 violation exit 0, missing file exit 2) on jq-1.8.1; documented `mktemp -u` + subshell `noclobber` create and `>>` append in `/tmp` (create 0, append 0, collision 1, content preserved); `python3 scripts/assemble-changelog.py --check` (PASS, 132 fragments); `git diff --check cb69991f..3cc9f417` (PASS); `wc -c CLAUDE.md .claude/routed-concerns.md` (31,163 and 31,902, both below 40k); scoped-file size checks; conflict-marker and stale-rule/name searches; all recorded timestamps parsed and all non-sentinel reviewed SHAs resolved. Compile/unit/integration tests: N/A for the scoped doc-only delta; no translation units changed. CI status: not queried because network/external access was unavailable and the request forbids outward action.
FILES:    /tmp/yuzu-advrev-gov2619/anchor-issue-2619.md; /tmp/yuzu-advrev-gov2619/anchor-issue-2620.md; /tmp/yuzu-advrev-gov2619/TARGET.md; origin/dev:CLAUDE.md; .claude/skills/governance/SKILL.md; .codex/skills/governance/SKILL.md; CLAUDE.md; .claude/agents/docs-writer.md; governance.d/README.md; governance.d/2619-ledger-provenance-prose-ownership.sW31cX.jsonl; changelog.d/README.md; changelog.d/2619-governance-ledger-provenance-and-prose-ownership.changed.md; docs/governance-skill-tuning-2026-07.md; .claude/routed-concerns.md.
