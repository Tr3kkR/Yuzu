# Agent spawn sink manifest

Owner: @Doomgoose. Population tracked in #2380. Governed by ADR-3002
(`docs/adr/3002-acquisition-ladder.md`) — read that first; this file is the
standing evidence ledger it mandates, a **governance artifact, not a plan**
(sequencing lives with the migration roadmap, not here).

Every spawn call site in `agents/plugins/*` and the covered agent-core paths
gets a row. The row — not the call site — is the single authority for the
site's evidence: the call site carries only its **site ID** and a one-line
rationale in a comment. Site IDs are stable and follow
`<plugin-or-component>/<function>#<n>` (`#<n>` disambiguates multiple sites
in one function; IDs are never reused after a site is deleted).

## Row schema

| Column | Meaning |
|---|---|
| Site ID | `<plugin-or-component>/<function>#<n>` — stable, never reused |
| Location | `path/to/file.cpp:function` (line numbers drift; function names anchor) |
| Mechanism | current spawn mechanism (`popen` helper, `system()`, direct `fork/exec`, runner argv, runner Decision-5 shape) |
| Platform | which OS paths reach this site |
| Provenance | where the interpolated values come from (operator/server params, derived, local-system, compile-time literal) |
| Mutating | mutating vs read-only — drives deadline/grace policy and migration gating (ADR-3002 runner contract) |
| Shell features | which shell capabilities the site actually uses (pipeline, redirection, glob, `~user` expansion, none) |
| Privilege | sudoers grant relied on, if any (exact grant form from `scripts/install-agent-user.sh`) |
| Ladder review | does this subprocess need to exist, or does a rung-1 interface answer the same question? |
| Rung + evidence | the rung the site sits at and the ADR-3002 Decision-1 evidence for every rung passed over |
| Registration | Decision-5 interpreter registration / Decision-7 shell exception, where applicable |

## Registered sites

Populated as the migration reaches each site (and immediately for any new or
interim site — ADR-3002 Decisions 1 and 2).

| Site ID | Location | Mechanism | Platform | Provenance | Mutating | Shell features | Privilege | Ladder review | Rung + evidence | Registration |
|---|---|---|---|---|---|---|---|---|---|---|
| `users/do_logged_on#1` | `agents/plugins/users/src/users_plugin.cpp:do_logged_on` | runner argv | macOS | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_sessions#1` | `agents/plugins/users/src/users_plugin.cpp:do_sessions` | runner argv | Linux | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_sessions#2` | `agents/plugins/users/src/users_plugin.cpp:do_sessions` | runner argv | macOS | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_local_users#1` | `agents/plugins/users/src/users_plugin.cpp:do_local_users` | runner argv | Linux | compile-time literal + account name derived from a local-system enumeration (dscl -list / getpwent) | read-only | none — pipeline eliminated (tail -1 replicated in-process, last non-empty line; max_lines caps the FIRST N and would invert the selection) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_local_users#2` | `agents/plugins/users/src/users_plugin.cpp:do_local_users` | runner argv | macOS | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public OpenDirectory C API in-tree; .mm ruled out of this run by the architect | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no public OpenDirectory C API in-tree, .mm ruled out this run | n/a |
| `users/do_local_users#3` | `agents/plugins/users/src/users_plugin.cpp:do_local_users` | runner argv | macOS | compile-time literal + account name derived from a local-system enumeration (dscl -list / getpwent) | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public OpenDirectory C API in-tree; .mm ruled out of this run by the architect | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no public OpenDirectory C API in-tree, .mm ruled out this run | n/a |
| `users/do_local_users#4` | `agents/plugins/users/src/users_plugin.cpp:do_local_users` | runner argv | macOS | compile-time literal + account name derived from a local-system enumeration (dscl -list / getpwent) | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public OpenDirectory C API in-tree; .mm ruled out of this run by the architect | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no public OpenDirectory C API in-tree, .mm ruled out this run | n/a |
| `users/do_local_users#5` | `agents/plugins/users/src/users_plugin.cpp:do_local_users` | runner argv | macOS | compile-time literal + account name derived from a local-system enumeration (dscl -list / getpwent) | read-only | none — env-var prefix carried by /usr/bin/env LC_ALL=C, an exec wrapper not an interpreter | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin; rung stays 2: /usr/bin/env is an exec wrapper, not an interpreter | n/a |
| `users/do_local_admins#1` | `agents/plugins/users/src/users_plugin.cpp:do_local_admins` | runner argv | macOS | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public OpenDirectory C API in-tree; .mm ruled out of this run by the architect | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no public OpenDirectory C API in-tree, .mm ruled out this run | n/a |
| `users/do_group_members#1` | `agents/plugins/users/src/users_plugin.cpp:do_group_members` | runner argv | macOS | operator param (group), gated by is_safe_identifier | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public OpenDirectory C API in-tree; .mm ruled out of this run by the architect | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no public OpenDirectory C API in-tree, .mm ruled out this run | n/a |
| `users/do_primary_user#1` | `agents/plugins/users/src/users_plugin.cpp:do_primary_user` | runner argv | Linux | compile-time literal argv | read-only | none — pipeline eliminated (head -200 -> max_lines=200, stop_after_max_lines=true) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_primary_user#2` | `agents/plugins/users/src/users_plugin.cpp:do_primary_user` | runner argv | macOS | compile-time literal argv | read-only | none — pipeline eliminated (head -200 -> max_lines=200, stop_after_max_lines=true) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_session_history#1` | `agents/plugins/users/src/users_plugin.cpp:do_session_history` | runner argv | Linux | operator param (count), numeric | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_session_history#2` | `agents/plugins/users/src/users_plugin.cpp:do_session_history` | runner argv | macOS | operator param (count), numeric | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `certificates/list_certs_macos#1` | `agents/plugins/certificates/src/certificates_plugin.cpp:list_certs_macos` | runner argv (`[/usr/bin/sudo -n --] /bin/launchctl asuser <uid> /usr/bin/sudo -n -u <user> -- /usr/bin/security find-certificate -a -p <login keychain>`) | macOS | argv built by `build_login_keychain_read_argv()` from an already-allowlist-validated uid/username plus a `getpwnam_r`-resolved, absolute-path-validated home directory (all local-system, not operator input) | read-only | none — `~user` expansion eliminated (resolved in-process via `getpwnam_r`, the same Directory Services database the shell's tilde expansion consulted, so a resolvable account — relocated/mobile/network homes included — yields a byte-identical path) and redirection eliminated (2>/dev/null -> merge_stderr=false) (#3406). One deliberate divergence, in the fail-closed direction: for an UNRESOLVABLE account the shell left `~user` literal and let `security` fail opaquely, whereas construction now fails and the action reports an honest sentinel | none beyond the LaunchDaemon's existing root context (ships with no `UserName` key — runs as root today; no sudoers grant needed) | Reviewed — no rung-1 API reaches a per-console-user login keychain without a session/user context decision (`SecItemCopyMatching` with `kSecMatchSearchList` bound to the user's login keychain is the rung-1 candidate, deferred to the ADR-3003 user-context bridge per #3406); the `launchctl asuser`/`sudo -u` session hop itself execs fine as plain argv | Rung 2 — clean multi-element argv through the bounded subprocess runner (was rung 3, `/bin/sh -c` wrapping the same command for `~user` expansion, before #3406). Decision-1 descent evidence vs rung 1: **hard requirement demonstrably unmeetable (privilege/session context)** — the rung-1 candidate (`SecItemCopyMatching` bound via `kSecMatchSearchList` to the user's login keychain) requires the caller to be executing INSIDE that user's per-user launchd/Aqua session, and this LaunchDaemon runs as root outside every Aqua session; there is no in-process API by which a system daemon can enter one, which is precisely the gap ADR-3003's user-session helper exists to close. Secondarily, which user's login keychain is read (fast-user-switching behaviour) is an operator-visible contract that must not be silently changed by a transport migration | Decision-1 ledger row (plain-binary argv through the bounded runner; the former Decision-7 shell exception is RETIRED — no interpreter is invoked and no script payload is embedded) |
| `certificates/details_cert_macos#1` | `agents/plugins/certificates/src/certificates_plugin.cpp:details_cert_macos` | runner argv (same invocation as `list_certs_macos#1`) | macOS | same as above | read-only | none — same eliminations as above (#3406) | same as above | same as above | same as above | Decision-1 ledger row (former Decision-7 shell exception RETIRED, #3406) |
| `certificates/keychain_contains_thumbprint#1` | `agents/plugins/certificates/src/certificates_plugin.cpp:keychain_contains_thumbprint` | runner argv (`/usr/bin/security find-certificate -a -p <path>`) | macOS | `keychain_path` is one of two fixed literal paths (`resolve_delete_keychain_path()`); no operator-controlled text reaches this argv | read-only (used for both the pre-delete presence check and the post-delete verify re-enumeration in `delete_cert_macos`) | none | none beyond the LaunchDaemon's existing root context | Reviewed against the rung-1 option that now exists in this same file — "no interface exists" is FALSE here: `read_keychain_secitem()` enumerates these exact two fixed keychains natively via `SecItemCopyMatching`, and its output was verified thumbprint-for-thumbprint identical to this site's `security find-certificate -a -p` (System.keychain 3/3, SystemRootCertificates.keychain 158/158, macOS 26.5.2 arm64, 2026-08-17). Promotion is blocked on a specific, checkable correctness dependency rather than on effort: this site's UP-5 contract requires distinguishing an unrelated per-certificate decode failure (skip it, keep proving absence) from a capped/truncated scan (prove nothing), and `SecItemKeychainResult` currently folds BOTH into a single `complete=false`. Promoting before splitting that flag would regress UP-5 — one unrelated malformed certificate anywhere in the keychain would turn a genuinely successful delete into a reported failure | Rung 2 — clean multi-element argv through the bounded subprocess runner. Decision-1 descent evidence: **disproportionate cost** (the ADR's weakest category, stated as the required written comparison) — rung 1 is implemented and available, but consuming it here first costs a split of `SecItemKeychainResult::complete` into capped-vs-decode-failed plus a re-verification of the delete-verification verdict table (`fold_presence_scan`); that is a behavioural change to a MUTATING action's proof-of-absence, outside WP-B's read-path scope. Tracked for promotion in #2380 | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site — no interpreter is invoked and no script payload is embedded) |
| `certificates/delete_cert_macos#1` | `agents/plugins/certificates/src/certificates_plugin.cpp:delete_cert_macos` | runner argv (`/usr/bin/security delete-certificate -Z <thumbprint> <path>`) | macOS | `<thumbprint>` is hex-validated (`is_valid_thumbprint`) + canonicalized operator input; `<path>` is one of two fixed literal paths | **mutating** | none | **none used today** — the macOS LaunchDaemon ships with no `UserName` key and runs as root, and this argv carries no `sudo` prefix at all. RECORDED MISMATCH (do not copy this grant as if it were live evidence): `scripts/install-agent-user.sh:467` and `docs/agent-privilege-model.md:107` both grant `/usr/bin/security delete-certificate -t /Library/Keychains/System.keychain *`, which CANNOT authorize this site's actual argv `delete-certificate -Z <thumbprint> <path>` — `-t` is the "also delete user trust settings" switch, not a selector (`security help delete-certificate`), so the first argument does not match. Inert while the daemon is root; the grant, the privilege matrix and a `sudo -n --` argv must be reconciled together before #1455 moves the service off root | Reviewed — a rung-1 delete does exist in principle (`SecItemDelete`/`SecKeychainItemDelete`), so "no interface exists at the rung" is FALSE and is not claimed. Not taken here: this is the plugin's only MUTATING macOS site, and its safety rests on the existing exit-code + re-enumeration verdict table (`security delete-certificate` can exit 0 without removing anything under SIP/ACL quirks), so swapping the delete mechanism means redesigning that proof | Rung 2 — clean multi-element argv through the bounded subprocess runner. Decision-1 descent evidence: **disproportionate cost** (written comparison): rung 1 would replace a CLI whose SIP/ACL failure modes the current verdict table is built and tested against, requiring that whole proof-of-deletion to be re-established against a different API's error surface; it buys one fewer child process on a single operator-initiated action. WP-B promotes read paths only | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `certificates/parse_pem_block_macos#1` | `agents/plugins/certificates/src/certificates_plugin.cpp:parse_pem_block_macos` | runner argv (`/usr/bin/openssl x509 -noout -in <tmpfile> -subject -issuer -startdate -enddate -serial -fingerprint -sha1 -text`) | macOS | `<tmpfile>` is a `yuzu::TempFile`-owned path (mkstemps/O_EXCL, mode 0600) holding one PEM block read from the login keychain — no operator text | read-only | none | none beyond the LaunchDaemon's existing root context | Reviewed — rung 1 is available and is IMPLEMENTED IN THIS BRANCH: `certificates_x509::parse_pem_certs()` parses exactly this kind of PEM block in-process, and the LibreSSL `-ext keyUsage` limitation that shaped this site is a limitation of the host CLI, not of rung 1 (the plugin links libcrypto directly). "No interface exists at the rung" is therefore FALSE and is not claimed. This is the strongest remaining promotion candidate in this plugin | Rung 2 — clean multi-element argv through the bounded subprocess runner. Decision-1 descent evidence: **disproportionate cost** (written comparison): promoting costs a fresh parity capture of the LOGIN-keychain output across all seven fields, because this site's result feeds `certificates_macos_parsers.hpp`'s `-text`-shaped line selection and the login path is frozen byte-identical by WP-B's spec; it buys removal of one `openssl` child process per PEM block, which is the largest per-call spawn saving left here. Deliberately DEFERRED, explicitly NOT ruled out — tracked in #2380 | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `certificates/resolve_console_user#1` | `agents/plugins/certificates/src/certificates_plugin.cpp:resolve_console_user` | runner argv (`/usr/bin/stat -f%Su /dev/console`) | macOS | none — fixed literal argv | read-only | none | none beyond the LaunchDaemon's existing root context | Reviewed — CORRECTION of an earlier claim in this row that the LaunchDaemon "has no SystemConfiguration framework access": that is FALSE. `agents/shared/macos_console_user.hpp:361` already implements `console_user()` via `SCDynamicStoreCopyConsoleUser`, and `agents/plugins/users/meson.build:12-19` links SystemConfiguration with the same `required: false` + `-D`-flag shape this plugin now uses for the Security framework — nothing prevents this plugin from doing likewise, so the omission is a BUILD CHOICE, not an unavailable rung. Rung 1 is therefore available and is not ruled out. Not taken in WP-B because the two mechanisms answer subtly different questions: `stat -f%Su /dev/console` reports the console DEVICE owner while `SCDynamicStoreCopyConsoleUser` reports the Aqua SESSION owner, and they diverge under fast user switching and screen sharing — WHICH user's login keychain this plugin reads is an operator-visible behavioural contract | Rung 2 — clean multi-element argv through the bounded subprocess runner. Decision-1 descent evidence: **disproportionate cost** (the ADR's weakest category, stated as the required written comparison): rung 1 is implemented and linkable, but adopting it changes console-user semantics under fast user switching / screen sharing and so needs its own behavioural verification rather than riding a certificate read-path migration. Tracked in #2380 | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site). **Sibling site `#2` (`/usr/bin/id -u <username>`) is RETIRED as of #3406 — ID `#2` retired, never reused.** That row already recorded that rung 1 existed for it (`getpwnam_r` resolves a username to a uid in process) and deferred only to keep console-user resolution one coherent mechanism. #3406 put a `getpwnam_r` call on this exact path anyway (the login-keychain leg needs `pw_dir`), and the SAME record carries `pw_uid` — so the spawn became pure redundancy and was removed rather than re-justified. This site (`#1`) is unaffected: it answers a different question (console DEVICE owner) that no passwd lookup can answer. |
| `quarantine/win_*` (21 sites) | `agents/plugins/quarantine/src/quarantine_plugin.cpp:win_quarantine`/`win_unquarantine`/`win_is_quarantined`/`win_get_whitelist`/`do_whitelist` (Windows branch) | runner argv (`netsh advfirewall firewall ...` / `netsh advfirewall show\|set allprofiles ...`), `netsh.exe` resolved via `GetSystemDirectoryW` (no PATH search) | Windows | operator-supplied IP (block/allow rules, whitelist add/remove), `is_safe_ip`-validated; else compile-time literal argv, EXCEPT the per-profile `firewallpolicy` restore, whose value is the host's own pre-quarantine policy read back from plugin KV storage — validated by `parse_profile_policies`/`is_complete_profile_policy` (strict: any malformed or incomplete record is rejected wholesale and never reaches an argv) before `netsh_restore_profile_policy_argv` builds it. The containment `set` itself remains the compile-time literal `kWinFirewallPolicyBlockBoth`, and `kWinFirewallPolicyDefault` is now only the honest fallback when no complete capture is available | 13 mutating, 8 read-only (up from 11/4 — #3284 branch A: `docs/quarantine-windows-firewall-precedence.md` verified live that Block overrides Allow, so win_quarantine no longer creates the two named `BlockAllInbound`/`BlockAllOutbound` rules — replaced by ONE profile-policy `set` call, net -1 mutating site there — and win_unquarantine gained a matching profile-policy restore, net +1 mutating site, so the mutating total is unchanged at 11; #3285 adds a `show allprofiles` read to win_quarantine's prior-policy capture and both a `show allprofiles` read and the previously-missing `dir=out` capture to win_is_quarantined, which before this PR read only `dir=in` and so could never see an outbound-only failure — net +3 read-only sites, 4 -> 7; then Gate 4 added a fourth read to win_quarantine — a probe for this plugin's OWN loopback rule, which is the discriminator that tells a genuinely block-both-hardened host from one this plugin already contained, so release no longer overwrites an admin's egress filtering with the Windows default — 7 -> 8; #3429 round 2 adds the IPv6 loopback mirror — two new named Allow rules, `AllowLoopbackIn6`/`AllowLoopbackOut6` — as sites `win_quarantine#3b`/`#4b`, net +2 mutating, 11 -> 13) | none — clean pre-split argv | none (service-account privilege already covers `netsh`) | no rung-1 API evaluated this PR — Windows does have a native COM firewall API (`INetFwPolicy2`), deliberately not pursued since this migration's scope is spawn-mechanism only | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 (`INetFwPolicy2`) passed over: disproportionate cost, out of this PR's scope. **Non-spawn external interaction, not itself a manifest entry (no subprocess involved):** the whitelist this plugin installs on quarantine now depends on `agents/core/src/server_address_resolver.cpp`'s `getaddrinfo()` call — a bounded (5s), agent-STARTUP-time-only DNS resolution of `--server`, never re-run at quarantine-dispatch time (deliberately, so the host being quarantined can never steer its own containment exception via its own live resolver state — #3429 round 4). | n/a |
| `hardware/do_bios#1` | `agents/plugins/hardware/src/hardware_plugin.cpp:do_bios` | runner argv (`/usr/sbin/system_profiler SPHardwareDataType`) + native parser (`hardware_macos_bios.hpp`) | macOS | none — fixed literal argv | read-only | none — pipeline+grep+awk eliminated (native parser replaces the shell pipeline) | none | Reviewed — no public Boot-ROM/System-Firmware-Version API exists on macOS; `system_profiler` is the only source for either field label | Rung 2 — clean argv via the bounded subprocess runner. Decision-1 descent evidence: no rung-1 API for this data on this OS in this plugin | n/a |
| `hardware/do_disks#1` | `agents/plugins/hardware/src/hardware_plugin.cpp:do_disks` | runner argv (`/usr/sbin/system_profiler SPStorageDataType SPNVMeDataType SPSerialATADataType -json`) | macOS | none — fixed literal argv | read-only | none | none | Reviewed — no public per-disk (NVMe+SATA+size+model) enumeration API exists; the JSON parser (`hardware_disks_macos.hpp`, PR1.8) is unchanged, only this acquisition call site moved | Rung 2 — clean argv via the bounded subprocess runner. Decision-1 descent evidence: no rung-1 API for this data on this OS in this plugin | n/a |
| `hardware/do_memory#1` | `agents/plugins/hardware/src/hardware_plugin.cpp:do_memory` | runner argv (`dmidecode -t memory`, path resolved via `probe_tool_path`) | Linux | none — fixed literal argv (path is one of two fixed candidates) | read-only | none | none — runs unprivileged; the existing EPERM→empty→`/proc/meminfo` aggregate-total fallback is preserved exactly (text parsing extracted to `hardware_linux_parsers.hpp::parse_dmidecode_memory`) | Reviewed — `dmidecode` is the only source of per-DIMM detail on Linux; `/proc/meminfo` gives an aggregate total only | Rung 2 — clean argv via the bounded subprocess runner. Decision-1 descent evidence: no rung-1 API for full per-DIMM detail on this OS | n/a |
| `device_identity/do_ou#1` | `agents/plugins/device_identity/src/device_identity_plugin.cpp:do_ou` | runner argv (`realm list`, path resolved via `probe_tool_path`) | Linux | none — fixed literal argv | read-only | none | none — unprivileged, no `sudo`; the existing `/etc/sssd/sssd.conf` fallback is preserved | Reviewed — sssd's InfoPipe (adopted by `do_domain` in this same PR) has no organizational-unit surface, so this site stays on the `realm` CLI rather than following `do_domain` onto sd-bus | Rung 2 — clean argv via the bounded subprocess runner. Decision-1 descent evidence: no rung-1 API for OU data on this OS | n/a |
| `device_identity/do_domain#1` | `agents/plugins/device_identity/src/device_identity_plugin.cpp:do_domain` | runner argv (`/usr/sbin/dsconfigad -show`) + native parser (`device_identity_macos.hpp`) | macOS | none — fixed literal argv | read-only | none | none | Reviewed — an OpenDirectory `.mm` native implementation is explicitly deferred here, coordinated with a parallel Wave-2 effort doing the same OpenDirectory novelty elsewhere; this ships the runner-argv floor cleanly rather than duplicating that work | Rung 2 — clean argv via the bounded subprocess runner (SHIPPED FLOOR). Decision-1 descent evidence: **disproportionate cost** — an OpenDirectory native implementation is a separate, coordinated novelty effort, not duplicated in this PR | n/a |
| `device_identity/do_ou#2` | `agents/plugins/device_identity/src/device_identity_plugin.cpp:do_ou` | runner argv (`/usr/sbin/dsconfigad -show`) + native parser (`device_identity_macos.hpp`) | macOS | none — fixed literal argv | read-only | none | none | Reviewed — same call shape and same OpenDirectory-deferral reasoning as `do_domain#1` above; a separate action, so a separate audit-relevant call site | Rung 2 — clean argv via the bounded subprocess runner (SHIPPED FLOOR). Decision-1 descent evidence: **disproportionate cost**, inherited from `do_domain#1` | n/a |
| `device_identity/do_domain#2` | `agents/plugins/device_identity/src/device_identity_plugin.cpp:do_domain` | runner argv (`realm list`, path resolved via `probe_tool_path`) | Linux | none — fixed literal argv | read-only | none | none — unprivileged, no `sudo` (same call shape as `do_ou#1` immediately above) | Reviewed — BR-011 (`/adversarial-review` finding): sssd's InfoPipe `allowed_uids` defaults to root-only (`sssd-ifp(5)`) and `/etc/sssd/sssd.conf` ships root:root mode 0600 (sssd.io), so on the documented unprivileged `yuzu` account both the sd-bus call AND the file-read fallback fail closed on a stock SSSD/AD host, and a genuinely joined host reported `joined\|false`. `realm list` is the confirmed-unprivileged path (same call `do_ou#1` already makes) restored as the fallback ahead of the file read | Rung 2 — clean argv via the bounded subprocess runner. Decision-1 descent evidence: this is the SAME call site as `do_ou#1` (identical tool, identical unprivileged posture), invoked from a second action as a privilege-correct fallback, not a new spawn primitive | n/a |
| `quarantine/linux_*` (42 sites) | `agents/plugins/quarantine/src/quarantine_plugin.cpp:linux_quarantine`/`linux_unquarantine`/`linux_is_quarantined`/`linux_get_whitelist`/`do_whitelist` (Linux branch) | `sudo_wrap({"/usr/sbin/iptables", ...})` and, mirrored site-for-site (#3282), `sudo_wrap({"/usr/sbin/ip6tables", ...})` | Linux | operator-supplied IP (per-IP ACCEPT/DROP rules, whitelist add/remove), `is_safe_ip`-validated and routed to the matching family via `ip_family()`; else compile-time literal argv | 34 mutating, 8 read-only (up from 20/2 — #3282 mirrors `linux_quarantine`, `linux_unquarantine`, `linux_is_quarantined`, `linux_get_whitelist`, AND `do_whitelist`'s add/remove branch onto ip6tables, each routed per-IP via `ip_family()`; Gate 4 then added two reads to `linux_is_quarantined` — the per-family `-L yuzu-quarantine -n` chain listing that proves the chain actually DENIES rather than merely being referenced, which was the one leg of the three verifying no enforcement at all — 6 -> 8; #3429 round 2 removes the unconditional `-m state --state ESTABLISHED,RELATED -j ACCEPT` sites `linux_quarantine#5`/`#17` on both families — they accepted any pre-existing established/related flow, not just the whitelisted management connection — net -2 mutating, 36 -> 34; IDs #5/#17 retired, never reused) | none — clean pre-split argv | `sudo -n` NOPASSWD, bare grant on `/usr/sbin/iptables` AND `/usr/sbin/ip6tables` (`install-agent-user.sh:474`, pre-existing, VERIFIED) — matches `sudo_wrap`'s canonical form regardless of specific args | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess, sudo-governed; rung 1 passed over: no rung-1 API for this data on this OS in this plugin. **Non-spawn external interaction:** same `server_address_resolver.cpp` DNS dependency noted on the `quarantine/win_*` row above — one shared, agent-startup-only resolution feeding this platform's whitelist derivation too. | n/a |
| `quarantine/macos_load_ruleset#1` | `agents/plugins/quarantine/src/quarantine_plugin.cpp:macos_load_ruleset` | `sudo_wrap({"/sbin/pfctl", "-f", tmp_file.path()})` — atomic full-ruleset load, `set skip on lo0` at the top of the generated ruleset | macOS | ruleset content built from operator/derived whitelist IPs (`is_safe_ip`-validated upstream), written to a `yuzu::TempFile` | **mutating, CRITICAL** | none | `sudo -n` NOPASSWD, bare grant on `/sbin/pfctl` | **Reviewed and explicitly REJECTED** — an earlier design used a named pf anchor (`pfctl -a yuzu-quarantine -f tmp` + a second `pfctl -f -` to attach it); that second call replaced the whole main ruleset and caused a live UAT connectivity-loss incident (commit `672896112`: the agent's own quarantine-command response never reached the server, box stayed firewalled until manual `pfctl -F all`). This PR migrates ONLY the spawn mechanism (popen → sudo-governed argv) and deliberately preserves the current atomic-single-call + `set skip on lo0` design that fixed that incident — do not reintroduce an anchor without re-deriving the connectivity-preservation proof from scratch | Rung 2 — clean multi-element argv through the bounded subprocess runner. Decision-1 descent evidence: this is the plugin's most safety-sensitive site; the shell-out itself is not the risk, the ruleset-reload STRATEGY is, and that strategy is unchanged by this PR | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `quarantine/macos_load_ruleset#2` | same | `sudo_wrap({"/sbin/pfctl", "-e"})` | macOS | compile-time literal argv | mutating, best-effort/idempotent (does not unwind the already-succeeded ruleset load; failure IS forwarded through the ABI4 result-status seam, not ignored — a stock-macOS pf-disabled-by-default failure here must not read as a successful quarantine) | none | `sudo -n` NOPASSWD | no rung-1 API for this data on this OS in this plugin | 2 — same as above | n/a |
| `quarantine/macos_load_ruleset#3` | same | `sudo_wrap({"/sbin/pfctl", "-s", "info"})` — post-enable verification read, issued UNCONDITIONALLY | macOS | compile-time literal argv | read-only, verification of the `#2` enable call immediately above, and it is the read that DECIDES — `#2`'s exit code decides nothing on its own, in either direction. Upward: `pfctl -e` can exit 0 without pf coming up (#3283). Downward: `pfctl -e` on an ALREADY-ENABLED pf calls `errx(1, "pf already enabled")` and exits 1, so gating this read on `#2` succeeding meant every re-quarantine and every whitelist repair of a contained macOS host skipped it and reported failure on a fully contained device (Gate 4). Failure to confirm `enabled` folds into `pf_enable_failed_out` | none | `sudo -n` NOPASSWD | no rung-1 API for this data on this OS in this plugin | 2 — same as above | n/a |
| `quarantine/macos_unquarantine#1` | `agents/plugins/quarantine/src/quarantine_plugin.cpp:macos_unquarantine` | `sudo_wrap({"/sbin/pfctl", "-f", "/etc/pf.conf"})` — restores macOS defaults, same restore path as before this PR | macOS | compile-time literal argv | mutating | none | `sudo -n` NOPASSWD | same anchor-rejection note as `macos_load_ruleset#1` — this restore path and `macos_load_ruleset`'s load path are the two halves of the SAME atomic-reload design | 2 — same as above | n/a |
| `quarantine/macos_unquarantine#2` | same | `sudo_wrap({"/sbin/pfctl", "-d"})` — fallback when `/etc/pf.conf` is unavailable | macOS | compile-time literal argv | mutating, fallback | none | `sudo -n` NOPASSWD | no rung-1 API for this data on this OS in this plugin | 2 — same as above | n/a |
| `quarantine/macos_is_quarantined#1` / `quarantine/macos_get_whitelist#1` | `agents/plugins/quarantine/src/quarantine_plugin.cpp:macos_is_quarantined`/`macos_get_whitelist` | `sudo_wrap({"/sbin/pfctl", "-s", "rules"})` | macOS | compile-time literal argv | read-only | none | `sudo -n` NOPASSWD | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess, sudo-governed; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `quarantine/macos_is_quarantined#2` | `agents/plugins/quarantine/src/quarantine_plugin.cpp:macos_is_quarantined` | `sudo_wrap({"/sbin/pfctl", "-s", "info"})` — live pf-enabled verification (#3283), NEW this PR: a blocking ruleset alone no longer reads `active` on its own | macOS | compile-time literal argv | read-only | none | `sudo -n` NOPASSWD | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess, sudo-governed; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `network_actions/flush_dns_windows#1` | `agents/plugins/network_actions/src/network_actions_plugin.cpp:flush_dns` | runner argv (`ipconfig /flushdns`) | Windows | compile-time literal argv | mutating (DNS cache flush) | none | none | no rung-1 API for a DNS-resolver-cache flush on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `network_actions/flush_dns_linux#1` | `agents/plugins/network_actions/src/network_actions_plugin.cpp:flush_dns` | for each of resolvectl/systemd-resolve in order: `sudo_wrap({probe_tool_path({candidate}), flag})`, retried on ANY failure of a found candidate (absent, spawn error, or nonzero exit) — not presence-only; the pure retry decision is `yuzu::network_actions::decide_dns_flush` (network_actions_parsers.hpp) | Linux | compile-time literal argv; tool path resolved from two candidates | mutating | none — shell `\|\|`-fallback-and-`\|\| true` eliminated; the fallback's retry-ON-FAILURE semantics are preserved in-process (real exit code checked and forwarded, never swallowed) | `sudo -n` NOPASSWD (`scripts/install-agent-user.sh`) | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess, sudo-governed; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `network_actions/flush_dns_macos#1` | `agents/plugins/network_actions/src/network_actions_plugin.cpp:flush_dns` | `sudo_wrap({"/usr/bin/dscacheutil","-flushcache"})` | macOS | compile-time literal argv | mutating | none | `sudo -n` NOPASSWD grant, exact match confirmed against `install-agent-user.sh:463` | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess, sudo-governed; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `network_actions/flush_dns_macos#2` | `agents/plugins/network_actions/src/network_actions_plugin.cpp:flush_dns` | `sudo_wrap({"/usr/bin/killall","-HUP","mDNSResponder"})` | macOS | compile-time literal argv | mutating | none | `sudo -n` NOPASSWD grant, exact match confirmed against `install-agent-user.sh:464` | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess, sudo-governed; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `network_actions/ping#1` | `agents/plugins/network_actions/src/network_actions_plugin.cpp:ping` | runner argv (system `ping`/`PING.EXE`) | Linux/macOS/Windows | operator-supplied host, validated by shared `is_safe_host_arg`; count digit-validated | read-only | none | none (system ping tool is setuid/cap-blessed, not the shared `IcmpSession`) | this action's documented contract is "run the system ping tool" — not a data-acquisition question a native API answers the same way | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 not applicable, the action IS "invoke system ping" | n/a |
| `services/enumerate_services_linux#1` | `agents/plugins/services/src/services_plugin.cpp:enumerate_services_linux` | runner argv (`systemctl list-units --type=service ...`) | Linux | compile-time literal argv | read-only | none — shell string `{"/bin/sh","-c",cmd}` hop eliminated (direct argv) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `services/enumerate_services_macos#1` | `agents/plugins/services/src/services_plugin.cpp:enumerate_services_macos` | runner argv (`launchctl list`) | macOS | compile-time literal argv | read-only | none | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `services/enumerate_services_macos#2` | `agents/plugins/services/src/services_plugin.cpp:enumerate_services_macos` | runner argv (`launchctl print-disabled system`) | macOS | compile-time literal argv | read-only | none | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `services/set_start_mode_linux#1` | `agents/plugins/services/src/services_plugin.cpp:do_set_start_mode_linux` | `sudo_wrap({systemctl_path, "enable"\|"disable"\|"mask"\|"unmask", "--", name})` | Linux | operator-supplied unit name, gated by `is_safe_service_name` | **mutating** | none | `sudo -n` NOPASSWD, argv form verified against the installed grant (a wildcard match, not byte-for-byte) (`install-agent-user.sh`, labelled `# services.set_start_mode`) — **fixes a live grant/code mismatch**: these four call sites previously ran completely unwrapped (no sudo at all) despite the installer granting exactly this form, so the action silently failed for every privilege-dropped agent | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess, sudo-governed; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `services/set_start_mode_macos#1` | `agents/plugins/services/src/services_plugin.cpp:do_set_start_mode_macos` | `sudo_wrap({"/bin/launchctl", "enable"\|"disable", "system/"+name})` | macOS | operator-supplied unit name, gated by `is_safe_service_name` | **mutating** | none | `sudo -n` NOPASSWD, argv form verified against the installed grant (a wildcard match, not byte-for-byte) (`install-agent-user.sh`, labelled `# services.set_start_mode`) — same grant/code mismatch fix as the Linux site above | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess, sudo-governed; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `interaction/input_windows#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_input` | runner argv, single element = trusted VB `InputBox` script string, `powershell.exe -NoProfile -NonInteractive -Command <script>`, `powershell.exe` resolved via `probe_tool_path` at its fixed system path | Windows | operator-supplied title/prompt/default, sanitized (`sanitize()`) before embedding in the script | read-only (reads user input, no host state change) | none at the outer invocation (clean argv, no `cmd.exe`/`/bin/sh`); PowerShell itself interprets its own `-Command` script | none (runs in the interactive agent/user session — needs a desktop) | Reviewed — the mechanism change removes the `_popen` shell mechanism, but PowerShell remains the deepest interpreter intentionally invoked (ADR-3002 Decision 5), so migrating the outer spawn does not change the rung | Rung 3 — Decision-5 governed-interpreter site; the raw `_popen` mechanism is gone, the interpreter registration is not | Decision-5 interpreter registration (unchanged from the prior `kActionDescriptors` entry — `powershell_inputbox`) |
| `interaction/survey_windows#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_survey` | runner argv, single element = trusted WinForms dialog script string, same `powershell.exe` invocation shape as `input_windows#1` | Windows | operator-supplied title/prompts/choices, sanitized (`sanitize()`) | read-only | same as above | none | same as above | Rung 3 — same as above | Decision-5 interpreter registration (unchanged — `powershell_winforms`) |
| `interaction/posix_osascript_notify#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_notify` (macOS) | runner argv (`osascript -e <fragment>`), clean argv resolved via `probe_tool_path` | macOS | operator-supplied title/message, sanitized (`sanitize()`) before embedding in the one `-e` fragment | read-only (posts a notification, no host state change) | none — no shell involved; `osascript`'s own multi-`-e` argv form carries a multi-statement script without a shell | none beyond the LaunchDaemon's existing context; no reachable Aqua/GUI session under a headless/root LaunchDaemon is reported honestly (`unavailable`), never a false success | Reviewed — AppleScript has no non-argv invocation form that avoids the `osascript` interpreter, but the OUTER spawn needs no shell: `-e` fragments are ordinary argv elements | Rung 3 — Decision-5 governed-interpreter site (ADR-3002 Decision 5: osascript is the deepest interpreter intentionally invoked and sets the rung; the outer process spawn is argv-clean, same principle as PowerShell's `-Command`) | Decision-5 interpreter registration |
| `interaction/posix_osascript_message_box#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_message_box` (macOS) | runner argv (`osascript -e <fragment> -e <fragment> ...`, `build_dialog_argv()`), clean argv | macOS | operator-supplied title/message, sanitized; `btn_spec` and every sentinel/`-e` fragment are fixed compile-time literals | read-only (displays a dialog and reads the button clicked, no host state change) | none — no shell involved | same as above | same as above | Rung 3 — Decision-5 governed-interpreter site, same as above | Decision-5 interpreter registration |
| `interaction/posix_osascript_input#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_input` (macOS) | runner argv (`osascript -e <fragment> ...`), clean argv | macOS | operator-supplied title/prompt/default, sanitized | read-only (reads user input, no host state change) | none — no shell involved | same as above | same as above | Rung 3 — Decision-5 governed-interpreter site, same as above | Decision-5 interpreter registration |
| `interaction/posix_osascript_survey_yesno#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_survey` (macOS, yesno leg) | runner argv (`osascript -e <fragment> ...`), clean argv | macOS | operator-supplied title/prompt, sanitized | read-only | none — no shell involved | same as above | same as above | Rung 3 — Decision-5 governed-interpreter site, same as above | Decision-5 interpreter registration |
| `interaction/posix_osascript_survey_choice#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_survey` (macOS, choice leg) | runner argv (`osascript -e <fragment> ...`), clean argv | macOS | operator-supplied title/prompt/choices, sanitized | read-only | none — no shell involved | same as above | same as above | Rung 3 — Decision-5 governed-interpreter site, same as above | Decision-5 interpreter registration |
| `interaction/posix_osascript_survey_text#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_survey` (macOS, text leg) | runner argv (`osascript -e <fragment> ...`), clean argv | macOS | operator-supplied title/prompt, sanitized | read-only | none — no shell involved | same as above | same as above | Rung 3 — Decision-5 governed-interpreter site, same as above | Decision-5 interpreter registration |
| `interaction/posix_zenity_message_box#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_message_box` (Linux) | runner argv (`zenity --question/--info --title <t> --text <m> ...`), clean argv resolved via `probe_tool_path` | Linux | operator-supplied title/message, sanitized, each its own argv element | read-only | none — no shell involved | none beyond the LaunchDaemon's existing context | Reviewed — no rung-1 API gives a cross-desktop dialog surface; `zenity` is a plain binary with no interpreter role, so rung 2 (clean argv) is the floor, not a Decision-7 shell exception | Rung 2 — clean multi-element argv through the bounded subprocess runner; no interpreter is invoked | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `interaction/posix_zenity_input#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_input` (Linux) | runner argv (`zenity --entry --title <t> --text <p> --entry-text <d>`), clean argv | Linux | operator-supplied title/prompt/default, sanitized, each its own argv element | read-only | none — no shell involved | same as above | same as above | Rung 2 — clean multi-element argv, same as above | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `interaction/posix_zenity_survey_yesno#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_survey` (Linux, yesno leg) | runner argv (`zenity --question --title <t> --text <p>`), clean argv | Linux | operator-supplied title/prompt, sanitized | read-only | none — no shell involved | same as above | same as above | Rung 2 — clean multi-element argv, same as above | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `interaction/posix_zenity_survey_choice#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_survey` (Linux, choice leg) | runner argv (`zenity --list --title <t> --text <p> --column=Option -- <choice1> <choice2> ...`), clean argv | Linux | operator-supplied title/prompt/choices, sanitized, each choice its own trailing positional argv element (no longer a space-joined, individually-quoted shell string); a `--` separator precedes the choices (ADR-3002 Decision 6 argv hygiene — sanitize() permits a leading `-`, so an unguarded positional choice could be parsed as a zenity option instead of data) | read-only | none — no shell involved | same as above | same as above | Rung 2 — clean multi-element argv, same as above | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `interaction/posix_zenity_survey_text#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_survey` (Linux, text leg) | runner argv (`zenity --entry --title <t> --text <p>`), clean argv | Linux | operator-supplied title/prompt, sanitized | read-only | none — no shell involved | same as above | same as above | Rung 2 — clean multi-element argv, same as above | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `interaction/posix_notify_send#1` | `agents/plugins/interaction/src/interaction_plugin.cpp:platform_notify` (Linux) | runner argv (`notify-send -u <urgency> -- <title> <message>`), clean argv resolved via `probe_tool_path` | Linux | operator-supplied title/message, sanitized, each its own argv element; `type` maps to a fixed literal `-u` urgency value; a `--` separator precedes the positional title/message (ADR-3002 Decision 6 argv hygiene — sanitize() permits a leading `-`, so an unguarded title could be parsed as a notify-send option, e.g. `-i` as `--icon`) | read-only (posts a desktop notification, no host state change) | none — no shell involved | none beyond the LaunchDaemon's existing context | Reviewed — no rung-1 API gives a cross-desktop notification surface; `notify-send` is a plain binary with no interpreter role, so rung 2 is the floor | Rung 2 — clean multi-element argv through the bounded subprocess runner; no interpreter is invoked | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `firewall/do_state_macos#1` | `agents/plugins/firewall/src/firewall_plugin.cpp:do_state_macos` | runner argv (`/usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate`) | macOS | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false default) | none | no rung-1 API for Application Firewall global state in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `firewall/do_state_macos#2` | `agents/plugins/firewall/src/firewall_plugin.cpp:do_state_macos` | runner argv (`/sbin/pfctl -s info`) | macOS | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false default) | none | no rung-1 API for pf packet-filter status in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `firewall/do_rules_macos#1` | `agents/plugins/firewall/src/firewall_plugin.cpp:do_rules_macos` | runner argv (`/sbin/pfctl -s rules`) | macOS | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false default) | none | no rung-1 API for pf rule listing in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `firewall/try_ufw_state#1` | `agents/plugins/firewall/src/firewall_plugin.cpp:try_ufw_state` | runner argv (`/usr/sbin/ufw status`) | Linux | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false default) | none | firewalld's D-Bus API answers this only when firewalld is the active backend; ufw has no D-Bus/library surface | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for ufw state | n/a |
| `firewall/try_ufw_rules#1` | `agents/plugins/firewall/src/firewall_plugin.cpp:try_ufw_rules` | runner argv (`/usr/sbin/ufw status numbered`) | Linux | compile-time literal argv | read-only | none | none | same as `try_ufw_state#1` | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for ufw rules | n/a |
| `firewall/try_iptables_state#1` | `agents/plugins/firewall/src/firewall_plugin.cpp:try_iptables_state` | runner argv (`/usr/sbin/iptables -S`) | Linux | compile-time literal argv | read-only | none | none | last fallback backend — no rung-1 API and no higher-level backend (firewalld/ufw) reachable at this point in the probe | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for raw iptables state | n/a |
| `firewall/try_iptables_rules#1` | `agents/plugins/firewall/src/firewall_plugin.cpp:try_iptables_rules` | runner argv (`/usr/sbin/iptables -S`) | Linux | compile-time literal argv | read-only | none | none | same as `try_iptables_state#1` | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for raw iptables rules | n/a |
| `windows_updates/do_installed#1` | `agents/plugins/windows_updates/src/windows_updates_plugin.cpp:do_installed` | runner argv (`/usr/bin/rpm -qa --last`) | Linux | compile-time literal argv | read-only | none — pipeline eliminated (`head -50` -> `max_lines=50, stop_after_max_lines=true`) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `windows_updates/do_installed#2` | `agents/plugins/windows_updates/src/windows_updates_plugin.cpp:do_installed` | runner argv (`/usr/bin/apt list --installed`), fallback when `#1` (rpm) produces no output | Linux | compile-time literal argv | read-only | none — redirection eliminated (`2>/dev/null` -> `merge_stderr=false`); pipeline eliminated (`head -50` -> `max_lines=50, stop_after_max_lines=true`) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `windows_updates/do_installed#3` | `agents/plugins/windows_updates/src/windows_updates_plugin.cpp:do_installed` | runner argv (`/usr/sbin/system_profiler SPInstallHistoryDataType`) | macOS | compile-time literal argv | read-only | none — the `grep -E '^ {4}\w\|Install Date:' \| head -100` pipeline stage moved in-process into `parse_install_history_macos` (windows_updates_parsers.hpp); redirection eliminated (`2>/dev/null` -> `merge_stderr=false`) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `windows_updates/do_installed#4` | `agents/plugins/windows_updates/src/windows_updates_plugin.cpp:do_installed` | in-process bounded WMI query (`yuzu::shared::wmi::run_bounded_wmi_query`, `SELECT HotFixID, Description, InstalledOn FROM Win32_QuickFixEngineering`) — no child process spawned | Windows | compile-time literal WQL | read-only | n/a — no shell/interpreter of any kind involved (replaces the prior `powershell -Command Get-HotFix` interpreter shell-out) | none beyond the agent's existing service context | Reviewed — promoted directly to rung 1: WMI has no CLI-argv shape to pass through on the way, so there is no intermediate rung-2 form here, only the interpreter (rung 3) this replaces | 1 — daemon-mediated bounded broker call through the WMI service (never `WBEM_INFINITE`), same status as the registry/wevtapi sites elsewhere in this Wave (see runner_status.hpp's EvtNext comment for the precedent) | n/a — not a spawn site as of this migration |
| `windows_updates/do_missing#1` | `agents/plugins/windows_updates/src/windows_updates_plugin.cpp:do_missing` | runner argv (`/usr/bin/apt list --upgradable`) | Linux | compile-time literal argv | read-only | none — redirection eliminated (`2>/dev/null` -> `merge_stderr=false`) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `windows_updates/do_missing#2` | `agents/plugins/windows_updates/src/windows_updates_plugin.cpp:do_missing` | runner argv (`/usr/bin/yum check-update`), fallback when `#1` (apt) produces no matching rows | Linux | compile-time literal argv | read-only | none — redirection+filter eliminated (`2>/dev/null \| grep -v '^$'` -> blank lines already dropped by the runner; `Loaded`/`Loading` banner filtering moved into `parse_yum_checkupdate`) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `windows_updates/do_missing#3` | `agents/plugins/windows_updates/src/windows_updates_plugin.cpp:do_missing` | runner argv (`/usr/sbin/softwareupdate -l`) | macOS | compile-time literal argv | read-only | none — redirection eliminated (`2>/dev/null` -> `merge_stderr=false`) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `windows_updates/do_missing#4` | `agents/plugins/windows_updates/src/windows_updates_plugin.cpp:do_missing` | in-process WUA COM async search (`IUpdateSearcher::BeginSearch`/`ISearchJob::IsCompleted` poll/`EndSearch`, `IsInstalled=0`) — no child process spawned | Windows | compile-time literal search criteria | read-only | n/a — no shell/interpreter of any kind involved (replaces the prior `powershell -Command New-Object -ComObject Microsoft.Update.Session` interpreter shell-out); deliberately NOT the synchronous `Search()` call, which is an unbounded broker call ADR-3002 forbids | none beyond the agent's existing service context | Reviewed — promoted directly to rung 1, same reasoning as `do_installed#4` | 1 — daemon-mediated bounded broker call (WUA service), polled under an explicit 120s deadline; on timeout, `RequestAbort()` only — no `CleanUp()` call and no detached cleanup thread (removed: could still execute plugin code after the agent `dlclose()`s this module). `BeginSearch`'s callback argument is `yuzu::agent::wua_noop_search_completed_callback()`, a process-lifetime singleton living in agent-core (`agents/core/src/wua_noop_callback.cpp`), never a plugin-local object — WUA can hold a reference to it past this call's own timeout, and only a callback whose vtable is never unmapped survives that. Not a spawn site at all as of this migration | n/a — not a spawn site as of this migration |
| `windows_updates/do_pending_reboot#1` | `agents/plugins/windows_updates/src/windows_updates_plugin.cpp:do_pending_reboot` | runner argv (`/usr/sbin/softwareupdate -l`) | macOS | compile-time literal argv | read-only | none — redirection eliminated (`2>/dev/null` -> `merge_stderr=false`) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess with an explicit 60s deadline, replacing the prior unbounded `popen()` call (the file's own TODO — could hang indefinitely on a headless/offline Mac); rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `windows_updates/do_pending_reboot#2` | `agents/plugins/windows_updates/src/windows_updates_plugin.cpp:do_pending_reboot` | `popen()` helper (`run_command`, string `uname -r`) | Linux | compile-time literal command string | read-only | none | none | a direct `uname(2)` syscall answers this without a subprocess at all — a real rung-1 candidate, not migrated in this PR | 3 — grandfathered raw `popen`, unbounded, no timeout; reviewed exception recorded here per ADR-3002 Decision 1 rather than migrated, out of scope for this PR's installed/missing-focused migration; tracked #2380 | n/a |
| `windows_updates/do_pending_reboot#3` | `agents/plugins/windows_updates/src/windows_updates_plugin.cpp:do_pending_reboot` | `popen()` helper (`run_command`, string `ls -t /boot/vmlinuz-* 2>/dev/null \| head -1`) | Linux | compile-time literal command string | read-only | glob (`vmlinuz-*`), redirection (`2>/dev/null`), pipeline (`\| head -1`) | none | a `std::filesystem` directory scan answers this without a subprocess or a shell — a real rung-1 candidate, not migrated in this PR | 3 — grandfathered raw `popen`, unbounded, no timeout; reviewed exception recorded here per ADR-3002 Decision 1 rather than migrated, out of scope for this PR's installed/missing-focused migration; tracked #2380 | n/a |
| `windows_updates/do_pending_reboot#4` | `agents/plugins/windows_updates/src/windows_updates_plugin.cpp:do_pending_reboot` | `popen()` helper (`run_command`, string `needs-restarting -r 2>&1`) | Linux | compile-time literal command string | read-only | redirection (`2>&1`) | none | no rung-1 API for this data on this OS in this plugin | 3 — grandfathered raw `popen`, unbounded, no timeout; reviewed exception recorded here per ADR-3002 Decision 1 rather than migrated, out of scope for this PR's installed/missing-focused migration; tracked #2380 | n/a |
| `antivirus/list_av_products_linux#1` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:list_av_products_linux` (via the shared `pgrep_running` helper) | runner argv (`pgrep -x clamd`) | Linux | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for "is clamd running" in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `antivirus/list_av_products_linux#2` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:list_av_products_linux` (via `pgrep_running`) | runner argv (`pgrep -x falcon-sensor`) | Linux | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `antivirus/list_av_products_linux#3` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:list_av_products_linux` (via `pgrep_running`) | runner argv (`pgrep -f sophos`) | Linux | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `antivirus/list_status_linux#1` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:list_status_linux` (via `pgrep_running`) | runner argv (`pgrep -x clamd`) | Linux | compile-time literal argv | read-only | none | none | no rung-1 API for clamd liveness; the definitions-freshness half of this same action IS rung 1 (a plain `stat()` on `/var/lib/clamav/daily.cvd`, no subprocess) | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over for the liveness check specifically (no D-Bus/socket API this plugin uses); the mtime read next to it is genuinely rung 1 | n/a |
| `antivirus/list_status_linux#2` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:list_status_linux` (via `pgrep_running`) | runner argv (`pgrep -x falcon-sensor`) | Linux | compile-time literal argv | read-only | none | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `antivirus/list_status_linux#3` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:list_status_linux` (via `pgrep_running`) | runner argv (`pgrep -f sophos`) | Linux | compile-time literal argv | read-only | none | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `antivirus/list_av_products_macos#1` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:list_av_products_macos` (via the shared `read_plist_version` helper) | runner argv (`/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' <XProtect.bundle Info.plist>`) | macOS | fixed literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public API reads a bundle's CFBundleShortVersionString without linking CoreFoundation's plist parser directly into this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no in-tree CoreFoundation plist-parsing dependency for this plugin | n/a |
| `antivirus/list_av_products_macos#2` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:list_av_products_macos` | runner argv (`systemextensionsctl list`) | macOS | fixed literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public API enumerates the endpoint-security system-extension registry other than this CLI | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `antivirus/list_av_products_macos#3` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:list_av_products_macos` | runner argv (`pgrep -x falcond`) | macOS | fixed literal argv | read-only | none | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `antivirus/list_av_products_macos#4` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:list_av_products_macos` | runner argv (`pgrep -f sophos`) | macOS | fixed literal argv | read-only | none | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `antivirus/xprotect_status_macos#1` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:xprotect_status_macos` (via `read_plist_version`) | runner argv (`/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' <XProtect.bundle Info.plist>`) | macOS | fixed literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public API reads a bundle's CFBundleShortVersionString without linking CoreFoundation's plist parser directly into this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no in-tree CoreFoundation plist-parsing dependency for this plugin | n/a |
| `antivirus/xprotect_status_macos#2` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:xprotect_status_macos` (via `read_plist_version`) | runner argv (`/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' <XProtect.app Info.plist>`) | macOS | fixed literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public API reads a bundle's CFBundleShortVersionString without linking CoreFoundation's plist parser directly into this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no in-tree CoreFoundation plist-parsing dependency for this plugin | n/a |
| `antivirus/xprotect_status_macos#3` | `agents/plugins/antivirus/src/antivirus_plugin.cpp:xprotect_status_macos` (via `read_plist_version`) | runner argv (`/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' <MRT.app Info.plist>`) | macOS | fixed literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public API reads a bundle's CFBundleShortVersionString without linking CoreFoundation's plist parser directly into this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no in-tree CoreFoundation plist-parsing dependency for this plugin | n/a |
| `bitlocker/report_filevault_status#1` | `agents/plugins/bitlocker/src/bitlocker_plugin.cpp:report_filevault_status` | runner argv (`/usr/bin/fdesetup status`) | macOS | none — fixed literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none beyond the LaunchDaemon's existing root context | No public rung-1 API reports FileVault's own on/off state (the SecItem/keychain APIs certificates.cpp uses answer a different question); `fdesetup` is Apple's own tool for this | Rung 2 — clean argv through the bounded subprocess runner (was rung 3, `/bin/sh -c "fdesetup status 2>/dev/null"`, before this migration) | n/a |
| `bitlocker/report_filevault_status#2` | `agents/plugins/bitlocker/src/bitlocker_plugin.cpp:report_filevault_status` | runner argv (`/usr/bin/diskutil apfs list`) | macOS | none — fixed literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none beyond the LaunchDaemon's existing root context | No public rung-1 API for per-APFS-volume encryption state; `diskutil` is Apple's own tool for this | Rung 2 — clean argv through the bounded subprocess runner (was rung 3, `/bin/sh -c "diskutil apfs list 2>/dev/null"`, before this migration) | n/a |
| `event_logs/do_errors#1` | `agents/plugins/event_logs/src/event_logs_plugin.cpp:do_errors` | runner argv (`/usr/bin/log show --predicate 'messageType == error' --last <N>h --style compact`) | macOS | compile-time literal argv; `<N>` is a clamped integer | read-only | none | none | no public rung-1 API for out-of-process unified-log reads (OSLog reader APIs are Swift/private; roadmap declares the M leg a rung-2 stuck leaf) | 2 — direct argv via yuzu::agent::run_bounded_subprocess (10s deadline, max_lines cap, stop_after_max_lines) | n/a |
| `event_logs/do_query#1` | `agents/plugins/event_logs/src/event_logs_plugin.cpp:do_query` | runner argv (`/usr/bin/log show --predicate 'eventMessage contains "<filter>"' --last 24h --style compact`) | macOS | `<filter>` is allowlist-sanitized (alnum/space/./-/_//) and passed as ONE argv element — never shell text | read-only | none | none | same as `event_logs/do_errors#1` | 2 — direct argv via yuzu::agent::run_bounded_subprocess (10s deadline, max_lines cap, stop_after_max_lines) | n/a |
| `event_logs/run_journalctl_fallback#1` | `agents/plugins/event_logs/src/event_logs_plugin.cpp:run_journalctl_fallback` | runner argv (`/usr/bin/journalctl -q ...` — errors and query argv variants built by the two callers) | Linux | numeric params clamped; NO operator-supplied value reaches argv at all — the keyword filter is applied in-process after capture (the former `--grep=<filter>` element was removed), so every argv element is a compile-time literal or a clamped integer | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | declared FALLBACK below the rung-1 sd_journal leg (descriptor `fallback` field): runs only when libsystemd is compiled out (-Dsystemd_guard) or the journal is unreachable at runtime | Rung 2 — clean argv through the bounded subprocess runner (was rung 3, `/bin/sh -c "journalctl ... 2>/dev/null"`, before this migration) | n/a |
| `installed_apps/get_installed_apps_linux#1` | `agents/plugins/installed_apps/src/installed_apps_plugin.cpp:get_installed_apps_linux` | runner argv (`dpkg-query -W -f='${Package}\|${Version}\|${Maintainer}\|${Status}\n'`) | Linux | compile-time literal argv (probe_tool_path-resolved tool path) | read-only | none — popen/`command -v` eliminated (probe_tool_path replaces command_exists; the format string's own literal `\n` is dpkg-query's format-interpreter escape, unchanged from the legacy shell-out) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `installed_apps/get_installed_apps_linux#2` | `agents/plugins/installed_apps/src/installed_apps_plugin.cpp:get_installed_apps_linux` | runner argv (`rpm -qa --queryformat '%{NAME}\|%{VERSION}-%{RELEASE}\|%{VENDOR}\|%{INSTALLTIME:date}\n'`) | Linux | compile-time literal argv | read-only | none — popen eliminated | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `installed_apps/get_installed_apps_linux#3` | `agents/plugins/installed_apps/src/installed_apps_plugin.cpp:get_installed_apps_linux` | runner argv (`pacman -Q`) | Linux | compile-time literal argv | read-only | none — popen eliminated | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `installed_apps/get_installed_apps_macos#1` | `agents/plugins/installed_apps/src/installed_apps_plugin.cpp:get_installed_apps_macos` | runner argv (`system_profiler SPApplicationsDataType -detailLevel mini`) + native parser (`installed_apps_parsers.hpp::parse_system_profiler_apps`) | macOS | compile-time literal argv | read-only | none — the `\| grep -E '^ {4}\w\|Version:\|Last Modified:'` pipeline stage moved in-process into the pure parser, replicating its exact line selection | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `installed_apps/get_inventory_linux#1` | `agents/plugins/installed_apps/src/installed_apps_plugin.cpp:get_inventory_linux` | runner argv (`dpkg-query -W -f='${Package}<TAB>${db:Status-Abbrev}<TAB>${Version}<TAB>${Architecture}<TAB>${Maintainer}<LF>'`) | Linux | compile-time literal argv | read-only | none — popen eliminated; format string carries real TAB/LF bytes (unchanged from the legacy shell-out) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `installed_apps/get_inventory_linux#2` | `agents/plugins/installed_apps/src/installed_apps_plugin.cpp:get_inventory_linux` | runner argv (`rpm -qa --queryformat '%{NAME}<TAB>%{EPOCH}<TAB>%{VERSION}<TAB>%{RELEASE}<TAB>%{ARCH}<TAB>%{PACKAGER}<TAB>%{INSTALLTIME:date}<TAB>%{SIGPGP}<TAB>%{RSAHEADER}<LF>'`) | Linux | compile-time literal argv | read-only | none — popen eliminated | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `installed_apps/get_inventory_linux#3` | `agents/plugins/installed_apps/src/installed_apps_plugin.cpp:get_inventory_linux` | runner argv (`pacman -Q`) | Linux | compile-time literal argv | read-only | none — popen eliminated | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `installed_apps/get_inventory_linux#4` | `agents/plugins/installed_apps/src/installed_apps_plugin.cpp:get_inventory_linux` | runner argv (`apk info -v`) | Linux | compile-time literal argv | read-only | none — popen eliminated | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `installed_apps/get_inventory_macos#1` | `agents/plugins/installed_apps/src/installed_apps_plugin.cpp:get_inventory_macos` | runner argv (`system_profiler SPApplicationsDataType -detailLevel mini`) + native enrichment (`installed_apps_macos_enrich.hpp`: `CFBundleCreate`/`SecStaticCodeCreateWithPath`/`SecCodeCopySigningInformation`, no subprocess) | macOS | compile-time literal argv; the per-app enrichment path is a fixed system-API call sequence, no operator input | read-only | none | none | no rung-1 API for the app enumeration itself; the #2273 enrichment IS the rung-1 API for signer/integrity (SecStaticCode), used directly rather than shelling out to `codesign` | 2 for the acquisition call (direct argv via yuzu::agent::run_bounded_subprocess; no rung-1 API for this data on this OS in this plugin); the enrichment itself is rung 1 (native Security.framework/CoreFoundation, zero subprocesses) | n/a |
| `installed_apps/get_inventory_macos#2` | `agents/plugins/installed_apps/src/installed_apps_plugin.cpp:get_inventory_macos` | runner argv (`pkgutil --pkgs`) | macOS | compile-time literal argv | read-only | none | none | no rung-1 API enumerates pkgutil receipts | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `installed_apps/get_inventory_macos#3` | `agents/plugins/installed_apps/src/installed_apps_plugin.cpp:get_inventory_macos` | runner argv (`pkgutil --pkg-info <id>`), one call per receipt, bounded at `kMaxPkgutilPackages` (5000) | macOS | package identifier is `#2`'s own prior output (local-system enumeration, not operator input) | read-only | none | none | no rung-1 API reads a single pkgutil receipt's version/install-time | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `installed_apps/do_list_per_user#1` | `agents/plugins/installed_apps/src/installed_apps_plugin.cpp:do_list_per_user` | runner argv (`brew list --versions`) | macOS | compile-time literal argv (probe_tool_path-resolved brew path) | read-only | none — popen/`command -v` eliminated | none | no rung-1 API for Homebrew's per-user formula inventory | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `msi_packages/do_list#1` | `agents/plugins/msi_packages/src/msi_packages_plugin.cpp:do_list` | runner argv (`pkgutil --pkgs`) | macOS | compile-time literal argv (probe_tool_path-resolved tool path) | read-only | none — the `/bin/sh -c` hop and its `shell_quote()` are gone entirely (direct argv, no shell to quote against) | none | no rung-1 API enumerates pkgutil receipts | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `msi_packages/do_list#2` | `agents/plugins/msi_packages/src/msi_packages_plugin.cpp:do_list` | runner argv (`pkgutil --pkg-info <id>`), one call per receipt, bounded at `kMaxPackages` (500) | macOS | package identifier is `#1`'s own prior output (local-system enumeration, not operator input); passed as its own argv element, never shell-interpolated | read-only | none — `shell_quote()` deleted; no shell in the call path to quote against | none | no rung-1 API reads a single pkgutil receipt | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `msi_packages/do_product_codes#1` | `agents/plugins/msi_packages/src/msi_packages_plugin.cpp:do_product_codes` | runner argv (`pkgutil --pkgs`) | macOS | compile-time literal argv (probe_tool_path-resolved tool path) | read-only | none — `/bin/sh -c` hop gone | none | no rung-1 API enumerates pkgutil receipts | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `network_config/do_dns_cache#1` | `agents/plugins/network_config/src/network_config_plugin.cpp:do_dns_cache` | runner argv (`resolvectl cache`), path resolved via `yuzu::agent::probe_tool_path` (no PATH search) | Linux | none — fixed literal argv | read-only | none — `/bin/sh -c` eliminated entirely | none | Reviewed — no public libsystemd/sd-bus API exposes resolved's cache contents; `resolvectl cache` is the only source | Rung 2 — direct argv via yuzu::agent::run_bounded_subprocess (was rung 3, `/bin/sh -c`, before this migration) | n/a |
| `network_config/do_dns_cache#2` | `agents/plugins/network_config/src/network_config_plugin.cpp:do_dns_cache` | runner argv (`systemd-resolve --statistics`), path resolved via `yuzu::agent::probe_tool_path` | Linux | none — fixed literal argv | read-only | none — `/bin/sh -c` eliminated entirely | none | Reviewed — fallback for hosts where `resolvectl` is absent | Rung 2 — direct argv via yuzu::agent::run_bounded_subprocess (was rung 3) | n/a |
| `script_exec/do_exec#1` | `agents/plugins/script_exec/src/script_exec_plugin.cpp:do_exec` | runner argv, argv[0] resolved via the pure `resolve_executable` (script_exec_parsers.hpp) against the PARENT process's own PATH (forwarded through `SubprocessOptions::extra_env`), not the runner's fixed four-directory default | Linux/macOS/Windows | operator-supplied `command`/`args` params — this action's entire contract is "execute an arbitrary admin-specified program"; the server enforces admin-only role checks before ever dispatching to this plugin | **mutating** | none — pre-split argv, no shell in between; the deleted `execvpe`/`execvp`/Win32 spawn paths never involved a shell either | none beyond the agent's existing service-account context; gated admin-only at the server, not by a sudoers grant | Reviewed — this action's contract IS "run an arbitrary caller-named binary"; no rung-1 API can answer that generically | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 not applicable, the action IS "invoke an operator-named program", same principle as `network_actions/ping#1` | n/a |
| `script_exec/do_bash#1` | `agents/plugins/script_exec/src/script_exec_plugin.cpp:do_bash` | runner argv (`/bin/bash -c <script>`), script as ONE argv element | Linux/macOS | operator-supplied `script` param (admin-only action, server-gated) | **mutating** | bash itself interprets the script text (pipeline/redirection/glob/etc. all live inside bash's own interpretation); the outer spawn carries it as one opaque argv element, no shell hop before bash | none beyond the agent's existing service-account context; admin-only action gate | Reviewed — bash is the interpreter this action exists to invoke; no rung-1 substitute for "run an operator-authored bash script" | Rung 3 — Decision-5 governed-interpreter site; bash is the deepest interpreter intentionally invoked, same principle as PowerShell's `-EncodedCommand` and osascript's `-e` | Decision-5 interpreter registration (`bash_c`, unchanged from the prior `kActionDescriptors` entry — now prefixed `subprocess_runner:`) |
| `script_exec/do_powershell#1` | `agents/plugins/script_exec/src/script_exec_plugin.cpp:do_powershell` | runner argv (`powershell.exe -NoProfile -NonInteractive -EncodedCommand <base64>`), `powershell.exe` at its fixed well-known System32 path (same location `interaction/input_windows#1` probes) | Windows | operator-supplied `script` param, Base64/UTF-16LE-encoded before assembly (admin-only action, server-gated) | **mutating** | PowerShell itself interprets the decoded script; the outer spawn is clean argv, no `cmd.exe`; `-EncodedCommand` exists specifically to remove all outer-shell escaping/injection surface | none beyond the agent's existing service-account context; admin-only action gate | Reviewed — PowerShell is the interpreter this action exists to invoke, same principle as `interaction/input_windows#1` | Rung 3 — Decision-5 governed-interpreter site, same as `interaction/input_windows#1` | Decision-5 interpreter registration (`powershell_encodedcommand`, unchanged from the prior `kActionDescriptors` entry — now prefixed `subprocess_runner:`) |
| `content_dist/do_execute#1` | `agents/plugins/content_dist/src/content_dist_plugin.cpp:do_execute` (via `content_dist_exec_seam.hpp:execute_verified_payload`) | runner argv, hash-verified staged payload as absolute argv[0]; Linux additionally enables B6 fd-exec verification | Linux/macOS/Windows | staged file re-verified against agent KV's recorded SHA-256 (#808) immediately before this call, plus operator-supplied split `args` (validated against a shell-metacharacter denylist, `is_safe_arg`) | **mutating** | none — pre-split argv, no shell in between; the deleted OS-specific spawn paths never involved a shell either | agent service account | Reviewed — this action's contract is execution of the staged, hash-verified payload; no rung-1 substitute exists for "run a previously staged installer" | Rung 2 — direct argv via `yuzu::agent::run_bounded_subprocess`; rung 1 not applicable, same principle as `script_exec/do_exec#1` | n/a |
| `tar/enumerate_services#1` | `agents/plugins/tar/src/tar_service_collector.cpp:enumerate_services` | runner argv (`systemctl list-units --type=service --all --plain --no-pager --no-legend`) | Linux | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false); no `/bin/sh -c` hop, `timeout 10` subsumed by the runner's own deadline | none | no rung-1 API for service enumeration in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess with a 10s deadline; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `tar/enumerate_services#2` | `agents/plugins/tar/src/tar_service_collector.cpp:enumerate_services` | runner argv (`launchctl list`) | macOS | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess with a 10s deadline; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `tar/enumerate_mapdrive#1` | `agents/plugins/tar/src/tar_mapdrive_collector.cpp:enumerate_mapdrive` | runner argv (`smbstatus -b`) | Linux | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false); `timeout 10` subsumed by the runner's own deadline | none | no rung-1 API for currently-connected inbound SMB sessions on Linux | 2 — direct argv via yuzu::agent::run_bounded_subprocess (10s deadline, 8 MiB output cap); rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `tar/enumerate_mapdrive_history#1` | `agents/plugins/tar/src/tar_mapdrive_collector.cpp:enumerate_mapdrive_history` | runner argv (`wevtutil qe Security /q:*[System[(EventID=4624)]] /c:5000 /f:text /rd:true`) | Windows | constant XPath filter, no interpolation; `wevtutil.exe` resolved via `system32_path` (absolute System32 path, not a PATH search) | read-only | none — the old `cmd /c`-level quoting around the XPath is gone (clean argv, no shell hop); `2>nul` subsumed by the runner's default stderr discard | none beyond the agent's existing Security-log read access | no rung-1 API for querying the Security event log's 4624 records with this filter in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess (15s deadline, 8 MiB output cap); rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `tar/enumerate_mapdrive_history#2` | `agents/plugins/tar/src/tar_mapdrive_collector.cpp:enumerate_mapdrive_history` | runner argv (`journalctl -u smbd --no-pager -o short-iso -n 5000`) | Linux | compile-time literal argv; fallback only when the on-disk `log.smbd` tail is empty | read-only | none | none | no rung-1 API for querying journald's smbd unit log with this filter in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess (15s deadline, 8 MiB output cap); rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |

## Seed inventory — known spawn surfaces awaiting transcription

Plugin-granularity seed from the ADR-3002 review inventory (dev @ 2026-07-23,
27 raw-spawning plugins of 49; three further plugins exec directly through
private helpers). Site-level transcription is #2380's first work item —
these rows are pointers, not evidence.

**Raw `popen`/`system` helpers:**
device_identity, hardware, installed_apps,
device_identity, event_logs, hardware,
ioc, license_scan,
network_diag, os_info, processes,
software_actions, vuln_scan, wifi, windows_updates.
(`vuln_scan` carries code slated for retirement per ADR-0028/ADR-0018 —
sequence that cleanup against migrating it, tracked in #2380.)

**Migrated off raw spawn (Wave 4, PR4.2):** `event_logs` (Windows
PowerShell `Get-WinEvent` via raw `_popen` promoted to rung-1 wevtapi
`EvtQuery`/`EvtRender` with a bounded `EvtNext` wait — zero Windows
subprocesses; Linux `journalctl` via `/bin/sh -c` promoted to a rung-1
bounded `sd_journal` read behind the shared `systemd_guard` feature, with
the declared rung-2 `journalctl` pre-split-argv fallback registered above
as `event_logs/run_journalctl_fallback#1`; the macOS `log show` argv sites
registered as `event_logs/do_errors#1` / `event_logs/do_query#1`. The
grandfather-list entry is removed and will not return).
**Migrated off raw spawn (Wave 4, PR4.3a):** `installed_apps` (Linux
dpkg-query/rpm/pacman/apk and macOS system_profiler/brew/pkgutil all moved
from popen()/command_exists() to the shared bounded argv runner + probe_tool_path,
rung 2 — see Registered sites above; the macOS `system_profiler | grep`
pipeline moved in-process into `installed_apps_parsers.hpp`'s
`parse_system_profiler_apps`, replicating the old grep's exact line
selection so `list`/`query`'s wire format is unchanged. New #2273 macOS
`list_inventory` enrichment: `pkgutil --pkgs`/`--pkg-info` receipts as
additional inventory rows (rung 2), plus a genuinely rung-1 per-app
enrichment — `CFBundleCreate`+`SecStaticCodeCreateWithPath`+
`SecCodeCopySigningInformation`, `installed_apps_macos_enrich.hpp`, zero
subprocesses — filling the previously-honest-empty publisher/
signature_status v2 fields). `msi_packages` gained its first manifest rows
in this same PR: its macOS `pkgutil --pkgs`/`--pkg-info` calls already rode
the bounded runner (K-7/CDX-07) but through a `/bin/sh -c` hop with a
`shell_quote()` helper; both are now direct rung-2 argv, `shell_quote()`
deleted entirely — see Registered sites above. Both plugins' descriptors
move from rung 3 to rung 2 on every migrated leg; Windows legs (native
registry / MSI API, rung 1) are untouched by this PR.

**Migrated off raw spawn (Wave 2, PR2.1a):** `users` (Windows session
history moved to wevtapi/EvtQuery, ProfileList to native enumeration; POSIX
sites moved to direct runner argv — see Registered sites above).

**Migrated off raw spawn (Wave 3, PR3.3-c1):** `firewall` (Windows netsh
promoted to rung-1 `INetFwPolicy2` COM — zero subprocesses; macOS ALF/pfctl
moved from `popen()` to rung-2 `run_bounded_subprocess` argv, same 3 sites —
see Registered sites above; Linux firewalld promoted to rung-1 bounded
sd-bus, ufw/iptables promoted to rung-2 `run_bounded_subprocess` argv with
new structured per-backend parsers — `parse_ufw_status`/`parse_ufw_rules`/
`parse_iptables_save` — replacing the single opaque `firewall-cmd --list-all
|| ufw status numbered || iptables -L -n --line-numbers` shell chain and its
`rule|<raw line>` passthrough. Linux nftables is intentionally left
unimplemented as a clean seam for the sequential follow-up PR
`feat/wave3-pr33c2-firewall-nftables`, which adds only rung-1 netlink code
with no raw spawn — will not re-add a grandfather-list entry).

**Migrated off raw spawn (Wave 2, PR2.1b):** `certificates` (Linux/macOS
System+SystemRoot promoted to rung-1 libcrypto/SecItem; the login-keychain
hop stayed a registered Decision-7 exception until #3406 argv-ized it to
rung 2 — see Registered sites above).

**Migrated off raw spawn (Wave 2, PR2.2b):** `quarantine` (43 sites: 15
Windows netsh, 22 Linux iptables, 6 macOS pfctl — see Registered sites
above; the macOS pfctl reload strategy is deliberately UNCHANGED by this
migration, see `macos_load_ruleset#1`'s row for the connectivity-loss
incident that strategy already fixed).

**Migrated off raw spawn (Wave 2, PR2.2a):** `network_actions` (Windows
`ipconfig`, Linux `resolvectl`/`systemd-resolve` + sudo, macOS `dscacheutil`/
`killall` + sudo, `ping` — see Registered sites above); `wol` (`check` moved
to the shared `IcmpSession` + TCP-connect fallback, rung 1; 0 spawn sites,
was 1 — no manifest rows since none survive); `services` (shell-string hop
eliminated for all five surviving argv sites, and a live sudo grant/code
mismatch fixed on the two mutating sites — see Registered sites above).

**Migrated off raw spawn (Wave 2, PR2.2c):** `interaction` (the two
remaining Windows PowerShell `_popen` sites move to the bounded argv
runner, staying rung 3 per Decision 5 — the interpreter, not the spawn
mechanism, sets the rung. macOS osascript and Linux zenity/notify-send were
already on the runner but via a `/bin/sh -c <script>` shell string; this PR
converts every one of them to clean argv — osascript's own multi-`-e` form
carries each AppleScript fragment as its own argv element (stays rung 3,
Decision 5), while zenity/notify-send have no interpreter role at all and
reclassify from a Decision-7 shell exception to an ordinary rung-2 argv
candidate. The Linux zenity nested-shell `__RC=$?` exit-code protocol is
also removed — the runner already returns a real exit code. All 14
interaction sites, Windows and POSIX, are transcribed under Registered
sites above).

**Migrated off raw spawn (Wave 2, PR2.1c):** `discovery` (0 spawn sites, was
5 — GetIpNetTable2/`/proc/net/arp`/sysctl ARP + a shared `IcmpSession`
sweep; no manifest rows since none survive).

**Migrated off raw spawn (Wave 3, PR3.2):** `hardware` (macOS
manufacturer/model/processors/system promoted to rung-1 sysctlbyname/IOKit;
Linux disks promoted to a rung-1 native `/sys/block` walk; macOS
bios/disks and Linux memory kept at rung 2 via the bounded runner — see
Registered sites above) and `device_identity` (Linux domain's AD-join check
promoted to a rung-1 bounded sd-bus call to sssd's InfoPipe; macOS
domain's hostname-suffix fallback promoted to rung-1 gethostname+getaddrinfo;
Linux ou and macOS domain/ou kept at rung 2 via the bounded runner — see
Registered sites above). Both plugins now carry zero raw spawn tokens and
are removed from `scripts/ci/check-plugin-spawn-lexical.sh`'s GRANDFATHERED
allowlist.

**Partially migrated (Wave 3, PR33d):** `windows_updates` -- `installed`/`missing`
fully migrated on every OS (Linux/macOS onto direct runner argv, Windows onto
in-process bounded WMI/WUA COM calls with zero spawn sites; see Registered
sites above) plus `pending_reboot`'s macOS leg (the file's own TODO —
unbounded `softwareupdate -l` — fixed onto a bounded 60s runner argv call).
Still listed in the raw-popen inventory above because `pending_reboot`'s
Linux leg (`uname -r`, `ls -t /boot/vmlinuz-*`, `needs-restarting -r`) was
out of scope for this migration and remains on the old unbounded
`popen()`-based `run_command` helper — recorded as three grandfathered
rung-3 exceptions (`windows_updates/do_pending_reboot#2/#3/#4`, see
Registered sites above) per ADR-3002 Decision 1 rather than migrated. Two of
the three (`uname -r`, `ls -t /boot/vmlinuz-*`) are also good rung-1
promotion candidates in their own right (a direct `uname(2)` syscall and a
`std::filesystem` directory scan respectively need no subprocess at all)
rather than a straight argv migration; tracked in #2380.

**Migrated off raw spawn (Wave 3, PR33d):** `sccm` (0 spawn sites, was 3 —
`client_version`'s `sc query ccmexec` text parse promoted to a native SCM
query (`OpenSCManagerW`/`OpenServiceW`/`QueryServiceStatusEx`); `site`'s two
PowerShell `Microsoft.SMS.Client` ComObject calls promoted to native
in-process late-bound `IDispatch` (`CLSIDFromProgID`/`CoCreateInstance`/
`GetIDsOfNames`/`Invoke`); `site`'s registry Authority-subkey fallback fixed
alongside the migration — it previously matched a literal, never-substituted
`SMS:{}` subkey name and so was permanently dead, now genuinely enumerates
`HKLM\SOFTWARE\Microsoft\CCM\Authority\` via `RegEnumKeyExW`; no manifest
rows since none survive, matching the discovery/PR2.1c precedent above).

**Migrated off raw spawn (Wave 3, PR3.3b):** `antivirus` (Windows `products`/`status`
moved from `powershell Get-CimInstance`/`Get-MpComputerStatus` to the shared
bounded WMI helper, rung 1, in-process; the new `av_exclusions` action reads
Defender's exclusion registry keys directly, also rung 1; Linux/macOS
`pgrep`/PlistBuddy/`systemextensionsctl`/`stat` sites moved to direct runner
argv — see Registered sites above. Linux `status` also gained a real leg —
ClamAV liveness + a rung-1 `stat()` definitions-mtime read, other EDRs
presence-only — replacing the previous hardcoded `not_available`) and
`bitlocker` (Windows: raw `manage-bde -status` popen replaced by an
in-process Win32_EncryptableVolume WMI query + per-volume
GetConversionStatus() method call, rung 1, 0 spawn sites. Linux:
`lsblk`+`cryptsetup status` shell-outs replaced by in-process libblkid TYPE
enumeration + plain `/sys/class/block/dm-*/dm/uuid` reads, rung 1, 0 spawn
sites. macOS: `fdesetup`/`diskutil` stay on the bounded runner, promoted
from a `/bin/sh -c` shell wrapper to direct argv, rung 2 — see Registered
sites above).

**Migrated off private helpers (Wave 5, PR5.1):** `script_exec` (deleted its
private CreateProcessA and fork+execvpe/execvp spawn paths — including the
plugin-local process-wide launch-lock usage the runner now owns internally
— onto `yuzu::agent::run_bounded_subprocess`; all three actions transcribed
above. Environment behaviour preserved exactly on both platforms — full
parent-environment inheritance on Windows, the curated seven-name allow-list
on POSIX, neither one narrowed or widened by this migration — and now
documented rather than left implicit: see the ENVIRONMENT comment block at
`script_exec_plugin.cpp`'s top).

**Migrated off raw spawn:** `tar` (service enumeration's `systemctl`/
`launchctl` popen calls and mapdrive's Linux `smbstatus`/Windows `wevtutil`
popen-style calls all moved to direct runner argv, rung 2 — see Registered
sites above). Not raw-spawn migrations, but wired the same wave: `arp`
gained native Linux (`/proc/net/arp`) and constrained macOS (`route_sysctl`
sysctl dump) legs, and mapdrive gained a constrained macOS leg
(`getfsstat`, outbound-live-only) — none of the three add a spawn site, so
none get manifest rows; `software` (dpkg_rpm/pkgutil) remains kPlanned on
Linux/macOS, not yet wired. `tar` carries zero raw spawn tokens now and is
removed from `scripts/ci/check-plugin-spawn-lexical.sh`'s GRANDFATHERED
allowlist.

**Direct exec via private helpers:** script_exec, content_dist, filesystem
(filesystem is fully migrated onto the runner on the #2321 branch;
content_dist's own `do_execute` site is now transcribed above as
`content_dist/do_execute#1`, BR-007 whole-branch review round 2).

**Agent-core sites:** trigger_engine (2 sites), dex_linux_collector,
dex_macos_collector.
