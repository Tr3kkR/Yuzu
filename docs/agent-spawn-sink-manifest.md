# Agent spawn sink manifest

Owner: @Doomgoose. Population tracked in #2380. Governed by ADR-3002
(`docs/adr/3002-acquisition-ladder.md`) — read that first; this file is the
standing evidence ledger it mandates, a **governance artifact, not a plan**
(sequencing lives with the migration roadmap, not here).

Every spawn call site in `agents/plugins/*` and the covered agent-core paths
gets a row. The row — not the call site — is the single authority for the
site's evidence: the call site carries only its **site ID** and a one-line
rationale in a comment. Site IDs are stable and follow
`<plugin-or-component>/<function>#<n>` (`#<n>` disambiguates multiple sites
in one function; IDs are never reused after a site is deleted).

## Row schema

| Column | Meaning |
|---|---|
| Site ID | `<plugin-or-component>/<function>#<n>` — stable, never reused |
| Location | `path/to/file.cpp:function` (line numbers drift; function names anchor) |
| Mechanism | current spawn mechanism (`popen` helper, `system()`, direct `fork/exec`, runner argv, runner Decision-5 shape) |
| Platform | which OS paths reach this site |
| Provenance | where the interpolated values come from (operator/server params, derived, local-system, compile-time literal) |
| Mutating | mutating vs read-only — drives deadline/grace policy and migration gating (ADR-3002 runner contract) |
| Shell features | which shell capabilities the site actually uses (pipeline, redirection, glob, `~user` expansion, none) |
| Privilege | sudoers grant relied on, if any (exact grant form from `scripts/install-agent-user.sh`) |
| Ladder review | does this subprocess need to exist, or does a rung-1 interface answer the same question? |
| Rung + evidence | the rung the site sits at and the ADR-3002 Decision-1 evidence for every rung passed over |
| Registration | Decision-5 interpreter registration / Decision-7 shell exception, where applicable |

## Registered sites

Populated as the migration reaches each site (and immediately for any new or
interim site — ADR-3002 Decisions 1 and 2).

| Site ID | Location | Mechanism | Platform | Provenance | Mutating | Shell features | Privilege | Ladder review | Rung + evidence | Registration |
|---|---|---|---|---|---|---|---|---|---|---|
| `users/do_logged_on#1` | `agents/plugins/users/src/users_plugin.cpp:do_logged_on` | runner argv | macOS | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_sessions#1` | `agents/plugins/users/src/users_plugin.cpp:do_sessions` | runner argv | Linux | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_sessions#2` | `agents/plugins/users/src/users_plugin.cpp:do_sessions` | runner argv | macOS | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_local_users#1` | `agents/plugins/users/src/users_plugin.cpp:do_local_users` | runner argv | Linux | compile-time literal + account name derived from a local-system enumeration (dscl -list / getpwent) | read-only | none — pipeline eliminated (tail -1 replicated in-process, last non-empty line; max_lines caps the FIRST N and would invert the selection) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_local_users#2` | `agents/plugins/users/src/users_plugin.cpp:do_local_users` | runner argv | macOS | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public OpenDirectory C API in-tree; .mm ruled out of this run by the architect | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no public OpenDirectory C API in-tree, .mm ruled out this run | n/a |
| `users/do_local_users#3` | `agents/plugins/users/src/users_plugin.cpp:do_local_users` | runner argv | macOS | compile-time literal + account name derived from a local-system enumeration (dscl -list / getpwent) | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public OpenDirectory C API in-tree; .mm ruled out of this run by the architect | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no public OpenDirectory C API in-tree, .mm ruled out this run | n/a |
| `users/do_local_users#4` | `agents/plugins/users/src/users_plugin.cpp:do_local_users` | runner argv | macOS | compile-time literal + account name derived from a local-system enumeration (dscl -list / getpwent) | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public OpenDirectory C API in-tree; .mm ruled out of this run by the architect | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no public OpenDirectory C API in-tree, .mm ruled out this run | n/a |
| `users/do_local_users#5` | `agents/plugins/users/src/users_plugin.cpp:do_local_users` | runner argv | macOS | compile-time literal + account name derived from a local-system enumeration (dscl -list / getpwent) | read-only | none — env-var prefix carried by /usr/bin/env LC_ALL=C, an exec wrapper not an interpreter | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin; rung stays 2: /usr/bin/env is an exec wrapper, not an interpreter | n/a |
| `users/do_local_admins#1` | `agents/plugins/users/src/users_plugin.cpp:do_local_admins` | runner argv | macOS | compile-time literal argv | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public OpenDirectory C API in-tree; .mm ruled out of this run by the architect | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no public OpenDirectory C API in-tree, .mm ruled out this run | n/a |
| `users/do_group_members#1` | `agents/plugins/users/src/users_plugin.cpp:do_group_members` | runner argv | macOS | operator param (group), gated by is_safe_identifier | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no public OpenDirectory C API in-tree; .mm ruled out of this run by the architect | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no public OpenDirectory C API in-tree, .mm ruled out this run | n/a |
| `users/do_primary_user#1` | `agents/plugins/users/src/users_plugin.cpp:do_primary_user` | runner argv | Linux | compile-time literal argv | read-only | none — pipeline eliminated (head -200 -> max_lines=200, stop_after_max_lines=true) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_primary_user#2` | `agents/plugins/users/src/users_plugin.cpp:do_primary_user` | runner argv | macOS | compile-time literal argv | read-only | none — pipeline eliminated (head -200 -> max_lines=200, stop_after_max_lines=true) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_session_history#1` | `agents/plugins/users/src/users_plugin.cpp:do_session_history` | runner argv | Linux | operator param (count), numeric | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `users/do_session_history#2` | `agents/plugins/users/src/users_plugin.cpp:do_session_history` | runner argv | macOS | operator param (count), numeric | read-only | none — redirection eliminated (2>/dev/null -> merge_stderr=false) | none | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `certificates/list_certs_macos#1` | `agents/plugins/certificates/src/certificates_plugin.cpp:list_certs_macos` | runner argv, single element = trusted shell script string | macOS | `cmd` built by `build_login_keychain_read_command()` from an already-allowlist-validated uid/username (local-system, not operator input) | read-only | `~user` home-directory expansion (the one feature `/bin/sh -c` is retained for) | none beyond the LaunchDaemon's existing root context (ships with no `UserName` key — runs as root today; no sudoers grant needed) | Reviewed — no rung-1/2 API reaches a per-console-user login keychain without a session hop; `security find-certificate` under `launchctl asuser`/`sudo -u` needs the shell for `~user` expansion | Rung 3 — Decision-7 governed-shell exception (ADR-3002); Decision-1 evidence: `cert_store.cpp`'s macOS post-mortem documents a prior clean-argv attempt failing to reach the per-user keychain session | Decision-7 shell exception |
| `certificates/details_cert_macos#1` | `agents/plugins/certificates/src/certificates_plugin.cpp:details_cert_macos` | runner argv, single element = trusted shell script string | macOS | same as above | read-only | same as above | same as above | same as above | same as above | Decision-7 shell exception |
| `certificates/keychain_contains_thumbprint#1` | `agents/plugins/certificates/src/certificates_plugin.cpp:keychain_contains_thumbprint` | runner argv (`/usr/bin/security find-certificate -a -p <path>`) | macOS | `keychain_path` is one of two fixed literal paths (`resolve_delete_keychain_path()`); no operator-controlled text reaches this argv | read-only (used for both the pre-delete presence check and the post-delete verify re-enumeration in `delete_cert_macos`) | none | none beyond the LaunchDaemon's existing root context | Reviewed against the rung-1 option that now exists in this same file — "no interface exists" is FALSE here: `read_keychain_secitem()` enumerates these exact two fixed keychains natively via `SecItemCopyMatching`, and its output was verified thumbprint-for-thumbprint identical to this site's `security find-certificate -a -p` (System.keychain 3/3, SystemRootCertificates.keychain 158/158, macOS 26.5.2 arm64, 2026-08-17). Promotion is blocked on a specific, checkable correctness dependency rather than on effort: this site's UP-5 contract requires distinguishing an unrelated per-certificate decode failure (skip it, keep proving absence) from a capped/truncated scan (prove nothing), and `SecItemKeychainResult` currently folds BOTH into a single `complete=false`. Promoting before splitting that flag would regress UP-5 — one unrelated malformed certificate anywhere in the keychain would turn a genuinely successful delete into a reported failure | Rung 2 — clean multi-element argv through the bounded subprocess runner. Decision-1 descent evidence: **disproportionate cost** (the ADR's weakest category, stated as the required written comparison) — rung 1 is implemented and available, but consuming it here first costs a split of `SecItemKeychainResult::complete` into capped-vs-decode-failed plus a re-verification of the delete-verification verdict table (`fold_presence_scan`); that is a behavioural change to a MUTATING action's proof-of-absence, outside WP-B's read-path scope. Tracked for promotion in #2380 | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site — no interpreter is invoked and no script payload is embedded) |
| `certificates/delete_cert_macos#1` | `agents/plugins/certificates/src/certificates_plugin.cpp:delete_cert_macos` | runner argv (`/usr/bin/security delete-certificate -Z <thumbprint> <path>`) | macOS | `<thumbprint>` is hex-validated (`is_valid_thumbprint`) + canonicalized operator input; `<path>` is one of two fixed literal paths | **mutating** | none | **none used today** — the macOS LaunchDaemon ships with no `UserName` key and runs as root, and this argv carries no `sudo` prefix at all. RECORDED MISMATCH (do not copy this grant as if it were live evidence): `scripts/install-agent-user.sh:467` and `docs/agent-privilege-model.md:107` both grant `/usr/bin/security delete-certificate -t /Library/Keychains/System.keychain *`, which CANNOT authorize this site's actual argv `delete-certificate -Z <thumbprint> <path>` — `-t` is the "also delete user trust settings" switch, not a selector (`security help delete-certificate`), so the first argument does not match. Inert while the daemon is root; the grant, the privilege matrix and a `sudo -n --` argv must be reconciled together before #1455 moves the service off root | Reviewed — a rung-1 delete does exist in principle (`SecItemDelete`/`SecKeychainItemDelete`), so "no interface exists at the rung" is FALSE and is not claimed. Not taken here: this is the plugin's only MUTATING macOS site, and its safety rests on the existing exit-code + re-enumeration verdict table (`security delete-certificate` can exit 0 without removing anything under SIP/ACL quirks), so swapping the delete mechanism means redesigning that proof | Rung 2 — clean multi-element argv through the bounded subprocess runner. Decision-1 descent evidence: **disproportionate cost** (written comparison): rung 1 would replace a CLI whose SIP/ACL failure modes the current verdict table is built and tested against, requiring that whole proof-of-deletion to be re-established against a different API's error surface; it buys one fewer child process on a single operator-initiated action. WP-B promotes read paths only | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `certificates/parse_pem_block_macos#1` | `agents/plugins/certificates/src/certificates_plugin.cpp:parse_pem_block_macos` | runner argv (`/usr/bin/openssl x509 -noout -in <tmpfile> -subject -issuer -startdate -enddate -serial -fingerprint -sha1 -text`) | macOS | `<tmpfile>` is a `yuzu::TempFile`-owned path (mkstemps/O_EXCL, mode 0600) holding one PEM block read from the login keychain — no operator text | read-only | none | none beyond the LaunchDaemon's existing root context | Reviewed — rung 1 is available and is IMPLEMENTED IN THIS BRANCH: `certificates_x509::parse_pem_certs()` parses exactly this kind of PEM block in-process, and the LibreSSL `-ext keyUsage` limitation that shaped this site is a limitation of the host CLI, not of rung 1 (the plugin links libcrypto directly). "No interface exists at the rung" is therefore FALSE and is not claimed. This is the strongest remaining promotion candidate in this plugin | Rung 2 — clean multi-element argv through the bounded subprocess runner. Decision-1 descent evidence: **disproportionate cost** (written comparison): promoting costs a fresh parity capture of the LOGIN-keychain output across all seven fields, because this site's result feeds `certificates_macos_parsers.hpp`'s `-text`-shaped line selection and the login path is frozen byte-identical by WP-B's spec; it buys removal of one `openssl` child process per PEM block, which is the largest per-call spawn saving left here. Deliberately DEFERRED, explicitly NOT ruled out — tracked in #2380 | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `certificates/resolve_console_user#1` | `agents/plugins/certificates/src/certificates_plugin.cpp:resolve_console_user` | runner argv (`/usr/bin/stat -f%Su /dev/console`) | macOS | none — fixed literal argv | read-only | none | none beyond the LaunchDaemon's existing root context | Reviewed — CORRECTION of an earlier claim in this row that the LaunchDaemon "has no SystemConfiguration framework access": that is FALSE. `agents/shared/macos_console_user.hpp:361` already implements `console_user()` via `SCDynamicStoreCopyConsoleUser`, and `agents/plugins/users/meson.build:12-19` links SystemConfiguration with the same `required: false` + `-D`-flag shape this plugin now uses for the Security framework — nothing prevents this plugin from doing likewise, so the omission is a BUILD CHOICE, not an unavailable rung. Rung 1 is therefore available and is not ruled out. Not taken in WP-B because the two mechanisms answer subtly different questions: `stat -f%Su /dev/console` reports the console DEVICE owner while `SCDynamicStoreCopyConsoleUser` reports the Aqua SESSION owner, and they diverge under fast user switching and screen sharing — WHICH user's login keychain this plugin reads is an operator-visible behavioural contract | Rung 2 — clean multi-element argv through the bounded subprocess runner. Decision-1 descent evidence: **disproportionate cost** (the ADR's weakest category, stated as the required written comparison): rung 1 is implemented and linkable, but adopting it changes console-user semantics under fast user switching / screen sharing and so needs its own behavioural verification rather than riding a certificate read-path migration. Tracked in #2380 | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `certificates/resolve_console_user#2` | `agents/plugins/certificates/src/certificates_plugin.cpp:resolve_console_user` | runner argv (`/usr/bin/id -u <username>`) | macOS | `<username>` is the output of site `#1` above, allowlist-validated (`is_valid_username`) before use | read-only | none | none beyond the LaunchDaemon's existing root context | Reviewed — rung 1 exists for this half too (`getpwnam_r` resolves a username to a uid in process, no framework needed), so "no interface exists" is FALSE and is not claimed. Deferred with `#1` rather than separately: this site consumes `#1`'s output, so promoting it alone would leave the pair split across two mechanisms for no benefit | Rung 2 — clean multi-element argv through the bounded subprocess runner. Decision-1 descent evidence: **disproportionate cost** (written comparison), inherited from `#1` — sequenced with that site's promotion so console-user resolution stays one coherent mechanism. Tracked in #2380 | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `quarantine/win_*` (15 sites) | `agents/plugins/quarantine/src/quarantine_plugin.cpp:win_quarantine`/`win_unquarantine`/`win_is_quarantined`/`win_get_whitelist`/`do_whitelist` (Windows branch) | runner argv (`netsh advfirewall firewall ...`), `netsh.exe` resolved via `GetSystemDirectoryW` (no PATH search) | Windows | operator-supplied IP (block/allow rules, whitelist add/remove), `is_safe_ip`-validated; else compile-time literal argv | 11 mutating, 4 read-only | none — clean pre-split argv | none (service-account privilege already covers `netsh`) | no rung-1 API evaluated this PR — Windows does have a native COM firewall API (`INetFwPolicy2`), deliberately not pursued since this migration's scope is spawn-mechanism only | 2 — direct argv via yuzu::agent::run_bounded_subprocess; rung 1 (`INetFwPolicy2`) passed over: disproportionate cost, out of this PR's scope | n/a |
| `quarantine/linux_*` (22 sites) | `agents/plugins/quarantine/src/quarantine_plugin.cpp:linux_quarantine`/`linux_unquarantine`/`linux_is_quarantined`/`linux_get_whitelist`/`do_whitelist` (Linux branch) | `sudo_wrap({"/usr/sbin/iptables", ...})` | Linux | operator-supplied IP (per-IP ACCEPT/DROP rules, whitelist add/remove), `is_safe_ip`-validated; else compile-time literal argv | 17 mutating, 5 read-only | none — clean pre-split argv | `sudo -n` NOPASSWD, bare grant on `/usr/sbin/iptables` (`install-agent-user.sh`) — matches `sudo_wrap`'s canonical form regardless of specific args | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess, sudo-governed; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |
| `quarantine/macos_load_ruleset#1` | `agents/plugins/quarantine/src/quarantine_plugin.cpp:macos_load_ruleset` | `sudo_wrap({"/sbin/pfctl", "-f", tmp_file.path()})` — atomic full-ruleset load, `set skip on lo0` at the top of the generated ruleset | macOS | ruleset content built from operator/derived whitelist IPs (`is_safe_ip`-validated upstream), written to a `yuzu::TempFile` | **mutating, CRITICAL** | none | `sudo -n` NOPASSWD, bare grant on `/sbin/pfctl` | **Reviewed and explicitly REJECTED** — an earlier design used a named pf anchor (`pfctl -a yuzu-quarantine -f tmp` + a second `pfctl -f -` to attach it); that second call replaced the whole main ruleset and caused a live UAT connectivity-loss incident (commit `672896112`: the agent's own quarantine-command response never reached the server, box stayed firewalled until manual `pfctl -F all`). This PR migrates ONLY the spawn mechanism (popen → sudo-governed argv) and deliberately preserves the current atomic-single-call + `set skip on lo0` design that fixed that incident — do not reintroduce an anchor without re-deriving the connectivity-preservation proof from scratch | Rung 2 — clean multi-element argv through the bounded subprocess runner. Decision-1 descent evidence: this is the plugin's most safety-sensitive site; the shell-out itself is not the risk, the ruleset-reload STRATEGY is, and that strategy is unchanged by this PR | Decision-1 ledger row (plain-binary argv through the bounded runner; NOT a Decision-5 interpreter site) |
| `quarantine/macos_load_ruleset#2` | same | `sudo_wrap({"/sbin/pfctl", "-e"})` | macOS | compile-time literal argv | mutating, best-effort/idempotent (failure ignored by design) | none | `sudo -n` NOPASSWD | no rung-1 API for this data on this OS in this plugin | 2 — same as above | n/a |
| `quarantine/macos_unquarantine#1` | `agents/plugins/quarantine/src/quarantine_plugin.cpp:macos_unquarantine` | `sudo_wrap({"/sbin/pfctl", "-f", "/etc/pf.conf"})` — restores macOS defaults, same restore path as before this PR | macOS | compile-time literal argv | mutating | none | `sudo -n` NOPASSWD | same anchor-rejection note as `macos_load_ruleset#1` — this restore path and `macos_load_ruleset`'s load path are the two halves of the SAME atomic-reload design | 2 — same as above | n/a |
| `quarantine/macos_unquarantine#2` | same | `sudo_wrap({"/sbin/pfctl", "-d"})` — fallback when `/etc/pf.conf` is unavailable | macOS | compile-time literal argv | mutating, fallback | none | `sudo -n` NOPASSWD | no rung-1 API for this data on this OS in this plugin | 2 — same as above | n/a |
| `quarantine/macos_is_quarantined#1` / `quarantine/macos_get_whitelist#1` | `agents/plugins/quarantine/src/quarantine_plugin.cpp:macos_is_quarantined`/`macos_get_whitelist` | `sudo_wrap({"/sbin/pfctl", "-s", "rules"})` | macOS | compile-time literal argv | read-only | none | `sudo -n` NOPASSWD | no rung-1 API for this data on this OS in this plugin | 2 — direct argv via yuzu::agent::run_bounded_subprocess, sudo-governed; rung 1 passed over: no rung-1 API for this data on this OS in this plugin | n/a |

## Seed inventory — known spawn surfaces awaiting transcription

Plugin-granularity seed from the ADR-3002 review inventory (dev @ 2026-07-23,
27 raw-spawning plugins of 49; three further plugins exec directly through
private helpers). Site-level transcription is #2380's first work item —
these rows are pointers, not evidence.

**Raw `popen`/`system` helpers:** antivirus, bitlocker,
device_identity, discovery, event_logs, firewall, hardware, installed_apps,
interaction, ioc, license_scan, network_actions, network_config,
network_diag, os_info, processes, sccm, services,
software_actions, tar, vuln_scan, wifi, windows_updates, wol.
(`vuln_scan` carries code slated for retirement per ADR-0028/ADR-0018 —
sequence that cleanup against migrating it, tracked in #2380.)

**Migrated off raw spawn (Wave 2, PR2.1a):** `users` (Windows session
history moved to wevtapi/EvtQuery, ProfileList to native enumeration; POSIX
sites moved to direct runner argv — see Registered sites above).

**Migrated off raw spawn (Wave 2, PR2.1b):** `certificates` (Linux/macOS
System+SystemRoot promoted to rung-1 libcrypto/SecItem; the login-keychain
hop stays a registered Decision-7 exception — see Registered sites above).

**Migrated off raw spawn (Wave 2, PR2.2b):** `quarantine` (43 sites: 15
Windows netsh, 22 Linux iptables, 6 macOS pfctl — see Registered sites
above; the macOS pfctl reload strategy is deliberately UNCHANGED by this
migration, see `macos_load_ruleset#1`'s row for the connectivity-loss
incident that strategy already fixed).

**Direct exec via private helpers:** script_exec, content_dist, filesystem
(filesystem is fully migrated onto the runner on the #2321 branch).

**Agent-core sites:** trigger_engine (2 sites), dex_linux_collector,
dex_macos_collector.
