# Resource Ledger - #4666 PR-1 bounded async log hand-off primitive

This ledger lists every thread, queue buffer, sink wrapper, and lock the PR-1 diff introduces, with
its owner and release path, for `agents/core/src/log_handoff.{hpp,cpp}`. A Resource Ledger is a
policy-floor artifact (CLAUDE.md standing rule 2), so it is a standalone file rather than a line in a
PR description. Nothing else changes in this PR — no production call site installs this primitive
yet (`main.cpp`/`service_win.cpp`/`guardian_engine.cpp`/`guardian_spark_runtime.cpp` are all
untouched; that wiring is PR-2). Format follows
`docs/resource-ledgers/spark-criterion10-timing-instrumentation.md`.

## `agents/core/src/log_handoff.{hpp,cpp}`

| Resource | Owner | Acquire | Release | Failure path |
|---|---|---|---|---|
| `spdlog::details::thread_pool` (one bounded queue + one worker thread) | `LogHandoff::pool_` (`std::shared_ptr`), constructed directly via `std::make_shared` — **never** `spdlog::init_thread_pool()`, so no spdlog registry co-owns it | `LogHandoff::create_with_sinks()`, after every earlier throwing step has already succeeded (sinks wrapped, error handler bound) | `LogHandoff::teardown()`'s T1 (`pool_.reset()`); `~thread_pool()` posts a `terminate` message under the `block` policy and **joins** its one worker | A healthy sink drains in order and the join returns promptly. A wedged sink means the call simply does not return; bounded by the `ShutdownDeadlineGuard` `teardown()` constructs around T1–T3 (`kLogTeardownGrace` 2 s default), which fires `hard_exit(kLogTeardownExitCode=5)` on the WHOLE PROCESS if the grace elapses — see the header's TEARDOWN CONTRACT for why this differs from the plan's original "caller wires an external watchdog" phrasing (U5's own requirement pins the self-contained design). `~LogHandoff()` runs `teardown()` if it was never called explicitly (fail-closed, matching `OrphanExitGuard`). |
| **Fixed RSS cost of the pool's queue — NOT proportional to traffic.** `spdlog::details::circular_q` pre-allocates all `kLogQueueCapacity + 1` (8193) slots **at `thread_pool` construction**, each slot a `spdlog::details::async_msg` (`sizeof == 408` bytes on x64, measured against the vendored spdlog 1.17.0 headers: a `log_msg_buffer` — four owning `std::string`s for logger name/payload/short-filename/funcname plus the scalar `log_msg` fields — an `async_msg_type`, and an `async_logger_ptr`). | The `std::vector<async_msg>` inside `circular_q`, owned transitively by `pool_` above | Same moment as the pool itself — this is not a high-water mark, it is paid in full up front | Same as the pool's own release | 8193 × 408 bytes = 3,342,744 bytes = **3.34 MB (≈3.19 MiB)**, always, for the life of any constructed `LogHandoff` — including one built only for a unit test. This is a corrected fact from the plan's final review (the original v1/v2 draft undercounted it as "~6 MB, only under load"); PR-2's own resource-ledger update and `docs/user-manual/server-admin.md` must both state the real figure again when the primitive is actually installed in the shipped binary. Payload bytes beyond the fixed per-slot allocation (a queued message's formatted text) are additional, unbounded by this primitive, and are measured on the rig in a later PR — never asserted here. |
| `spdlog::async_logger` (name `""`, `overrun_oldest`) | `LogHandoff::logger_` (`std::shared_ptr`) | Same `create_with_sinks()` call, immediately after the pool | `teardown()`'s T3 (`logger_.reset()`) | Holds only a `std::weak_ptr` back to the pool (never a cycle). `sink_it_`/`flush_` throwing (pool gone, formatter/allocation exception) routes to the non-I/O error handler below, never escapes uncaught into caller code (spdlog's own `SPDLOG_LOGGER_CATCH`). |
| `StallObservableSink` wrapper, one per caller-supplied inner sink | `LogHandoff::wrapped_sinks_` (`std::vector<std::shared_ptr<StallObservableSink>>`); each wrapper owns its inner `spdlog::sink_ptr` by `shared_ptr` | Same `create_with_sinks()` call, one per element of the caller's `sinks` vector | `teardown()`'s T3 (`wrapped_sinks_.clear()`), which drops `LogHandoff`'s OWN last reference to each wrapper and (transitively) its inner sink | The wrapper's own state is two plain atomics (`in_flight_` counter via an RAII `WriteGuard`, `started_at_ticks_`) — no lock, no allocation, cannot itself fail. `log()`/`flush()` forward to the inner sink and let its exception propagate (the RAII guard still decrements on unwind). `set_pattern()`/`set_formatter()` forward unconditionally (the v1→v2 regression the final review caught — dropping this silently reverts `--log-format json` to the default text pattern; pinned by unit test U-json). **Correction (third-round adversarial review, Fable): T3 dropping `LogHandoff`'s own reference does NOT by itself guarantee final inner-sink destruction runs under the watchdog** — `install()`/`logger()` (`log_handoff.hpp`) return `shared_ptr<spdlog::logger>` copies to the caller by design; the async_logger holds the wrapper sinks via `spdlog::sink_ptr`, so a caller-held logger copy transitively keeps a wrapper (and its inner sink) alive past T3 if that copy outlives `teardown()`. This is a LOGGER-lifetime question, distinct from the pool-join race the round-1/round-2 fixes closed — PR-2's job is to decide how long its own copy of the installed logger lives relative to calling `teardown()`, not something this primitive can enforce from inside `wrapped_sinks_.clear()` alone. |
| Non-I/O error handler state (`LogHandoff::ErrorState`: a `std::mutex` guarding a count + a 256-byte-truncated last-message string) | `LogHandoff::error_state_` (`std::shared_ptr<ErrorState>`), captured by value into the `set_error_handler` lambda | Same `create_with_sinks()` call | Object lifetime ends when both `LogHandoff::error_state_` and the logger's own captured copy (inside `custom_err_handler_`, released at `logger_.reset()` in T3) drop | Reached **concurrently** from the pool's one worker thread (a sink `log()`/`flush()` throw) and from any producer thread (a formatter/allocation exception, or "pool gone") — this is why it is a plain `std::mutex`, not a lock-free/single-writer scheme (a spec bug in an earlier draft, corrected in the final review). The handler itself is wrapped in its own `try`/`catch(...)` so an allocation failure while recording the error message cannot escape back into spdlog's error-handling machinery. |
| Global drain-lookup slot (`DrainHandle`: `owner` raw-pointer identity tag + `std::weak_ptr` to the pool/logger/each wrapped sink + a STRONG `std::shared_ptr<DrainGate>`) | File-local `std::shared_ptr<DrainHandle>` behind a function-local-static `std::mutex` (`drain_mutex()`/`drain_slot()` stay in `log_handoff.cpp`'s anonymous namespace; `DrainHandle` itself moved to file scope in the governance hardening round so it can be forward-declared in the header — see the fixes index below — but is still never named or constructed outside `log_handoff.cpp`) | `LogHandoff::register_global_drain_handle()`, called once at the end of a successful `create_with_sinks()`, now receiving an already-populated `DrainHandle` built BEFORE the `LogHandoff` object exists (governance hardening round, see below) | `LogHandoff::clear_global_drain_handle()`, called as the FIRST step of `teardown()` (T0), identity-checked against the calling instance so a newer instance's registration is never clobbered by an older one's teardown | Holds **only weak references to the pool/logger/sinks** — `drain_log_bounded()` locks a temporary strong ref for the minimum span of each read. **Fixed defect, round 1 (#4666 PR-1 adversarial review, both reviewers independently reproduced with standalone repros): an earlier revision of this row claimed the residual race where `drain_log_bounded()` momentarily holds the pool's last reference was harmless — it was not.** `teardown_body()`'s own `pool_.reset()` could become a non-destructive ref-decrement while the drainer still held a copy, letting `teardown()`'s `ShutdownDeadlineGuard` cancel on a NORMAL scope exit before the real blocking work had happened — so the drainer's later release could be the one running `~thread_pool()`'s blocking join, on the drain thread, with no watchdog covering it. **Fix: the drain-reader lease** (`DrainGate`/`DrainLease`, held strongly by `DrainHandle`) — `drain_log_bounded()` holds a `DrainLease` for its entire pool-touching span (declared first, so it destructs last, strictly after every local pool `shared_ptr` it creates); `teardown()`'s T0 calls `LogHandoff::close_drain_admission()` (stop admitting new leases) before doing any teardown work, then — still INSIDE its own already-armed `ShutdownDeadlineGuard` scope, not after it — calls `LogHandoff::wait_for_drain_quiescence()`, which blocks until every admitted lease has released. **Corrected scope (round 2 — the round-1 text above OVERCLAIMED what this alone guarantees, a second truth defect caught in a follow-up adversarial review):** the lease keeps `drain_log_bounded()`'s OWN `wait` bound honest — it does NOT make `teardown_body()`'s `pool_.reset()` the guaranteed last-reference dropper in general, because an ORDINARY producer thread calling `logger()->info()`/`flush()` can ALSO transiently hold a strong pool reference (see the `WorkerExitSignal` row below, which is what actually closes that). |
| `WorkerExitSignal` (mutex + condvar + one-shot `exited` bool) — the round-2 fix, and the PRIMARY cross-thread guarantee (the drain-reader lease above is an addition to this, not a substitute) | `LogHandoff::worker_exit_` (`std::shared_ptr`), captured by value into the pool's `on_thread_stop` callback (spdlog's 4-arg `thread_pool` constructor) | `create_with_sinks()`, built BEFORE the pool (same "everything that can throw happens before the object exists" phase) so it can be captured into the pool's own constructor | Object lifetime ends when `LogHandoff::worker_exit_` and the pool worker's own captured copy (released when the worker thread's lambda — `on_thread_start(); worker_loop_(); on_thread_stop();`, verified directly in `thread_pool-inl.h` — finishes) both drop | **Fixed defect, round 2 (#4666 PR-1 second adversarial review round, Fable): `spdlog::async_logger::sink_it_()`/`flush_()` both do `if (auto pool_ptr = thread_pool_.lock()) pool_ptr->post_log(...)` (or `post_flush`)** — verified directly in `async_logger-inl.h` — so ANY thread calling ordinary `logger()->info()`/`flush()`, not just `drain_log_bounded()`, transiently holds its OWN strong `shared_ptr<thread_pool>`, entirely outside the drain-reader lease's visibility. Before this fix, `teardown_body()`'s `pool_.reset()` could be a non-destructive ref-decrement whenever an ordinary producer thread held that transient reference, letting the `ShutdownDeadlineGuard` cancel on a normal return while the sink was still genuinely wedged — **reproduced empirically, 150/150 under 4-thread producer saturation against a wedged sink; ~1/200 under light logging.** TSan does NOT catch this (confirmed clean, both by the reviewer and independently here) — `pool_` is a correctly refcounted `shared_ptr`, so there is no data race, only an ownership-order race on WHICH thread ends up running the destructor; this class of bug is fundamentally invisible to a data-race detector. **Fix:** anchor the wait to the pool's WORKER THREAD'S OWN exit signal instead of trying to track every possible external strong-ref holder. `teardown_body()` calls `LogHandoff::wait_for_worker_exit()` immediately after `pool_.reset()`, still inside the same `ShutdownDeadlineGuard` scope — this blocks until the worker has genuinely exited, independent of which thread's `shared_ptr` reset happens to trigger `~thread_pool()`'s destructor call. Null-safe, same reasoning as `DrainGate`'s guards. |
| `LogHandoff::construction_fault_for_test_` (`static inline std::atomic<bool>`) | Process-wide, test-only | Set by `set_construction_fault_for_test(true)` | Auto-consumed (reset to `false`) by the very next `create_with_sinks()` call, so it cannot leak into an unrelated later test | Production code never sets this; it exists purely to exercise the pool/logger-construction-failure return path without depending on the OS actually refusing to create a thread. |
| `ShutdownDeadlineGuard` (from `shutdown_deadline_guard.hpp`, reused unmodified) — a detached watchdog thread | Local to `teardown()`'s (or `teardown_with_action_for_test()`'s) stack frame; the watchdog's own detached worker thread is spawned via `io_detail::spawn_detached` (that primitive's own established orphan-exit contract, unchanged by this PR) | First statement inside `teardown()`'s/`teardown_with_action_for_test()`'s try block, after T0's deregistration/admission-close | RAII: the guard's destructor calls `cancel()` when `wait_for_drain_quiescence()` (drain-reader-lease wait) plus `teardown_body()` (T1–T3, which now itself includes `wait_for_worker_exit()` right after `pool_.reset()`) both return normally, before the enclosing stack frame unwinds | If `wait_for_drain_quiescence()` never returns (a lease never releases) OR `wait_for_worker_exit()` never returns (the pool's worker never exits — a genuinely wedged sink, regardless of which thread's `shared_ptr` reset ends up triggering `~thread_pool()`) OR the rest of `teardown_body()` never returns, the watchdog fires its action — `hard_exit(kLogTeardownExitCode)` in production, or a caller-supplied test callback via `teardown_with_action_for_test()` — after the grace elapses; the CALLING thread of `teardown()` remains blocked regardless (the watchdog terminates the whole process in production; the test-action variant is what lets U5/the two BLOCKER regression tests observe the deadline firing without actually killing the test binary — the test then still has to release the gate itself so every blocked thread's own `join()` can complete). |
| `DrainGate` (mutex + condvar + `active_readers` count + `closed` flag) | `LogHandoff::drain_gate_` (`std::shared_ptr`), also held strongly by the global `DrainHandle` above (a tiny, non-blocking-destructor control block — safe to hold strongly, unlike the pool) | `create_with_sinks()`, in the same "everything that can throw happens before the object exists" phase as `pool_`/`logger_`/`error_state_` | Object lifetime ends when both `LogHandoff::drain_gate_` and the global `DrainHandle`'s copy (cleared at T0) drop | Null-safe by construction: `close_drain_admission()`/`wait_for_drain_quiescence()` both no-op if `drain_gate_` was never assigned (a construction failure before that line, which cannot reach a live `LogHandoff` object per the "only now" comment in `create_with_sinks()`). |
| `GatedCaptureSink`/`ThrowOnceSink` (test-only spdlog sink doubles, `tests/unit/test_log_handoff.cpp`) | Test-local `std::shared_ptr`s | Constructed per `TEST_CASE`/`SECTION` | Destructor runs once every `shared_ptr` owner (the test's own copy, if not explicitly dropped, plus the `LogHandoff`'s wrapped copy) releases | `GatedCaptureSink`'s pause gate is a `std::mutex`+`std::condition_variable`; its `closed_flag()` is a SEPARATE `shared_ptr<std::atomic<bool>>` control block (not `this`-bound) specifically so a test can observe "was the sink object actually destroyed" after dropping every `shared_ptr<GatedCaptureSink>` it holds (U4). No production code path creates either double. |
| **`teardown_done_mu_`/`teardown_done_cv_`/`teardown_done_`** (mutex + condvar + bool) — the /governance hardening-round fix for a concurrent-`teardown()` use-after-free | `LogHandoff` members directly (not behind a `shared_ptr` — these are plain object state, not shared with any other owner) | Default-constructed with the object, at `create_with_sinks()` | Destroyed with the `LogHandoff` object itself; no separate release path | **Fixed defect (governance run on this PR, Gate 4 unhappy-path finding UP-2, independently verified by the orchestrator against the actual code): `torn_down_`'s atomic exchange only decided WHO runs `teardown_body()`, not whether a losing concurrent caller could safely return** — a caller that immediately destroyed/freed the `LogHandoff` right after ITS OWN losing `teardown()` call returned could do so while the WINNER (on another thread) was still mid-`teardown_body()`, touching `pool_`/`logger_`/`wrapped_sinks_` — a use-after-free the header's own THREAD-SAFETY CONTRACT paragraph documented as a caller obligation but did not actually prevent. **Fix:** the loser now calls `wait_for_teardown_completion()` instead of returning immediately; the winner calls `mark_teardown_complete()` once `teardown_body()` returns normally (never reached on the `hard_exit()` path, since `hard_exit()` does not return — nothing needs waking if the whole process is exiting). Bounded by the winner's own `ShutdownDeadlineGuard`: a genuine wedge either lets `teardown_body()` finish (notifying the loser) or terminates the process (ending the loser's wait by ending everything) — never an independently-unwatched wait. |

## Fixes from the /governance run on this PR (2026-09-25) — kept as a compact index; full rationale is in the rows above and in `log_handoff.{hpp,cpp}`'s own comments at each site

One BLOCKING (policy floor) and eight SHOULD findings, converged on independently by up to three parties each (cpp-safety, quality-engineer, happy-path, unhappy-path, cpp-expert, docs-writer, compliance-officer, sre — see `governance.d/` for the full run ledger), all fixed in one hardening commit per Gate 7's fold rule (none opened a new independently-reviewable surface):

- **BLOCKING, policy floor:** `U5` and `BLOCKER-1 regression` (`tests/unit/test_log_handoff.cpp`) called `REQUIRE(ok)` BEFORE joining their spawned `std::thread`s — on exactly the regression each test exists to catch, `REQUIRE`'s throw would unwind the stack while the thread was still joinable, and `std::thread::~thread()` on a joinable thread calls `std::terminate()` (SIGABRT of the whole binary). Fixed: cleanup (release the sink, join every thread) now runs unconditionally before any assertion, matching `BLOCKER round-2`'s already-correct pattern. Verified red-then-green by the orchestrator directly: against the true pre-fix `9646bd9ed` primitive, `BLOCKER-1` with the fixed test code now fails CLEANLY (`REQUIRE(ok)` false, exit 42) instead of `SIGABRT`ing.
- **SHOULD:** `teardown()`'s T0 (`clear_global_drain_handle`/`close_drain_admission`) ran BEFORE the `try` block, so a `std::mutex::lock()` throw there would escape the `noexcept` function via `std::terminate()` instead of the documented fail-closed `hard_exit()`. Fixed: both calls moved inside the `try`, still before the `ShutdownDeadlineGuard` is constructed (preserving the "fast, never blocks" timing intent).
- **SHOULD:** `register_global_drain_handle()`'s allocating work (`make_shared<DrainHandle>`, the sinks vector copy) ran AFTER the `LogHandoff` object already existed, so a `bad_alloc` there would unwind through a live object's destructor, mutating global logger state despite `create_with_sinks()`'s "nothing is installed" construction-failure contract. Fixed: that work now happens before the object exists; `DrainHandle` moved from an anonymous namespace to file scope so it can be forward-declared in the header and passed as a parameter. One small, accepted residual remains (a bare mutex lock after the object exists) — see the CONSTRUCTION FAILURE note in `log_handoff.hpp`.
- **SHOULD:** `install()` had no guard against being called after `teardown()`; `logger_` is null post-teardown, and `spdlog::registry::set_default_logger(nullptr)` unconditionally nulls the registry's `default_logger_` even for a null input (verified directly against `registry-inl.h`) — reintroducing the exact straggler-segfault hazard T2's null-sink swap exists to prevent. Fixed: `install()` now returns `nullptr` and does nothing if `logger_` is already null.
- **SHOULD:** the `construction_fault_for_test_` flag was consumed AFTER the `sinks.empty()` check, so a fault flag set ahead of an (accidental) empty-sinks call was never consumed and could leak into the next, unrelated test. Fixed: the flag is now consumed first, unconditionally.
- **SHOULD (doc accuracy):** the header's numbered T0/T1 step list had not been updated across either fix round, even though the free-standing prose paragraphs below it had. Fixed: the list now mentions `close_drain_admission()` at T0 and `wait_for_worker_exit()` at T1.

### Gate 8 targeted re-review of the hardening commit above (cpp-safety, security-guardian, quality-engineer) — 3 further findings, all fixed in a second commit

- **CRITICAL, BLOCKING (cpp-safety):** `~LogHandoff()`'s existing guard — `if (!torn_down_.load()) teardown();` — meant that when `torn_down_` was ALREADY `true` (set by a concurrent EXPLICIT `teardown()` call on another thread whose `teardown_body()` had not yet finished), the destructor never called `teardown()` at all, so it never reached the new `wait_for_teardown_completion()` and proceeded straight to destroying `pool_`/`logger_`/`wrapped_sinks_`/etc while the winner's `teardown_body()` could still be reading or writing those same members — a genuine use-after-free/data race, and precisely the defect class the concurrent-teardown-completion fix above exists to close. The header's own THREAD-SAFETY CONTRACT paragraph and this ledger's row on `teardown_done_mu_`/etc both named "the destructor" as one of the forms now covered — that claim was false for this exact interleaving. **Fixed: `~LogHandoff()` now calls `teardown()` unconditionally**, dropping the guard. Safe and cheap for the ordinary single-threaded case (the same thread's own earlier explicit `teardown()` call already set `teardown_done_`, so the exchange-then-wait returns immediately, no self-deadlock).
- **MEDIUM (security-guardian):** `teardown_with_action_for_test()` had no `try`/`catch` around T0/the guard/`teardown_body()`, unlike production `teardown()`. `ShutdownDeadlineGuard::~ShutdownDeadlineGuard()` unconditionally calls `cancel()` on ANY unwind through its scope, so an exception there would suppress the watchdog action (if not already fired) AND skip `mark_teardown_complete()` — leaving a concurrent loser on the same object blocked in `wait_for_teardown_completion()` with nothing watching it, on this test-only path specifically. **Fixed:** wrapped in a `try`/`catch(...)` that calls `mark_teardown_complete()` before rethrowing, preserving the documented "exception fails the TEST_CASE, not the whole binary" contract while also waking any loser.
- **BLOCKING (quality-engineer):** in fixing the BLOCKING finding above, the first hardening commit deleted `BLOCKER-1 regression`'s `yuzu::test::ScopeExit release_on_exit{[&] { h.sink->release(); }};` with nothing replacing its coverage for the test's FIRST assertion (which runs before either spawned thread exists) — reopening the identical whole-binary-`hard_exit()`-on-a-still-paused-sink hazard the file's own GATE DISCIPLINE convention exists to prevent, on a different trigger. **Fixed:** the `ScopeExit` is restored (idempotent alongside the explicit `release()` calls later in the test, same pattern `BLOCKER round-2` already uses). `U5` had the identical, pre-existing gap (predating this PR's hardening commit) — fixed in the same pass since it was cheap and already in scope.

All three verified: full `[log_handoff]` suite green (12094 assertions/14 cases) across 5 standalone runs plus the full `--suite agent` pass, and a TSan run, all after these three fixes.

### Second Gate 8 pass on the destructor fix (cpp-safety) — 1 further BLOCKING + 1 SHOULD, both fixed in a third commit

The destructor fix above (unconditional `teardown()`) is correct and necessary, but tracing its loser-branch call to `mark_teardown_complete()`'s counterpart all the way through surfaced a heap-use-after-free this commit's own change made a ROUTINE occurrence rather than a rare corner case:

- **BLOCKING (heap use-after-free):** `mark_teardown_complete()` called `teardown_done_cv_.notify_all()` AFTER releasing `teardown_done_mu_` (mirroring `DrainLease`'s own "notify after unlock" pattern) — a valid optimization there ONLY because `DrainGate` is kept alive independently by a `shared_ptr` the caller holds. Here the notified object (`teardown_done_mu_`/`teardown_done_cv_`) is a PLAIN MEMBER of the same `LogHandoff` a loser is about to destroy: if the winner unlocked before notifying, a loser that acquired the lock in that window, saw the predicate already true (`condition_variable::wait`'s predicate overload never actually blocks in that case), and returned could proceed straight into `~LogHandoff()` — destroying `teardown_done_mu_`/`teardown_done_cv_` — before the winner's own `notify_all()` call ran, which would then be a UAF on the WINNER's thread. This is exactly the "destructor races an in-flight `teardown()`" scenario the unconditional-destructor fix above exists to handle correctly, and that fix is precisely what makes this window a routine occurrence rather than a corner case. **Fixed:** `notify_all()` now runs WHILE STILL HOLDING the lock, so by the time the winner releases the mutex the notify has already happened — a loser can never observe the predicate true without the notify having already completed.
- **SHOULD:** `teardown()`'s loser branch called `wait_for_teardown_completion()` (which constructs a `std::unique_lock`, and `std::mutex::lock()` is permitted by the standard to throw `std::system_error`) with no `try`/`catch`, unlike T0's already-covered mutex locks — an exception there would escape the `noexcept teardown()` via `std::terminate()`. Pre-existing since the first hardening commit, but the unconditional-destructor fix above makes this branch execute on essentially every already-torn-down destruction, raising its practical exposure well past "rare corner case." **Fixed:** wrapped in the same `try { ... } catch (...) { hard_exit(kLogTeardownExitCode); }` shape T0 already uses.

Verified: full `[log_handoff]` suite green across 5 more standalone runs, the full `--suite agent` pass, a fresh TSan run, AND (new for this pass, since this specific bug class — a notified object's lifetime racing its own notifier — is ASan's domain, not TSan's) an ASan+UBSan build (`x64-linux-asan` triplet, `-Db_sanitize=address,undefined`) run clean across 3 runs. Honest caveat: no existing test constructs the exact "explicit `teardown()` on one thread races the owning `unique_ptr`'s destruction on another" interleaving this bug lived in, so the clean sanitizer runs corroborate (nothing else regressed) rather than directly falsify this specific fix — the correctness argument for the fix itself rests on the code reading (notify now provably happens before the mutex can be released), not on having reproduced the race under a sanitizer.
- **SHOULD (test coverage):** added a `static_assert(sizeof(spdlog::details::async_msg) == 408, ...)` so a future spdlog version bump that changes this figure fails to compile instead of silently invalidating the FIXED RESOURCE COST math in this ledger and the header.
- **Forward-looking, PR-2/PR-3's job, recorded here per compliance-officer's and sre's own recommended mechanism (mirroring the existing RSS-figure obligation above), not actioned in this PR:** (a) `docs/user-manual/server-admin.md`'s exit-code enumeration (codes 1/3/4) needs a new entry for code 5 (`kLogTeardownExitCode`) once PR-2 wires a call site that can actually reach it; (b) PR-3's planned heartbeat-poller commitment for the observability accessors (`overrun_total()` etc.) has no tracking issue number yet — file one before or with PR-3; (c) PR-2 must re-derive the sequential shutdown-grace composition arithmetic (per the existing `service_win.cpp`/#3737 precedent that independent watchdogs compose in wall-clock time, not nested) for wherever it places the `teardown()` call site, against the applicable SCM/systemd stop timeout.

## Trigger rows and invariants this change was checked against

- **Spark detection layer row, clause (4) — APPLICABLE, not "not triggered".** An earlier draft of
  this ledger stated this row was "not triggered" because this PR touches no `spark_engine.*`/
  `spark_mechanism.*`/`guardian_spark_*` file and calls neither of `hard_exit.hpp`'s
  `wait_for_workers_to_drain()`/`OrphanExitGuard` by name. **That was a truth defect, caught in
  adversarial review**: `log_handoff.cpp` `#include`s `hard_exit.hpp` and calls
  `hard_exit(kLogTeardownExitCode)` at two sites (`teardown()`'s catch-all and its
  `ShutdownDeadlineGuard`-default action) implementing exactly the pattern clause (4) governs —
  "`hard_exit()` rather than race teardown against an unjoinable worker" — and the row's own
  "Loaded by" column lists `hard_exit.hpp` as a trigger path regardless of which specific primitive
  in that file is used. **Applicability proof (post round-2 fix):** the unjoinable-worker case here
  is `~spdlog::details::thread_pool()`'s blocking join, guarded exactly as clause (4) requires — see
  the `DrainGate`/`WorkerExitSignal`/`ShutdownDeadlineGuard` rows above for the corrected mechanism.
  Two rounds of adversarial review were needed to close this fully: round 1's drain-reader lease
  closed the interleaving specific to `drain_log_bounded()` (found and reproduced by both round-1
  reviewers), but round 2 (Fable) found and reproduced a WIDER door the lease alone did not cover —
  ANY ordinary producer thread's `logger()->info()`/`flush()` call can transiently hold the same
  strong pool reference `spdlog::async_logger::sink_it_()`/`flush_()` take internally, which the
  `WorkerExitSignal` mechanism closes by anchoring teardown's wait to the pool's worker thread's own
  genuine exit rather than to any specific caller's `shared_ptr` reset. This row does NOT concern
  `spark_engine.*`/`guardian_spark_*`/`GuardianDetachedWorkerRole` (this diff touches none of those
  and arms no Spark mechanism) — it concerns the shared `hard_exit.hpp` primitive clause (4) names,
  which this diff DOES use, correctly, after both fixes above.
- Durable agent command idempotency row: not triggered — no `command_dedup_store.*`/Subscribe command
  loop code is touched.
- `common/include/` firewall: not triggered — `log_handoff.{hpp,cpp}` lives in `agents/core/src/`, not
  `common/include/`.
- Resource ownership / RAII (`docs/cpp-conventions.md`): every new owner above is either a `shared_ptr`
  with a single, clearly-stated release point, or a plain atomic/mutex-guarded scalar. No raw `new`/
  `delete`, no manual thread join outside `~thread_pool()`'s own (spdlog-owned, unmodified) join, no
  borrowed `string_view`/`span` stored past its source's lifetime.
- Known and recorded, not resolved here (PR-2's job): nothing in the shipped binary installs, calls, or
  tears down this primitive yet — `main.cpp`, `service_win.cpp`, `guardian_engine.cpp`, and
  `guardian_spark_runtime.cpp` are byte-for-byte unchanged by this PR. The synchronous-logging hazard
  this primitive exists to retire (`docs/spark-flip-gate.md` section 7) therefore remains exactly as it
  was before this PR until PR-2 lands.
