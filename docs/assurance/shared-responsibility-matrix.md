# Shared Responsibility Matrix

<!-- yuzu:anchor release=v0.13.0 -->

**This matrix describes `v0.13.0`, the latest tagged release — every row
is verifiable at that tag (`git show v0.13.0:<path>`).** A few rows below
describe a control that is SQLite-backed, or documentation/design-only
rather than implemented, at this release even though a fuller posture is
underway on `dev`-HEAD; those rows say so explicitly in place rather than
describing the `dev`-HEAD state as current. See
`docs/assurance/security-whitepaper.md` for the companion narrative and
its own "Not in v0.13.0" appendix, which this matrix is consistent with.

Workstream G (Customer Assurance), `docs/enterprise-readiness-soc2-first-customer.md`
§3.7. Yuzu is self-hosted software, not a managed SaaS — nearly every control
area below is a **shared** responsibility, split between what the Yuzu
software does automatically, what the operator (the customer running Yuzu)
must configure or execute, and what the underlying infrastructure provider
(cloud platform, on-prem datacenter, or managed-service vendor) is
responsible for. Every row cites the doc backing the claim; no cell asserts
a control this repo does not actually implement at `v0.13.0`.

**Legend:** ● = full responsibility · ◐ = shared/partial · — = not
applicable to this party. "Planned" in a cell means not shipped yet — see
the cited doc for status.

<!-- Machine-checkable markers for the release-sensitive table rows below.
     Grouped here rather than inline per-row because an HTML comment line
     inside a GFM table terminates the table early — this block is the
     closest placement that doesn't break rendering; each id names the row
     it governs. -->
<!-- yuzu:claim id=matrix-ca-custody-sqlite status=shipped evidence=server/core/src/ca_store.cpp#sqlite3 -->
<!-- yuzu:claim id=matrix-ca-custody-postgres status=planned evidence=server/core/src/ca_store.cpp#PgPool -->
<!-- yuzu:claim id=matrix-secrets-mfa-plaintext status=shipped evidence=server/core/src/auth_db.cpp#mfa_totp_secret BLOB -->
<!-- yuzu:claim id=matrix-secrets-mfa-envelope-encryption status=planned evidence=server/core/src/auth_db.cpp#SecretCodec -->
<!-- yuzu:claim id=matrix-ha-postgres-not-shipped status=planned evidence=docs/user-manual/ha-postgres.md#Patroni -->
<!-- yuzu:claim id=matrix-rbac-store-sqlite status=shipped evidence=server/core/src/rbac_store.cpp#sqlite3 -->
<!-- yuzu:claim id=matrix-rbac-authorize-list-read-docs-only status=planned evidence=server/core/src/authz_gates.cpp#authorize_list_read -->

| Control area | Yuzu (software) | Operator (self-hosting customer) | Infrastructure provider (cloud/on-prem host) | Source |
|---|:---:|:---:|:---:|---|
| **Secure-deployment configuration (read this row first)** | ◐ Ships every control below in code, but four ship OFF or at their weakest setting by default: idle session timeout disabled (`session_inactivity_secs{0}`), MFA enforcement `"optional"`, local-password login enabled (`auth_mode{"standard"}`, no SSO-only enforcement), and RBAC disabled (`rbac_enabled='false'` at first boot) | **● Explicitly reconfigure all four before treating the corresponding rows below as active**: `--session-inactivity-secs` (recommended `900`), `--mfa-enforcement admin-only\|required`, `--auth-mode sso-only` (requires OIDC first), and the RBAC Settings-page toggle / `yuzu-server.cfg`'s `[rbac]\nenabled = true` (read `docs/user-manual/rbac.md`'s pre-enable checklist first — a lockout risk if flipped without an existing management-group role grant) | — | whitepaper §3.8 |
| **Network transport encryption (TLS/mTLS)** | ● Auto-generates a per-install CA; shipped image `CMD`s omit the `--no-tls`/`--no-https` flags that exist in the server binary, so TLS is on by default — **not uniformly mutual**: agent→gateway (`:50051`) is one-way (server-authenticated) TLS by design, direct agent→server and both gateway↔server hops are mutual TLS (whitepaper §1 per-hop table) | ◐ Decide whether to trust/replace the auto-generated CA; front the dashboard with a reverse proxy if desired; keep `:50051` off the public internet | ◐ Network ACLs/firewalling/VPC boundary that the "don't expose :50051" guidance depends on | `docs/pki-architecture.md` |
| **CA / key custody** | ◐ Root private key never stored in the app database (behind `KeyProvider`); CA metadata, issued-cert inventory + CRL versions live in a local SQLite `ca.db` file **at `v0.13.0`** (`dev`-HEAD migrates this to a `ca_store` PostgreSQL schema, ADR-0053 — see whitepaper appendix; the key itself is unaffected either way). **Qualification:** "never in the database" describes only the live application boundary, not a guarantee that a *backup* of the CA/keys directory stays protected — this branch's shipped backup procedure (`docs/operations/disaster-recovery.md`, `scripts/yuzu-backup.sh`, both unmodified from `origin/dev`) writes that identical root private key to a file with no `chmod`/`umask` handling; the fix is tracked in **issue #4135** — treat this row as ● only once that fix has landed in the procedure you run, ◐ (shared) until then. **Adjudication:** an earlier round of review derived this at CRITICAL; on re-review four independent readings (including the control's domain owner) derived LOW — a real, disclosed, operator-remediable gap (`umask 077`/`chmod 0700` on the backup output closes it), not a false vendor-owned assurance claim once this row states it accurately, which it now does. Recorded at LOW per that adjudication. | ● Choose/operate the `KeyProvider` backend for production (HSM, cloud KMS, or the default); back up the certs volume alongside every database backup (paired restore requirement); **protect that backup with the same discipline as the live key** — `umask 077`, `chmod 0700` on the backup directory, `0600` on the archive — this is the operator's custody, not something the software enforces on a backup artifact it doesn't create the permissions for | ◐ HSM/KMS availability and its own access control, if used as the `KeyProvider` backend | `docs/pki-architecture.md`, `docker-compose.reference.yml` header; fix tracked in issue #4135 |
| **Secrets-at-rest encryption** | ◐ Envelope encryption (`SecretCodec`) for secrets in the stores that use it (e.g. offline-endpoint, vuln-finding); **the MFA TOTP secret in `auth.db` is plaintext at `v0.13.0`, protected only by the file's `0600` mode** — envelope encryption for it is `dev`-only (see whitepaper §2.2 and appendix) | ◐ Operate the KEK provider (`KeyProvider`/`KekProvider`) for the secrets that use it; for MFA specifically, protect `auth.db` backups with the same discipline as the CA key row above until the encryption lands | — | `docs/adr/0010-secrets-at-rest-envelope-encryption.md`, `docs/ops-runbooks/auth-db-recovery.md` |
| **Host OS patching / hardening** | — | ● Patch the OS, container runtime, and Docker/Podman engine hosting Yuzu | ◐ Underlying hypervisor/hardware patching (cloud) or none (on-prem, fully operator-owned) | — |
| **PostgreSQL substrate — provisioning & patching** | ◐ Ships a reference Postgres image + compose service for convenience; fully supports pointing at an external/managed instance instead | ● Choose and operate the Postgres instance (bundled or external/managed); apply Postgres security patches on the bundled path | ◐ Managed-Postgres provider (if used) owns patching/backups of the underlying service | `docker-compose.reference.yml` header, ADR-0006 |
| **PostgreSQL substrate — HA / failover** | — **Not shipped at `v0.13.0`.** A Patroni-managed 3-node profile with measured automatic-failover RTO/RPO exists only on `dev`-HEAD (`docs/user-manual/ha-postgres.md` does not exist at the `v0.13.0` tag) — see whitepaper appendix | ● Choose whether to deploy the HA profile vs. single-node once it ships; size the quorum | ◐ Underlying compute/storage availability the HA profile would assume | whitepaper appendix (not shipped at this release) |
| **Backup execution** | ◐ Documents the exact `pg_dump`/`pg_restore`/`tar` procedure and provides `scripts/yuzu-backup.sh`/`yuzu-restore.sh` for the SQLite/config half — **this branch's copies are unmodified from `origin/dev` and carry NO permission handling** (no `chmod`/`umask` anywhere in `docs/operations/disaster-recovery.md` or either script, verified) | ● Schedule and run backups (**no automatic cron/systemd-timer ships today** — this is a documented manual/scriptable procedure, not an automated one); store backups off-host; test restores periodically; **protect the backup artifacts at rest yourself** — `umask 077`, `0700` directories, `0600` files, on every artifact this procedure produces, not only the CA/keys one (this duty is REAL and UNDISCHARGED in this branch's shipped runbook — the fix is tracked, not yet merged, so do not wait for the software to do this for you) | ◐ Storage durability for wherever backups are written | Restore drill and the corrected DR procedure: tracked in issue #4135; not part of this branch's documentation set |
| **Restore execution & DR** | ◐ Procedure is documented in `docs/operations/disaster-recovery.md`; **not executed on this branch** — the executed drill and its measured timings are tracked separately | ● Execute the restore when needed; own the RTO/RPO the operator's own backup cadence delivers | ◐ Infrastructure to stand the restored stack back up on | Restore drill and the corrected DR procedure: tracked in issue #4135; not part of this branch's documentation set |
| **Server-listener availability (`/readyz`)** | ◐ Single process today; SLO target reflects that (99.5%/30d) — a second-replica HA posture is planned (ADR-2002 Phase B), not shipped | ● Deploy behind a load balancer / process supervisor for restart-on-crash; monitor the proposed dead-man's-switch alert once wired | ◐ Compute availability the process runs on | `docs/ops-runbooks/slo.md` §1 |
| **RBAC configuration** | ◐ Enforces the permission model and management-group scoping (`RbacStore`, a SQLite `rbac.db` file at `v0.13.0` — the PostgreSQL migration, ADR-0041, is `dev`-only). **The admit-then-filter `authorize_list_read` chokepoint (ADR-0017) is documentation/design only at `v0.13.0` — no implementation exists in source at this tag** (confirmed: `git grep -l authorize_list_read v0.13.0` matches only docs); list-shaped reads at this release rely on the bare-global-permission pattern for every route, not only an unmigrated handful — see whitepaper §3.5 and appendix | ● Define roles, management-group hierarchy, and grant assignments for their own organization | — | `docs/user-manual/rbac.md`, `docs/user-manual/management-groups.md`, `docs/auth-architecture.md`, whitepaper §3.5 |
| **SSO / identity federation (OIDC, SAML, SCIM)** | ● Implements OIDC/SAML SP + SCIM provisioning, group→role mapping, provenance guard, deprovision propagation | ● Configure and operate the IdP itself (Okta, Entra, etc.); own the IdP-side group membership that Yuzu maps from; re-authenticate sessions after an IdP-side revoke to close the #1836 propagation-delay window | ◐ IdP uptime/availability (a third-party SaaS, out of Yuzu's or the operator's direct control) | `docs/auth-architecture.md` §§3.2-3.3 |
| **MFA enrollment & enforcement** | ● Implements TOTP MFA + JIT elevation mechanics | ● Decide which actions require MFA step-up; enroll admin accounts | — | `docs/auth-architecture.md` "MFA / TOTP" |
| **Agent deployment & enrollment** | ● Per-agent mTLS issuance, enrollment tiers, stable C-ABI plugin host | ● Deploy the agent package/bundle to managed endpoints; approve/deny enrollment per fleet policy | ◐ Endpoint OS/hardware the agent runs on | `docs/architecture.md`, `docs/pki-architecture.md` |
| **Monitoring / alert response** | ◐ Ships 115 Prometheus alert rules + 1 recording rule (116 total per `promtool check rules`, `docs/prometheus/yuzu-alerts.yml` — measured at the time of this writing, not CI-bound to any doc that quotes it; re-derive with `promtool check rules` rather than trusting this number, the file is actively churning). **No stack on this branch loads them** (#2857 remains open here) — a wiring attempt, its own infrastructure defects (stickiness, false-positive checks, reload-staleness, missing self-scrape, missing retention config), and their fixes are all tracked separately (issue #2857); this branch's assurance claims do not depend on that work having landed | ● Wire an Alertmanager (**not shipped by this repo** — routing is entirely the operator's to build) and staff the on-call rotation that responds to a page | ◐ Alerting egress (email/Slack/PagerDuty integration) is whatever the operator's Alertmanager sends to | `docs/prometheus/yuzu-alerts.yml`; overlay status: issue #2857 |
| **Incident response process** | — | ● Own the detection→triage→containment→customer-communication→postmortem lifecycle | — | `docs/enterprise-readiness-soc2-first-customer.md` §3.4 |
| **Supply-chain verification** | ● Signs (`cosign`), attests (SLSA v1.0), and SBOMs (CycloneDX + SPDX) every release artifact | ◐ Actually run the verification steps before deploying a new release (the tooling is provided, the choice to invoke it in a deploy pipeline is the operator's) | — | `docs/user-manual/release-verification.md` |
| **Audit log retention & export** | ● Clock-guarded, capped retention sweep; append-only by policy (not cryptographically tamper-evident — no hash chain exists today); `503`/`Sec-Audit-Failed` fail-hard writes + deny-on-degrade reads scoped to behavioural-PII REST reads specifically (dashboard HTML + MCP are set-and-proceed on a write failure) | ● Decide `audit_retention_days` for their compliance window; export/forward to an external SIEM if required; restrict direct database access (the access-control boundary this control actually relies on, absent a cryptographic one) | — | `docs/user-manual/audit-log.md`, `docs/ops-runbooks/audit-store-clock-guard.md`, `server/core/src/rest_audit.hpp` |
| **Data residency / hosting location** | — | ● Choose where to deploy Yuzu (self-hosted software has no inherent data-residency constraint of its own) | ● Physical/regional location of the chosen infrastructure | — |
| **Vulnerability management (of Yuzu itself)** | ● Dependabot/OpenSSF scorecard program on the codebase; signed releases | ◐ Apply updates promptly; subscribe to release notes/security advisories | — | `docs/user-manual/release-verification.md` |

## Notes for the reviewer

- This matrix intentionally does not claim Yuzu is a managed service — every
  "Operator" column entry reflects that self-hosting carries real
  operational responsibility, which is the honest answer for this product
  shape.
- Any cell not backed by a "Source" citation above is a general shared-hosting
  responsibility (host patching, incident process, data residency) rather
  than something specific to Yuzu's implementation — flagged with `—` in the
  Yuzu column rather than invented.
- See `docs/assurance/security-whitepaper.md` for the narrative version of
  the controls summarised here.
