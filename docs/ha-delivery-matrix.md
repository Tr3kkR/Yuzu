# High Availability — Delivery Matrix

The delivery spine for **ADR-2002 (High Availability Architecture)**. ADR-2002 records the *model and
principles*; this matrix tracks *what ships, in what order, who reviews it, and how we know it's done*.
Workstreams WS-0…WS-10 come from ADR-2002 §Decomposition; WS-11…WS-14 are delivery/ops workstreams the
three-model adversarial review (Fable + gpt-5.6-sol + Kimi-K2.7) surfaced as missing. If a row here
disagrees with ADR-2002, the ADR wins.

**Companion docs:** `docs/adr/2002-high-availability-architecture.md` (the decisions),
`docs/postgres-migration-ladder.md` (authoritative per-store migration status),
`docs/adr-1005-execution-plan.md` (presentation/core/engine split + engine-tier + MCP Decision-15 work
this matrix *shares*, not duplicates).

## Two independent axes (read first)

- **Server-tier HA** — N presentation + core replicas (WS-0…WS-6, WS-8, WS-10, WS-11, WS-13).
- **Storage HA** — HA Postgres (WS-7). A single Postgres is a valid, supported deployment; **server-tier
  HA does not require it.**

"HA" as a customer claim = **both** axes + WS-9 (validation) + WS-11/WS-12 (operability).

## ⚠️ The safe-to-scale gate (read before sequencing)

**Do not equate "Phase A done" with "safe to run a second replica."** The three-model review's
strongest, unanimous finding: a second server replica is safe **only after the full prerequisite set
lands** —

> **Gate — a second server replica may be enabled only when: WS-0, WS-1, WS-2(outbox), WS-3, WS-4,
> WS-5, WS-6, WS-10 are done, and WS-8's per-tier `/readyz` exists.**

Miss any one and the failure is not cosmetic: without **WS-3** (fenced leader) both replicas run the
singleton workers → **double-dispatch of destructive commands** (software-deployment, quarantine,
policy-remediation); without **WS-4/WS-5** commands can't reach agents / scope evaluation drops agents
on the replica that lacks the local stream → mis-targeting; without **WS-6** CRL numbering / enrollment
diverge across replicas. **WS-7 (HA Postgres) is NOT in this set** — it gates the *storage* axis (no
SPOF / RPO=0), not the second *server* replica (which is safe against a single Postgres). The
"Gates 2nd replica?" column below encodes this per row.

## Is the presentation/core split a prerequisite? — RESOLVED: no, decoupled (ratified 2026-09-07)

- **Ratified by the ADR-1005 owner (Dave Rae), 2026-09-07:** monolith active-active is the target; the
  presentation/core split is a **parallel programme, not a gate** for a 2nd replica. The single
  in-process thing that can't go active-active (the direct agent `Subscribe` stream) is solved by
  **gateway-fronting (WS-4)**, not the split. Phase-B HA work proceeds on the monolith track; the split
  is decoupled and does not reshuffle it.
- **Kimi's dissent (recorded, rebutted):** Kimi called the split a hard safety prerequisite. Every
  safety property it could mean — singleton double-dispatch (WS-3), in-memory sessions (WS-1a, **done**),
  stream routing (WS-4) — is closed in the monolith. What the split buys is presentation scale-out +
  separate readiness signals: **capacity, not safety.**
- **One-way dependency (the split consumes HA, never the reverse):** the split *inherits* WS-1 sessions,
  *rides* WS-2a's durable `event_outbox` for its core→presentation event spine, and takes MCP replay
  durability from WS-2b. WS-2b spine and WS-8 tier-split `/readyz` are no-ops on the monolith and land
  with the split. Split delivery is tracked in its own control plane —
  `docs/presentation-core-split-delivery-matrix.md` (the `/split` skill) — not here.

## Status legend

**Status:** `planned` · `in-progress` · `blocked` · `done`. **Prio (build-effort tier):** P0 · P1 · P2.
**Gates 2nd replica?** whether the safe-to-scale gate above depends on this row (the load-bearing
column — distinct from Prio).

## The matrix

| WS | Delivers | Current state (symbol / cite at HEAD) | Depends on | Gates 2nd replica? | Reviewers (routed) | Prio | Status |
|----|----------|--------------------------------------|-----------|:---:|--------------------|------|--------|
| **WS-0** | Durable agent-side command idempotency **+ terminal-outcome replay** (dup replays stored result, not bare `REJECTED`) | **Shipped**: `CommandDedupStore` (`command_dedup_store.{hpp,cpp}`, agent SQLite `command_dedup.db`, `synchronous=FULL`, terminal-only ring); wired in `agent.cpp` claim/`record_command_terminal`/release. Follow-ups open: stale-in-flight reconciliation, fleet-side observability (WS-11) | — | **Y** | `cpp-safety` + `cpp-expert` | P0 | **done (PR #3662 merged to dev)** |
| **WS-1** | Server-plane state → Postgres. **Milestones:** (1a) sessions (DB-time); (1b) `execution_tracker` + command-correlation w/ atomic counters; (1c) HA-critical store subset | **1a DONE** (durable `SessionStore`, write-through + generation validate-cache, AND DB-clock authority: timestamps authored from Postgres `now()` in-SQL, `find` returns `db_now_ms` in the same read, adjudication derives a local monotonic `steady_clock` deadline with clamped ceilings + a wall suspend-backstop — `session_store.{hpp,cpp}`, `auth.cpp`, #3715; cross-host skew fixed, the multi-replica prerequisite closed). **1b DONE**: `execution_tracker` migrated to Postgres (ADR-0065, atomic in-transaction counter recompute); command-correlation moved off the in-proc `cmd_execution_ids_` map into `ExecutionTracker`'s PG-backed `command_execution` table (`record_command_execution`/`lookup_execution_id`, clock-guarded retention sweep) — a response landing on any server replica now resolves. **1c DONE**: ADR-2002 §9 deliberately states *criteria* for "HA-critical," not a fixed store list ("a point-in-time list here drifts"); every store matching those criteria (`execution_tracker`, `schedule_engine`, `ca_store`, `instruction_store`+`product_pack_store`, `tag_store`, `device_token_store`, `baseline_store`, `quarantine_store`, `software_deployment_store`, `policy_store`+`approval_manager`/`workflow_engine`) shows `Migrated Postgres` on `docs/postgres-migration-ladder.md` as of Wave 4's close (2026-08-30), each with its HA-shaping work (atomic-counter SQL, advisory-lock coordination, race-safe upserts) folded into its own per-store ADR rather than deferred. `nvd_db` is explicitly withdrawn by §9 itself (moved to the engine tier per ADR-0023/ADR-1005 Phase 7). `concurrency_manager` is actually NAMED by §9 criterion 2 as HA-critical ("fleet-wide limits") — it is excluded on a DIFFERENT basis, the ladder's own note: zero production callers anywhere in `server.cpp` (verified 2026-08-27), compiled and unit-tested but never instantiated, so it has no LIVE state that can diverge across replicas; if it is ever wired up it must migrate first, per that same ladder note. **WS-1 is fully closed** on the current criteria/ladder state. | migration ladder (per store; **serializes at the migration-version counter**, not fully parallel) | **Y** | `authdb`+`security-guardian` (1a); `architect`+`sre`+`cpp-safety` (1b/1c) | P0 | **done — 1a+1b+1c (#3702, #3715, HA WS-1(1b), 1c subsumed by the general Postgres-migration Wave 4 close)** |
| **WS-2** | (**2a**) durable **event outbox** + NOTIFY fan-out [monolith-OK]; (**2b**) **core→presentation event spine** [*defers to ADR-1005 split*]; MCP session/replay durability | **2a-1 DONE**: durable append-only `event_outbox` table (ExecutionTracker migration v4, global monotonic `event_id`), the ExecutionEventBus events (`agent-transition`/`execution-progress`/`execution-completed`) appended ATOMICALLY inside each state-write txn (`upsert_agent_status_once`/`refresh_counts_once`/`mark_cancelled`), clock-guarded `reap_event_outbox` (24h). No consumer yet — bus still serves live SSE. **2a-2 DONE (#3924)**: cross-replica delivery — each replica runs a ~2s poll (`poll_event_outbox_once`) that re-publishes execution events which originated on OTHER replicas onto its own live SSE bus, cursored on a Postgres commit-settle horizon (`w_xid < pg_snapshot_xmin(pg_current_snapshot())`) so it never straddles an in-flight event, skips own rows via `origin_replica`, at-least-once. The live SSE `id:` stays the reconnect-safe per-channel counter (the durable `event_outbox.event_id` is kept for the poll cursor + the later durable failover-replay slice, NOT on the live bus). Loss-free reconnect across replicas (durable outbox replay ORDER BY `event_id`) is a 2a-2 follow-up + precondition for a 2nd replica. 2b/MCP session-replay durability outstanding. | 2a: WS-1(1b) ✓. 2b: **ADR-1005 split** | **Y** (2a) | `architect`+`sre`+`security-guardian`(MCP)+`docs-writer` | P1 | **2a done (2a-1 + 2a-2 #3924); 2b/MCP outstanding** |
| **WS-3** | Coordination seam: **fenced `LeaderElector`** (monotonic epoch checked in claim txn) + leader/**transactional-outbox**/receiver-idempotency worker refactor incl. policy remediation | **3.1 DONE (#4011)**: fenced `LeaderElector` primitive — dedicated never-recycled coordination connection + session advisory lock + monotonic epoch minted into `leader_state`; the fence is an embeddable `epoch_fence_sql()` predicate spliced into the claim's WRITE (NOT a check-then-act boolean read). INERT — no loop wired yet, so no runtime behaviour change. Workers still dispatch per-proc; Deployment CAS + sweep advisory locks already correct. Follow-ups: #4013 (3.2 connection robustness — folded into 3.2), #4014 (WS-11 leader metrics + /readyz), #4015 (3.3 fence claim-protocol), #4016 (routed-concern row — landed with 3.2). **3.2 DONE (#4134 merged)**: election loop wired into `ServerImpl` (dedicated coordination connection, connect_timeout+keepalives+tcp_user_timeout, off-`mu_` lock-free `is_leader()`/`epoch()`, backoff+jitter — #4013); the four `FencedLeaderOnly` passes (`schedule_runner.tick`/`policy_evaluator.dispatch_due`/`quarantine_reconciler.tick`/`ca.publish_crl`) gate leader-only via `leader_gate.hpp` consuming WS-10's `background_jobs.hpp` classification (anti-drift — no second list); the WS-2a event poll AND `policy_evaluator.collect_ready` (the operator evaluate/remediate completion path — PR #4134) stay per-replica; the operator-synchronous CRL/remediation planes stay unfenced. `quarantine_reconciler.tick` reclassifies stream-partitioned at WS-4 (#4119). Split question RESOLVED: decoupled (ratified by the ADR-1005 owner 2026-09-07 — see the section above; Phase-B HA proceeds on the monolith track). **Exit-criteria note (governance, WS-3 3.2 sre/compliance):** #4014 (WS-11 leader identity/epoch + CRL-freshness-staleness metrics + split-brain alert + the `/readyz` decision) is a **HARD prerequisite before a 2nd replica or any WS-9 failover scenario** — 3.2's pause-forever degraded state (a lost coordination connection silently pauses schedule/policy/quarantine/CRL) is LOG-ONLY today, acceptable while single-replica but blind on active-active. And on multi-replica the SCHEDULED policy-remediation dispatch + quarantine-reconcile **audit rows emit leader-only** (an audit evidence-topology change — one emitter, not N — that an auditor mapping coverage-per-node must account for; extend with the #4119 stream-locality reclass when WS-4/5 land). NOTE (PR #4134): this is the *scheduled* half only — an OPERATOR-initiated evaluate/remediate completes (and emits any completion audit) on the *accepting* replica, since `policy_evaluator.collect_ready` runs per-replica. **3.3 DONE (PR #4169 merged)**: the durable transactional command outbox — born-on-PG `CommandOutboxStore` (occurrence_id-PK idempotency + embedded `epoch_fence_sql()` defense-in-depth) + the leader-gated `CommandOutboxDelivery` loop (new `command_outbox.deliver` FencedLeaderOnly pass); `ScheduleRunner` now ENQUEUES a durable occurrence instead of dispatching inline, and the delivery loop re-authorizes + dispatches on the plain confined path (R1: NO ADR-1007 concurrency claim — a re-drive is absorbed by the agent's WS-0 command_id dedup). Effectively-once, not exactly-once. ADR-1007 per-device concurrency is **retired for the scheduled/outbox path** (was enforced inline) — restore tracked #4165. Same-txn is a documented §6 exception (ScheduleRunner uses stable-occurrence-key idempotency, not literal same-txn — see ADR-2002 §6). Follow-ups: #4166 (retention reap + collision guard), #4167 (dead-letter/attempts-cap), #4168 (delivery audit-emission + seam tests). **3.4 DONE**: the PolicyEvaluator §6-finding-6a remediation redesign — a durable per-(policy,agent) claim via `PolicyStore::claim_remediation` (guarded-column CAS on `remediation_claim_at`, migration v2), claim-before-dispatch, release at FixWait maturation or stranded-fixing sweep. Mechanism is a plain guarded CAS on an ordinary pooled connection, NOT the epoch fence — the operator-synchronous path runs on any replica. Both findings (6a) addressed: `sent_count` threaded through, and failed delivery does not burn retry attempts. Behavioral change: remediate() for exhausted retry cap now refused at claim time (HTTP 409). | **WS-0, WS-1, WS-2(2a outbox)** | **Y** | `architect`+`cpp-safety`+`security-guardian` | P1 | **3.1 done (#4011); 3.2 done (#4134); 3.3 done (#4169); 3.4 done** |
| **WS-4** | Gateway routing + multi-cluster: fenced `agent→cluster` directory, **net-new distributed intra-cluster agent→node routing**, `gateway_node` convergence | `pg` broadcast-only; per-agent lookup node-local ETS (`yuzu_gw_registry.erl:90`); `gateway_node`+caps now via `set_gateway_route` (single call; NotifyStreamStatus `:637`/`:660`). **4.1 DONE**: born-on-PG fenced `GatewayRouteStore` (`gateway_route_store.{hpp,cpp}`, schema `gateway_route_store`, migration v1 — see `docs/postgres-migration-ladder.md`) — the agent→cluster routing directory (`agent_routes(agent_id PK, cluster_id, gateway_node, connection_epoch, session_id, lease_until, updated_at)` + `connection_epoch_seq`). A SERVER-minted monotonic `connection_epoch` (guarded upsert `WHERE EXCLUDED.connection_epoch > stored`) orders a fresh `ProxyRegister` against an existing row — server-internal ordering only, never on the wire; `session_id` equality guards every post-register write (`announce_connected`/`deregister`/`renew_leases`), so a delayed replay from a superseded session cannot overwrite or tear down a newer re-home in the DURABLE STORE (this guarantee was store-only until HA WS-4 4.4's post-build review closed the matching in-memory `AgentRegistry::set_gateway_route` gap — see the 4.4 BLOCKER 2 fix below and `gateway_route_store.hpp`'s SESSION GUARDS section for the corrected, now-end-to-end claim). Wire gained `StreamStatusNotification.cluster_id = 7` (4.1) and later `stream_home_id = 8` (`#4324`, below); the gateway now carries the agent's existing `x-yuzu-session-id` on a circuit-recovery ProxyRegister REPLAY so the server re-announces (reuses the session) instead of minting a new one. **INERT**: written on connect/disconnect/heartbeat, nothing reads it for dispatch yet (that's 4.2). Posture: construction fail-CLOSED (ADR-0012); runtime write failure was fail-**OPEN** on all six writer call sites through 4.2a — **4.2b Task B DONE**: a PER-SITE fail-closed contract (not a uniform flip, and landed independent of dispatch-authority) — `register_fresh` (the one row-CREATING write) now returns `UNAVAILABLE` on a degraded store, mirroring the #3401 `register_agent` precedent; the other five writes (`announce_connected`, both ProxyRegister `renew_leases` branches, BatchHeartbeat's `renew_leases`, `deregister`) stay fail-open by design (see `record_route_store_failure`'s header comment, gateway_service_impl.cpp, for the per-site rationale). Same slice also closed `#4246` #10: `renew_leases` now correlates BOTH `agent_id` AND `session_id` (parallel-array `unnest()` join), not `session_id` alone, against the DURABLE binding already stored in the directory (the trust anchor, never a caller-supplied value) — `BatchHeartbeat`'s `agent_id` is resolved from the same per-replica `gateway_sessions_` map as the `#4246` #3 known-session check below (the wire's `HeartbeatRequest` carries no `agent_id`), so #10 inherits #3's per-replica-in-memory limitation rather than introducing a new one: a heartbeat for a locally-unknown session is excluded from the renew batch and now surfaced via `yuzu_server_gateway_route_desync_total{op="renew_leases",outcome="unknown_session"}` instead of silently dropped, unreachable on today's single-replica monolith. **4.2a DONE** (writer-path hardening, `#4246` items #2/#5/#7/#8-counters/#9 — **#4 RE-SCOPED, not closed** (see below)): `deregister` tombstones instead of deleting (closes the late-CONNECTED **resurrection** direction — #5 — so a late CONNECTED for a now-gone session no-ops against the tombstone via `announce_connected`'s fallback INSERT); a clock-guarded background reaper `GatewayRouteStore::reap_stale_routes()` (`ReplicaSafe`, `background_jobs.hpp`, ~5-minute cadence) sweeps expired-past-grace leases and purge-aged tombstones; `ProxyRegister`'s mechanism-(c) branch (a presented-but-locally-unknown session) now renews the presented session in the directory instead of calling `register_fresh`, closing the fresh-branch epoch-clobber gap; a new `yuzu_server_gateway_route_desync_total{op,outcome}` counter gives guard-rejection visibility distinct from the write-failure counter; hot-path writers dropped to a 500ms timeout (reaper keeps 2000ms). Directory was **still INERT as of 4.2a** — 4.2 itself (dispatch-authoritative wiring) landed later, in 4.2b (below). **4.2b Task A DONE**: `announce_connected` is now the sole writer of placement (`register_fresh`'s guarded upsert NULLs `cluster_id`/`gateway_node` on a winning re-register instead of COALESCE-preserving them) and a batched `lookup_routes(span<agent_id>)` read landed (routable computed in-SQL against the DB clock). **4.2b Task C DONE**: `lookup_routes` is now wired into confined dispatch as a FALLBACK-ONLY consult (`dispatch_route_fallback.hpp`'s `GatewayRouteFallback`) — one batched read per arm walk, fired ONLY for a candidate with no local `AgentRegistry` session, and every send still passes the existing `authz::in_scope` confinement intersection before going out. **Changes ZERO monolith routing outcomes** (proven: `[dispatch]` 2163 assertions unchanged-green + a decoy-directory-row test): a direct-connected agent never gets a directory row, so a local miss on today's single-replica monolith still means the agent is genuinely absent. A routable local-miss queues via the new `AgentRegistry::send_via_directory` onto `gw_pending_` carrying an optional `cluster_id`, inert until 4.3 (one `gw_mgmt_stub_` today). Defined+produced `ConfinedDispatchOutcome::route_unreadable` for a degraded batched read (`store_unavailable`/`db_error`). **4.2b Task D DONE**: consumes `route_unreadable` — the command outbox delivery loop RESCHEDULES on it (mirrors `containment_unreadable` exactly; new `yuzu_server_command_outbox_deliver_retry_cause_total{cause}` counter), it is discriminated at the four zero-reach cascade sites that can actually reach it (`#3424`/`#3511`: `mcp_server.cpp`/`command_routes.cpp`/`workflow_routes.cpp`/`dashboard_routes.cpp` — `server.cpp`'s legacy-forward site is Broadcast-only and correctly has no `route_unreadable` branch) plus two additional consumers a grep of every `containment_unreadable` site caught (`deployment_engine.cpp`, `policy_evaluator.cpp`), and both `#4246` alert-rule halves shipped in `docs/prometheus/yuzu-alerts.yml`'s `yuzu-gateway` group: `YuzuGatewayRouteWriteFailed` (#1, rate-based on the write-failed counter) and `YuzuGatewayRouteUnreadable` (the closest available signal for #8 — keyed on the `route_unreadable`-caused retry, NOT a direct alert on every `yuzu_server_gateway_route_desync_total` guard-rejection outcome, which stays unalerted pending real fleet background-rate data). **4.2b's review RE-VERIFIED the once-per-session property (`#4324`) before Task C landed** (`governance.d/ha-ws4-42b-4324-reverification.md`: single `CONNECTED` emit site unchanged at `yuzu_gw_agent.erl:129`, atomic `ets:take/2` admission in `yuzu_gw_registry.erl:234-236` unchanged by the merged 4.2a #4299, concurrent-barrier test present and green in `yuzu_gw_registry_tests.erl:38-39`) — the fence itself remained deferred at that point. **`#4324` (the per-home stream-generation fence) is now CLOSED end-to-end and MERGED to `origin/dev`** (PR #4492, merge commit `c37306113`, 2026-09-17T22:05:58Z; three tasks): task 1 (`46f1e72b6`) adds `StreamStatusNotification.stream_home_id = 8` — an opaque id the gateway mints once per `yuzu_gw_agent` process instance (`crypto:strong_rand_bytes(16)`, hex-encoded) and stamps on both the CONNECTED and DISCONNECTED notification that instance ever sends; task 2 (`4b248b504`) adds a nullable `agent_routes.stream_home_id` column (migration v3, `docs/postgres-migration-ladder.md`) plus an ASYMMETRIC tombstone predicate on `deregister` — **`stream_home_id IS NULL OR stream_home_id = $3`** (see the PR-review fix below; an EARLIER form of this predicate was buggy and is documented there, not here) — under which a STAMPED stored home id requires an EXACT match to be torn down, never a row a stamped CONNECTED has since re-homed (safe across a rolling gateway upgrade, mixed old/new-build nodes being the normal state of one); task 3 (`feabccea7`) threads the real `stream_home_id` through `AgentRegistry::set_gateway_route`/a new `gateway_stream_home_id()` accessor and restructures `NotifyStreamStatus`'s DISCONNECTED branch to resolve the fence ONCE, at the top, before ANY of the three teardown effects (registry clear, store deregister, `gateway_sessions_`/`lost_race_sessions_` erase) run — fencing only a subset of the three would be WORSE than the pre-`#4324` unfenced behavior (a live re-homed session left permanently untearable by its own future events). An oversized incoming `stream_home_id` (>64 bytes) clamps to empty rather than rejecting the RPC, counted via new `yuzu_server_gateway_route_desync_total{op="announce_connected" or "deregister",outcome="malformed_home_id"}`; a genuine stale-home mismatch counts as `{op="deregister",outcome="stale_home"}`. Regression test: `tests/unit/server/test_gateway_route_wiring.cpp`. This slice's own review RE-VERIFIED the once-per-session property a SECOND time before making the fence live (`governance.d/ha-ws4-4324-stream-fence-reverification.md`) — no regression found. **Post-doc-update PR-review fix (pre-merge, HIGH)**: external reviewer FortitudeEtc (Codex+Kimi panel, with empirical reproduction) found the ORIGINAL task-2 predicate — `stream_home_id = $3 OR (stream_home_id IS NULL AND $3 = '')` — wrongly required the INCOMING value to also be empty before a stored-NULL row would admit it; under the single-producer invariant, stored-NULL can also mean "this session's own `announce_connected` (from CONNECTED) hasn't run yet," and the gateway gives NO ordering guarantee between a session's own paired CONNECTED/DISCONNECTED RPCs (independently `spawn_monitor`'d), so an ORDINARY (non-re-home) connect/disconnect could have its DISCONNECTED arrive first, get misclassified `stale_home`, skip the tombstone, and let the delayed CONNECTED publish a route for an already-dead stream — a regression vs. the pre-`#4324` unfenced behavior, reachable via ordinary operational churn (no second CONNECTED/DISCONNECTED pair needed). Fixed pre-merge to the predicate shown above, `stream_home_id IS NULL OR stream_home_id = $3` (commit `f7f12bd59`, mirrored in the in-memory fence's `home_matches`); a targeted Gate 8 re-review then caught a second stale copy of the old formula on `deregister()`'s own declaration comment (`5160e0eeb`, doc-only). Both commits are part of the same PR #4492 and merged with it. **Deliberately NOT closed by `#4324`**: a stale `DISCONNECTED(home1)` landing BEFORE a live re-home's `CONNECTED(home2)` (two independent RPCs, no ordering guarantee) still tombstones the row, and `announce_connected`'s `ON CONFLICT DO NOTHING` fallback cannot re-arm a tombstoned row — **resolution UPDATED (WS-4 4.3a design review, 2026-09-18): NOT `register_fresh`'s ordered epoch** (a gateway-synthesized fresh session on re-home starves the agent's own heartbeat lease renewal — the wrong mechanism, superseded) — unreachable BY INVARIANT instead (agent-driven reconnect + the gateway's `NOT_FOUND` refusal on an unknown session), backed by a standing design constraint + a server-side tripwire folding into `#4464`; see `gateway_route_store.hpp`'s file-header FORWARD NOTE for the full resolution. The `duplicate_connected` positive tripwire on `notify_stream_status`'s desync counter that `#4324`'s own checklist deferred (needs a `gateway_sessions_` value-type change reaching several call sites) is tracked separately as `#4464`. **`#4555` update (2026-09-18, IN PROGRESS, Fable-adjudicated design — ADR-2002 §7b): gateway multi-node cluster formation implemented** — `yuzu_gw_cluster_discovery` (new gen_server, always-on fixed-interval redial loop, required because `{connect_all, false}` means nothing else in the VM self-heals a partial mesh) resolves a DNS seed name (`YUZU_GW_SEED_DNS_NAME`, default `gateway`, matching Compose's own embedded multi-A-record resolution for a scaled service — no Kubernetes-specific code needed) or an explicit static override (`YUZU_GW_SEED_NODES`, replaces DNS outright when set) to peer addresses and dials each via `net_kernel:connect_node/1`. Required a foundational node-identity change: every gateway replica now shares one fixed short name, distinguished only by a boot-time-resolved IP literal (`deploy/docker/gateway-entrypoint.sh` sets `YUZU_GW_ADVERTISE_ADDR` before the relx boot script starts the BEAM, preserving `check_distribution_cookie/0`'s ordering) — never the old hardcoded-per-node `vm.args` name, and never a hostname (the OTP handshake needs an exact match against a resolved A record). Added a minimum distribution-cookie length floor (32 chars) to `evaluate_cookie/3`: DNS-sourced dial targets let a compromised/misconfigured DNS answer mount an offline cookie brute-force from a legitimately-dialing node (the handshake's initiator sends the cookie hash first), which a hardcoded operator-typed seed list never exposed. Pinned `inet_dist_listen_min`/`max` (9100-9105) so the distribution port range can actually be firewalled. New metrics `yuzu_gw_cluster_peers_resolved`/`peers_connected` gauges + `connect_failures_total` counter + a `YuzuGatewayClusterPartiallyFormed` alert (`docs/prometheus/yuzu-alerts.yml`), since fail-open boot semantics mean a misconfigured seed name fails silently by design otherwise. Ships a new scale-capable reference rig (`docker-compose.reference-gateway-cluster.yml`) since every OTHER shipped compose file pins `container_name:`/host-published ports, which block `--scale` — without it the "free with Compose scaling" property was unverifiable in this repo. Empirically verified: `do_tick/1` (the tick body, parameterised on target list for testing) forms/reforms a real distribution connection against a real OTP `peer` (`yuzu_gw_cluster_formation_multinode_tests.erl`), and the running gen_server's `net_kernel:monitor_nodes(true)` wiring telemeters both directions. **Deliberately scoped OUT of `#4555`**: periodic re-resolution beyond the always-on redial loop's own per-tick resolve is not separate work (the loop itself IS the periodic mechanism); adjacency/health/latency tracking (the blueprint's planned `yuzu_gw_cluster` gen_server) and cross-cluster gateway fan-out remain 4.4/the rest of 4.3. **Remaining WS-4 sub-work UPDATED (4.3a shipped, 2026-09-18)**: a durable cross-replica session lookup (`#4246` #3, re-homed to its own slice under WS-5 shared presence — this is also what would make the fallback reader BEHAVIORALLY live, not merely wired, since only then can a directory row outlive the writing replica's own in-memory registry); the REST of 4.3 (cross-cluster gateway fan-out, today one `gw_mgmt_stub_` — the intra-cluster half of 4.3 is DONE, see 4.3a below — **and** `#4555` cluster formation itself, not fan-out logic alone); and 4.4 (`gateway_node` convergence reconcile + gateway-side replay-session writeback `#4246` #6). The pre-CONNECT-race ordering gap `#4324` left open is **NOT** still open as this sentence previously said — see the resolution note above (unreachable BY INVARIANT). **WS-9 note**: a routing failover scenario for this reader has nothing to exercise yet — the fallback path changes zero outcomes on the monolith, so it lands once the reader is behaviorally live (WS-5), not in 4.2b or `#4324`. | **WS-1, WS-3, WS-0** (its no-loss/no-dup DoD needs the outbox + receiver dedup) | **Y** | `gateway-erlang`+`security-guardian`+`architect`+**`cpp-safety`** | P1 | **in progress (4.1 + 4.2a + 4.2b Tasks A–D + `#4324` per-home stream-generation fence all MERGED to `origin/dev`, PR #4492 `c37306113` 2026-09-17. 4.3a DONE (intra-cluster `pg`-group agent→node lookup + the `fanout_terminal` cross-node-routing fix + `remote_dispatched` telemetry — `yuzu_gw_registry.erl`/`yuzu_gw_agent.erl`/`yuzu_gw_router.erl`) — code-complete and empirically verified (compile/dialyzer/eunit clean, OTP `peer`-based 2-node test suite) but COMPONENT-COMPLETE-AND-INERT: zero gateway multi-node cluster-formation code exists in production today (no `net_kernel`/`net_adm` anywhere in `gateway/`, tracked as `#4555`), so this mechanism has no effect until that ships — do not read this as "cross-node routing now works" in any deployed cluster. **`#4555` MERGED (2026-09-19, `495cf24c4`, PR #4604)**: gateway multi-node cluster formation implemented (`yuzu_gw_cluster_discovery` always-on redial loop, DNS-A-record + static-override discovery, dynamic address-only node identity, cookie length floor, new metrics/alert, scale-capable reference rig — see the narrative cell above for the full mechanism), Fable-adjudicated, empirically verified against a real OTP `peer`. **4.4 DONE (2026-09-20, ADR-2002 §7c)**: `gateway_node` convergence + the `#4246` #6 replay-session writeback, closed as one slice after a Fable pre-implementation review rejected the originally-proposed two-mechanism design (a directory-sourced read-repair would publish a route with no capabilities, and gateway-side session adoption is explicitly prohibited by `gateway_route_store.hpp`'s FORWARD NOTE) and surfaced the REAL root cause: `ProxyRegister` called `register_agent` — which always installs a brand-new `AgentSession`, wiping placement — unconditionally, before deciding fresh-vs-replay-adopt. Fixed by deciding ADOPT-vs-REFUSE first (`GatewayRouteStore::reclaim_tombstoned_session`, a new guarded CAS, closes the "session unknown locally AND no live directory row" case that used to mint a throwaway fresh session; a genuine stale/zombie replay is now REFUSED outright via `FAILED_PRECONDITION` rather than silently minting one), with convergence coming from the gateway's own still-live process re-announcing its CONNECTED (`yuzu_gw_agent:reannounce/2`) rather than a new server-side carry-over mechanism. Also fixed a pre-existing latent crash in `yuzu_gw_upstream.erl`'s `do_rpc` (its error-clause shape never matched what `grpcbox_client:unary/5` actually returns — unreachable before 4.4, since this RPC never previously returned a real non-OK, non-transport grpc status). **Round-2 Fable review (post-implementation) found and fixed 3 more real bugs before push**: F1 (BLOCKING) — the adopt decision let an in-memory `gateway_sessions_` hit override the store, so a >270s gateway-uplink partition (core itself UP, no restart) could ADOPT a session whose row had already been tombstoned/deleted, wiping placement with no repair path, and separately let a stale local session zombie-overwrite a genuinely-live one on a different node; fixed by making the store govern unconditionally whenever configured. F2 (BLOCKING) — the reannounce mechanism's CONNECTED notify shares `?MAX_NOTIFY_INFLIGHT` (10) with everything else and was silently dropped at capacity with zero telemetry, an I6a false-assurance risk the replay drip's own notify burst made newly reachable; fixed with drop telemetry + drip backpressure. F3 (BLOCKING) — `yuzu_gw_heartbeat_buffer.erl` had the SAME impossible error-clause shape as `do_rpc`, fixed identically. Plus a non-blocking `connecting`-state reannounce gap (N1). **A full `/governance` pass (round 3) then found 3 MORE blocking issues**: sec-H1 — round 2's reannounce fix converged placement only when its notify was actually delivered, and a drop left `cluster_id` permanently NULL while BatchHeartbeat's `renew_leases` kept extending the row's lease AND `updated_at` forever regardless, so nothing ever re-tombstoned/purged it for a later reclaim; fixed by making `renew_leases` freeze BOTH `lease_until` AND `updated_at` for a `cluster_id`-NULL row — freezing `lease_until` alone (the round-3 fix as first shipped) does nothing for a `register_fresh` row, whose `lease_until` is already NULL from creation, so only sweep (b) (the `updated_at`-keyed tombstone/never-announced purge) can ever reach it; caught and closed by a targeted post-build Fable review before push (`docs/adr/2002-high-availability-architecture.md` §7c has the full correction). This closes the loop only as far as making the row DELETED and OBSERVABLE, not an instant self-heal — actual convergence still needs the next replay or reconnect. NEW-1/NEW-2 — round 2's own claim that both drop reasons "now emit telemetry" was FALSE as shipped (never wired into `yuzu_gw_telemetry.erl`'s `?EVENTS`/`handle_event`/`declare_metrics`, a pure no-op) — caught independently by two Gate 6 reviewers in the same wave; fixed for real, with a new test file exercising the real handler (every other module's tests mock `telemetry` itself, which can't catch this). UP-4/COMP-6 — the decision's own `renew_leases`/`reclaim_tombstoned_session` calls were fail-OPEN on a degraded store, bypassing the entire stale/zombie-refusal mechanism; fixed to fail-CLOSED, mirroring `register_fresh`'s own precedent. Deliberately deferred as the one remaining follow-up (not WS-4-gate-blocking): the reap-vs-replay-drip race after a >270s outage at fleet scale (every agent the drip reaches after tombstoning correctly reclaims; a large fleet just sees a transient wave rather than instant convergence) — filed as `#4627`, with related hardening filed alongside it (`#4628`-`#4632`). One further, deliberately-deferred limitation from the round-3-of-round-3 review, filed as `#4634` rather than fixed in this slice: an `UNAVAILABLE`-refused replay entry (the UP-4 fail-closed fix) is popped from the drip queue with no retry within that recovery cycle and can strand an agent server-unknown until its own next reconnect. The `yuzu_gw_cluster` gossip/adjacency gen_server named in `#4555`'s design notes is judged OUT of WS-4's scope entirely (capacity/rebalancing, not reachability). **Post-build review (PR #4636, FortitudeEtc — Kimi+Codex empirical panel) found and fixed 2 more real, HIGH, independently-cross-examined BLOCKING bugs before merge, both downstream of the store's own (correct) fencing**: BLOCKER 1 — `ProxyRegister`'s decide (`renew_leases`/`reclaim_tombstoned_session`) and its own install (`register_agent`/`register_fresh`/`map_session`) had no lock spanning them, so an entirely separate FRESH registration for the same agent could complete in full between a REPLAY's decide and its install (ordinary reconnect-storm timing, no partition needed) — `AgentRegistry::register_agent`'s own two-phase guard only catches a second call that STARTS during the first's revoke window, not one that already finished — letting the replay's later install silently clobber the fresh registration in memory while the durable store stayed correct on the fresh session (a real store/registry split-brain, `send_to` dispatching via the stale one). Fixed with a per-agent striped lock (`GatewayUpstreamServiceImpl::registration_lock_for`) serializing the whole decide→install span per `agent_id`. BLOCKER 2 — `AgentRegistry::set_gateway_route` (the CONNECTED handler's in-memory write) took no `session_id` and unconditionally overwrote whichever session was currently installed, unlike its sibling `gateway_stream_home_id` accessor, which already had this exact guard — a delayed CONNECTED for a session already superseded by a genuine newer registration could clobber the newer session's `gateway_node`/capabilities/`stream_home_id` in memory even though the durable store's own session guard correctly rejected the matching `announce_connected` write; the pre-existing regression test for this scenario asserted only the store row, never the registry object, so it stayed green while the registry was corrupted. Fixed by making `set_gateway_route` session-checked (returns `false` on a mismatch) and having `NotifyStreamStatus` reject the RPC outright on a mismatch rather than falling through. Both fixes have new regression tests in `tests/unit/server/test_gateway_route_wiring.cpp` (a deterministic concurrency test using a new `proxy_register_interleave_hook_for_test_` seam for BLOCKER 1; an extended registry-level assertion on the existing stale-CONNECTED test for BLOCKER 2); full targeted suite (506 test cases, 9876 assertions) green after both. Also corrected two doc/comment overclaims this same review caught: `gateway_route_store.hpp`'s SESSION GUARDS section and this row's own "cannot overwrite or tear down a newer re-home" claim were store-only, not the end-to-end guarantee they read as (now genuinely end-to-end after BLOCKER 2); a truncated mid-sentence comment at `gateway_route_store.cpp:383` that a round-1 governance ledger row had incorrectly recorded as already fixed. **Rest of 4.3 DONE (2026-09-21, ADR-2002 §7d): cross-cluster gateway fan-out.** Closes the last named WS-4 4.3 gap — 4.3a was intra-cluster (agent on a different node, one Erlang mesh); this is cross-CLUSTER (multiple independently-meshed gateway clusters, one per trust zone/region, per §7's model). Core previously dialed exactly one gateway mgmt endpoint (`gw_mgmt_stub_`, one CLI flag) for every command regardless of the target agent's actual cluster; `GatewayPendingCmd::cluster_id` (added 4.2b Task C, inert until now) is now live. Scope: core-side (C++) plus one small required Erlang fix — the gateway's own intra-cluster fan-out needed no change. Shipped: `AgentSession` gains `cluster_id` (published by `set_gateway_route`, same call/lock as `gateway_node`/capabilities/`stream_home_id` — previously only the 4.2b directory-fallback path carried one, so the far more common `send_to`/`send_to_all` session path always left it `nullopt`); `GatewayMgmtStubPool` (`gateway_mgmt_stub_pool.hpp`) — an EAGER immutable per-cluster stub map built once at boot, replacing the single `gw_mgmt_channel_`/`gw_mgmt_stub_` pair, one shared credentials object reused across every cluster's channel; a two-mode resolution rule (single-cluster mode, the default, ignores any announced `cluster_id` and always resolves to `gateway_command_address` — byte-for-byte the pre-4.3 behavior, zero migration; multi-cluster mode, `--gateway-cluster-addr` configured, auto-aliases `"default"` and treats an unmapped id as a real config defect, dropped+counted+logged, never a silent wrong-cluster fallback); `--gateway-cluster-addr cluster_id=host:port` (repeatable/comma-separated, `YUZU_GATEWAY_CLUSTER_ADDR`), parsed by a pure `parse_gateway_cluster_addrs` helper validated BEFORE `Server::create()` (a malformed/duplicate entry is a CLI exit, not a lenient warning — unlike `--trusted-nat-cidr`'s own precedent); a response-agent guard (`resp.agent_id() != gp.agent_id` now refused+counted `agent_mismatch`, closing the one response-forgery shape where a cluster answers as a DIFFERENT agent than the one dialed — it does NOT close a rogue gateway CLAIMING an agent's own identity via `ProxyRegister` and then legitimately answering for it, since that path needs no per-agent secret for an already-approved agent; **multi-cluster mode did not provide trust-zone isolation between clusters for a given agent's identity — `#4669`, mitigation 1 (agent↔cluster affinity) now CLOSED (see the closure note at the end of this row); mitigation 2 (peer-identity binding) still open** — see ADR-2002 §7d, amended pre-push); `yuzu_server_gateway_forward_total` gains a `cluster_id` label (always the resolved config key or the literal `"unknown"`, never the raw gateway-asserted wire value — a bounded-length but still attacker-influenced string is a metric-cardinality risk even after the paired `kMaxClusterIdLen` ingest clamp). **The one Erlang fix**: `yuzu_gw_mgmt_service.erl`'s `stream_responses/3` (was `/2`) now threads the request's `CommandId` through and stamps it on a `command_error`-derived response instead of a hardcoded `command_id => <<>>` — a gateway-side "agent not connected on this cluster" error was previously invisible end-to-end (`resolve_execution_id("")` → `nullopt`, no tracker terminal, `forward_gateway_pending`'s own metric recorded it as `"ok"`); now counted distinctly as `status="not_connected"`. This was a rare, low-impact gap pre-4.3 (a genuinely gone agent); multi-cluster fan-out makes it the PRIMARY signal of a stale/wrong cluster resolution, so it had to be correlatable. The `command_error` message tuple itself is unchanged, so no other sender/receiver/test needed touching. All five findings above (the upgrade-breaking original resolution rule, the response-forgery gap, the metric-cardinality risk, the pool design simplification lazy→eager, and the Erlang fix) came from a **pre-implementation Fable adversarial design review** (same pattern as `#4555`/4.4) — folded into the shipped design before any code was written, not caught in a later round. New tests: `tests/unit/server/test_gateway_mgmt_stub_pool.cpp` (21 cases as of the pr-rev fix round — `parse_gateway_cluster_addrs` + `GatewayMgmtStubPool::resolve`'s two-mode rule + `classify_gateway_forward_response`, no live gRPC needed) plus `test_dispatch_chokepoint.cpp` cluster_id-carry coverage for `send_to`/`send_to_all`. Full `yuzu-server` + `yuzu_server_tests` build clean; targeted `[gateway]`/`[chokepoint]`/`[gateway_mgmt_stub_pool]` suites green; gateway `rebar3 eunit`/`dialyzer` clean. **`#4672` CLOSED**: `forward_gateway_pending` no longer fire-and-forgets past `gw_pending_` on any terminal-failure branch (`unauthenticated`/exhausted `unavailable`/`unknown_cluster`/no-legitimate-resolution `agent_mismatch`, plus the pre-existing unnamed `"other"` grpc-status branch) — each now synthesizes a terminal `FAILURE` `CommandResponse` (`build_gateway_forward_terminal_failure`, `gateway_mgmt_stub_pool.hpp`) applied through the SAME `process_gateway_response` path a real gateway response uses, so the command_id/execution row always resolves instead of idling forever. **Deliberately still no durable outbox re-drive** — see ADR-2002 §7e for the two load-bearing reasons (the WS-3 3.3 outbox's producer API is epoch-fenced for a leader-gated-loop-only caller, a shape `forward_gateway_pending` structurally isn't; and #3279's untracked detached-thread lifetime hazard, which a retry producer would widen). §7's "undeliverable stays pending, re-driven" design note is accurate for the LEADER-DRIVEN background plane (`route_unreadable`, WS-3 3.3) and explicitly does not describe this forwarding path. **Remaining WS-4 gate item: WS-5** (durable cross-replica session lookup / shared presence — also what makes the 4.2b directory-fallback reader BEHAVIORALLY live, not merely wired). **Trust-zone isolation for multi-cluster mode is a SEPARATE, not-yet-closed item — `#4669`** (a rogue gateway can claim an agent's identity via `ProxyRegister`, which needs no per-agent secret for an already-approved agent, then legitimately answer for it; multi-cluster mode has zero production deployments today, but this must close before it is presented as providing trust-zone isolation).** **`#4669` update (2026-09-21): mitigation 1 CLOSED.** `GatewayRouteStore` migration v4 adds a STICKY `home_cluster_id` affinity column (distinct from the ephemeral `cluster_id` `register_fresh` NULLs on every fresh registration); `announce_connected`'s guarded UPDATE atomically refuses a session-matched write claiming a different cluster than the bound affinity (TOFU-binds on first contact), and `NotifyStreamStatus` adds a read-only pre-check before publishing to the in-memory registry. Closes the claim-then-answer hijack — a rogue winning `ProxyRegister`'s unconditional epoch race can no longer move an already-bound agent's `cluster_id`/dispatch target to itself. Two known, accepted, non-blocking gaps (zero production multi-cluster deployments today): a rollout window (pre-existing rows have no affinity until their next CONNECTED post-upgrade) and a fail-open pre-check exposure window bounded by the duration of a sustained Postgres degradation (`announce_connected`'s own write remains the enforcement of record either way). Mitigation 2 (per-cluster peer-identity binding) is NOT implemented — see `gateway_mgmt_stub_pool.hpp`'s TRUST BOUNDARY section, now "PARTIAL" not "NOT a trust-zone boundary". **pr-rev hardening (2026-09-22, FortitudeEtc/Codex+Kimi, APPROVED round 3):** two rounds of empirically-reproduced BLOCKING/CRITICAL findings closed before merge — round 1 (the reap-sweep tombstone-purge predicate had no regard for a bound `home_cluster_id`, letting a single rogue `register_fresh`-then-wait manufacture a re-home window; fixed by excluding any affinity-bound row from the hard-delete predicate, parking it soft-tombstoned instead) and round 2 (that same soft-tombstone orphaned the durable `session_id` while preserving `home_cluster_id`, which both affinity-classification probes matched only exactly — so the rogue's still-live original gateway session resending CONNECTED after the orphan fell through to an unaudited benign outcome instead of a refusal; fixed by extending both probes to also match a session-orphaned row). |
| **WS-5** | Shared agent presence / health / **scope-eval population** across core replicas | **Slice 1 DONE** (ADR-2002 §7a, 2026-09-22): `OfflineEndpointStore` (schema `endpoint_state`, previously viz-only) extended in place — migration v3 adds `session_id` + PG-authored `last_seen_at` — into the cross-replica presence source, rather than forking a second store or overloading WS-4's fenced `GatewayRouteStore` (both rejected via a pre-implementation Fable design review). `AgentRegistry::all_ids()`/`evaluate_scope()` merge live presence rows (via a `live_presence()` short-TTL read cache, added in governance hardening after Gate 3 performance+sre+architect independently found the bare per-call read put a synchronous Postgres round trip on every single-replica install, not just HA ones) for ids absent from the local registry — local always wins, no duplicates. Graceful disconnect session-guard-deletes the presence row (`agent_service_impl.cpp`/`gateway_service_impl.cpp`) so the single-replica monolith's `evaluate_scope` outcome is unchanged from pre-slice-1 behavior. Also closed a latent `dispatch_confined_arms.hpp` Broadcast-arm bug the same governance round surfaced across all THREE production `ConfinedDispatchSink` construction sites — `server.cpp`'s `make_confined_dispatch_sink` (the primary `/api/command`/MCP/dashboard sink), `server.cpp`'s `forward_legacy_command` (the legacy REST forwarder), and `dispatch_scope_ladder.hpp`'s `wire_and_dispatch_confined` (the third site, missed by the diff's own author, found only by a second Gate 3 architect pass): the unfiltered fast path could return a presence-only id with no routing-directory consult, landing it silently in `not_sent`; now gated on `AgentRegistry::has_remote_presence(candidates)` — a single-lock DIRECT MEMBERSHIP check against the already-fetched candidate list, not a two-call size comparison (an earlier `local_agent_count()`-vs-`known_agent_ids().size()` design was itself found racy by Gate 4 unhappy-path — two separate lock acquisitions could let concurrent local churn make the SIZES coincidentally match while the SETS differed) — and the filtered path calls `prepare_route_fallback` like its Group/Scope/Ids siblings always did. Gate 4 happy-path additionally caught a REAL BLOCKING bug specific to `forward_legacy_command`: it had no `prepare_route_fallback` at all (a pre-WS-5 design decision, valid when every Broadcast candidate was guaranteed local, invalidated the moment `all_ids()` could return a presence-only id) — a cross-replica agent reached via this route was silently dropped with the overall dispatch still reporting plain success; fixed by wiring the same `GatewayRouteFallback` the other two sites use. **A follow-up external review (2026-09-22, `@Doomgoose`) found 3 more BLOCKING bugs in the same "silently narrower dispatch target set" class**, none covered by the first governance round: (1) `command_routes.cpp` and `forward_legacy_command` both short-circuited on `AgentRegistry::has_any()` — a LOCAL-only check, unmodified by this slice — BEFORE ever consulting presence, so a replica with zero local sessions but a healthy presence-visible fleet rejected every dispatch with a false 503 (deterministic, not a race); fixed with a new presence-aware `has_any_reachable()`. (2) `remove_agent_if_session` returned `void` and the disconnect handlers fired the durable presence delete unconditionally regardless of whether the LOCAL removal actually matched the current session — since the durable row's `session_id` is heartbeat-driven, not registration-driven, an ordinary same-replica reconnect (old session superseded locally, new session hasn't heartbeated yet) could delete a LIVE agent's presence row; fixed by making the method return whether it removed the current session and gating the durable delete on that. (3) `forward_legacy_command`'s own `route_unreadable` fix (above) was incomplete — the response cascade had no branch reading it, and a stale comment (in the SAME diff that had just made the claim false) still said this sink never wires `prepare_route_fallback`; both fixed, mirroring `command_routes.cpp`'s existing branch. **Deliberately deferred, not silent**: decommission-cascade integration for the new presence columns (TTL decay bounds exposure instead — decommission is a GDPR-erasure concern orthogonal to this slice's zero-monolith-outcome-change bar), `ids_missing_plugin`/plugin-capability cross-replica visibility, and a `yuzu_fleet_agents_present` gauge/alert (needs its own periodic sampler + WS-10 background-job classification — `yuzu_agents_connected` stays per-replica, unchanged, to avoid permanently tripping `YuzuAgentFleetDrop`). A `yuzu_server_agent_presence_store_failed_total{op,reason}` counter was added in the same hardening round so a degraded presence read/delete is observable, not silent (see `docs/user-manual/metrics.md` → "Agent presence metrics"). **Remaining WS-5 gate items**: the deferred scope above, plus a WS-9 failover scenario once presence is reachable in a real multi-replica deployment (not yet — WS-6/WS-8-readyz still planned). | **WS-4, WS-1, WS-3, WS-10** (scope-eval is a background job; its replica-safety class must be fixed first) | **Y** | `security-guardian`+`architect`+**`sre`**+`docs-writer` | P1 | **slice 1 done; decommission-cascade + plugin-capability visibility + fleet gauge + WS-9 scenario remain** |
| **WS-6** | PKI/CA HA: CA key → `SecretCodec` blob in PG, `CaStore` → PG, **durable CRL numbering + publication state machine**, KEK versioning/rollout/rollback, enrollment → PG | CA key on local disk (`key_provider.hpp:148`); CRL numbering in-proc `crl_publish_mu_` (#1240 UP-4); enrollment on-disk | **WS-1(`ca_store`), WS-3** (fenced leader for CRL publish/enrollment) | **Y** | `security-guardian`+`cpp-safety`+`docs-writer` | P1 | planned |
| **WS-7** | **HA-PG delivery**: shipped Patroni+etcd+HAProxy Compose profile, **selectable durability (3-node quorum default, distinct failure domains)**, operator-plane LB profile | **Shipped**: `deploy/docker/docker-compose.ha-postgres.yml` + `deploy/docker/ha-postgres/` (Patroni entrypoint, HAProxy, Dockerfile); single `postgres:18` compose remains available as the non-HA default deployment | — (storage axis; parallel) | **N** (gates storage-HA / RPO=0 claim, not the 2nd server replica) | `release-deploy`+`build-ci`+`sre` | P1 | **done (PR #3627 merged to dev)** |
| **WS-8** | Per-tier health contract (`/livez` vs `/readyz`; presentation `/readyz`→operator LB, core `/readyz`→presentation→core routing). **BYO-LB doc** + LB/session semantics (sticky-vs-stateless, TLS termination, long-lived-stream failover) | Four health paths exist; contract not tier-split | conceptual on WS-1/WS-2; **readyz needed before LB fronts replicas** | **Y** (readyz) | `docs-writer`+`release-deploy`+`sre` | P0 (readyz) / P2 (BYO doc) | planned |
| **WS-9** | **Failover test harness** — **continuous, incremental scenarios added as each feature lands** (not a final gate): sessions survive, leader re-elect no double-dispatch, outbox effectively-once, cursor-poll no lost events, re-home races, quorum-degrade, standby loss | Single-container test PG only | scenarios track WS-0…WS-7 as they land; stack needs WS-7 | N | `build-ci`+`release-deploy`; scenarios by `chaos-injector` | P1 (continuous) | planned |
| **WS-10** | **Background-job replica-safety classification** — checked-in, CI-auditable table (job → fenced-leader-only / independently-replica-safe / disabled-until-fixed); bring #2508 wall-clock passes to clock-guard | **10.1**: `background_jobs.hpp` `kBackgroundJobs` classifies all 40 server background passes (exhaustive sweep 2026-09-07), catastrophic-class + must-run-per-replica sites `consteval`-gated; **10.2**: `app_perf_fleet_store`/`preflight_run_store`/`deployment_run_store` prunes now run through the shared `pg::run_clock_guarded_prune` (advisory-locked, PG-clock). The concurrency reconciler gained the same advisory-lock single-writer (its OWN `pg_try_advisory_xact_lock`) but **NOT** the shared clock — its clock authority stays replica-local (`now_epoch()`/caller `now`, #4093), so it is classed **DisabledUntilFixed** (not ReplicaSafe) for a 2nd replica, same posture as `nvd_sync` | audit + `disable-until-fixed` need nothing; **`fenced-leader-only` ENFORCEMENT (10.3) is delivered by WS-3 3.2** (the `leader_gate.hpp` runtime gate consumes this table's classification) | **Y** | `cpp-safety`+`sre`+`compliance-officer` | P0 | **10.1/10.2 done (#4092); 10.3 enforcement wired by WS-3 3.2** |
| **WS-11** | **HA-state observability** (NEW): leader identity/epoch, replica lag, quorum state, outbox backlog, failover duration, routing re-homes, split-brain alerts — Prometheus metrics + alert rules | None; ADR gestures at DB-clock monitoring (§4) + gauge coherence (§7a) only | WS-3, WS-7 | N (but ship *with* the 2nd replica — operating blind otherwise) | `sre`+`docs-writer` (+ the alert-rule gate, `docs/observability-conventions.md`) | P1 | planned |
| **WS-12** | **Single→HA cutover + DR (NEW):** runbook to move an existing single-Postgres/single-server deployment to shipped HA-PG + gateway-front reconfiguration rollout; **backup/PITR/WAL under HA-PG** (replaces the superseded `disaster-recovery.md`) | ADR supersedes `disaster-recovery.md` but nothing replaces backup/PITR | WS-7 | N | `release-deploy`+`sre`+`docs-writer` | P1 | planned |
| **WS-13** | **Agent-side gateway failover rollout (NEW):** agent endpoint discovery/failover (agent takes one `server_address` → one channel, `agent.cpp:1384`) + fleet rollout flipping direct-connect fleets to gateway-front | Single configured endpoint; no failover | WS-4 | Y (for gateway-fronted fleets) | `cross-platform`+`security-guardian` (agent) | P1 | planned |
| **WS-14** | **Coordination-substrate security & capacity review (NEW):** threat review of advisory locks / etcd / Patroni / HAProxy / fencing tokens; capacity + failure-domain + sync-quorum latency validation | None | WS-3, WS-7 | N | `security-guardian`+`sre` | P2 | planned |

## Delivery phases (dependency-ordered)

Prio is build-effort; the **safe-to-scale gate** above is the real constraint. Phases:

1. **Phase A — Foundation (build now, in parallel where the ladder allows):**
   `WS-1` (state → PG; milestones 1a/1b/1c pace on the migration ladder — *not* fully parallel),
   `WS-0` (agent idempotency+replay), `WS-2a` (durable outbox), `WS-10` (job-safety audit), and
   `WS-7` (HA-PG delivery — storage axis, fully parallel; unblocks WS-9's stack).
2. **Phase B — Multi-instance safety mechanisms (ALL required before a 2nd replica):**
   `WS-3` (fenced leader), `WS-4` (gateway routing), `WS-5` (presence), `WS-6` (PKI), `WS-8`-readyz,
   `WS-13` (agent gateway-front rollout, for gateway-fronted fleets). **The split question (§1c) is
   RESOLVED — decoupled** (ratified by the ADR-1005 owner Dave Rae, 2026-09-07; see above), so Phase B
   proceeds on the monolith track.
3. **Phase C — Enable, validate, operate:**
   flip on the second replica; `WS-9` runs continuously throughout B/C; `WS-11` (observability) ships
   *with* the second replica; `WS-12` (cutover/DR), `WS-14` (security/capacity). `WS-2b` (spine) and
   `WS-8` tier-split readyz land with the ADR-1005 split.

## Shared with ADR-1005 (do not duplicate)

HA defers to the accepted presentation/core/engine decomposition:
- **Engine-tier HA** (UCE replicas/jobs, engine DB, leader-among-engines) — incl. **NVD/CVE sync**,
  withdrawn from WS-1 (ADR-0023 / ADR-1005 Phase 7).
- **MCP Decision-15 pre-commitments (a)–(k)** — WS-2's MCP durability *inherits* these; the open
  pin-lifecycle item is tracked there.
- The **presentation/core split** — WS-2b and WS-8 tier-split readyz land with it; the monolith
  satisfies the pre-split rows by co-location (no separate presentation to violate the boundary).

## Definition of done (per WS)

Each WS is done when its ADR-2002 guarantee holds **and has a WS-9 scenario that passes** (WS-9 is
continuous, so this is not circular): effectively-once (WS-0/1/3) — one effect **and** the original
outcome across an agent bounce + a primary failover; events (WS-2) — no committed event lost across
failover, replay from cursor on a *different* replica; leadership (WS-3) — no double-dispatch under
forced split-brain; routing (WS-4) — no lost/dup command across re-home / gateway restart / core
restart / dropped `set_gateway_route`; durability (WS-7) — RPO=0 across a host failure with 3-node
quorum on **distinct failure domains**, quorum-loss blocks writes (fail-closed).

**Status-column "done" vs this section's "done" (recorded 2026-09-01, fable/enterprise-architect
review of the WS-0/WS-1/WS-7 re-stamp):** the two are NOT currently the same claim, and this row
predates that distinction — the Status column has tracked "code merged to dev" since the matrix's
first version (WS-1's Status already read "1a+1b done" before any of the three had a passing WS-9
scenario). WS-0, WS-1, and WS-7 are marked `done` in Status as of their respective merged PRs; NONE
of them yet has the WS-9 scenario this section requires (WS-9's own row shows "Single-container test
PG only" as of the same date — no scenarios have landed for ANY workstream). Read the Status column
as **shipped**, not **validated**, until WS-9 catches up — which is the gap WS-9's continuous,
per-feature scenario design exists to close, not a new one this note introduces.

## Maintenance

Update **Status** as workstreams move; keep **Current state** cites in step with the code — **prefer
symbol names, they drift** (the three-model review already found `agent.cpp`/`agent_registry.hpp` line
numbers had moved since the ADR was written). Store rows defer to `docs/postgres-migration-ladder.md`.
ADR-2002 governs on any conflict.
