FIXED POINT: c2f6176d416f5c20e730d3283001defb9d0718cf (dev tip, PR #4444 merged)
HEAD: 3aee0abf8 (branch fix/execution-artifacts-amcache-hardening)
DIFF COMMAND: git diff c2f6176d416f5c20e730d3283001defb9d0718cf...HEAD

COMMIT LIST:
3aee0abf8 A-I1..A-I5: README caveat, test registration, doc regen, changelog, clock-guarded-retention register row
2cd19b934 A2: injectable seam for amcache subkey walk + prefetch enumeration, TU-inclusion internals tests
7467253f3 A1: handle-relative stale scratch-dir sweep (confined_fs, ownership-verified) + init/pre-dispatch wiring + tests

TOOLCHAIN: macOS (Darwin), clang, meson+ninja, build dir build-macos (debug, tests enabled).
This branch is almost entirely Windows-specific code: a new stale scratch-directory sweep on
handle-relative confined_fs primitives (closes #4390), and an injectable test seam for the amcache
subkey walk + prefetch enumeration (closes #4392). Only the portable selection-policy header
(execution_artifacts_scratch_sweep.hpp — no OS calls, just name/age predicates) and small
plugin.cpp/meson.build edits compile on macOS. execution_artifacts_win.cpp,
execution_artifacts_scratch_sweep_win.cpp, and test_execution_artifacts_win_internals.cpp are
Windows-only (meson.build gates them to host_machine.system() == 'windows') and have ZERO compile
or runtime evidence on this host. This is a known, accepted limitation — the branch's own plan
called for real MSVC verification on a Windows host (the-rig) before merge; that has not yet
happened in this review cycle. Grade what CAN be verified rigorously; be explicit and firm about
what cannot be verified at all from this evidence, and do not let a plausible-looking static read
of Windows-only code substitute for actual compile/run evidence.

EVIDENCE (verbatim, this session, current committed HEAD):

=== compile ===
Full from-scratch compile: yuzu-agent, execution_artifacts.dylib (only the macOS-compiled portions:
plugin.cpp, the portable scratch_sweep.hpp header, parsers.hpp — NOT execution_artifacts_win.cpp,
NOT execution_artifacts_scratch_sweep_win.cpp), yuzu-server, both test binaries. EXIT:0, no errors.

=== targeted suite: agent [execution_artifacts] (meson wrapper) ===
2/2 OK (cert_store + agent unit tests). EXIT:0.

=== direct catch2 run: build-macos/tests/yuzu_agent_tests '[execution_artifacts]' ===
All tests passed (65278 assertions in 32 test cases). EXIT:0.
(32 = the pre-existing 30 + 2 new portable scratch_sweep cases; this worktree has NOT absorbed
sibling workstream B's dev-merge yet, so it doesn't have B's new prefetch-token test case — that's
expected, B and A are separate branches off the same base, B lands first and A absorbs it later.)

=== scratch_sweep tag: build-macos/tests/yuzu_agent_tests '[scratch_sweep]' ===
All tests passed (13 assertions in 2 test cases). EXIT:0.
These are ONLY the two portable (OS-free) cases from the new scratch-sweep test file: is_scratch_dir_name
name-matching and is_stale age-threshold logic. The file's ~10 Windows-only cases (junction handling,
in-flight protection, planted-foreign-owner, init()/pre-dispatch end-to-end) are inside
#if defined(_WIN32) and did not run here at all.

=== win_internals tag: build-macos/tests/yuzu_agent_tests '[win_internals]' ===
"No test cases matched '[win_internals]'" — EXIT:2 (Catch2's own no-match code, not a failure).
This is BY DESIGN: test_execution_artifacts_win_internals.cpp's entire body is guarded
#if defined(_WIN32), so on macOS the TU compiles to nothing and registers zero tests. None of its
~10 named cases (ScratchDirGuard deletion-failure counter, subkey_cap, enum_<code>,
subkey_open_failed, value_read_failed, value_enum_incomplete, file_cap, byte_cap,
prefetch_enum_<code>) have ANY execution evidence from this host.

=== full server suite (meson test --suite server), run earlier this session, same HEAD ===
19/19 targets OK, 0 Fail.

=== doc/changelog gates, run earlier this session ===
python3 tools/plugin-doc-gen/plugin_doc_gen.py --check: 0 error(s), 0 warning(s)
python3 -m pytest tests/test_plugin_readmes.py -q: 15 passed
python3 scripts/assemble-changelog.py --check: OK

=== independent branch review already run by an external Codex reviewer (gpt-6-astra), earlier this session, PASS verdict with 3 non-blocking MEDIUM findings, all static-read (this reviewer also could not build/run Windows code on this macOS host) ===
[B1] MEDIUM — execution_artifacts_scratch_sweep_win.cpp:288 — the candidate directory open requests
no DELETE access itself; claims a live dispatch's directory handle may not prevent the sweep from
deleting currently-unopened child files (e.g. amcache.hve) before the final directory-removal step,
under a clock jump or an unusually slow copy-then-load window; the new in-flight test checks
directory survival but never checks the child file's survival specifically.
[B2] MEDIUM — execution_artifacts_scratch_sweep_win.cpp:287 — claims the 2-second wall-time deadline
is checked only inside enumerate_at, so the child-deletion loop and subsequent candidate
opens/owner-queries could in principle continue past the advertised deadline.
[B3] MEDIUM — execution_artifacts_scratch_sweep_win.cpp:214 — claims that since enumeration always
restarts from the beginning of the root directory listing on every pass with no persisted cursor,
64+ persistent-failure entries occurring earlier in enumeration order could permanently starve
later, genuinely-removable orphans past the per-pass removal cap — contradicting the "eventually
reclaimed" claim in the README/changelog.
These three are worth your own independent verification — don't just take them on faith, but also
don't dismiss them as noise; they're about a security-sensitive deletion primitive.

The repository at /Users/alex/yuzu-dev/Yuzu-worktrees/fix-execution-artifacts-amcache-hardening
is READ-ONLY for you: execute nothing, write nothing there. File probe requests for anything only a
run can settle — but note that NO probe you request can be executed against real Windows behavior on
this host; say so explicitly for any Windows-specific probe rather than assuming it will be run.
Do NOT delete or clean up ANY file in this review directory
(/private/tmp/claude-501/-Users-alex-yuzu-dev/e7e17ebe-9977-441c-89a8-8357360ee5cc/scratchpad/codereview-execution-artifacts-A).
