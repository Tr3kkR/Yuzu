REAL CAPTURE
host: the-rig (desktop-04dnsig, Windows 10.0.26200)
date: 2026-09-08
command: probe_wave9_print_win.exe, run via the yuzu-wave9-print-probe SYSTEM scheduled task,
         after wrapper.ps1 paused "Microsoft Print to PDF" (WMI Win32_Printer.Pause) and queued
         two jobs with `"probe job N" | Out-Printer -Name 'Microsoft Print to PDF'`; output
         section == EnumJobsW(level 2) ==
note: cl /std:c++latest /EHsc /nologo /W3 probe_wave9_print_win.cpp winspool.lib compiled clean,
      no warnings. Same shape (2 jobs, level-2 fields populated) also captured under the admin
      SSH identity -- see the-rig-print-probe-findings.md.
