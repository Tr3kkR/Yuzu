status: REAL CAPTURE

host: this Mac (macOS, arm64)
date: 2026-09-08 (UTC, see `date -u` at capture time)
identity: id -u = 501 (alex, non-root — no sudo used or required)

request: CUPS-Get-Default (0x4001), request-id 1, built by
`build_request.py cups-get-default`.

exact command:
  cd tests/unit/fixtures/wave9/printing/macos
  python3 build_request.py cups-get-default > /tmp/req_default.ipp
  curl --unix-socket /private/var/run/cupsd -s -X POST \
      -H 'Content-Type: application/ipp' \
      --data-binary @/tmp/req_default.ipp \
      http://localhost/ -o real_cups_get_default_none.ipp

decoded result: 110 bytes; status-code 0x0406 (client-error-not-found);
status-message = "No default printer." — this host has no configured
default printer, so CUPS-Get-Default legitimately returns this status.

This is a REAL, unprivileged capture against the live `/private/var/run/
cupsd` socket — no sudo, no queue creation. Captured by `capture.sh`
Phase A. Exercises `kCupsGetDefault` decode coverage that no other fixture
in this set reaches.
