Tree: tests/unit/fixtures/wave10/browser_policy/macos/ (tree.manifest, MATERIALIZED AT RUNTIME by
tests/unit/test_browser_policy_macos_plist.cpp into a yuzu_test_browser_policy_ temp root).
Package: P2b-1 (browser_policy macOS leg), workstream ws-10.2b, 2026-09-21.

Manifest format: one `<relative-path><TAB><payload>` line per file; payload is `T:<text>` (escapes
\n \t \\), `B:<base64>` or `L:<symlink target>`; lines starting with `#` are comments. Plists are
multi-line (XML) or binary, so they live in the manifest rather than as tracked files.

This Mac (macOS 26.6.2) has no /Library/Managed Preferences/com.google.Chrome.plist or
com.microsoft.Edge.plist (neither browser installed, no MDM profile), so no real capture of these
domains exists to take; the leg's absent case is what a real run here reports (zero rows, OK).

== Library/Managed Preferences/com.google.Chrome.plist ==
== Library/Managed Preferences/alice/com.google.Chrome.plist  (byte-identical copy, per-user scope) ==
status: SYNTHETIC -- not a real capture (Alex sign-off 2026-09-21; Phase-0 decision 2, Chrome/Firefox only).
schema source: Chrome managed-preferences plist (one top-level key per policy):
        https://support.google.com/chrome/a/answer/7517624
content: six documented policy names (string, bool x2, int, array, dict). XML plist; validated with
        `plutil -lint`. Carries the header as an XML comment; asserted by the test TU.

== Library/Managed Preferences/com.microsoft.Edge.plist ==
status: RECONSTRUCTION -- hand-written from Microsoft's documented Edge policy names, not captured from a
        host: https://learn.microsoft.com/deployedge/microsoft-edge-policies
        Stored as a BINARY plist (what cfprefsd writes) so the binary decode path is exercised:
        authored as XML (HomepageLocation string, SmartScreenEnabled true, InPrivateModeAvailability 1),
        then `plutil -convert binary1` and base64-encoded into the manifest. A binary plist cannot carry
        the XML comment header, so the label lives here and in the manifest's comment lines.

Date/data CF types and malformed plists are exercised by inline hand-written XML in the test TU
(RECONSTRUCTION), not by fixture files.
