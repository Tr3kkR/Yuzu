host: the-rig (Windows 11 Pro 10.0.26200, x64); identity nt authority\system (scheduled task, RunLevel Highest); 2026-09-21T15:29:55+01:00 (rig session A)
status: REAL CAPTURE of the four values (`reg query HKLM\SYSTEM\CurrentControlSet\Control\CI\Policy`: EmodePolicyRequired 0x0, SkuPolicyRequired 0x0, VerifiedAndReputablePolicyState 0x0, SAC_PreviousState 0xffffffff; all REG_DWORD, in that order).
          The serialisation is RECONSTRUCTION: session A captured with `reg query`, not `reg export`, so the file wraps those values in the `reg export` grammar (UTF-16LE + BOM, CRLF). A `reg export` capture from rig session C should replace it.
