status: RECONSTRUCTION, ASSUMED SHAPE, not a capture. UEFISecureBootEnabled=1 is the documented Secure Boot ON state (key SecureBoot\State, REG_DWORD 1). No SecureBoot\State capture exists yet: rig session C should replace this with `reg export` output as SYSTEM (ON; the OFF state, if the firmware toggle is permitted). The OFF and unmodelled states are built in the test by rewriting the dword text of this file.
