status: RECONSTRUCTION, ASSUMED SHAPE, not a capture. Value names are the documented VBS/HVCI/Credential Guard configuration values; the data (VBS=1, RequirePlatformSecurityFeatures=3, HVCI=1, scenario Enabled=1) is invented to exercise every mapper and a multi-key export with an empty key (Scenarios). LsaCfgFlags is not in this export: it lives under Control\Lsa (Microsoft Learn, "Configure Credential Guard"); the test covers that key with an inline RECONSTRUCTION. The real host's export is deviceguard_real.reg (it configures none of these values); this reconstruction stays to exercise the configured/ON states the-rig does not have.
