host: braga, Docker Desktop linuxkit VM, debian:12 (root, unprivileged container); os: Linux 7.0.12-linuxkit; date: 2026-09-21
command: cat <SecureBoot efivar> <SetupMode efivar> /sys/kernel/security/lockdown   (one `<path> err <text>` line each)
status: REAL CAPTURE of ABSENCE: the VM boots without UEFI variables (no /sys/firmware/efi) and its kernel has no Lockdown LSM, so all three reads are ENOENT.
