---
import { NAV } from '../nav.mjs';
import Search from './Search.astro';
const { active } = Astro.props;
const base = import.meta.env.BASE_URL; // ends with '/'
---
<nav class="sidebar">
  <a class="brand" href={base}>yuzu</a>
  <Search />
  {NAV.map((sec) => (
    <div class="nav-grp">
      <div class="gh">{sec.heading}</div>
      {sec.entries.map((e) => (
        // Plugin READMEs route under /plugins/<name>/ (src/pages/plugins/[slug].astro);
        // every manual entry routes under /manual/<slug>/.
        <a href={e.slug.startsWith('plugins/') ? `${base}${e.slug}/` : `${base}manual/${e.slug}/`} class={e.slug === active ? 'on' : ''}>{e.title}</a>
      ))}
    </div>
  ))}
</nav>
