# Meson native file: Linux x64 with Clang 19
# Usage: meson setup builddir --native-file meson/native/linux-clang19.ini
# NOTE: Set CC/CXX env vars to control compiler selection (e.g. CC='ccache clang-19')

[built-in options]
cpp_std = 'c++23'
