# Meson native file: Linux x64 with Clang 18
# Usage: meson setup builddir --native-file meson/native/linux-clang18.ini
# NOTE: Set CC/CXX env vars to control compiler selection (e.g. CC='ccache clang-18')

[built-in options]
cpp_std = 'c++23'
