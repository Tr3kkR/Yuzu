# Meson native file: Linux x64 with GCC 13
# Usage: meson setup builddir --native-file meson/native/linux-gcc13.ini
# NOTE: Set CC/CXX env vars to control compiler selection (e.g. CC='ccache gcc-13')

[built-in options]
cpp_std = 'c++23'
